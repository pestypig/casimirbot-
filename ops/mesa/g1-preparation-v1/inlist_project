! G1 preparation v1: NOT AUTHORIZED FOR EXECUTION.
! Scientific observation freeze and runtime safeguards remain prerequisites.
! This is the initial trial; regenerate initial_Y/Z/alpha for every trial.
&star_job
   create_pre_main_sequence_model = .true.
   load_saved_model = .false.
   pre_ms_T_c = 3d5
   pre_ms_relax_to_start_radiative_core = .false.
   pre_ms_relax_num_steps = 100
   set_initial_age = .true.
   initial_age = 0d0
   change_initial_net = .true.
   new_net_name = 'pp_and_cno_extras.net'
   initial_zfracs = 3
   change_rotation_flag = .true.
   new_rotation_flag = .false.
   pgstar_flag = .false.
   save_model_when_terminate = .true.
   save_model_filename = 'solar-final.mod'
   write_profile_when_terminate = .true.
   filename_for_profile_when_terminate = 'solar-final.profile'
/
&eos
! Exact defaults are bound to the image and recorded SHA-256.
/
&kap
   use_Type2_opacities = .false.
   kap_file_prefix = 'OP_gs98'
   kap_lowT_prefix = 'lowT_fa05_gs98'
/
&controls
   initial_mass = 1d0
   initial_y = 0.27d0
   initial_z = 0.0187d0
   mixing_length_alpha = 2d0
   max_age = 4.57d9
   num_adjusted_dt_steps_before_max_age = 10
   max_model_number = 20000
   use_gold2_tolerances = .true.
   mesh_delta_coeff = 0.5d0
   varcontrol_target = 1d-4
   max_years_for_timestep = 1d6
   max_timestep_factor = 2d0
   MLT_option = 'Henyey'
   steps_before_use_TDC = 0
   mlt_Pturb_factor = 0d0
   use_Ledoux_criterion = .false.
   overshoot_scheme(:) = ''
   overshoot_f(:) = 0d0
   overshoot_f0(:) = 0d0
   alpha_semiconvection = 0d0
   thermohaline_coeff = 0d0
   am_D_mix_factor = 0d0
   use_other_D_mix = .false.
   use_other_energy = .false.
   use_other_mesh_functions = .false.
   mass_change = 0d0
   atm_option = 'table'
   atm_table = 'photosphere'
   do_element_diffusion = .true.
   do_diffusion_heating = .true.
   diffusion_dt_limit = 7d11
   diffusion_T_full_on = 1d3
   diffusion_T_full_off = 1d3
   diffusion_calculates_ionization = .true.
   diffusion_num_classes = 4
   diffusion_class_representative(1) = 'h1'
   diffusion_class_representative(2) = 'he4'
   diffusion_class_representative(3) = 'o16'
   diffusion_class_representative(4) = 'fe56'
   diffusion_class_A_max(1) = 2
   diffusion_class_A_max(2) = 4
   diffusion_class_A_max(3) = 16
   diffusion_class_A_max(4) = 10000
   diffusion_use_isolve = .true.
   diffusion_rtol_for_isolve = 1d-4
   diffusion_atol_for_isolve = 1d-5
   diffusion_maxsteps_for_isolve = 1000
   diffusion_isolve_solver = 'ros2_solver'
   energy_eqn_option = 'dedt'
   history_interval = 1
   profile_interval = 100
   photo_interval = 100
   terminal_interval = 100
/
&pgstar
/
