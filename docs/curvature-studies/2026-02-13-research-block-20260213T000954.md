# Research Block: Natário strict-mode parity (run 20260213T000954)

Status: READY FOR REVIEW (UI parity step pending)  
Run bundle: `tmp/curvature-check-cycle/20260213T000954`  
Gate artifact: `artifacts/curvature-congruence-report.json`

## 1) Strict admissibility check (runtime contract)
- `diagnostics.strict.strictCongruence = true`
- `diagnostics.strict.latticeMetricOnly = true`
- `diagnostics.strict.anyProxy = false`
- `diagnostics.strict.grCertified = true`
- `diagnostics.gate.banner = CERTIFIED`
- `diagnostics.render_plan.mode = natario`
- `diagnostics.render_plan.thetaWarpWeight = 0`
- `diagnostics.render_plan.sourceForAlpha = gr-brick`
- `diagnostics.render_plan.sourceForBeta = gr-brick`
- `diagnostics.render_plan.sourceForTheta = gr-brick`
- `diagnostics.render_plan.sourceForClockRate = gr-brick`
- `diagnostics.canonical.chart = comoving_cartesian`
- `diagnostics.canonical.observer = eulerian_n`
- `diagnostics.canonical.normalization = si_stress`

Notes:
- `diagnostics.render_plan` does not carry `chart`, `observer`, `normalization` as scalar fields in this runtime version, so those checks are validated from `diagnostics.canonical.*`.

## 2) Curvature-congruence report matrix
Overall: `PASS`

PASS checks:
- `strictCongruence`
- `latticeMetricOnly`
- `proxyBlocked`
- `grCertified`
- `banner`
- `mode`
- `thetaWarpWeight`
- `sourceForAlpha`
- `sourceForBeta`
- `sourceForTheta`
- `sourceForClockRate`
- `thetaDefinition`
- `kijSignConvention`
- `gammaFieldNaming`
- `fieldProvenanceSchema`
- `gttResidual` (`p98=0`, `timelike_frac=1`)
- `thetaK` (`p98=0`)
- `truthFields`
- `gammaInputPresence`
- `invariantAvailability` (`14 invariants`)
- `fieldProvenance` (all required truth channels + curvature)

## 3) Runtime mismatch vs expected protocol
- `time-dilation-activate-response.json` has `activation.accepted = false` while persisted diagnostics and strict checker are `PASS`.
- This is an orchestration return-value artifact in this run, not a strict-congruence math failure.

## 4) Remaining research block decision
- Mathematical/runtime evidence for strict Natário congruence is sufficient in this bundle.
- Remaining research-step work is limited to:
  - manual UI parity confirmation on `/helix-core` at the same runtime instant,
  - then interpretation write-up.

## 5) Suggested immediate next command sequence
```powershell
$BASE = "http://127.0.0.1:5173"
npx tsx -e "const fs=require('fs'); const d=JSON.parse(fs.readFileSync('tmp/curvature-check-cycle/20260213T000954/time-dilation-diagnostics.json','utf8')); const r=JSON.parse(fs.readFileSync('artifacts/curvature-congruence-report.json','utf8')); console.log(JSON.stringify({strict:d.strict,gate:d.gate,capture:{mode:d.render_plan?.mode,canonical:d.canonical,strict:d.strict},checker:r.primary?.status},null,2));"
```

## 6) Formal PASS/WARN/FAIL matrix (research package)

### PASS
- `diagnostics.strict.strictCongruence == true`
- `diagnostics.strict.latticeMetricOnly == true`
- `diagnostics.strict.anyProxy == false`
- `diagnostics.strict.grCertified == true`
- `diagnostics.gate.banner == "CERTIFIED"`
- `diagnostics.render_plan.mode == "natario"`
- `diagnostics.render_plan.thetaWarpWeight == 0`
- `diagnostics.render_plan.sourceForAlpha == "gr-brick"`
- `diagnostics.render_plan.sourceForBeta == "gr-brick"`
- `diagnostics.render_plan.sourceForTheta == "gr-brick"`
- `diagnostics.render_plan.sourceForClockRate == "gr-brick"`
- `diagnostics.canonical.chart == "comoving_cartesian"`
- `diagnostics.canonical.observer == "eulerian_n"`
- `diagnostics.canonical.normalization == "si_stress"`
- `artifact: artifacts/curvature-congruence-report.json -> primary.status == PASS`
- `gttResidual` PASS (`gttRelativeResidual=0`, `timelikeFrac=1`)
- `thetaK` PASS (`thetaKRelativeResidual=0`)
- `truthFields` PASS (alpha/beta/gamma/theta/kTrace resolved from GR brick)
- `invariantAvailability` PASS (14 invariants)
- `fieldProvenance.*` PASS for required channels and curvature

### WARN
- None from strict check math/provenance perspective.
- `activation.accepted=false` in `tmp/curvature-check-cycle/20260213T000954/time-dilation-activate-response.json` is an execution-path telemetry mismatch, not a metric validity mismatch.  
  - Interpret as **WARN / workflow-level** until orchestration semantics are confirmed.

### FAIL
- None for strict Natário math/runtime congruence in this run.

## 7) Interpretation summary for this run
- Strict Natário congruence evidence is sufficient for research interpretation:
  - metric identity (`g_tt`) and `theta + K` identities are exact-pass.
  - provenance chain is complete for required channels.
  - wall/invariant data available.
- The only remaining non-mathematical follow-up is a manual UI parity spot-check on `/helix-core` and reconciliation of the `activation.accepted` signal.
