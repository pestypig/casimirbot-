# Durable pairing visual consumer and catalog boundary

Classification: evidence re-entry and source admission. Source inspection found
the four visual-sequence MCP tools still inspecting the raw binding store. That
path fails closed for durable bindings outside their fresh authorization scope.
They now use the shared durable access facade, preserving the legacy fallback
when no durable facade exists. Each awaits exact binding validation before reads
and again after asynchronous manifest/artifact reads before returning evidence.
Developer-account, exact-client, active-binding and epoch checks remain intact.
No capture, visual content, model sampling or environment action was performed.

The existing real MCP coordination test now uses an accepted durable grant with
an empty visual-catalog fixture. It verifies successful catalog access and typed
rejection when device trust is withdrawn during the asynchronous catalog read.
This fixture covers the list handler and shared guard. Individual contact-sheet
and frame-image payload paths do not gain dedicated acceptance from this test.

Verification on 2026-09-08 at approximately 21:43–21:44 local:

- `npx vitest run server/mcp/__tests__/helix-mcp-local-supervisor-coordination.test.ts --pool=forks --maxWorkers=1 --minWorkers=1`: 29 passed.
- `npm run build:server`: exit 0. Four existing warnings persist: duplicate
  `traceId` in agi.demonstration; duplicate Mercury case in halobank-solar/derived;
  duplicate `benchmark_target_id` in structure-mesa and oscillation-gyre.

Current-task tool metadata inventory confirms callable Ready up, reasoning prompt
submit, legacy binding claim, steering read and acknowledgement. It does not list
the new destination register, pairing accept or pairing recover tools. This
inventory is not a live service call or evidence that the package contains the
new implementation. No reconnect, service replacement or EXE restart was attempted.

Remaining: full identity/continuation discipline regression, the rest of O5,
automatic host delivery, native/package qualification, original CS1–CS4 and final
CS5. Component/build success does not close ET6 or promote NAV acceptance.
