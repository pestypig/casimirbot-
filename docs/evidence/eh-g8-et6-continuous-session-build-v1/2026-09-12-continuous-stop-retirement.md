# Retire child deliveries after a validated resident stop

Emergency-stop integration also passed: one selected test, one filtered out; 27.95 seconds test time / 34.37 seconds total. All four requests were emergency_stopped, each attempted once, with stable replay and no redelivery. Final evidence: [cancellation broker](2026-09-12-retired-broker-cancel.json), [cancellation native](2026-09-12-retired-native-cancel.json), [emergency broker](2026-09-12-retired-broker-emergency.json), [emergency native](2026-09-12-retired-native-emergency.json). Documentation audit passed.

CS3/CS4 evidence-normalization repair under the [continuous-session packet](../../work-packets/eh-g8-et6-continuous-session-build-v1.md), addressing the active-looking child rows retained in the immutable [stop baseline](2026-09-12-continuous-chain-stops.md).

After a validated temporal workflow event reports canceled or emergency_stopped with controls released, the broker now retires active child delivery rows under that exact resident in the same transaction. Admission retention selects the children; the root is excluded. Admitted, leased, running, paused-manual-override and cancel-requested children receive the resident stop state and a resident_terminal_delivery_retired reason. Existing terminal statuses remain unchanged. No synthetic child result is created, no child is claimed successful, and its event/effect evidence is preserved. Exact event replay returns before updates.

The SQL regression covers cancellation, emergency stop, rejected non-stop states, exact-root scoping, unretained rows, already-terminal rows and unchanged timestamps on repeated retirement. Result-canonicalization plus delivery-status suites passed 57/57 tests (44 + 13). Static Helix discipline check passed with classification evidence normalization.

The first connected cleanup run did not reach stop validation: forced recompilation took 94 seconds, outlasting the fixture's 60-second request deadline, and the first successor was unavailable. This was not a stop-cleanup verdict. Continuous fixture launches now use Gradle's test-task `--rerun`, preserving forced test execution while allowing unchanged compilation dependencies to remain up to date. Production authority/request deadlines and the fixture movement window were not widened. Cold compilation can still require separate preparation before this bounded test.

Corrected cancellation integration passed: one selected test, one filtered out; 27.15 seconds test time / 33.51 seconds total. All four requests were canceled with attempt_count=1. The runtime performed two handoffs and stopped before queued successor three activated. Exact terminal event replay left statuses and completion/update timestamps unchanged; successor delivery returned null and the ordinary queue returned no work.

This is a simulated-clock/player integration using real compiler/broker/native runtime, fixture identity/perception and a bounded pg-mem schema. It does not prove ordinary human UI/control ingress, authenticated revocation, races with in-flight delivery, durable database restart, fresh live observation reasoning, physical Minecraft movement or packaged recovery. Those requirements remain open. No EXE/JAR was deployed; original ET6 and NAV1 status do not advance.
