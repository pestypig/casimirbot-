# One-successor normal broker baseline

CS3 prerequisite evidence following [fixture revalidation](2026-09-12-native-broker-fixture-revalidation.md). The earlier lost-response run retained `uncertainPoll`, no queued successor, and the root wire; it proved non-replay under uncertainty, not successful successor consumption. This distinction applies when interpreting its passing result.

The normal wide-window mode passed through the actual Java runtime, TypeScript broker, frontier publisher and retained admission code: one selected test passed, one unrelated test filtered, 32.39 seconds. JDK 21 and Gradle 8.14.3 paths and opt-in publication/roundtrip settings are the same as the prior record. `HELIX_NATIVE_BROKER_WIDE_RUNWAY=1`, lost-response and advancing-clock-fault flags are 0. The wide positive mode selects Java's elapsed-clock acceptance test itself. An initial invocation combining wide mode with the advancing-clock fault flag was correctly rejected before execution; no timing budget was widened.

[Immutable measured output](2026-09-12-native-broker-normal-timing.json) records one root and one child. Checkpoint-to-acceptance was 736 ms; HTTP roundtrip 236 ms; response-to-acceptance 17 ms; evidence-fence wait 483 ms. The enclosing calculation including its safety margin totals 746 ms and reports 241 ms remaining. Acceptance was at tick 14 before stop tick 20, with committed activation tick 21. Checkpoint-to-activation was 752 ms; acceptance-to-activation 17 ms. These are one fixture sample, not percentiles or live capacity.

The acceptance-budget clock advances from JVM elapsed time. Activation remains explicitly scripted in the fixture, and physical snapshots are fixture-supplied. Server and JVM monotonic timestamps are not directly subtracted. Required native snapshot persistence was disabled, provider sampling/proposal was not measured, complete five-stage coverage is false, cross-process clock mapping and live window budget remain null. This is not a complete continuous moving-runway trial.

The next three-successor extension must generalize the same resident root ownership across three broker admissions, fresh checkpoint/frontier identities, exact predecessor hashes, per-successor leases and consumptions. It must not concatenate three invocations of this pair fixture or preaccept fabricated broker results. Lost/reordered responses, backpressure, reconnect, actual useful motion and genuine interruption remain separate requirements. No CS3, CS4, original ET6 or NAV qualification is claimed. Production app state and source were unchanged in this baseline run.
