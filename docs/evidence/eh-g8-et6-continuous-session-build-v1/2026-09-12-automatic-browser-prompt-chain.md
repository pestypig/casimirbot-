# Automatic browser pairing through prompt acknowledgement

O3/O5 and CS2 prerequisite evidence under the
[onboarding plan](../../work-packets/eh-g8-cs-onboarding-pairing-plan-v1.md).

Extended all four automatic real-browser cases (pointer/keyboard, normal/lost
provider reply) through runtime binding restoration, exact chat-scoped typed
HTTP prompt submission and identical retry. The real durable binding access
reads one event with the matching text/origin/identity and acknowledges it.

A separate fixture agent-origin event is dispatched through internal access,
replayed identically, and displayed once by the production prompt component.
Reload after acknowledgement shows that same article acknowledged. There are
two steering rows total: typed and agent-origin. Rendered pairing revocation
removes the display and rejects subsequent task pickup. Invitation delivery
still has one provider send/message and no secret in browser storage.

The four extended Chromium cases pass (11.4 seconds). Earlier eight copy cases
were not rerun after this test-only extension confined to the automatic branch.
The retained import.meta/IIFE warning remains.

Typed submission uses browser fetch through the actual HTTP handler, not the
full composer. Agent-origin dispatch uses fixture internal access, not current
MCP authentication. Provider and human identities remain isolated fixtures.
This is not native packaged or live acceptance and does not establish game
effects, actual host attestation or full CS2 completion. The original complete
goal, CS1–CS4 and ET6 requirements remain unchanged and open.
