Program gate: G8 — environment-harness release evaluation.
Workstream: ET6 prerequisite continuous-session build.
Capability or component: CS1 exact-task preparation and readiness presentation.
Lifecycle stage: source admission, evidence normalization and presentation.
Reaction timescale: bounded setup and five-second visible status observation.
Authority owner: Runtime Codex prepares; Helix validates; the human approves binding and finite gameplay authority.
Current maturity: specified for the integrated prerequisite; existing component evidence unchanged.
Target maturity: deterministically verified repaired presentation, followed by packaged rehearsal.
Required evidence: failing fixtures, focused regressions, exact package identity and same-task MCP/native observations.
Explicit non-goals: no human consent automation, private runtime, ET6 closure, NAV1 work or gameplay success from setup receipts.
Downstream gate unlocked: none at this checkpoint.

## Exact continuation preflight

The user authorized this single project-chat handoff. Persistent build goal and
authenticated continuation: `01a081e3-1973-76a3-b35b-0bd6d541933d`.
No old claim, reasoning binding or task continuation was reused.

This task's callable v2 catalog contains session Ready up (including
`goal_bootstrap`), prompt submission, binding claim, steering read and steering
acknowledgement. At 16:40 UTC Ready up `read_preparation` invoked successfully
and returned no intents. Action authorization status subsequently returned
ready with no missing required scopes. Prompt submit/claim/read/ack were
discovered, not yet exercised against a human-approved binding.

Authenticated supervisor presence returned service
`service_instance:e7580e5a042cad2d79b138b3dd76f507` and derived client session
`supervisor_client:6cf57a2a42be08bfc9b9f266ac2b8ee0`.
The initial 300-second heartbeat request was rejected by the published
180-second maximum; the bounded retry succeeded. The default tool-activity-only
declaration truthfully left continuation unavailable. A continuation declaration
without checkpoint support was rejected; the complete public-checkpoint policy
and polling declaration succeeded. These are caller setup corrections, not
product regressions or evidence of an idle-task wake.

All six observed CasimirBot processes used
`apps/desktop/release-cs2-ingress-20260908/win-unpacked/CasimirBot.exe`.
SHA256 was independently rechecked as
`148cdc75ce8ee245d82d0d2cc6ed1a72b60d8283d0666e41ae2ad345ecb026a5`.
No keyed server was replaced or newly started. The MCP service identity was
revalidated by authenticated invocation; no old port was used as proof.

## Supported preparation observations

The existing native Agent Access screen detected authenticated task presence.
Preparation room `shared_realtime_room:07c009a9-cb6f-44f9-a1c4-48baa2e079ef`
was selected through the ordinary UI and its one-hour preparation requested.
No human-only binding or authority control was activated.

At 16:43:29 UTC the browser created intent
`environment_preparation:79571bdf9b0d7e3c4163e174cfaceae6`, request
`6eb01f1f-65e6-49ab-901c-8dd954a380e0`, exact chat
`b04b840e-1bc7-433d-b624-43709f235456`, with a ten-minute delivery deadline.
This exact task read it through MCP. Run discovery returned no candidates.
`prepare_run` used a caller-authored bounded development objective and returned
`run_69cb152a-dcb9-40ff-a988-d95e75acacff` at 16:43:50 UTC, with verified run-room
binding `agent_room_binding:a91bedfe-399b-4087-a89c-4c2932fb610b`, version 1.
The run budget is not binding consent or gameplay permission. Ready remained false.

Device Check first reported the ET6 source expired/offline. The supported
`helix_environment_source_pair_local` repaired only that expired read credential
for `room_source_binding:5eaf90ee-96be-49da-88e7-02d978d35f44`, using one stable
idempotency key and a one-hour credential duration. Pairing material stayed in
the opaque inbox. At 16:44:16 UTC Device Check reported the same environment
`environment_binding:legacy:bbfc260b8f11ee109fc80bd8f1cc40d7df86e56b` online,
active-credential, probe-ready, with no blockers. No healthy source was rotated.

The first lifecycle launch omitted address and failed
`minecraft_loopback_server_not_listening`. Read-only inspection of the exact
profile-owned server's port fields and listener established `127.0.0.1:25566`
(PID 22668). The supported MCP launch with that explicit address succeeded:
client PID 23200, profile `helix-combat-c0-isolated`,
`fabric-loader-0.18.4-1.21.8`, mod loaded, autojoin staged, memory 86.6%.
Launch authority was trusted-device workstation lifecycle; action authority
was null. This is launch evidence, not a player-action pass. Ordinary default
port resolution remains a separate packaged-workflow limitation.

At 16:49 UTC the exact source reported DatDamPig online in a fresh directory,
but its retained subject binding required epoch re-verification. The only
returned movement authority `environment_action_authority:43041779-1262-4f7e-8985-25ac3f7461a5`
expired at 07:47 UTC; connector controls were not asserted. Human finite
authority and exact-task binding remained missing.

## Reproduced presentation failures

1. Agent Access stopped its read-only status poll when task presence became
   ready, although the verified environment run was still absent. The UI told
   the operator to keep waiting. A five-second fake-clock fixture failed to
   reveal the prepared-run checkbox. The repaired poll includes visible,
   unbound chats waiting for a verified run and stops when the run appears or
   a binding is active. Selection remains unchecked and no claim is generated.
   Setup/preparation suites passed 44/44 after this repair.
2. A subsequent readiness recheck could not display the prepared run. The real
   resolver adds `run_expires_at`, while the strict status schema rejected it.
   A route fixture using the resolver's actual result shape reproduced HTTP
   500 instead of 200. The shared status schema now accepts the typed optional
   deadline for compatibility with older projections. Server expiry and
   exact-association admission checks remain unchanged. Combined verification
   and packaging are pending at creation of this record.

Native input initially reported missing coordinate geometry and then an
off-window accessibility click. Fresh screenshot-derived coordinates allowed
ordinary preparation. These tooling observations are separate from the two
reproduced product defects.

No prompt ingress acceptance, resident movement, three linked successors,
interruption, fresh action-result re-entry, revoke/stale acceptance or CS1–CS4
closure is claimed. This record is a dated observation, not a replacement for
the canonical work-program stage ledger.
