# Saved server startup: CS5 supplement

This immutable snapshot supplements the continuous-session and onboarding
packets. G8 remains active. Every full CS1-CS4 exit remains incomplete; original
ET6 remains unpassed and NAV1 remains unqualified.

The [package comparison](2026-09-13-saved-server-properties-package.json) verifies
eight host artifacts, 646 runtime files and 635 renderer files. The separate
[fixture scan](2026-09-13-saved-server-properties-fixture-scan.json) found no known
fixture content. The latest launched EXE is
`apps/desktop/release-saved-server-properties-20260913/win-unpacked/CasimirBot.exe`,
SHA256 `6ef13aa466b0242680cd104b20edd3221ba9e316a96dbb6341b2dd9dab6f5e97`.

## Evidence limits

56 focused tests passed. The real server PowerShell provider ran 17 isolated OS
scenarios, including cross-process mutex contention, uncertain spawn outcome,
timeout/reconciliation, PID reuse, port conflict, case-sensitive configuration
and unchanged EULA/properties. After the first packaged rejection of dotted Java
property names, the provider and both owner-profile tests passed again with
ordinary `query.port` and `rcon.port` keys. UI tests reject invented success and
retain timestamped server effects after client failure. These are deterministic
component tests, not environment acceptance.

The [actual packaged rehearsal](2026-09-13-saved-server-properties-launch.json)
started server PID 27252 at `2026-09-14T01:24:10.3132425Z` through MCP. It listened
on `127.0.0.1:25566`. A second call reused that exact PID/start time; no duplicate
server was observed. The same source binding redeemed the opaque handoff and
published fresh read-only capabilities. No credentials entered this evidence.
Client launch stopped at `minecraft_fabric_profile_selection_required` both
times, with the server observation preserved. The selected isolated profile
exists. Normal Launcher selection changes its visible button without changing
`lastUsed`; that historical timestamp is the next incorrect admission boundary.

Native trust was visible and reused after restart without another consent. One
computer-control helper reported a stale window/capture failure; resetting that
helper restored native capture. This was not an observed CasimirBot crash.

The installed service rejected standalone adapter CLI access with HTTP 401
`desktop_session_required`; that boundary was not bypassed. The separate
[isolated authenticated real adapter HTTP check](2026-09-13-saved-server-properties-adapter-verification.json)
returned PASS, firstFail null, certificate integrity true, hash
`d2821c7d650d8d4c86f5270c2510b94ed7cd8c45b12d807e0420613f9fe7ce5d`.
It evaluates measured scoped build/test telemetry, not production-session access,
full-repository qualification or physical viability. Its trace refs refer to the
isolated process; the JSON result is retained here. The initial helper was stopped
after its result was saved because imported services retained event-loop handles;
the later helper explicitly exited and returned zero.

## Requirement-by-requirement handoff

| ID | Evidence added / remaining proof |
| --- | --- |
| CS1.1 | Shared server/client executor now includes fixed saved-server startup; fully ready MCP/EXE agreement remains required |
| CS1.2 | Exact task/owner/room/source retained and source admitted; current run/player/lease/controller/goal chain remains required |
| CS1.3 | Actual server repeated call reused PID/start time; three integrated Ready up calls and zero unnecessary rotations remain required |
| CS1.4 | Saved device trust reused without renewed human consent; independent finite pairing/action/goal permissions remain required |
| CS1.5 | Server journal reconciliation passes fixtures; full wrong-profile/stale-subject/durable-goal restart matrix remains required |
| CS2.1 | No accepted task pairing or prompt trace; visible ingress, truthful origin, pickup and acknowledgement remain required |
| CS2.2 | No new live ingress negatives; exact-chat/run/epoch and duplicate-effect proof remain required |
| CS2.3 | Automatic task delivery and public checkpoints remain unqualified; authoritative post-observation answer path remains required |
| CS3.1 | No movement; useful course and three linked successors through compiler/broker/resident executor remain required |
| CS3.2 | Server startup timestamps retained only; movement observation/admission/delivery/activation/runway/stall/release timings remain required |
| CS3.3 | Startup faults and cross-process reuse tested; complete four-plan fault and live reconnect/backpressure matrix remain required |
| CS4.1 | No gameplay interruption; override/stop, successor invalidation and release latency remain required |
| CS4.2 | No gameplay authority was granted; full broker/connector/executor revocation and fresh no-motion proof remain required |
| CS4.3 | Fresh source manifest and partial startup observations re-entered this task; fresh gameplay observation changing reasoning remains required |
| CS4.4 | New package compared and launched, fixed server script hash verified; complete companion/package qualification remains required |
| CS4.5 | Actual saved server startup/source pairing/reuse passed; client profile admission, consent/prompt/result/recovery workflow remain required |
| CS5.1 | All original requirements retained without promotion; complete CS1-CS4 acceptance handoff remains required |

The finite preparation run from the earlier rehearsal expired and is not being
claimed as current. No old claim handle, task pairing, player identity or action
lease was reused. The persistent goal remains active.
