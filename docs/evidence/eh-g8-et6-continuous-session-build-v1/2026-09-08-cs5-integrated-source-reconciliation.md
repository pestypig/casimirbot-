# CS5 integrated-source reconciliation — incomplete handoff

This supplements, without rewriting, the earlier CS5 pretrial snapshot. It is
not CS5 closure. All original CS1–CS4 and ET6 requirements remain in force.
The full integrated-source discipline run is currently pending in session 38434;
its start record identifies seven source hashes. No pass is inferred from it.

| Requirement | New inspected evidence | Still needed before closure |
| --- | --- | --- |
| CS1 shared idempotent Ready up | Awaitable durable association is wired through preparation, readiness, recovery and admission; accepted-pairing UI validates a matching runtime binding before exposing existing Ready up | Current packaged MCP/UI trace, three repeated calls preserving healthy identities, exact consent and durable goal recovery |
| CS1 durable exact-task consent | Encrypted issue/accept/recover/revoke tests; idle registration; five-field destination correlation; rendered real-handler normal and lost-response cases | Native disk/process restart, account/device changes, ordinary human consent, complete joined negative matrix |
| CS2 scoped ingress and visible delivery | Real-handler joined test submits/replays one typed prompt and verifies exact delivery and idempotent ack; prior MCP agent-origin tests still apply | One visible natural prompt and actual task pickup/ack on the packaged new pairing path; all wrong-chat/run/epoch and origin cases joined to display |
| CS3 three linked moving successors | Existing compiler/broker/resident evidence remains unchanged by pairing repair | All original live useful-motion, fresh re-entry, changed-affordance, backpressure, transport and zero-duplicate-effect criteria |
| CS4 interruption/revocation/recovery | Pairing revocation invalidates access and clears matching UI controls; visual consumer rechecks after asynchronous reads | Actual manual override, emergency stop, action revocation, fresh observations and ordinary packaged recovery with release of incompatible controls |
| O1 reproducibility | Legacy expected failures, new pointer/keyboard and rendered real-handler tests | Full new workflow timer/restart/input matrix; do not count legacy expected failures as implemented behavior |
| O2 durable pairing | Public HTTP/MCP runtime wiring, request-ID read reconciliation and exact runtime linkage are implemented and have focused tests | Durable replacement policy, all downstream stored-binding consumers, full restart/isolation matrix |
| O3 exact task and automatic delivery | Owner registration list uses authenticated client-declared exact IDs; fallback is explicit | Host-attested task titles/list/send and real automatic delivery/acceptance; an in-process fixture is not host access |
| O4 fluent controls | Task/duration/run/consent/Copy/status/revoke controls, retained drafts, GET recovery, pending expiry observation; pointer/keyboard at two widths and a covering overlay | Unify legacy/durable guidance, prove whole-panel lifecycle and full workstation/native layout, all clipboard/late-response and denial states |
| O5 deterministic integrated matrix | Two rendered real-handler cases over encrypted pg-mem plus separate Chromium fixtures; authentication negatives and replay checks | Real-handler Chromium path, complete matrix rows in the O1–O6 plan, persisted outbox/event recovery and package fixture-isolation audit |
| O6 package | Current client and server builds pass; no new native package qualified | Exact EXE/host/service/client/companion hashes and content comparison, ordinary consent, host delivery, accepted session, visible prompt, idle/native restart and revoke workflow |

Current external catalog check lists Ready up, reasoning prompt submit, legacy
claim, steering read and acknowledge. New register/accept/recover tools are absent.
Packaging and supported catalog revalidation precede any request for new human
consent; no replacement task or repetitive reconnect cycle is authorized.

The original ET6 measurement rows in the earlier CS5 pretrial reconciliation
remain null for the new integrated trial, including horizon/queue/concurrency
sweeps, timing distributions, useful movement ratio, evidence latency/volume,
interruption/replan timing, three rolling cycles and zero stale/duplicate effects.
No new movement was performed during this source integration. NAV's separately
admitted lanes are unchanged and are not dispatched by this goal.

Next order: finish the existing full regression; resolve its first failure if
any; continue the missing deterministic workflow/authority boundaries; stage and
compare an exact package; revalidate callable tools and current native state;
request only the genuine remaining human consent when the path is ready; execute
and record each original live CS exit. This checkpoint cannot mark the goal done.
