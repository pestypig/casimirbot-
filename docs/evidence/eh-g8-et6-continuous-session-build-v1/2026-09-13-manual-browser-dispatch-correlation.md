# Manual browser dispatch correlation

The operator reported clicking the diagnostic. Read-only inspection of the actual
Chrome DevTools console on the harmless local page then showed two
"Launched external handler" entries for its fabricated access_denied callback.
The app journal still ended at the ordinary 18:49:53 UTC startup. No new
process_entry, second_instance or received event accompanied those browser entries.
This supports browser dispatch initiation, not successful Windows launch.

The registered HKEY_CLASSES_ROOT command and running EXE both referenced
release-oauth-status-recovery-20260913. A fresh Windows protocol positive control
at 19:35:51 UTC produced process_entry, second_instance, received, rejected. The
control used a fabricated invalid state, never an OAuth code or real grant.
ELECTRON_RUN_AS_NODE was absent from the diagnostic shell process, user and machine
environment settings; Chrome's inherited process environment was not inspected.

The diagnostic closes the ambiguity about merely missing a link click. The first
remaining divergence is caller-specific browser dispatch to application entry.
The current app and registration work for the Windows control. No token-exchange,
consent, account-link, or integrated workflow success is established. The root
cause of Chrome's missing application entry remains unresolved; neither a browser
console launch statement nor the control proves it repaired.

No further human OAuth retry was requested in this correlation. The prior native
computer-control URL-safety failures remain separate tool limitations. Diagnostic
tab cleanup between turns caused avoidable operator confusion and was acknowledged.
All CS1–CS4/O1–O6 exits remain incomplete, CS5 handoff remains mandatory, ET6 remains
unpassed and NAV1 unqualified.
