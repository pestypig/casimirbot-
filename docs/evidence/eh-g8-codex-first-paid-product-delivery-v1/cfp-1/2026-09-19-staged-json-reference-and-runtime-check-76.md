# CFP-1 staged JSON reference and runtime check — 2026-09-19

Status: read-only C12/C13 inclusion and customer-claim inspection at local HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` with a dirty shared worktree. The four source SHA-256 values still equal the earlier [staged-data source map](2026-09-19-staged-service-data-source-map-40.md); no source JSON, builder, route, account, release or production state changed. This is not creator/rights evidence, scientific validation or an extracted signed-installer inventory.

| Component | Source-backed finding | CFP-1 consequence |
| --- | --- | --- |
| C12 ideology graph | `docs/ethos/ideology.json` has 123 node IDs and 55 explicit node `references`: 33 `doc`, 21 `panel`, one `repo_module`; 28 of the `doc` references point back to the graph itself. None of these 55 entries has an external `url` field. The graph has a long Git history under the owner's identified `pestypig` author string. | Internal reference links and Git custody do not prove that all prose, ideas, assets or imported passages were created by the owner or licensed for commercial redistribution. A selected customer-facing moral/ideology claim needs its own content and provenance review. |
| C12 verifier configuration | `configs/ideology-verifiers.json` has five mappings, each to an existing graph node. `npm run verify:ideology-verifiers` passed with `mappings=5 nodes=123`. Its 19 unique `ref`/`refs` strings include 14 that are not literal existing repository paths after removing any `#` fragment; these may be symbolic provider IDs, but this inspection found no file-level proof for them. The schema requires nonempty strings, and the node cross-check verifies duplicate/unknown node IDs; neither check proves every referenced test, metric, attestation or tool runs. The config also contains VCU/mint/payout vocabulary from the separate contribution-verification domain. | Do not treat the pack's successful structural validation as executed verifiers, customer proof certification, or the selected hosted subscription/credit product. Determine whether this config is needed in the first customer service, preserve its domain separation, and review any user-facing contribution claims independently. |
| C13 StarSim registry and reference pack | The 22-key solar product registry and 14-document reference pack retain their earlier hashes. All 14 document URLs remain under `https://example.com/starsim/`, and the pack labels its overview a local development placeholder. `server/modules/starsim` loads these artifacts for solar-observed requests; `server/routes.ts` mounts `/api/star-sim` outside `fastBoot` and `server/routes/star-sim.ts` defines resolve/run/job endpoints. The route inspection alone does not settle global account middleware or installed customer reachability. | The files are not merely loose source examples: current desktop build/stage scripts select them and service code uses them. If C13 is excluded from the first artifact, CFP-3 must change the build and affected service behavior under review. If retained, correct the reference/source chain and obtain a qualified rights disposition before a customer-facing scientific-source claim. |

The C12 `ref` check is deliberately a **literal-path** check, not a claim that fourteen symbolic references are invalid. A reviewer or component owner must map any symbolic IDs to real executable providers and evidence, or limit the claim. The product owner's earlier C01–C15 baseline made C12/C13 conditional rather than clearing their rights or assigning customer benefits. The owner has been asked whether C13 should be excluded from the first customer build or retained subject to repair; no selection is recorded in this checkpoint.

The [component rights submission](../../../work-packets/eh-g8-cfp1-component-rights-review-submission-v1.md) requires per-file creator/import/redistribution evidence and an include, revise or exclude disposition. The [planned inclusion selector](../../../work-packets/eh-g8-cfp1-planned-release-inclusion-and-rights-review-v1.md) controls later build extraction. CFP-1 remains **active (specification)** and CFP-2/3 **blocked** under the [work program](../../../helix-environment-harness-work-program-v1.md).
