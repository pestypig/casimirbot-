# CFP-1 desktop dependency and notice evidence gap — 2026-09-19

Status: read-only inspection at source HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` for C02/C03/C04 and R-BIN-01/R-NOTICE-01/R-LGPL-01. The inspected `apps/desktop/dist`, `runtime` and `release/win-unpacked` are local candidates, not one reserved signed customer cohort. This records package metadata and build selection, **not** a license interpretation, complete SBOM, signed-byte inventory or permission to distribute. No build, package, vendor, release or runtime setting changed.

| Evidence | Current observation | Review consequence |
| --- | --- | --- |
| `apps/desktop/package-lock.json` (SHA-256 `35d137eeb3dd27eda1f6fc6aa9bf48ef9f712bee053cb984a3bdcc9519218306`) | 49 non-dev package entries, 23 marked optional. Their lockfile `license` strings include MIT, Apache-2.0, LGPL-3.0-or-later, mixed Apache/LGPL, Python-2.0, ISC, BlueOak-1.0.0 and 0BSD. The lock includes optional `@img` binaries for multiple operating systems/architectures, including three Windows architectures. | This is an overinclusive dependency candidate list, not a list of files in a Windows x64 installer or a rights disposition. Review the actual platform closure and license texts. Do not count all optional packages as shipped or dismiss the mixed native label. |
| Local `apps/desktop/node_modules/@img` and old `release/win-unpacked/resources/app.asar.unpacked/node_modules/@img` | Both inspected directories contain `sharp-win32-x64`; the installed package metadata says version `0.34.3`, `Apache-2.0 AND LGPL-3.0-or-later`, and contains `LICENSE`, `versions.json` and `lib/sharp-win32-x64.node`. The old unpacked output also contains that package and a separate `sharp` tree with `LICENSE`. | These are concrete reviewer inputs for C02, but the old unpacked output is not the final signed cohort. Qualified review must identify applicable binary/notice/source-offer conditions for the **selected** package bytes and actual extraction, including Electron/updater closure separately. |
| `apps/desktop/scripts/build-host.mjs:85–154`; current `dist/service-dependencies.json` | esbuild bundles host, preload and service outputs. The generated service-dependencies file records the service hash, `sharp` as required external runtime package and two data assets; the build does not emit an esbuild metafile or an embedded-module rights inventory. The local output declares source commit `256554ca2637b2978a83616d9f9670069fdfd8c4`, older than inspected HEAD. | The service manifest proves a hash and narrow external-package requirement, not which third-party modules were embedded in `service.mjs` or the Electron host bundles. CFP-3 must generate and review a module/package origin graph for the reserved build, then map it to actual ASAR/unpacked bytes and notices. |
| `apps/desktop/scripts/stage-runtime.mjs`; `apps/desktop/electron-builder.config.cjs` | Staging copies the entire `dist/public` tree; the current local client and staged client each contain 636 files. In the local client, 414 `.js`, 105 `.mjs` and two `.css` files sit outside the earlier [100-file media/font/WASM byte crosswalk](2026-09-19-desktop-media-byte-crosswalk-13.md). The builder also selects `dist/**` for the desktop host. | The 100-file media map does not cover bundled client code, remote map/style use, Electron output or every staged dependency. A future signed tree needs a generated complete file manifest plus source/bundle-origin mapping, followed by reviewer-approved notices and customer-accessible notice location. |
| Tunnel-client v0.0.13 staging | The [pinned vendor inspection](2026-09-19-tunnel-redistribution-closure-12.md) found a vendor `NOTICE`, third-party report and SPDX file; current staging and `runtime/licenses` include only the executable and LICENSE. | Decide whether the tunnel is selected for the customer transport, then obtain a qualified disposition for which vendor/product notices must ship or be available. An upstream SPDX report is not the CasimirBot product SBOM. |

The next CFP-1 reviewer evidence request is a *planned* dependency/notice mapping for the selected C02–C04 boundaries: source/package version or bounded rule, license-text location, expected packaged path, notice/source-offer condition, responsible owner, and explicit `include/exclude/conditional` result. CFP-3 then generates an exact artifact manifest, bundler input graph and product SBOM/notice index from one reserved build; compares those to extracted signed EXE, ASAR, native unpacked files and separately provisioned resources; and rejects unexplained bytes or missing required notices. The lockfile-wide label, stale local output, or a green build cannot replace that comparison.

**Stage effect:** CFP-1 remains active (specification); CFP-2/3 remain blocked. The C01–C15 baseline stays provisional pending qualified rights disposition and final component/version/claim freeze. No legal clearance or customer shipping claim follows from this inspection.
