# CFP-1 public backend readiness and build recheck — 2026-09-20

Program gate: G8 — Environment-harness release evaluation. CFP-1 remains active (`specified`), CFP-2/3 blocked. This is a read-only, unauthenticated HTTP check of public endpoints, not a deployment, account test, database verification, billing-continuity proof or hosted-action acceptance. No credentials, customer data, payment state or setting were read or changed.

At approximately 16:32 UTC, `GET https://casimirbot.com/api/ready` returned HTTP 200 with JSON `status: ok`, `ready: true`, `appReady: true`, `artifactsReady: true`, `healthReady: true`, and null artifact/bootstrap errors. `GET https://casimir-bot-pestypig.replit.app/api/ready` returned the same ready flags and HTTP 200. `GET /api/agi/runtime-parity/fingerprint` on **both** hosts reported `execution_mode: compiled_production`, source/runtime/artifact commit `42c5b19b4be2a95d2d2de937ef9f550df8b6f2b4` and build time `2026-09-01T19:18:38.201Z`. This is the same deployed source identity observed in the [September 8 alignment audit](../../../work-packets/eh-g8-cfp1-code-deployment-alignment-audit-v1.md), not current Desktop HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` or its uncommitted CFP-1/NAV work.

`GET https://casimirbot.com/api/desktop-release/latest` returned HTTP 200 with `available: false`, `approved: false`, `reason: not_configured`. `GET /healthz` on both hosts returned HTTP 404 HTML rather than the JSON defined by the inspected current `server/index.ts`, while `/api/ready` succeeded. The 404 may involve the deployed revision or public routing; this read does not identify the cause. Future CFP-3/4 deployment acceptance must select and verify the supported health route and reject a static-page-only readiness inference.

This observation narrows the [September 20 Replit dashboard ambiguity](2026-09-20-cfp1-replit-deployment-and-usage-recheck-115.md): a backend API responded as ready **at this instant** on both hosts despite the earlier credits/offline banner. It does not establish continuous paid/trial availability, that the production database is durable or using the intended DSN, that Auth0/Google callbacks work, that any paid entitlement/room/guest action can be admitted, or that Replit credits/billing are reconciled. The readiness payload in inspected source is based on app/artifact flags; it is not a sponsor-action or database transaction probe. The trial must still not start unless the rights-cleared shared action is available under the final admission contract.

D07 still lacks paid/trial marginal cost; D08/C15 still lacks a signed public installer; D11 qualified returns and D12 freeze remain open. CFP-1 stays active (`specified`), CFP-2/3 blocked and G8 active.
