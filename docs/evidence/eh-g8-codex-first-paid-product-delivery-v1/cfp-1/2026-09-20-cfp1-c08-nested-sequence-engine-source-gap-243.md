# CFP-1 C08 nested sequence-engine source gap — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This read-only source and schema check extends the [C08 customer-channel admission evidence](2026-09-20-cfp1-c08-server-admission-source-gap-241.md). It neither runs a customer connector nor proves a native Baritone effect or any commercial right.

The outer `execute_sequence` request can name `requested_control_engine=native_fabric` while a nested `workflow_action` names `navigate_to` with `engine_preference=baritone`. `shared/helix-minecraft-fluid-sequence.ts:347–355` uses the general player-action schema for each workflow node; `shared/helix-minecraft-player-capabilities.ts:165–180` permits the Baritone preference. The broker checks the sequence arguments with that schema at `server/services/environment-connectors/actions/action-broker.ts:3352–3362`; the source check found no separate nested-engine restriction there. On the client, `FluidSequenceEngine.java:542–555` selects `baritone` from the nested preference and constructs the child `ActionRequest` with it. That dispatch is separate from the outer request's engine field. Other account, room, owner, manifest, action and native checks still apply.

A one-off `npx tsx -e` schema fixture constructed a three-node `survival_tas` sequence with native scheduler, one `navigate_to` workflow action whose preference was `baritone`, and success/failure terminals. `helixMinecraftFluidSequenceArgumentsSchema.safeParse` returned `{"accepted":true,"issues":[]}`. This is a **schema result only**; no broker, native execution, profile, customer build or mod was run.

SHA-256 at this read: player-action schema `191701A475E4CFE516835F91037004DA11A2E29016212D69B1D2C03DDF02E2D7`; fluid-sequence schema `B8B5C6991B23A5C1AE6724C711F83C6DE7C95733CA6A8000160627213BB46356`; `FluidSequenceEngine.java` `02B9A57D130A44C9629B9254B08EE6B55422013A464765B89034F89B13DD5227`; broker `8F59C420CBB813FB52532B9C11384C908B38A20CEC8C0F72812CB5DD7C0C93BA`. The broker is concurrently modified in the shared worktree; these hashes identify this read only.

**Release-acceptance consequence:** The customer-channel C08 exclusion must inspect engine selection in nested accepted operations as well as the top-level manifest and ordinary request field. If `execute_sequence` is unavailable in the selected customer profile, prove a typed denial before any child action. If it is supported, a schema-valid nested Baritone preference must be denied or safely constrained to an allowed native engine under a reviewed contract **before child start**, with truthful result/evidence and supported native sequences preserved. Test both isolated absent/present-Baritone profiles and a direct/forged sequence request with outer native engine and nested Baritone preference. The same inventory principle applies to any other composite action with embedded player arguments; this note establishes this specific sequence path, not an uninspected claim about every composite executor.
