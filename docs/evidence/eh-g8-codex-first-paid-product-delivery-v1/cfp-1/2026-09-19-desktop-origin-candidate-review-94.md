# CFP-1 desktop package-origin candidate review — 2026-09-19

The [C02/C03 package-origin candidate](2026-09-19-desktop-lockfile-old-asar-candidate-93.md) and its [50-row CSV](2026-09-19-desktop-lockfile-old-asar-candidate-93.csv) compare the current 49-entry non-dev desktop lockfile with an older local ASAR/unpacked candidate. They identify 26 old packaged manifests, including a nested package absent from the current lock topology, the top-level `semver` version mismatch, one package with no top-level license-like file, and three old native sharp DLL/add-on hashes. The [component rights submission](../../../work-packets/eh-g8-cfp1-component-rights-review-submission-v1.md) now includes this as reviewer input.

An independent read-only reviewer reproduced the source and archive hashes, counts, package/manifest labels and versions, all 25 captured license/notice hashes, unpacked-presence flags, the `lazy-val` top-level-text gap and all three native binary hashes. The review returned **PASS** for the bounded evidence claim and links. It did not review legal obligations or any signed customer artifact.

The environment documentation audit returned `ok: true`, local links resolved and `git diff --check` passed on affected tracked packets. The inventory still lacks bundled JS/service origin, Electron/Chromium and the actual reserved release cohort; qualified C02/C03 rights dispositions and CFP-3 extraction remain required. CFP-1 stays active (`specified`), CFP-2/3 remain blocked and G8 remains active under the [work program](../../../helix-environment-harness-work-program-v1.md).
