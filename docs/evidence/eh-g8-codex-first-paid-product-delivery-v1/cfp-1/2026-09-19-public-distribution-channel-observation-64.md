# CFP-1 public distribution-channel observation — 2026-09-19

Status: read-only public endpoint and source inspection at 20:46 UTC, with local HEAD and remote `main` both `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`. No GitHub repository setting, release, domain metadata, install, build or feed changed. This snapshot covers **publicly observable routes only**; it cannot enumerate draft/private releases, manually shared installers, dormant installations or customer machines.

| Surface | Direct observation | Scope of conclusion |
| --- | --- | --- |
| [GitHub source repository](https://github.com/pestypig/casimirbot-) | Anonymous GitHub page labels `pestypig/casimirbot-` **Public**. `git ls-remote origin HEAD refs/heads/main` resolves both to the inspected HEAD. | The source repository is currently publicly readable. A later private-source decision cannot retract earlier public copies or grants. This does not select a future repository policy. |
| [Public GitHub Releases page](https://github.com/pestypig/casimirbot-/releases) | Anonymous page says “There aren’t any releases here.” A read-only tag lookup found `refs/tags/casimirbot`; a Git tag is not an approved downloadable desktop release. | No **public GitHub Release** is visible at inspection time. Draft/private releases or installers distributed by another path are not ruled out. |
| `GET https://casimirbot.com/api/desktop-release/latest` | HTTP 200 returned `{"schemaVersion":"casimir_desktop_release_status/1","available":false,"approved":false,"reason":"not_configured"}`. `GET /download` returned HTTP 200 HTML. | The observed domain release API advertises **no approved installer** at inspection time. A reachable landing page is not proof of a downloadable or signed product. The local `client/src/pages/download.tsx` shows the download link only for validated release metadata and otherwise displays “No approved installer yet.” Browser rendering and alternate deployment paths were not accepted here. |
| Current builder/site source | `apps/desktop/electron-builder.config.cjs` still points publishing at `pestypig/casimirbot-`; the [C15 route proposal](../../../work-packets/eh-g8-cfp1-customer-distribution-route-decision-v1.md) traces the updater, site metadata and server validator to that repository. | Changing only the web link will not redirect an installed updater. The selected new route needs a reviewed compatible client/site/feed change and exact signed acceptance. |

The next inventory is **not** “assume zero users” from an empty public Releases page. Before freezing an old-channel bridge, the product/release owner must inspect any prior installer delivery channels and records, the local alpha builds' actual distribution, installed update configuration/version and active/dormant population without collecting unnecessary personal data. If any supported old install could query the current GitHub feed, CFP-3 must provide a signed forward bridge and a manual same-or-newer repair path. If the owner establishes and independently reviews that no such install exists, record that evidence and omit a bridge that has no recipient; still prove a clean new-channel install/update and source-independent download. Unknown population fails the decision rather than silently taking the zero-install branch.

This observation changes neither the provisional C01–C15 treatments nor the unselected public binary-only GitHub-versus-HTTPS channel choice. It supports the truthful current claim “no approved public installer is available through the observed domain API/GitHub Releases paths,” not “the app has never been installed” or “the repository can safely be made private now.” CFP-1 remains **active (specification)**; CFP-2/3 remain blocked.
