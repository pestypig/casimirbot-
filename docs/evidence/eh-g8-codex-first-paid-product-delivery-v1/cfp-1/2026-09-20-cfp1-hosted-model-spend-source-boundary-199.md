# CFP-1 hosted model-spend source boundary — 2026-09-20

Read-only source inspection at HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` in the shared Desktop checkout. No runtime, provider credential, account, billing or production setting changed.

| Current source | Bounded finding | Offer implication |
| --- | --- | --- |
| `shared/helix-account-session.ts`, `server/routes/agi.realtime-room/http-context.ts` | The ordinary `user` policy locks `shared_realtime_rooms`; room routes check that flag. The `developer` policy includes it. | Enabling current room access by subscription is not an existing public entitlement. Do not promote purchasers to `developer` or equate an experimental-room switch with hosted admission. |
| `shared/helix-shared-realtime-room.ts`, `server/services/helix-ask/realtime-room/room-store/repository.ts`, `server/routes/agi.realtime-room/runtime-routes.ts` | The current Shared Realtime Room fixes two participants. Its readiness requires microphone-to-room, microphone-to-model, transcript-to-room and model-audio-output consent; runtime reservation defaults to `gpt-realtime-2.1` and binds the owner's GPT Live session. | Existing room readiness and one-model runtime do not implement the selected initial hosted collaboration path without funded inference. Room membership/source/consent primitives may be reuse candidates only behind a distinct, reviewed admission/readiness contract. |
| `server/routes/agi.realtime-session.ts`, `server/services/helix-ask/realtime-session/adapter.ts`, `server/services/helix-ask/realtime-session/sdp-transport.ts`, `apps/desktop/src/provider-credential-broker.ts` | SDP exchange selects a configured `OPENAI_API_KEY` or the loopback desktop broker. The direct path calls the OpenAI Realtime calls endpoint; the broker's SDP path also requires an OpenAI API key and calls that endpoint. Without either configured route, exchange is denied. | The broker is a credential transport, not proof that a customer's Codex App subscription funds a GPT Live call. A first-offer flow that starts this path would violate the selected no-funded-inference boundary or require a separately selected, costed provider-access mode. |
| `shared/helix-account-session.ts`, `shared/helix-language-model-policy.ts` | Existing `model_tokens_per_turn`/`model_tokens_per_day` values shape model policy. | These are model-token quotas, not the proposed sponsor-term platform-budget-token (PBT) ledger or a cost cap for hosted collaboration. |

The [selected product goal](../../../architecture/casimirbot-environment-harness-product-goal-v1.md#commercial-delivery-direction) excludes CasimirBot-funded inference, including managed GPT Live, from the first offer. The current room's model/voice path remains a separate G8 capability and later commercial decision; this inspection does not assert it is live-enabled for public users or priced. The [new handoff](../../../work-packets/eh-g8-cfp1-hosted-provider-spend-and-room-seam-v1.md) requires a selected hosted collaboration admission path, provider-call denial/absence fixtures and separately costed PBT resource inputs before D07/D12 freeze. It does not close D07, D11 or D12.
