# CFP-1 authenticated GitHub channel inventory — 2026-09-20

Program gate: G8 — Environment-harness release evaluation. CFP-1 remains active (`specified`), CFP-2/3 blocked. This is a read-only inventory through the connected GitHub account, not repository creation, publication, source-visibility change, name reservation, rights clearance or installed updater acceptance.

The connected GitHub profile identifies the `pestypig` account. `github_get_repo` returned repository `pestypig/casimirbot-` with numeric owner ID matching the connected profile, `visibility: public`, default branch `main` and `permissions.admin: true`. The Desktop checkout's `origin` URL points to that repository. This verifies current connected-owner control and public source visibility at this checkpoint; it does **not** authorize privatization or change existing grants, and it does not establish a customer binary feed.

Authenticated exact-name `github_get_repo` calls for `pestypig/casimirbot-desktop-releases` and `pestypig/casimirbot-connector-kit` each returned GitHub API 404. The connected owner-filtered repository listing returned four accessible repositories and included neither proposed name. These are **access observations**, not global proof that a private repository cannot exist through another installation, that either name is reservable, or that either public channel has been provisioned. The prior anonymous 404 for the desktop target likewise proved only absent public access. No repository, release, tag, file, permission or setting was created or changed.

For D08/C15, retain `pestypig/casimirbot-desktop-releases` as the **proposed exact repository target under the selected separate public binary-only route** and require the release owner to provision or verify it, confirm authenticated control and anonymous asset/notice access, then rehearse the old-feed bridge/manual repair under the [distribution contract](../../../work-packets/eh-g8-cfp1-customer-distribution-route-decision-v1.md). For D09/C10, retain `pestypig/casimirbot-connector-kit` as an unprovisioned archive-first planning identifier and require analogous control/public-access verification plus separate package-scope, rights and clean-install proof under the [artifact decision](../../../work-packets/eh-g8-cfp1-c10-public-kit-artifact-decision-v1.md). Neither target can be named as an existing download in customer copy. Existing source-publication rights and future private-source treatment require their own qualified review; a new binary repository would not erase prior public grants.

D08/D09 remain provisional, D07/D11/D12 remain open, CFP-1 stays active (`specified`), CFP-2/3 blocked and G8 active.
