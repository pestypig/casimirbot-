# CFP-1 client-public media Git provenance leads — 2026-09-19

Status: read-only R-ASSET-01 provenance lead inspection at 21:17 UTC, local HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` with a dirty shared worktree. This supplements the [desktop media byte crosswalk](2026-09-19-desktop-media-byte-crosswalk-13.md); it is **not** a creator attestation, license grant, qualified rights opinion, exact signed-installer inventory or customer claim. No source, build, runtime, distribution or production state changed.

The crosswalk identifies 36 media/model/audio paths whose inspected `dist/public` bytes matched `client/public`. All **36 current source files** still match those crosswalk SHA-256 values. The [per-file Git trail](2026-09-19-client-public-media-git-provenance-leads-70.csv) records each source path, matched-byte hash and first recorded add commit/author/date/subject from `git log --follow --diff-filter=A`. Its SHA-256 is `CB3FFE90FCF7C2255B31F63B4817111632231FFD68E6A01EDAD2ED2000DED385`. Every path has one located first-add record, all under author `pestypig`, across seven commits. The owner has separately asserted that the `pestypig` and `Danyle Bruce` author identities are theirs. A Git add author is evidence of repository custody, **not** evidence that the author created every image, model or sound or acquired commercial redistribution rights.

| Reviewer cluster | Files in the 36-path subset | Additional observed lead and unresolved question |
| --- | ---: | --- |
| Galaxy map | 1 | `client/public/README_GALAXYMAP.txt` calls for a map image “provided by the user.” Identify that actual image's creator/source, any original data or licensed artwork, and the owner's right to include it commercially. The readme is not the permission record. |
| Luma model and portrait | 2 | `luma/Butler.glb` has glTF generator `Khronos glTF Blender I/O v3.2.43`; `luma/Luma_29.png` entered in the same 2025-08-11 commit. The generator names the exporter, not the model or image creator. Obtain original design/source or license and branding rights. |
| Ellipsoid fixture models | 2 | Both `needle-ellipsoid*.glb` assets have glTF generator `codex-ellipsoid-fixture`; the repository's `tests/ellipsoid-fixtures.spec.ts` and visualization roadmap identify them as fixtures. Preserve their generation/origin evidence and verify any imported mesh or material inputs before first-party treatment. |
| Luma mood SVGs | 7 | The local emote README names the fixed file slots but gives no creator, commission or license evidence for the SVG bytes. Obtain the original artwork/source and permitted product use. |
| SurfaceKit worlds | 14 | Seven desktop and seven mobile SVGs were introduced in one 2026-05-15 commit. Confirm artwork generation/source, any borrowed textures or icons, and commercial use for each selected world. The shared commit is a grouping lead, not a shared license. |
| Sound and impulse responses | 5 | Three `kb-textures/irs/*.wav` and two `originals/default/*.wav` source files are selected by the broad client build. Local manifest/code show use but no recording, sample-library, performer, generator or license record. Obtain those per-file origins or exclude through a reviewed build/input change. |
| Brand/UI assets | 5 | `icons/helix-arrow.svg`, `icons/helix-icon.svg`, its 192/512 PNGs and `loading/helix-loading-mark.svg` need creator/brand-chain evidence. The separately generated Windows icon derived from `helix-icon.svg` is C14 and needs a source-to-generated output check in CFP-3. |

The counts above total **36**. The other 64 media crosswalk rows are package-derived KaTeX fonts and Tree-sitter WASM and retain their separate package/license/notice review; bundled JS/CSS, maps, native binaries, raw `public/` files outside the extension filter and separately provisioned JARs remain outside this inspection. `stage-runtime.mjs` copies all of `dist/public`, so a source file's apparent lack of UI use does not prove it is absent from a later packaged client.

For each retained client-public item, the owner should return `creator or generation source`, `original input/source link or private record`, `commission/employment or other contributor terms if any`, `commercial binary and web redistribution permission`, `attribution/notice`, `brand/model/audio restrictions`, and `include/exclude/replacement decision`. A qualified reviewer then maps that evidence to the selected C03/C14 artifact and claims. Any unknown origin requires evidence, replacement or exclusion through a reviewed build change; it cannot be cleared by Git authorship or a generated-file label alone. CFP-3 must independently inventory the exact signed client/EXE bytes before shipping. CFP-1 remains **active (specification)** and CFP-2/3 **blocked** under the [work program](../../../helix-environment-harness-work-program-v1.md).
