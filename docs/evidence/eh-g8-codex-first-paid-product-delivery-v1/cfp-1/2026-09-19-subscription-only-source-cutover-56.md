# CFP-1 subscription-only source cutover check — 2026-09-19

Status: read-only source inspection at `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` in the shared Desktop checkout. This is a CFP-1-to-CFP-3.COMMERCE implementation handoff, not a Stripe account read, charge, runtime change, active SKU, installed acceptance or stage promotion. The [selected offer](../../../work-packets/eh-g8-cfp1-product-rights-and-offer-contract-v1.md) keeps supported personal MCP use free and sells a recurring hosted subscription with a separate seven-day no-card action-inclusive trial. The reported $5/$10 Stripe presets are owner-reported; this audit did not verify either Price object's ID, amount, currency, recurrence or mode. The available Stripe dashboard opened at sign-in, so no account-specific settings were read.

| Inspected source | Current behavior | CFP-3 contract consequence |
| --- | --- | --- |
| `shared/helix-billing-entitlement.ts` | Checkout target enum contains `billing_checkout:plan:starter_monthly` **and** `billing_checkout:prepaid:500`; the status schema publishes included/prepaid/available credit balances. | Freeze a hosted-only customer schema/target and keep the historical credit schema in a clearly developer-only compatibility surface. A public hosted price must not be inferred from these target names. |
| `server/services/helix-account/stripe-sandbox-client.ts` | `resolveStripeSandboxConfig()` requires **both** plan and prepaid `price_` IDs even when `createCheckout()` selects subscription mode. The plan request writes `casimir_included_credit_minor=1000`; the other target uses one-time payment mode. | Prove subscription checkout remains available when no prepaid Price ID is configured, and cannot request the historical prepaid target. Remove included-credit metadata from the new hosted SKU. Preserve test-mode and idempotency protections for sandbox acceptance. |
| `server/routes/stripe-sandbox-webhook.ts` and `billing-entitlement-store.ts` | Paid checkout is normalized into either `plan_activated` with an included-credit amount or `prepaid_credit_purchased`; the store enforces the `starter_monthly` credit ceiling, posts ledger amounts and reports balance. `charge.refunded` refers to a credit entry. | Introduce a versioned hosted term/trial projection keyed to verified account and selected SKU, with event dedupe/order and refund/expiry meaning frozen by CFP-1. Do not derive room eligibility from a credit balance or checkout return. The no-card trial needs its own authoritative start without checkout or credit posting. |
| `server/routes/desktop-auth0-step-up.ts`, `server/routes/installed-account-services.ts` and `client/src/components/workstation/InstalledServicesPanel.tsx` | Billing step-up/status require an installed desktop session and `developer` account with `installed_service_management`; the panel labels Stripe sandbox billing, shows credits and offers a prepaid button. | Preserve developer access to the sandbox diagnostic surface. Create the separately selected ordinary `user` hosted purchase/status/portal route with server-side owner policy and appropriate step-up. Direct requests by another profile, guest or unauthenticated caller must fail. Customer copy must show term, trial and limits without credit or model-allowance language. |

The currently recognized webhook event set and status projection are scoped to the sandbox ledger. CFP-3 must define positive and negative transitions for the **selected** paid term, scheduled versus effective cancellation, failed renewal, refund/dispute, account switch, same-identity trial return, stale/duplicate/equal-time events and service outage before mapping them to hosted eligibility. The selected one-host sponsorship, explicit eligible-host handoff and per-program owner grant remain independent of that billing state; neither payment nor trial grants membership or a native effect. The [hosted participant lifecycle](../../../work-packets/eh-g8-cfp1-hosted-participant-entitlement-lifecycle-v1.md) and [CFP-3.LICENSE](../../../work-packets/eh-g8-cfp3-license-enforcement-v1.md) own that composition.

Required negative cutover fixtures: a never-subscribed user completes all selected personal operations; a trial starts with no Stripe checkout/card/credit ledger entry; a selected paid subscription can be offered with prepaid configuration absent; old `billing_checkout:prepaid:500` cannot be reached from the public hosted flow or by direct public API; a success redirect or `starter_monthly` ledger balance grants no hosted room/action; a guest cannot manage the sponsor's subscription; hosted expiry leaves independently authorized personal work and stop/revoke available. CFP-3 should retain the existing sandbox tests as legacy regression evidence and add new tests for the frozen hosted contract, then run an attended deployed sandbox lifecycle and installed customer-path acceptance in their proper stages.

Open CFP-1 inputs remain the actual Stripe test/live Price metadata and fees, costed single-SKU choice, paid room/session limits, exact trial start/expiry/refund/outage terms, qualified rights disposition and selected action/context register. No live Stripe setting or source code was changed by this inspection.
