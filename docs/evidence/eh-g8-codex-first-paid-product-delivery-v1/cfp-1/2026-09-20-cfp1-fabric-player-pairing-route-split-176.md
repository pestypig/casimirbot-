# CFP-1 D01/C07 Fabric player pairing-route split — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`), CFP-2/3 blocked. Read-only source audit at HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` in the shared dirty Desktop checkout; no pairing, database, JAR, installed runtime or release was changed. This corrects the route assumption in the earlier [three-version crosswalk](2026-09-20-cfp1-fabric-player-package-identity-crosswalk-161.md), which remains an immutable point-in-time inventory of distinct labels.

| Path | Observed source flow | Consequence for first-customer proof |
| --- | --- | --- |
| Selected same-host player P03 | Full MCP `helix_environment_player_pair_local` calls `createConnectorBootstrapPairing` with `actionCredentialRequested: true`, then stages `/helix-player pair <one-time code> <local endpoint>` (`server/mcp/helix-mcp-server.ts`, current source around 3601–3634 and 8025–8070). `HelixFabricPlayerAgentClient` passes `PlayerActionRuntime.ADAPTER_VERSION` (`0.4.11`) to core `ConnectorPairingClient.redeem`, which posts `connector_version` to `/api/environment-connectors/v1/pairing/redeem`. The public route calls `redeemConnectorBootstrapPairing`, which verifies the expected action connector kind and issues an action credential. | This path uses a bootstrap pairing code, action-authority/installation identity and subsequent action manifest. It does **not** send the directory `package_version_id` or execute the directory `/pairing/start` → `/pairing/claim` flow in the inspected path. The current `0.4.0` directory label therefore cannot itself prove or block this local player pairing. |
| Separate connector directory/device pairing | Public `/pairing/start` accepts `package_version_id` and calls `startEnvironmentConnectorPairing`; `/pairing/claim` calls `claimEnvironmentConnectorPairing` (`server/routes/environment-connector-platform.ts`, around 349–427). `pairing/service.ts` uses the code-owned built-in package record and attempts immutable package-row insertion with `ON CONFLICT ... DO NOTHING`. | Persisted-row equality and the `0.4.0` package's descriptor/content hash matter if this **separate directory path** is selected or advertised. They cannot be silently substituted for the selected P03 bootstrap/JAR/manifest proof. |
| Public directory listing | GET `/api/environment-connectors/v1/directory/packages` invokes `listPublicEnvironmentConnectorDirectory`; that function seeds built-in package rows and selects rows in active/deprecated/withdrawn/revoked states (`server/services/environment-connectors/directory/index.ts`). | If the first-customer service retains this public route and unfiltered built-in row, `0.4.0` has a separate public catalog/trust claim even though P03 does not use it. D12 must review its persisted bytes and wording or explicitly exclude/filter it from the customer surface. |
| Player action runtime | `PlayerActionRuntime.manifest()` sends `helix.environment_action.connector_manifest.v1`, adapter version `0.4.11`, protocol and capability descriptors. `action-broker.ts` stores/checks an action capability catalog snapshot tied to the admitted manifest and binding; it is distinct from the built-in package-directory row. | Freeze the planned **bootstrap → installed JAR → submitted action manifest → action catalog snapshot** relation, selected capability subset and exact-denial fixtures. Actual signed/installed JAR hash, embedded core bytes and accepted manifest remain CFP-2/3 evidence. A source version string or runtime self-report alone cannot attest JAR bytes. |

Relevant SHA-256 at this read: MCP server `3EF75F150C797888A7B3984CD6B85289823ABAAEFDDE632AD68D6CA02658965E`; public connector route `B56A642E1A4A9C5E20618EAE5D31689AF36705D86ECFCCF26795ACB3B146EA01`; directory listing service `858B4CC50F99C21CF22CD88DE35E80898D1A831D854109AA964AD7F06352AEFB`; directory pairing service `DE9AC7F5AC14BD9D5F50A4088858A3CFBA328B8031C2DECC4D65F4D57C67EBD5`; bootstrap service `B77D13D3A3A77E7683A51AB7C8F1D16F1DA12B7F3F0617FD1BEB80C2DF22F8A0`; action broker `8F59C420CBB813FB52532B9C11384C908B38A20CEC8C0F72812CB5DD7C0C93BA`; player client `3692A0E38447566FDC83A3847B13B4D4EFCCFAFE9EE6ECB2C85D93C28BF53EBD`; core pairing client `7B5A94F08DC0180F4A1C142F7C2F1D1EC95ADBD18A7C29B0C29E44F3B9EAED7F`.

**Stage consequence:** Rewrite the D01/C07 planned identity and FP fixtures to follow the actually selected bootstrap route. Directory-package/persisted-row fixtures apply to a retained public directory listing or separately selected directory pairing; the currently public unfiltered listing requires an explicit retain-and-review versus exclude/filter decision before release. The current `0.4.12` mod and `0.4.11` adapter labels may be independently versioned, but no signed-byte compatibility or ordinary-user effect is accepted here. Component/commercial rights and selected customer copy remain open.
