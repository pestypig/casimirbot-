# CFP-1 Fabric identity contract independent review — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This is an independent read-only review of the D01/C07 specification and source evidence, not an installed package, rights or commercial acceptance.

An independent reviewer compared the [identity contract](../../../work-packets/eh-g8-cfp1-fabric-package-runtime-identity-contract-v1.md) and [source recheck 165](2026-09-20-cfp1-fabric-identity-admission-gap-165.md) to the current pairing catalog, pairing service, action broker, parity test and player/sensor/core build scripts. The first pass identified one C07 omission: the player and sensor JARs each embed `com.casimirbot:helix-minecraft-connector-core:0.2.0`, but the planned tuple named only the mods. The contract was corrected to cover that library coordinate/version, nested-byte hashes and provenance in the tuple and ID-01/ID-06. A follow-up found that evidence 165 needed the sensor build-script hash to pin its source claim; that hash was independently verified and added. The final reviewer verdict was **PASS**.

The reviewer confirmed the distinct package/mod/manifest version claims, `ON CONFLICT ... DO NOTHING` persisted-row drift gap, runtime manifest/catalog checks without JAR-byte attestation in the inspected paths, fail-closed rule as a **proposal**, ID-01–06 fixture coverage, rights/stage holds and local links. The focused `npx vitest run server/__tests__/environment-action-profile-catalog-parity.test.ts --pool=forks` passed four source-parity tests. That test does not cover persisted database equality, nested/installed JAR hashes, actual received manifests or ordinary-user pairing.

`npm run helix:environment-harness:docs-audit` returned `ok: true` with zero failures; `git -c core.safecrlf=false diff --check` and relative-link checks passed. The exact customer tuple, database row, signed companion bytes, D01/C07 rights and R-MC-01 commercial treatment remain unresolved. No runtime, production, account, payment or repository setting changed.
