# CFP-1 Fabric player package identity crosswalk — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This read-only D01/C07 check extends [source-version recheck 140](2026-09-20-cfp1-personal-operation-source-version-recheck-140.md); it does not change the connector, pair a player, accept a signed artifact or clear commercial rights.

At source HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` in the shared dirty Desktop worktree, SHA-256 was `EAC21073722D3DBEC0174FB38B3588A5E0CDE71707B8AB6BDF3286D0CEF2DFAD` for `server/services/environment-connectors/catalog/index.ts`, `DE9AC7F5AC14BD9D5F50A4088858A3CFBA328B8031C2DECC4D65F4D57C67EBD5` for `server/services/environment-connectors/pairing/service.ts`, `DB31F0322A705E2385889819E299974ED1398F8924BA45042F95DD26A545BE8E` for the player `build.gradle.kts`, and `E21D66F4744A10B56EE539183FDD3314CD6DD842AA5C97C45BE991B17288700C` for `PlayerActionRuntime.java`.

| Identity layer | Current read-only observation | Acceptance meaning |
| --- | --- | --- |
| Pairing-directory package | `catalog/index.ts` registers `connector_package_version:com.casimirbot.minecraft.fabric-player:0.4.0` with `packageVersion: 0.4.0`. `pairing/service.ts` looks up that exact ID and rejects unknown IDs. Its `packageContentHash` covers package ID/version, adapter profile, host compatibility and capability descriptors. The service **attempts to insert** a `first_party`/`approved` row, but uses `ON CONFLICT (package_version_id) DO NOTHING`. | This is the server-owned **capability/pairing package record**. Its content hash does not include JAR bytes, and its approval field alone is not a release-rights or installed-binary attestation. An existing database row may retain an older hash/descriptors under the same `0.4.0` ID; current database equality is unverified. |
| Local player mod build | Player `build.gradle.kts` sets `version = "0.4.12"`; `fabric.mod.json` substitutes that version. The current local `build/libs/HelixFabricPlayerAgent-0.4.12.jar` reports mod ID `helix_fabric_player_agent`, version `0.4.12`, SHA-256 `9E9B6A83CEE0F995FC9F0A44B4B35196D32F5D54BFFCF4E12DE47B38C7EA3B29`. | This is an **unsigned, mutable development output**, not a selected or rights-cleared release JAR. It cannot be inferred from the catalog package `0.4.0` record. Rehash on any later installed or signed cohort. |
| Runtime action manifest | `PlayerActionRuntime.java` reports `ADAPTER_VERSION = "0.4.11"` in its action manifest. [Recheck 140](2026-09-20-cfp1-personal-operation-source-version-recheck-140.md) records its schema/protocol and the separate sensor/core versions. | This is the executing **action-adapter claim** in source, not the mod package version or pairing package ID. It may be intentional independent versioning or stale metadata; the present read does not decide. |

The current parity test `server/__tests__/environment-action-profile-catalog-parity.test.ts` asserts the `0.4.0` catalog row and descriptor/profile parity, but does not compare that row to the local `0.4.12` JAR or `0.4.11` runtime manifest. The pairing start route accepts a caller-supplied `package_version_id`; the inspected Java player source did not show this catalog ID as a constant. No reviewed catalog→artifact→manifest compatibility map or signed-byte linkage was found in these inspected paths. That is an **evidence gap**, not proof that current pairing fails or that the three values must be equal.

For D01/CFP-2 and C07/CFP-3, freeze a planned release tuple containing the code-owned catalog package ID/version/content hash, planned player/sensor/core mod IDs/versions, action and observation manifest schema/protocol/adapter versions, selected game/Fabric/Java versions, client/node versions and connector epoch. CFP-2/3 then bind actual installed/signed JAR and manifest hashes to that tuple. An explicit reviewed mapping may permit different numbers; otherwise align the source metadata and rebuild. Before relying on the catalog, compare the **persisted database row** and its content hash/metadata to the reviewed code-owned tuple; if they differ, version/migrate the row under a reviewed rule or fail closed. Test that the actual selected client/JAR can pair against the intended persisted catalog record, advertise only the approved capability subset, admit the selected personal read/mine cases, and reject a swapped or stale JAR/manifest under the same catalog label. Retain the local-output observation only as a development baseline. R-MC-01 and C07 rights review must cover the **actual proposed bytes and delivery route**, not infer permission from the server catalog's `approved` trust classification.
