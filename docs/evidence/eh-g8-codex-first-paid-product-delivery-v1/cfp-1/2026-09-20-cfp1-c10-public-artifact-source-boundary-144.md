# CFP-1 C10 public artifact source boundary — 2026-09-20

Program gate: G8 — Environment-harness release evaluation. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This is a read-only source audit and conditional product selection at HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`, not a package build, publication, rights clearance or installed acceptance.

The current `connectors/environment/` tree has eleven tracked files and no local package manifest, license/notice file or standalone export pipeline. `contract/v1/index.ts` re-exports all 71 names from four shared modules; [the selected ledger](../../../work-packets/eh-g8-cfp1-c10-public-symbol-boundary-v1.md) targets only 43 and defers 28. The four modules have state-snapshot and sensor-scope dependencies; selected TypeScript schemas use `zod`, and the helper uses Node `crypto`, `fetch`/`Response` and `Buffer`. The current root README includes a private keyed-launcher procedure. Copying the tree verbatim would expose extra symbols and internal instructions. `package.json` declares MIT for the root, but there is no tracked root `LICENSE`/`NOTICE`; the C10 package and first-party/prior-publication rights still need explicit review. Installed `zod` 3.25.76 has its own MIT license/notice, which the final archive must handle.

The [artifact decision](../../../work-packets/eh-g8-cfp1-c10-public-kit-artifact-decision-v1.md) chooses a **planning target**, not a created publication: a dedicated `pestypig/casimirbot-connector-kit` GitHub Releases channel, installable `@casimirbot/environment-connector-kit` `0.1.0` tarball, generated 43-name contract, reviewed helpers/examples, standalone non-game mock/report, public guide and exact manifest/notices. Repository and package-scope control are unverified, and npm registry publication is not selected. The wire contract `v1` and registered system-clock package `1.0.0` are separate versions. CFP-3 must verify final channel, rights, runtime/compiler support, hashes, parity and clean external installation before any public claim.

Two source findings narrow the acceptance claim. `server/services/environment-connectors/pairing/service.ts` admits packages through `readBuiltinEnvironmentConnectorPackage` and otherwise returns `pairing_package_unknown`; `catalog/index.ts` registers `com.casimirbot.system.clock:1.0.0` with `node:20+` compatibility. Independent developers may test the registered read-only starter, but current source does not prove arbitrary third-party registration. Separately, `sdk/typescript/index.ts` rejects a heartbeat unless `command_execution` equals `command_execution_not_enabled`, while `conformance/mock-helix.ts` returns only `health`; current conformance uses a Minecraft manifest fixture. The future public package must repair and exercise those tests with a non-game fixture.

This audit changes no runtime, account setting, repository visibility, license or customer claim. The user has separately held external game-counsel outreach in [evidence 129](2026-09-20-cfp1-game-counsel-inquiry-owner-hold-129.md); no contact follows from this C10 selection. D09 still awaits rights and channel proof, D07 still awaits a costed enforceable offer, D11 qualified returns and D12 integrated freeze. G8 and the stage states are unchanged.
