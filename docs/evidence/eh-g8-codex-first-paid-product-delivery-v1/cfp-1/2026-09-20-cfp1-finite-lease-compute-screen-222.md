# CFP-1 D07 finite-lease compute-plus-database screen — 2026-09-20

## Source and scope

The operator-private sensitivity `2026-09-20-d07-finite-lease-compute-plus-database-sensitivity.md`, SHA-256 `1831F3F3A07AFE36639C8CF08899CB5B37D1F906FDBB9A6FCF8E0C0674FEED93`, combines dated private compute/database rates from the [completed Replit period](2026-09-20-cfp1-completed-replit-period-cost-source-173.md) with the [observed one-instance Autoscale configuration](2026-09-20-cfp1-replit-deployment-and-usage-recheck-115.md). [Replit documents 18 compute units per CPU-second, two per GB-second, request-work billing](https://docs.replit.com/billing/deployment-pricing), and a [five-minute production-database idle tail](https://docs.replit.com/billing/about-usage-based-billing). The screen assumes the configured instance is fully busy during each admitted lease, plus the conditional database intervals in [assay 218](2026-09-20-cfp1-cost-token-finite-lease-assay-218.md). Exact account rates, derived USD/PBT quantities and invoice details remain in access-controlled evidence.

It compares ten verified trial effects over ten or thirty separated visits at five- or fifteen-minute candidate leases; ten paid sponsors use the existing ten-/twenty-hour load samples, and thirty trial starts are included in the hypothetical $5/$10 public-fee cohort comparison. All modeled room intervals are arranged without overlap on **one shared project instance and database**, with otherwise idle B. Actual overlapping rooms share provider work; a different schedule requires recomputing the project union and sponsor allocation. The paid sample omits extra database tails. Neither visits nor paid hours are selected customer allowances.

## Result and limits

Under the declared full-busy stress, the hypothetical $5 comparator fails every compute-plus-database cohort probe and some $10 probes also fail before request fees, egress, storage, identity, transport, delivery, fixed plan, support, refunds, tax and free-personal allocation. The result is an adverse partial-resource screen, **not** likely utilization, a whole-offer margin or a selected price. Replit's public guide uses a GB-second unit while the deployment label uses GiB; the private calculation follows the earlier numerical configuration assumption. Verify the account meter convention before treating its compute row as a conservative bound. Also prove room-origin work stops with a finite lease and each operation has a finite deadline; otherwise an idle threshold cannot bound request compute.

The D07 evidence still needs a selected first-party browser guest/action B/P/T workload or sourced bounds for each missing input, actual account fee verification when available, an enforceable per-sponsor PBT and per-resource ceiling, cohort/tier sensitivity and qualified financial/privacy review. No runtime, provider, payment or production setting changed. CFP-1 remains active at `specified`; D07/D11/D12 remain open, CFP-2/3 blocked and G8 active.
