# CFP-1 D08 distribution-route selection consistency recheck — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. Read-only source and public-access check at Desktop checkout HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`; the shared worktree has unrelated edits. This corrects stale pre-selection language in the [C15 route packet](../../../work-packets/eh-g8-cfp1-customer-distribution-route-decision-v1.md), not the owner's [selected route type](2026-09-20-cfp1-recommended-direction-owner-selection-106.md) or the earlier [authenticated GitHub inventory](2026-09-20-cfp1-authenticated-github-channel-inventory-148.md).

`git remote -v` still points at `pestypig/casimirbot-`. `apps/desktop/electron-builder.config.cjs` still embeds GitHub publisher `pestypig/casimirbot-` with draft release type; `apps/desktop/src/updater.ts` still disallows downgrade. `apps/desktop/scripts/site-release-metadata-lib.mjs` and `shared/desktop-release.ts` still require that old repository for generated site metadata and immutable download URL validation. `.github/workflows/desktop-release.yml` still publishes via `GITHUB_REPOSITORY`, which is its source-workflow repository in the current arrangement. A new public artifact repository cannot be made active by changing a site link alone; CFP-3 must move publisher, updater, metadata validator, API and domain copy together and rehearse the installed bridge/manual repair.

An unauthenticated GitHub repository API request for `pestypig/casimirbot-desktop-releases` returned HTTP 404 at this check. The local `gh` CLI was unavailable. This establishes no **public** repository at that URL now; it does not prove name availability or absence of a private repository accessible through a different authenticated account. The earlier authenticated inventory found the proposed name absent from its connected owner listing. No repository or release was created.

The packet now states one consistent decision: the **route type** is owner-selected as a separate public binary-only GitHub Releases channel; `pestypig/casimirbot-desktop-releases` remains an exact **proposed target name**, not a provisioned feed or final retention/operator arrangement. The known local unsigned alpha and any other affected supported installs still require the [migration contract](../../../work-packets/eh-g8-cfp1-customer-distribution-route-decision-v1.md#required-migration-contract-for-either-route). D08 final channel/access and D11/D12 remain open. No runtime, repository visibility, account, billing or production setting changed.

An independent read-only reviewer compared selection 106, the corrected packet, canonical backlink and the current builder/updater/site/schema/workflow sources. It returned **PASS with limits**: route type and remaining exact-target/provisioning gap are now distinguished correctly; the old embedded feed and same-repository workflow remain current. The reviewer did not independently repeat the remote GitHub 404 check, and this review is not installed updater migration or release acceptance. The canonical work program's older pre-selection statements are dated checkpoints superseded by the later selected-route entries.
