# CFP-1 client-public copy-origin candidate — 2026-09-19

This read-only C03/R-ASSET-01 inventory uses source HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`, current local `dist/public`, `apps/desktop/runtime/dist/public`, `client/public`, `vite.config.ts` and local npm package files. Both output trees are **local candidates**, not one reserved signed customer cohort. The [636-row output manifest](2026-09-19-client-public-local-tree-candidate-95.csv) records path, byte length and SHA-256 for every local `dist/public` file, plus same-path source and desktop-staging equality. The [109-row explicit-copy map](2026-09-19-client-explicit-vendor-copy-candidate-96.csv) records the exact package-file source, package version/metadata license label, root LICENSE hash and byte equality for Vite's explicit vendor/WASM copy targets. A hash match is a provenance lead for these local bytes, not a rights disposition or future-build guarantee.

| Local candidate observation | Consequence for C03 review |
| --- | --- |
| `dist/public` has **636 files** and all **636** have byte-identical same-path files in `apps/desktop/runtime/dist/public`. The current desktop stager copies this entire tree. | The staged client is broad; the first customer build needs an extracted complete file manifest and an explicit accepted allowlist/claim boundary. This local equality does not establish what a later signed EXE contains. |
| **50** files are byte-identical at the same relative path under `client/public`, including the previously reviewed 36 media paths and 14 other static HTML/JS/JSON/text/manifest files. | The [Git provenance leads](2026-09-19-client-public-media-git-provenance-leads-71.md) cover only the 36 media paths. The remaining direct-static code/data files still need source/creator and customer-use review where retained. Git custody alone is not a license. |
| `vite.config.ts` explicitly copies **105** vendor files: one Plotly file from `plotly.js@3.0.3` and 104 Mermaid files from `mermaid@11.16.0`. It also copies **four** Tree-sitter WASM files from `web-tree-sitter@0.25.10`, `tree-sitter-typescript@0.23.2` and `tree-sitter-javascript@0.25.0`. All **109** local outputs match those exact `node_modules` paths and staged copies. | Reviewer can inspect the versioned package files and root LICENSE byte hashes in the CSV. Their local package metadata says MIT, but bundled/transitive content, notices, commercial use and accessible distribution treatment still require qualified review; the label alone is not clearance. |
| **477** output files have neither a same-path `client/public` byte match nor a mapping through these explicit Vite copy targets. The full tree includes 414 `.js`, 105 `.mjs` and two `.css` files; other evidence already traces some package-derived media/fonts. | Generate a reserved-build Vite/Rollup module-origin graph for bundled and transformed outputs, reconcile the [100-file media crosswalk](2026-09-19-desktop-media-byte-crosswalk-13.md), and map every retained file to its source/package and notice decision. A file absent from the two direct-copy mappings is not necessarily unowned or third-party. |

This inventory adds one large, directly identifiable package-copy boundary to the [desktop dependency/notice gap](2026-09-19-desktop-dependency-rights-evidence-gap-61.md); it does **not** establish the embedded module graph of generated chunks, package-internal third-party obligations, current online map/tile service rights or media creator permissions. The [component rights review submission](../../../work-packets/eh-g8-cfp1-component-rights-review-submission-v1.md) requests those dispositions. CFP-3 later compares exact signed-installer and staged client bytes, notices and source-offer treatment with that qualified result. CFP-1 stays active (`specified`), CFP-2/3 blocked and G8 active under the [work program](../../../helix-environment-harness-work-program-v1.md).
