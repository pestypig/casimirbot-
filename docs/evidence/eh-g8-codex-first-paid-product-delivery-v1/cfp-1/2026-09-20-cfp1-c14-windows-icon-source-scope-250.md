# CFP-1 C14 Windows icon source scope — 2026-09-20

This is a read-only source and local-output inspection, not a branding-rights opinion or signed-artifact acceptance. At HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`, tracked `client/public/icons/helix-icon.svg` has SHA-256 `E28C0F08D294BF8F0BF3E994779DDDD566E7D09F49BD9CFF89742531A96D527D`. `git log --follow` shows its introduction in commit `18ce6dd4b2d975111569f12f62840e1e25ad114c` by `pestypig` on 2025-12-19, alongside two small client PNG icon variants. The owner has separately asserted that the named author identities are theirs; a commit author or assertion alone does not establish originality or all distribution rights.

The inspected 12-line SVG consists of a local linear gradient and `rect`, two `path` and `circle` shapes. It has no embedded raster image, external URL/reference, imported font, script or license metadata. `apps/desktop/scripts/prepare-brand-assets.mjs` reads this SVG and uses `sharp` to generate `apps/desktop/dist/icon.png`; `apps/desktop/electron-builder.config.cjs` selects that PNG as `win.icon` for the Windows installer. The mutable local `dist/icon.png` was visually consistent with the vector and had SHA-256 `E08D6657E2986F943015B4A5920EE8BC64D09A0686E930E60183CF3F7015DAAE` at inspection. It is **not** a reserved or signed customer build, and the generation script's validation is not proof of brand ownership.

C14 qualified review can focus on SVG creator/source records, whether any outside brand reference or commission terms apply, commercial use of the mark, and the planned binary/web distribution. CFP-3 must reproduce and hash-bind the exact SVG-derived generated icon in the selected signed cohort, including source and output bytes. The broader C03 client PNG/media set remains separately itemized. C14 stays conditionally included; D11/D12 and CFP-1 remain open. No file, build, account, runtime or production setting was changed by this inspection.
