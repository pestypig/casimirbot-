# CFP-1 D02 account session and client-grant source gap — 2026-09-20

Read-only source checkpoint at `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93`. This is a specification input, not an installed-security or public-account acceptance.

| Source | Observed boundary | D02 implication |
| --- | --- | --- |
| `server/services/helix-account/account-session-store.ts` (`insertSession`, `signInWebAccountSession`, `getSessionRowById`) | The session insert accepts an optional expiry and stores `null` by default. Verified Google/Auth0 subject sign-in calls it without an expiry. Session lookup accepts `expires_at IS NULL`. A special guest-room path has a 24-hour expiry, but does not establish the public web-session term. | A browser cookie's expiry is not a finite authoritative server session. A public session policy must set and enforce server expiry for every admitted sign-in path. |
| `server/services/helix-account/session-cookie.ts`, `server/routes/google-auth.ts`, `server/routes/auth0-web-auth.ts` | The ordinary cookie defaults to seven days. Google and Auth0 routes set it after their sign-in receipt. | Seven days is a candidate matching the existing cookie behavior; it is not a server-side limit today. |
| `server/routes/google-auth.ts`, `server/services/helix-account/auth0-web-account-session.ts` | Google verifies an ID-token audience and requires a stable `sub`; Auth0 verifies an access token and maps its subject. They create CasimirBot sessions. | First-customer identity must bind the verified provider subject to a stable internal profile. Email equality and local profile selection cannot establish that binding. |
| `server/routes/desktop-auth0-step-up.ts`, `server/services/helix-account/auth0-step-up.ts`, `server/services/helix-account/auth0-step-up-receipt-store.ts` | Native device/session management uses a purpose-bound Auth0 receipt and currently requires developer access. Config defaults to a five-minute maximum authentication age and a two-minute receipt, bounded to five minutes. No equivalent ordinary-user Google management path is accepted by this read. | Public enrollment/recovery needs an ordinary-user, provider-compatible fresh-authentication path. Do not call the current developer route public support. |
| `server/services/helix-account/installed-security-store.ts` (`registerDevice`, `recoverDevice`) | Recovery advances `recovery_generation` and clears trust. Re-registering a revoked same-ID row restores `active` but does not advance that generation, although it clears full-harness trust. | Require a single recovery fence for all reactivation paths before issuing or accepting a new client grant. |
| P1 source and [stdio qualification packet](../../../work-packets/eh-g8-cfp1-p1-stdio-local-bridge-qualification-v1.md) | There is no accepted signed stdio command mode, IPC verifier or per-installation P1 grant yet. | Any numerical lifetime here is a proposed CFP-1 term and CFP-2 test input, not a current credential or working connection. |

The next decision is a finite web-session and separate client-grant lifecycle that preserves free personal access and reconnects without a manual token paste. Its exact values and step-up policy require account/security review; the source above cannot prove a specific duration safe by itself.
