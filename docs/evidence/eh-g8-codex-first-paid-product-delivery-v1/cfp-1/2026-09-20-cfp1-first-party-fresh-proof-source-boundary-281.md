# CFP-1 first-party fresh-proof source boundary — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This read-only working-tree source check was made against HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` with concurrent uncommitted development changes present. It narrows D02 alternatives; it is not an account/security review or installed authentication trace.

| Current source | Observed behavior | D02 implication |
| --- | --- | --- |
| `server/routes/account-session.ts` and `server/services/helix-account/account-session-store.ts` | Password sign-up/sign-in, verification and reset routes exist. Sign-up uses `publicProfileIdForEmail(email)` and a `password` credential; sign-in looks up that credential. | This is a first-party **password account**, not a challenge bound to a pre-existing Google-owned profile or to one sensitive-operation intent. Its existence does not satisfy Google-only P1 enrollment/recovery. |
| `server/routes/google-auth.ts` and `signInWebAccountSession` in `account-session-store.ts` | Google login verifies an ID-token `sub`, finds an explicitly linked provider subject if present and otherwise uses a `google:<sub>` profile ID. Email is profile data, not the authority to move a provider subject to an email-derived profile. | A matching email between password and Google sign-in is not an account link or fresh proof. A new password signup cannot silently gain the Google profile's devices, rooms or grants. |
| `server/routes/desktop-auth0-step-up.ts`, `server/services/helix-account/auth0-step-up.ts`, `shared/desktop-auth0-step-up.ts` | The current operation-bound receipt controller is Auth0 MFA based. Step-up start requires a developer account session and native desktop; subsequent native endpoints have their own intent/receipt checks and do not establish an ordinary-user public flow. | It is the implemented seam to qualify for ordinary users, subject to the factor-after-intent, linked-subject, enrollment/recovery and plan checks in D02/D07. |
| Focused `rg` over `server`, `shared`, `client/src` and `apps/desktop/src` TypeScript source, plus account migrations and `package.json`/`package-lock.json` | No server-side WebAuthn/FIDO2/TOTP authenticator or matching package was found in that scope. `client/src/components/ui/input-otp.tsx` is a generic input widget, not a verifier. | A first-party passkey/TOTP path is **new implementation and security-review work**, not a latent existing zero-provider-cost option. This bounded search does not prove no related code exists anywhere in the repository. |

**Qualification consequence:** keep the source-tested linked Auth0 MFA path first. If its plan, cost, reachability or recovery fails, compare a separately designed first-party authenticator or linked Auth0 database identity under the same Google-owned-profile binding, operation-time proof and recovery requirements. A password-account login, password reset email, generic OTP input, Auth0 passkey label or matching email cannot be counted as GP-02/GP-11 success. Price any alternative's build, maintenance, delivery and recovery/support load in D07 rather than entering a false $0 cost. The independent account/security owner must still accept the exact proof rule.

No source, account, provider, security, billing, runtime or production setting was changed by this read. D02/D07/D11/D12 remain open; no customer claim or stage advancement follows.
