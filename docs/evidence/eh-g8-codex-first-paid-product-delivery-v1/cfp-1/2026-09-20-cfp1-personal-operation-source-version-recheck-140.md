Program gate: G8 — Environment-harness release evaluation
Workstream: CFP-1.SCOPE / CFP-1.CLAIMS D01 source-baseline recheck
Capability or component: Selected FP-01–13 personal MCP operations and Minecraft connector identity
Lifecycle stage: personal admission, observation, execution and recovery specification
Reaction timescale: source baseline before CFP-2 fixture freeze
Authority owner: Product owner selects supported claims; MCP/connector owners establish version and installed compatibility
Current maturity: specified
Target maturity: specified with exact versioned fixture inputs and review
Required evidence: source registrations, catalog labels, package and manifest versions, installed cohort and rights disposition
Explicit non-goals: no runtime edit, JAR rebuild, installed acceptance, release claim or stage promotion
Downstream gate unlocked: none

# D01 personal operation source-version recheck — 2026-09-20

At source HEAD `cc5a7a4c1ac956606aea756f59e6fcb0324a9f93` in a shared dirty checkout, an independent read-only source audit found the FP-01–13 MCP names and four selected probe-kind mappings still present in `server/mcp/helix-mcp-server.ts` and `server/services/environment-connectors/catalog/index.ts`. Actor, inventory, local map, reachability and mine descriptors are `@1`; spatial-region is `@2`. These are **catalog capability labels**, not an executing connector or signed desktop version. The full MCP registration still requires room context for selected handlers; source registration and mocked handler tests cannot establish the free ordinary-user `personal_environment` path. `helix_minecraft_situation_probe` lacks the transition shadow used by some other selected names, so a shadow catalog is not proof of the FP-03–06 tools.

For this source observation, SHA-256 of the dirty `server/mcp/helix-mcp-server.ts` was `3EF75F150C797888A7B3984CD6B85289823ABAAEFDDE632AD68D6CA02658965E`; `server/services/environment-connectors/catalog/index.ts` was `EAC21073722D3DBEC0174FB38B3588A5E0CDE71707B8AB6BDF3286D0CEF2DFAD`; player `PlayerActionRuntime.java` was `E21D66F4744A10B56EE539183FDD3314CD6DD842AA5C97C45BE991B17288700C`; sensor `FabricConnectorRuntime.java` was `6B52B848540070540B665E0C966FA9699867E6203C213E2F624FD0575C8967E1`. These pin inspected source bytes, not a build or installed JAR.

The source version tuple is not one number. `apps/desktop/package.json` is `0.1.0-alpha.11` with Electron `43.4.0`. `minecraft/helix-fabric-player-agent/build.gradle.kts` sets package `0.4.12`, while `PlayerActionRuntime.java` reports action manifest `adapter_version: 0.4.11`, schema `helix.environment_action.connector_manifest.v1` and protocol `helix.environment_action.v1`. `minecraft/helix-fabric-sensor/build.gradle.kts` and `FabricConnectorRuntime.java` both identify sensor package/runtime adapter `0.3.0`; its source manifest schema is `helix.environment_source_manifest.v1`. Both modules declare Minecraft `1.21.8`, Fabric Loader `0.18.4`, Fabric API `0.136.1+1.21.8`, Java `21` and embedded connector core `0.2.0`. A package/runtime difference may be intentional independent versioning or stale metadata; this read cannot decide which. The CFP-2 release fixture must either align it or document and verify the supported compatibility mapping against the **actual installed manifests and hashes**. Do not silently substitute JAR filename, catalog label or source constant for a received manifest version.

`helix_minecraft_actor_status` now returns an actor-status observation and a separately labeled `perception_snapshot_compatibility` observation. Its input can opt in to navigation-collision data only for the compatibility observation; the default does not request that data. FP-02 must record the actor and compatibility outcome separately, including a typed compatibility failure, without treating either as an answer or using compatibility data as mine admission. The actor handler's 30,000 ms **observation-age freshness threshold** is not the bounded-effect candidate's **at-most-1,000 ms admission freshness**; action admission must revalidate that stronger limit. The focused source test in `server/mcp/__tests__/helix-mcp-minecraft-action.test.ts` covers registration and mock behavior, not an ordinary installed user.

The [living selected subcases](../../../work-packets/eh-g8-cfp1-selected-operation-acceptance-subcases-v1.md), [candidate operation register](../../../work-packets/eh-g8-cfp1-candidate-operation-context-register-v1.md) and [bounded-assistance packet](../../../work-packets/eh-g8-cfp1-bounded-assistance-acceptance-v1.md) receive these exact fixture inputs. D01 still needs the selected client build, final signed cohort, executing manifest/rights map, ordinary-user account route and per-verb positive/denial thresholds before a customer claim; CFP-2/3 remain blocked and CFP-1 active (`specified`).
