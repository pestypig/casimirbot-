# CFP-1 P1 stdio priority independent review — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This is an independent read-only review of the D02 technical qualification-order refinement, not installed acceptance or account/security approval.

An independent CFP-1 reviewer returned **PASS** on the [priority decision](2026-09-20-cfp1-p1-stdio-priority-decision-163.md), [stdio/IPC qualification packet](../../../work-packets/eh-g8-cfp1-p1-stdio-local-bridge-qualification-v1.md), and aligned owner queue, first-customer claim, component and CFP-2 handoff. The reviewer checked the cited source HEAD and hashes, the current Electron packaging and absence of a stdio command mode, and [OpenAI's MCP client documentation](https://learn.chatgpt.com/docs/extend/mcp?surface=cli). The configuration `identity.command` allowance is not service authentication. The proposal preserves the owner's free same-host P1 product journey, makes stdio → reviewed signed EXE command mode → authenticated local IPC the **first route to qualify**, and holds plain loopback HTTP pending wrong-listener proof. It neither reverses the dated provisional loopback implementation suggestion nor claims the bridge currently exists.

The review found the provisional same-Windows-user threat boundary, exact IPC identity and first-instance behavior, finite grant/session terms, packaged command-mode behavior and installed P1S-01–07 evidence visibly open for account/security and CFP-2 owners. A second helper would reopen C01/C15. No rights disposition, public claim or G8 stage promotion follows. The reviewer also confirmed that the existing [internal platform-budget-token contract](../../../work-packets/eh-g8-cfp1-hosted-resource-budget-token-contract-v1.md) treats time-based room cases as cost sampling while preserving the continuous seven-day trial clock and separate provisional benefit/safety ceilings.

Local validation: `npm run helix:environment-harness:docs-audit` returned `ok: true` with zero failures; `git -c core.safecrlf=false diff --check` passed. Relative Markdown links in the two new D02 files resolved. This review does not select a numeric PBT allowance, Stripe Price or customer capacity; D07/D11/D12 remain open.
