# CFP-1 D07 finite-lease account-rate screen — 2026-09-20

## Source and calculation boundary

The operator-private file `2026-09-20-d07-finite-lease-account-rate-sensitivity.md` has SHA-256 `14C18B370CAB36DB8025F18EE2628D2378E37E3AF30603A8E5F1243066B7A3D5`. It applies the [completed Replit period](2026-09-20-cfp1-completed-replit-period-cost-source-173.md)'s dated project database rate to the [finite-lease workload probes](2026-09-20-cfp1-cost-token-finite-lease-assay-218.md), using [Replit's documented five-minute production-database idle rule](https://docs.replit.com/features/data-and-storage/development-and-production). Exact account rates, derived USD/PBT quantities and invoice details remain in access-controlled evidence.

The calculation checks ten or thirty separated trial visits at five- or fifteen-minute nonrenewed candidate leases, with ten verified effects in each case; it compares ten paid sponsors at the existing ten-/twenty-hour **load samples** and thirty trial starts with the hypothetical $5/$10 public-fee comparators. Trial database-time ceilings require proved suspension with no hidden room-origin requests and no unbounded lease renewal. The additive cohort table assumes the modeled paid/trial active intervals do not overlap each other or the B baseline on the shared database; actual overlap lowers the attributable project union. The paid hours are assumed billable database time only for this scenario and omit extra paid idle tails. Neither dimension is a selected customer allowance. A separate adverse construction staggers sustained seven-day trial rooms so the **shared project database** stays active across one declared thirty-day provider period; it uses the project's billable active-time union, never thirty separate full-trial database charges.

## Result and decision use

Several higher-visit/load combinations exceed the hypothetical $5 comparator's fee-net receipts on database compute alone; others leave only thin partial-resource room. The $10 comparator has positive **database-only** remainder in the finite-lease probes but no whole-offer margin finding. Under the adverse *staggered, continuously active project-database* construction and an otherwise idle baseline, database compute alone exceeds either comparator; fully overlapping starts or an already active B database would lower the attributable increment. Autoscale work, requests, egress, storage, identity, transport, delivery, fixed plan, support, refunds, tax, free-personal allocation and paid idle tails are still missing or unbounded. Actual workload, rates or tier changes could also alter the result.

The screen supports the decision to cost a finite, reserved room lease and cumulative sponsor-term PBT before price selection. It does **not** select an idle threshold, PBT amount, $5/$10 price, model-spend benefit or customer limit. Matched B/P/T or sourced bounds for each cost input, account fee verification when available, cohort sensitivity and qualified financial/privacy review remain D07/D11 conditions. CFP-1 remains active at `specified`; D12 is not frozen, CFP-2/3 blocked and G8 active.
