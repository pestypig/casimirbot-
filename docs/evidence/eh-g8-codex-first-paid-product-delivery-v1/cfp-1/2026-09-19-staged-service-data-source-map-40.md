# CFP-1 staged service-data and reference provenance — 2026-09-19

Status: read-only source inspection for planned R-ASSET-01/R-OWN-01/R-NOTICE-01 inclusion and claim review. The [planned release selector](../../../work-packets/eh-g8-cfp1-planned-release-inclusion-and-rights-review-v1.md), [CFP-1 contract](../../../work-packets/eh-g8-cfp1-product-rights-and-offer-contract-v1.md) and [G8 work program](../../../helix-environment-harness-work-program-v1.md) retain selection and stage authority. Inspected Desktop HEAD `256554ca2637b2978a83616d9f9670069fdfd8c4` with a shared dirty worktree. No binary was rebuilt or shipped; no data, package or runtime file changed. Hashes below identify current source bytes only, not a signed release cohort.

`apps/desktop/scripts/build-host.mjs` explicitly copies both StarSim JSON files into `apps/desktop/dist/data` and writes their SHA-256 values to `service-dependencies.json`. `apps/desktop/scripts/stage-runtime.mjs` rejects a missing or mismatched pair and copies the ideology graph and verifier configuration into `apps/desktop/runtime`. `apps/desktop/electron-builder.config.cjs` selects `dist/**` and the staged runtime files for the NSIS package. Thus these four source files belong in the **planned** extraction/provenance review even though the general `dist/public` media crosswalk omits them.

| Planned file | Current byte identity and source-history clue | Inclusion/claim consequence |
| --- | --- | --- |
| `data/starsim/solar-product-registry.v1.json` | 11,754 bytes; SHA-256 `50c92e15cad73a0ac4ab8cb8e90a667cdb25b4626ac0af3d0f43b2bde3d049c2`; 22 product keys. `git log --follow` shows one introducing commit, `6eeed7995` on 2026-05-20, attributed to `pestypig`. | Builder requires this data. The author string and current file structure do not determine origin or commercial rights for its product descriptions or derived values. |
| `data/starsim/solar-reference-pack.v1.json` | 51,849 bytes; SHA-256 `691b11a0ff6b9d072f068356ef2574d8daa3eb7b185ad1c28e9aa4baa89db132`; 14 `docs` entries; same introducing commit. All 14 `url` values point under `https://example.com/starsim/`. The file itself labels its overview a “Local development reference placeholder”; 119 `reference_doc_ids` mentions parsed from its anchors resolve to those local IDs. | Internal reference-ID consistency is not verified external sourcing. The placeholder URLs must not become customer-facing citations or evidence of scientific provenance. Select reviewed actual references/terms and test them, label the pack as a development placeholder in any retained surface, or exclude it by an explicit reviewed build change. |
| `configs/ideology-verifiers.json` | 5,154 bytes; SHA-256 `c6014008e2ad2272ea0b539e64687b1b2ed1cb929890cc3b7d97784fa7484595`; `version` and `mappings` top-level keys. First `git log --follow` entry is `526bb8dd8` on 2026-01-08, attributed to `pestypig`. | Stager copies it as a runtime config. Select its origin, terms and whether it is needed in the customer profile; preserve any required notice with the exact output. |
| `docs/ethos/ideology.json` | 417,094 bytes; SHA-256 `70d2f9bc97c19b5440f412ee128186fbf5ae767ed00d7770f998f00000b9e642`; `version`, `rootId`, `actionGatePolicy` and `nodes` top-level keys. `git log --follow` shows a long evolving history beginning at `d658636cd` on 2025-11-14, attributed to `pestypig` in the inspected history. | Stager copies the full graph, which can include narrative, references and user-facing content. Review the selected version's first-party/imported content and prior publication terms; Git attribution alone is not a rights disposition. |

The owner has asserted that the named Git identities and product work are theirs; that statement is recorded in the separate [authorship capture](2026-09-19-first-party-authorship-owner-attestation-29.md). This inspection does not independently verify contributor agreements, imported text/data, publication terms, scientific validity, or the signed output. A qualified reviewer should return an `include`/`exclude`/`revise` disposition and conditions for each planned file, with explicit reference and notice treatment. CFP-3.SIGNING must compare actual installer/runtime bytes and reject unexpected files, changed data hashes, or unresolved placeholder citations in any selected customer-facing claim.

**Stage decision:** CFP-1 remains active (specification). This map narrows four planned payload rows and identifies a concrete reference-quality gap, but selects no shipping rights or public StarSim claim and admits no downstream stage.
