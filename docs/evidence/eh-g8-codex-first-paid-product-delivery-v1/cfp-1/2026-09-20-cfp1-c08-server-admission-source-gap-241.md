# CFP-1 C08 server admission source gap — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked. This read-only check extends the [client runtime-discovery evidence](2026-09-20-cfp1-c08-runtime-discovery-and-catalog-source-gap-239.md) to the server action path. It proves a source-policy gap, not execution on an installed customer profile, bundled upstream code, or commercial permission.

At this read, `server/services/situation-room/environment-action-adapter-registry.ts:75–98` registers `navigate_to` with both `native_fabric` and `baritone` as allowed control engines. In `server/services/environment-connectors/actions/action-broker.ts:843–887`, manifest admission rejects a capability engine only if it falls outside that registered set. A compatible client manifest that lists Baritone for navigation therefore is **not rejected for that engine alone**. The broker separately persists top-level `available_control_engines` (`:1055–1086`), so a top-level Baritone claim must also be covered by the customer-channel test even when a particular capability lists only native. The action request schema explicitly parses `requested_control_engine=baritone` (`shared/helix-environment-action.ts:766–770`). The broker carries admitted capability engines into the action context (`:3290–3308`) and, on ordinary enqueue, accepts an explicitly requested engine if it appears in the live capability (`:3511–3521`). The client source separately resolves an explicitly requested `baritone` engine and tests its availability before execution (`minecraft/helix-fabric-player-agent/.../PlayerActionRuntime.java:837–843`). All other room, owner, consent, lease and action checks still apply; this is not a claim that an arbitrary guest can execute navigation.

SHA-256 at this read: registry `150F31D379594357F6B83D8E1698BD5DB535EDBF8F2293A81FC4E9EE7FAA27B0`; broker `8F59C420CBB813FB52532B9C11384C908B38A20CEC8C0F72812CB5DD7C0C93BA`; player runtime `92933A8F10D0FDA6365EC176F1A97FC5828A117CA8295578AE52AFEF202B1CE5`. The broker and player runtime are concurrently modified in the shared worktree; hashes identify this point-in-time source only.

**CFP-3 release acceptance consequence:** Customer-channel C08 exclusion needs a trusted server-side admission boundary as well as a customer JAR/profile and catalog rule. For the selected customer profile/channel, deny or sanitize both top-level `available_control_engines` and navigation capability `control_engines` evaluation claims before publication, and deny a forged, schema-valid `requested_control_engine=baritone` with a typed policy result before effect, even when a compatible user-owned mod is present. A separate evaluation profile may retain that capability only under its own explicit build/channel identity. CFP-3 can choose the exact implementation after reviewing the full adapter contract, but client UI hiding or package-byte omission alone cannot establish exclusion. Test both engine-absent and engine-present isolated customer profiles, and ordinary, direct-diagnostic and forged requests. Supported native actions must remain usable; no existing user-owned profile is altered.
