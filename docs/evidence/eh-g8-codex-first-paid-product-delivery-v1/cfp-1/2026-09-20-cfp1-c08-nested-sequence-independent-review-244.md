# CFP-1 C08 nested sequence independent technical review — 2026-09-20

Program gate: **G8 — Environment-harness release evaluation**. CFP-1 remains active (`specified`); CFP-2/3 remain blocked.

**Verdict: PASS for the specific `execute_sequence` source gap and CFP-3 acceptance handoff.** The independent reviewer reproduced [evidence 243](2026-09-20-cfp1-c08-nested-sequence-engine-source-gap-243.md)'s read-only three-node schema fixture: native outer scheduler with a nested `navigate_to.engine_preference=baritone` returned `{accepted:true,issues:[]}`. The recorded source hashes matched. The broker parses the sequence without a nested engine filter; the client sequence executor turns the nested preference into a Baritone child request. Its graph/action validation and `PlayerActionController.start` do not add an engine-admission guard. Native execution may still deny when the optional engine is unavailable, so no installed effect is claimed.

The reviewer confirmed that the reactive-program schema also embeds player-action arguments but its current native runtime hardcodes `native_fabric` for the child. This review is limited to the demonstrated sequence route, not every composite action. The CFP-3 negative case for a schema-valid outer-native/nested-Baritone sequence with a compatible external mod, plus either typed denial of the unselected composite or an accepted native-only child rule and positive native regression, matches the selected C08 exclusion. D11 rights/commercial review and D12 final component/claim freeze remain open. No runtime, account or production setting changed.
