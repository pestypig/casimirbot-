# CasimirBot domain, accounts and delivery plan v1

Status: specified product/deployment plan, not an implemented production account or billing service. [CFP-1](../work-packets/eh-g8-cfp1-product-rights-and-offer-contract-v1.md) controls the offer; the [environment work program](../helix-environment-harness-work-program-v1.md) controls stage admission. This plan changes no runtime, database, provider configuration, source visibility or deployment.

## One platform, three delivery surfaces

| Surface | Intended experience |
| --- | --- |
| Public domain landing page | Explain free personal tools and paid hosted collaboration; examples, developer documentation, Download and Open app calls to action. Public download does not require source-repository membership or purchase. |
| Browser service | Sign in, manage the account/subscription, create or join eligible rooms, invite participants and inspect permitted collaboration history. Clearly explain when a connected local harness is required. |
| Installed harness | Free supported personal MCP tools; connect local programs, approve scoped sharing and enforce local effects/stop/recovery. Hosted membership does not give the browser ambient access to a computer. |

Use one authoritative development repository initially, with shared contracts and web/desktop build targets. Replit is a deployment target built from that source, not an independently edited mirror. Follow the existing [Local/Replit parity contract](../replit-parity-contract.md); capture source and build fingerprints, compatible schema versions and explicit environment differences. Desktop and web may release at different cadences but must negotiate supported contract versions.

Private first-party implementation is the intended source boundary where rights permit. Publish selected developer contracts/examples/SDK components under reviewed licenses and provide binary downloads through a public release channel independent of source access. Free software, source publication and subscription eligibility are separate policies. Do not split repositories solely to introduce payment. Audit current GitHub/Replit linkage, artifact origins and updater migration before any visibility or deployment change.

## Domain account and database authority

The domain routes customers to the service; the backend and its durable database own account and hosted-entitlement records. The browser and installed clients call authenticated APIs and never connect directly to the database. Use a production PostgreSQL deployment with migrations, backups and tested restore. Do not treat a Replit process filesystem, in-memory database or desktop snapshot as the source of production subscription truth.

Source inspected for this plan: server/services/helix-account/account-session-store.ts uses server/db/client.ts and account/provider/session tables; migration 026 creates helix_accounts. Auth0 web/native account-linking routes exist, and the account session store explicitly supports Google sign-in identities. The database client supports DATABASE_URL and pg-mem/local-snapshot fallbacks. The existing billing store uses persistent entitlement/event/ledger records, but shared/helix-billing-entitlement.ts explicitly describes a sandbox credit-oriented product. These facts establish reuse candidates, not deployed production readiness or the selected subscription contract.

| Logical record | Required role and invariants |
| --- | --- |
| Account/profile | Stable internal account ID, status and profile data. Keep account_type user/developer separate from paid plan; no session remains user under existing policy. Purchase never promotes developer privileges. |
| Login identity | Bind verified provider issuer/subject to one internal account; email is a contact attribute, not permission to merge accounts. Linking another login requires authenticated ownership proof and deliberate confirmation. |
| Session and installation | Expiring/revocable web session, separately enrolled device/node and credential references. Bind each to the account; never copy a browser session or another app's credentials into a connector. |
| Billing account and subscription | Server-only Stripe customer/subscription references, billing owner, selected plan/price version, authoritative status, effective period end and cancellation state. Test and production mappings are isolated. |
| Billing event and reconciliation | Unique processor event ID, processing status, authoritative revision and sanitized audit facts. Apply verified events transactionally; duplicates cannot grant twice and older events cannot resurrect expired/revoked eligibility. |
| Hosted entitlement | Derived versioned hosted capability/limit grants for the selected payer scope. Do not infer access from a credit balance, client flag or checkout redirect. |
| Room/member and program grant | Membership/role plus a separate program owner grant binding node, subject, capability, limits, duration and revocation revision. Room owner, payer, participant and program owner need not be the same person. |

These are logical responsibilities, not approval to create parallel tables. Audit and reuse existing account, identity, session and room records; migrate only missing subscription semantics under CFP-3 ownership. The owner selected [one host sponsoring invited guests](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-host-sponsorship-owner-selection-21.md) and [explicit handoff to another eligible host](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-host-handoff-owner-selection-22.md); handoff protocol, seat limits and $5/$10 benefits remain to freeze.

## Google sign-in and one account

The owner confirmed Google sign-in as a supported convenience option. Google authenticates the person; the domain service resolves their stable CasimirBot account. Subscriptions, room membership and program grants belong to that internal account, not to a particular sign-in button. A Google login does not grant a subscription, developer role or access to another person's programs.

Before deployment, audit whether the configured Google path is direct or brokered through Auth0 and preserve its verified identity mapping. If a returning customer adds another sign-in method, require explicit authenticated linking; do not merge on matching email alone or create a duplicate billing account. Verify Google cancellation, failed callback and returning-customer flows as well as first sign-in. This is a usability and acceptance requirement, not a claim that production OAuth configuration was tested here.

## Authentication and subscription flow

1. The visitor downloads the personal harness freely or selects Open app. Hosted sign-in prominently offers Continue with Google for ease of use, alongside other supported methods. Reuse the existing Google/Auth0 identity integration rather than creating a separate Google-only account or billing system. Validate the login response server-side, including issuer/audience and flow protections; resolve the stable internal account and issue a protected, revocable service session. Use CSRF protection for cookie-authenticated state changes.
2. The desktop links to that same account through the supported native authorization flow with PKCE/state or the existing reviewed equivalent. Authenticate the owner and enroll the installation; never match accounts merely by a typed email, reuse a development session or ask for a Codex account password. Local personal tool access remains free; free does not mean authentication/consent checks are removed.
3. An authenticated billing owner explicitly starts checkout. The backend selects an allowed hosted-subscription price and binds checkout to the internal billing account. Stripe hosts payment entry. The service stores processor references, not card details. The success page can show pending verification; it cannot grant access.
4. The webhook endpoint verifies the processor signature against the exact request body and expected environment, durably records the event, and updates subscription state and derived hosted grants in a transaction. Reconcile disputed/out-of-order state with the processor; unknown customer/account bindings fail to manual investigation rather than guessed assignment. Recover missed events through a bounded reconciliation process.
5. Hosted requests resolve the verified host's current paid eligibility, or trial eligibility **only for operations selected in the trial benefit set**, plus each participant's room membership and the relevant program owner's grant. The host may sponsor invited authenticated guests within the later selected limits; guests need no separate purchase merely to join an admitted room. Neither host payment nor trial includes another person's model access or program authority.
6. Cancellation, failed renewal, refund and dispute follow separately frozen effective-boundary policies. Revoke affected hosted grants and propagate revision changes within the accepted bounds. New ineligible collaboration stops; free personal tools remain available under their own permissions. Stop/revoke and identity-protected history/export/account recovery remain accessible.

Authentication answers who is acting. Hosted entitlement answers which paid service is available. A program grant answers what may happen to whose program. All three must be checked where applicable; none substitutes for another.

The owner has also selected a [seven-day hosted trial with no card required](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-hosted-trial-owner-selection-20.md) that [demonstrates the paid shared-action experience with trial-specific limits](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-trial-shared-action-benefit-owner-selection-42.md). Its enrollment is a distinct authenticated server operation before any checkout: it records a verified trial start/end and permits only exact selected and accepted trial room/action operations under the normal member, consent and program-grant checks. At least one invited guest-requested action from the accepted, rights-cleared paid benefit must be demonstrable; the exact connector, operations and limits remain to freeze. A browser success message or local timer is not eligibility. Trial expiry by itself authorizes no charge; conversion terms remain unselected, and a paid term requires explicit customer authorization and the verified event in steps 3–4. Trial start/reuse, outage and in-flight expiry rules remain to freeze. The download and independently authorized personal path never depend on a trial.

The owner [selected provisional trial limits](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-provisional-hosted-trial-limits-owner-selection-48.md): one active room, one invited guest, one connected program and ten verified bounded effects per verified sponsor. This is a product target pending an exact accepted effect unit, cost and installed validation; it is not current server enforcement or a final number to advertise. The entitlement and action owners must keep the sponsor-level counter and in-flight reservations independent from room membership, program grants and connector leases.

The [explicit Start Trial proposal](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-hosted-trial-start-proposal-50.md) remains an unselected working trigger. If adopted, present the seven-day interval and accepted action before an authenticated activation, then atomically persist one start/end/revision under the verified sponsor identity. Concurrent clicks or uncertain responses return the same trial; Google sign-in, download, personal work and room setup do not silently start it.

The [trial/account source map](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-trial-account-identity-source-map-37.md) identifies a further identity decision before implementation. The owner has now [selected that a used trial stays used after deletion and return with the same verified sign-in identity](../evidence/eh-g8-codex-first-paid-product-delivery-v1/cfp-1/2026-09-19-trial-reuse-owner-selection-39.md). Verified provider subjects and explicit links must resolve to one durable sponsor identity; re-enrollment cannot silently reset a consumed trial or revive old room and program grants. The current soft-delete and provider-link paths do not establish that rule. Freeze retry/concurrent-start behavior, exact relink/recovery, minimum necessary retention and privacy notice before CFP-3 adds trial state; never infer account equivalence from matching email. Trial start invokes no payment checkout, collects no card and posts no credit.

The host is the trial and subscription sponsor. The selected explicit handoff to another eligible host needs an authenticated room/account transition that distinguishes sponsor from room owner, mission lead and every program owner. Current owner leave closes the room and revokes bindings, so the new handoff cannot be inferred from today's leave path. Until a successor is verified and the revised room grants are accepted, new hosted operations fail closed; no guest payment, consent, model account or program authority transfers automatically. Freeze initiator/acceptor, lost-host recovery, replay/race and in-flight release rules in CFP-3/4.

## Source-backed account and connection journey — 2026-09-14

This preparation implements the documentation priority in the [platform build plan](../work-packets/eh-g8-cfp1-harness-platform-build-plan-v1.md). References below were inspected in the shared Desktop worktree at HEAD `256554ca2637b2978a83616d9f9670069fdfd8c4` with uncommitted work present. They establish current mechanisms and gaps, not public deployment behavior. Existing component tests were inspected for coverage; no live OAuth, payment or program operation was executed.

| Step and user-visible outcome | Current source mechanism | Implementation handoff / evidence still needed |
| --- | --- | --- |
| Visit the domain; choose Download or Open app | `client/src/App.tsx:179` maps both `/` and `/open` to the workstation, with `/download` separate | Public entry owner supplies product explanation and availability-aware download while preserving deep links. DOM-01; do not assume landing copy or installer availability from route existence. |
| Sign in to CasimirBot with Google | `client/src/components/auth/GoogleSignInButton.tsx:103`; `server/routes/google-auth.ts:30` verifies audience, `:61` checks CSRF and `:72` passes verified identity to the session store | Show canceled/failed/successful sign-in distinctly. A successful CasimirBot login does not sign in to, verify ownership of, or purchase an external reasoning account. DOM-02. |
| Return to the same CasimirBot account | `server/services/helix-account/account-session-store.ts:1472` resolves provider/subject and existing explicit links | Preserve the internal account ID; audit issuer/tenant namespace before accepting another provider configuration. Current lookup is provider/subject, not an issuer/tenant tuple. Direct Google and Auth0 identities do not automatically converge. |
| Link the current native account to a verified external identity | `server/routes/desktop-auth0-account-link.ts:88`; `shared/desktop-auth0-account-link.ts:7`; `auth0-native-account-link.ts:260` under account services; `agent-account-link-store.ts:414` | Existing native Auth0 start/callback requires desktop launch context plus active account session, PKCE/state and binding checks. Prove account continuity across the selected deployed Google/Auth0 profiles without typed-email merge or credential copying. This identity link and coordination bootstrap do not establish installed-device enrollment or a program grant. |
| Enroll and inspect the installation separately | `server/services/helix-account/installed-security-store.ts:144,363`; `server/routes/desktop-auth0-step-up.ts:180` | Reuse profile-scoped device registration and versioned trust. Current trust/management paths require developer authority; PUBLIC must admit the selected ordinary-user operations explicitly. Tunnel trust grants no program authority. |
| Choose and authorize the external reasoning client | `shared/helix-agent-client-profile.ts:15` declares `codex_app` and `standard_mcp`; `client/src/components/agent-access/agentConnectionSetupState.ts:6` defines choose/account/authorize/connect/check/ready | Reuse these profiles and steps; source-declared compatibility still needs actual-client acceptance. Persist navigation preferences only, then recheck authority. Client selection proves neither app identity nor task steering. No mandatory bundled runtime or API purchase. |
| See what the connection can do now | `server/routes/agent-connections.ts:177`; `shared/helix-agent-client-readiness.ts`; `AgentConnectionSetup.tsx:588` under agent-access | Present client authorization, online presence, catalog state and optional steering separately. The readiness route currently projects environment `not_selected`; its `agent_ready` result cannot authorize a program or certify an installed task. Preserve the existing tool-only explanation. |
| Select a program and approve an action scope | `shared/helix-account-session.ts:116`; existing environment subject, consent and authority services | PUBLIC and the capability owner trace each selected handler through ordinary account, node, target and consent checks. Current user policy still locks important installed/room/action paths. Do not use developer promotion or hosted payment as the repair. |
| Optionally obtain hosted service and join/share a room | `server/routes/desktop-auth0-step-up.ts:180`; `server/routes/stripe-sandbox-webhook.ts:69`; `server/services/shared-live-room-control/service.ts:640` | Current payment path is desktop/developer step-up and sandbox product machinery; room entry uses feature/scope/member controls. Implement ordinary billing-owner access and selected hosted grants without replacing membership or program consent. DOM-03–05/COLLAB. |
| Return, switch accounts or revoke access | `account-session-store.ts:1554`; `installed-security-store.ts:144` under account services; existing binding/consent owners | Reconcile the specific changed authority. Preserve valid connection identity, reject stale grants and report pending/unknown action outcomes. Recovery evidence is specific to the tested artifact and account/device path. |

### Public identity boundary that must be resolved

The local profile-selection route is not verified external login. `POST /api/account/session/sign-in` accepts profile/email in `server/routes/account-session.ts:81`; `account-session-store.ts:969` selects/creates the profile and inserts a session without external identity proof. `server/routes.ts:255` mounts that router. The outer desktop guard permits non-desktop operation (`server/security/desktop-session.ts:79`); the global JWT layer enforces a bearer only when `ENABLE_AUTH === '1'` (`server/auth/jwt.ts:5,41`, mounted in `server/index.ts:872`). Thus those source guards do not themselves prove ownership of the submitted CasimirBot profile. The production-role test at `server/__tests__/account-session-panel.test.ts:444` expects local sign-in to return a user role; a lower role is not proof of account ownership.

The account/deployment owner must make the public authentication modes explicit: retain legitimate local development/profile selection, while public hosted account authority requires verified identity ownership. Test the actual middleware chain and selected deployment mode, including an unauthenticated request and a generic bearer attempting to select another profile. This is a source-level public-release gap, not evidence that the current domain is exposed or that an account was accessed. Do not probe live accounts to close it.

### Session and callback lifetime gaps

- Google/web sign-in currently calls `insertSession` without `expires_at` (`account-session-store.ts:1509`); that field defaults to null (`:758`). A finite browser-cookie lifetime alone does not prove finite server-side session validity. Freeze intended absolute/idle server expiry, test expiry and revocation independently, and preserve safe account recovery. No hosted subscription term should silently become the personal session lifetime.
- Auth0 web OAuth transactions use an in-process map (`auth0-web-account-session.ts:180` under account services). Native linking also keeps pending state in process (`auth0-native-account-link.ts:276,316`), even though other intent/binding records are durable. Define a typed restart/callback-loss outcome or admit appropriate durable transaction storage through its owner. Do not promise mid-login recovery across redeploy/load balancing from durable account tables alone.
- Existing explicit native linking rejects wrong profile/session, consumed/expired intent and cross-profile provider conflicts (`agent-account-link-store.ts:414–489`); fresh deliberate OAuth may reactivate a same-profile binding. Preserve that distinction: background reconnect must not silently reactivate a revoked link.

### Recovery and permission transitions

These are required outcomes for the selected platform journey, not a statement that every propagation path is implemented. Retain existing stricter stop/revocation bounds; freeze any missing bounds in the responsible packet before evaluation.

| Event | Required visible result | Authority and acceptance consequence |
| --- | --- | --- |
| Sign-in canceled or callback lost | Explain that sign-in did not finish and offer a clean retry, preserving non-secret choices | No authenticated grant from the failed attempt. DOM-02/06 includes replay, wrong state/issuer/audience and restart. |
| Browser sign-out | Show the browser signed out | Current source signs out the chosen session. Do not describe this as account deletion or global device revoke; independently authorized activity follows its own current authority. |
| Revoke an external identity/client binding | Identify the revoked connection and its affected reuse | `agent-account-link-store.ts:719–779` under account services marks the account binding revoked. This differs from browser sign-out or device removal. Fresh deliberate same-profile OAuth may reactivate through the existing native path; background readiness checks may not. |
| Switch from account A to B | Re-read B's account, device and connection status | Never relabel A's binding, subscription, target or history as B's. A separately authenticated external client is not silently transferred to B. |
| Client offline, tool-only or stale catalog | Identify the failed axis and the supported repair | Preserve valid account/pairing records. Lack of task steering is not lack of ordinary MCP tool access; fresh catalog requirements still apply to the requested tools. |
| Device recovery | Show which installation was recovered and what consent must be renewed | `installed-security-store.ts:144–348` scopes devices by profile and rejects stale trust revisions; recovery increments generation and clears full trust. Prove no restored old tunnel or program authority. |
| Program consent expires, manual stop or owner revoke | Show the exact target/scope affected and whether effects stopped or remain uncertain | Reject subsequent stale actions and preserve outcome evidence. A valid subscription cannot override this boundary. Use existing capability/CS/NAV owners for native stop evidence. |
| Room removal or hosted subscription expiry | Remove affected collaboration access with an explanation | Retain independently eligible personal tools and authorized stop/revoke/history/recovery. Room membership, payer policy and program sharing each have their own boundary. |
| Account deletion | Present the selected data and service consequences before the existing deletion operation | `account-session-store.ts:1576` signs out profile sessions, soft-deletes account/storage and revokes specified credentials. That excerpt does not establish complete room/node/program-grant or billing teardown; inventory those consumers and freeze retention/cancellation semantics before DOM-05/06 acceptance. |
| Hosted database unavailable or restored | Show service unavailable or reconciliation pending | Fail closed for hosted account/billing authority; preserve only personal work already permitted by its existing independent local authority. Restored rows cannot resurrect revoked grants. |

### Exact preparation and review handoff

CFP-2.PUBLIC/ONBOARD and existing account/PNA owners should prepare one reviewed account/connection change set from the routes, services, policy and UI files named above, with exact non-overlapping ownership before dispatch. Start with public identity ownership and server-side session lifetime, then verified web/native continuity and truthful per-axis readiness/recovery. Coordinate account/database changes with their current owners; this specification does not replace active CS pairing repairs.

Extend existing tests rather than build another account subsystem. Coverage candidates already exist in `server/__tests__/google-auth.test.ts`, account-service `auth0-web-account-session`, `auth0-native-account-link`, `agent-account-link-store` and `installed-security-store` tests, `server/routes/__tests__/desktop-auth0-account-link.test.ts`, `server/routes/__tests__/agent-connections.test.ts`, and `client/src/components/agent-access/__tests__/AgentConnectionSetup.spec.tsx`. Reuse DOM-02/04/05/06 and the CFP-2 personal journey. The negative matrix must include wrong provider/issuer/tenant/profile, same-email distinct users, callback replay/expiry, unverified local profile selection, revoked/stale device authority, account switch, no subscription and expired hosted subscription. Add isolated route-chain and durable database/restart checks where mocked component tests cannot cover the requirement. These are coverage requirements, not a report of tests run in this specification pass.

## Failure, privacy and deployment rules

- Production hosted identity/billing must fail closed if the durable database is missing or unavailable; do not silently fall back to a fresh local account or empty subscription store. Preserve personal operation where its existing local authorization permits; never turn an outage into paid access.
- Keep session/refresh secrets in protected service or native stores; store only secure verifiers for opaque tokens where applicable. Public/MCP projections contain safe capability and expiry summaries, not processor IDs, tokens, passwords or another member's billing data.
- A browser sign-out, device revoke, room removal, subscription cancellation and account deletion are distinct operations. Define their affected credentials, grants, retention and recovery explicitly. Account linking, lost-device recovery and billing changes require the existing supported fresh-authentication/step-up policy.
- Replit Preview/test and production have separate databases, identity callbacks and payment credentials. No production customer data in preview fixtures. Deployment health checks must verify database/migration and contract compatibility before serving hosted entitlements.
- Backups/restores must reconcile current processor status and revocation history before renewing hosted authority; restored stale data cannot silently reactivate old grants. Stage rollout and rollback around compatible migrations, with one authoritative subscription writer.

## Ordered implementation and acceptance handoff

1. CFP-1 audits existing domain routes, GitHub/Replit deployment linkage, identity mappings and database migrations; freezes the personal/hosted capability map, payer policy and customer-facing lifecycle terms.
2. CFP-2.PUBLIC/ONBOARD proves a free personal ordinary-user journey and correct account/device linking without role promotion or room setup. Public landing/download and browser route work gets exact file ownership before dispatch.
3. CFP-3.COMMERCE owns hosted subscription persistence, verified webhook/reconciliation and account portal behavior. CFP-3.LICENSE owns trusted hosted eligibility and revision enforcement. CFP-3.DISTRIBUTION owns public artifact/download and source-independent update delivery. No additional account stack is implied.
4. CFP-4 repeats the account/billing/room journey on the accepted web deployment and signed client; CFP-5 performs only separately authorized attended production payment/pilot actions. The following cases supplement existing ENT/COLLAB/DIST requirements.

| Case | Required evidence |
| --- | --- |
| DOM-01 public entry | Logged-out visitor can understand the offer and download a verified artifact without repository access; Open app reaches the intended sign-in/service route. |
| DOM-02 account continuity | Google first sign-in, returning sign-in and explicitly linked alternate login resolve the same intended account and hosted subscription across web and desktop; canceled/failed sign-in creates no authenticated grant. Same authenticated person links web and desktop to one stable account; forged callback, wrong issuer, email-only merge and cross-account link fail. Purchase never changes account type. |
| DOM-03 payment and trial authority | Forged success redirect, invalid-signature webhook, duplicate event, wrong environment/account, reused trial and out-of-order event cannot misgrant hosted access. Verified active trial or eligible subscription activates exactly its selected hosted capabilities; trial expiry alone neither charges nor extends access. Any paid conversion needs its separately selected and authorized path. |
| DOM-04 permission separation | A paying or trial-active host without an owner grant cannot control another program; an invited guest needs no purchase merely to join but cannot exceed their room role or program-owner grant; a never-subscribed user retains supported personal tools. |
| DOM-05 lifecycle | Cancel, trial/paid expiry, revoke, missed webhook, sponsor loss, explicit eligible-host handoff and reconnect produce the frozen outcome without restoring stale authority, transferring another owner's program grant or disabling free personal use. |
| DOM-06 durable deployment | Restart/redeploy preserves account and billing state; unavailable DB fails closed; backup/restore and processor reconciliation do not resurrect revoked grants. Preview cannot read or modify production state. |
| DOM-07 distribution parity | Web/client contract compatibility and deployment source/build fingerprints match the declared release; downloads/updates work without private-source membership. |

Freeze exact test files, versions, time bounds and resources in implementation packets. No database migration, payment activation, site publication or repository-visibility change is authorized by this planning document.
