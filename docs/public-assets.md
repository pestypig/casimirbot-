# Public Assets

This repo ships a small set of public assets that are served directly by the web
runtime.

Files
- `public/mobile-sw.js` provides the mobile service worker entrypoint.
- `public/geo/world-countries.json` supplies country boundary data for map views.
- `public/skybox/warp-skybox.lut.json` contains the warp skybox lookup table.
