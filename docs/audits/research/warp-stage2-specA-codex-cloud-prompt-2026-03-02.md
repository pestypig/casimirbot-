# Warp Strong-Claim Closure: Stage 2 (Spec A) Codex Cloud Prompt

## Hard boundary statement (must remain verbatim in all generated outputs)
“This campaign defines falsifiable reduced-order full-solve gates and reproducible evidence requirements; it is not a physical warp feasibility claim.”

## Mission
Implement **Stage 2 only** of the strong-claim closure plan: **Spec A operator-semantics closure** for the G4/QI track.

Do not run Stage 3+ work in this prompt.

## Current commit context (starting point)
- Branch: `main`
- Expected starting head when this prompt was authored: `75557ef5a98ab1a4d1c57c87a1e2c4b4f12c48be`
- Canonical campaign state: `decision=INADMISSIBLE`, `firstFail=G4`
- Canonical scoreboard currently shows: `PASS=6, FAIL=1, UNKNOWN=0, NOT_READY=1, NOT_APPLICABLE=1`
- Governance aggregate class: `evidence_path_blocked`
- Decision ledger class now aligned to governance aggregate: `canonicalDecisionClass=evidence_path_blocked`, `finalDecisionClass=evidence_path_blocked`

## Non-negotiable constraints
1. No threshold/policy weakening.
2. No canonical override by promoted/exploratory lanes.
3. Keep Stage 1 Spec B/C behavior intact.
4. Preserve boundary statement verbatim.
5. Fail-closed on missing operator-mapping evidence.
6. Do not edit unrelated mission/Helix files.
7. Ignore any concurrent drift from other agents unless directly required for this stage.

## Stage 2 scope (Spec A only)
Allowed to change:
- `tools/warpViability.ts`
- `scripts/warp-full-solve-campaign.ts`
- `scripts/generate-g4-decision-ledger.ts` (only if needed for operator-evidence plumbing)
- New Stage-2 operator mapping script(s) and tests
- Canonical artifacts/docs that are regenerated by required pipeline commands

Suggested new files:
- `scripts/warp-g4-operator-mapping-audit.ts`
- `tests/warp-g4-operator-mapping-audit.spec.ts`
- `artifacts/research/full-solve/g4-operator-mapping-audit-2026-03-02.json`
- `docs/audits/research/warp-g4-operator-mapping-audit-2026-03-02.md`

## Spec A acceptance criteria (must all PASS)
### A1: Operator mapping evidence artifact exists and is machine-readable
Artifact must include at least:
- `operatorTarget` (expected canonical target form)
- `quantitySemanticBaseType`
- `quantitySemanticType`
- `quantitySemanticTargetType`
- `quantityWorldlineClass`
- `qeiStateClass`
- `qeiRenormalizationScheme`
- `qeiOperatorMapping`
- `qeiSamplingNormalization`
- `mappingComparable` (boolean)
- `mappingBridgeReady` (boolean)
- `mappingMissingFields` (array)
- `mappingDerivationRef` (string, deterministic)
- `provenance.commitHash`
- `boundaryStatement`

### A2: Fail-closed semantics in gate/report/forensics
If required operator-mapping fields are absent or non-comparable:
- mark operator evidence as blocked
- emit deterministic reason token(s) (for audit scripts)
- do not allow a promoted/candidate pass to be interpreted as canonical closure

### A3: Deterministic integration into campaign outputs
Campaign report must include a dedicated operator-mapping section showing:
- pass/blocked state
- missing fields (if any)
- provenance/freshness
- explicit canonical-authoritative statement

### A4: Tests
Add/update tests proving:
- deterministic operator-mapping classification
- fail-closed behavior when mapping evidence missing
- stable mismatch reason formatting
- non-mutation of canonical report for temp `--out` runs (if touched)

## Required commands
Run all and include outcomes:
- `npx vitest run tests/warp-g4-operator-mapping-audit.spec.ts` (if new)
- `npx vitest run tests/warp-full-solve-campaign.spec.ts tests/g4-decision-ledger.spec.ts tests/warp-viability.spec.ts`
- `npm run warp:full-solve:canonical:bundle`
- `npm run warp:ultimate:check`
- `npm run warp:evidence:pack`
- `npm run warp:publication:bundle`
- `npm run casimir:verify -- --ci --url http://127.0.0.1:5050/api/agi/adapter/run --trace-out artifacts/training-trace.jsonl`
- `curl -fsS http://127.0.0.1:5050/api/agi/training-trace/export -o artifacts/training-trace-export.jsonl`

If `5050` is unavailable, retry on `5173` and report both attempts.

## Output contract (required)
Return exactly:
1. Exact files changed.
2. Spec A acceptance checklist (A1..A4) with PASS/FAIL.
3. Canonical counts + decision + firstFail.
4. Decision classes:
   - `canonicalDecisionClass`
   - `scanDecisionClass`
   - `finalDecisionClass`
   - mismatch reason(s)
5. Operator mapping artifact summary:
   - path
   - key fields
   - blockedReason (if any)
6. Casimir verify fields:
   - `verdict`
   - `firstFail`
   - `certificateHash`
   - `integrityOk`
   - `traceId`
   - `runId`
7. Commit hash + push status.
8. Verbatim boundary statement confirmation.

## Stop conditions (fail-closed)
If any acceptance item fails, output:
- `blocked=true`
- `blockedReason=<deterministic_token>`
- `nextRequiredFix=<short actionable item>`
- do not claim completion.
