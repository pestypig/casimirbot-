# SPINE Bookkeeping Receipts

This folder stores non-ToE bookkeeping receipts (for example `SPINE-*`).
Do not place these files under `docs/audits/ticket-results/`, because the ToE
ticket validator enforces `TOE-*` schema contracts in that directory.
