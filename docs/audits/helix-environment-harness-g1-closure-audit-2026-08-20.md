# Helix Environment Harness G1 Closure Audit — 2026-08-20

Status: immutable evidence snapshot for G1 canonical lifecycle authority.

This audit records the evidence that closed G1. Current program status remains
owned by `docs/helix-environment-harness-work-program-v1.md`.

## Work packet

```text
Program gate: G1 — Canonical lifecycle authority
Workstream: Environment harness lifecycle authority
Capability or component: Canonical lifecycle authority and poisoned-projection resistance
Lifecycle stage: Execution through presentation, with emphasis on evidence re-entry and terminal authority
Reaction timescale: Short semantic replanning
Authority owner: Codex for reasoning and completion; Helix for evidence identity, admission, and terminal eligibility
Current maturity: specified
Target maturity: live accepted
Required evidence: G1 closure evidence 1–8 in the canonical work program
Explicit non-goals: G2 parity, Minecraft capability expansion, live-mail control, resident-controller runtime, and concurrent reasoning
Downstream gate unlocked: G2 — A0 / A1 / B parity
```

## Implemented authority boundary

- `payload.turn_lifecycle` is the sole current-turn lifecycle authority. Debug,
  compatibility, rail, itinerary, UI, and voice records are derived
  projections and cannot replace it.
- A fixed-precedence reducer consumes the append-only event stream. Snapshot
  completeness scoring and compatibility-based proof of observation re-entry
  are not authoritative.
- Tool receipts and provider projections count as grounded model evidence only
  when exact current-turn runtime events prove observation re-entry. An exact
  route-contract exception remains available for validated deterministic
  self-terminal products; it does not authorize ordinary model synthesis.
- A provider-authored answer becomes complete from the runtime completion
  facts and final agent-message event. Terminal outcome is a later Helix
  eligibility decision, not circular proof that Codex finished reasoning.
- Typed-failure reconciliation becomes read-only once a verified canonical
  lifecycle exists. It may diagnose a contradiction but cannot mutate the
  authoritative facts or rewrite a completed Codex candidate.
- Failed attempts remain in provenance. A strictly later verified current-turn
  execution, re-entry, and reasoning sequence may supersede their blocking
  effect without deleting the failure.

## Keyed natural tool acceptance

The approved opaque launcher started the keyed server at
`http://127.0.0.1:1522`. Readiness checks passed for
`/api/account/session`, `/api/helix/pipeline`, and
`/api/agi/agent-providers`; Codex was launchable.

Unchanged natural prompt:

> Find the environment harness work program in the docs and tell me which
> program gate is active now. Cite the specific document evidence you used.

Accepted turn:

- Turn ID: `ask:3c446519-c06d-4f3c-a726-34acbee1086f`
- Capability: `docs.search`
- Result: correctly identified G1 and cited the canonical work program and
  product-goal architecture document.
- Canonical event sequence:
  `turn.started → runtime.turn.failed → route.committed → capability.admitted → tool.call.started → tool.call.completed → observation.reentered → agent.message.completed → runtime.turn.completed → terminal.eligibility.checked → turn.completed`
- The initial native-provider `provider_quota_exhausted` failure remained in
  provenance. The later compatibility execution and reasoning recovery were
  recorded as new facts rather than relabeling the earlier failure.
- `turn_lifecycle.integrity.ok=true`
- `turn_lifecycle_projection_audit.ok=true`
- `evidence_reentry_proof.passed=true`
- `lifecycle_differential_audit.ok=true`; all continuity checks passed and
  `first_divergence_stage=null`.
- Candidate, materialized product, terminal writer, terminal authority, and
  visible presentation shared SHA-256
  `5be91331a138233e6b794419f53cdf4dec39296302dfa34dd764040845f07c8f`.
- All three selected terminal support references were preserved through
  materialization and terminal selection.

This is the equivalently deep natural keyed tool turn permitted by G1 closure
criterion 8. A live Fabric player/source was not available, so this audit does
not claim a new Minecraft rescue acceptance or G2 parity result.

## Poison and failure evidence

Focused regressions cover:

- poisoned debug and compatibility lifecycle projections;
- canonical source precedence and stale projection resistance;
- exact observation/candidate identity continuity;
- compatibility evidence that lacks runtime re-entry;
- failed native execution followed by a strictly later verified recovery;
- typed-failure projection attempts after canonical completion;
- invalid self-terminal route-product attempts;
- text/Realtime relay disagreement with canonical grounding authority.

Representative hard failures remain typed and fail closed when runtime
re-entry, route authority, evidence identity, or terminal eligibility is absent.
No receipt, route label, compatibility projection, or visible UI state is
promoted into a model-authored answer.

## Verification

- Focused G1 lifecycle/re-entry/terminal battery: `190/190` passed before the
  final recovery regression; the added recovery suite then passed `9/9`.
- API parity unit matrix: `31/31` passed.
- Prompt-solving benchmark: `36/36` passed.
- Capability plan contract: `75/75` passed.
- Capability lifecycle ledger: `8/8` passed.
- Terminal equivalence: `6/6` passed with the host memory guard still active
  and a test-only estimated-burst value of 128 MiB.
- Live-source continuation: `26/26` passed.
- Live-source identity audit: `9/9` passed.
- `npm run helix:ask:discipline:full`: passed, including all prompt and API
  shards plus the server build.
- Server build completed with four unrelated pre-existing duplicate warnings.

The separate keyed API parity probe remained `14/16`: two older live-source
identity fixtures expected `field_evaluations_missing` while the runtime
reported `fresh_observation_not_in_situation_run`. Every affected rail still
reported runtime-event-log observation re-entry. This is identity-fixture debt,
not a G1 lifecycle-authority regression, and was not patched in this increment.

Casimir verification was not applicable: this increment did not touch warp/GR,
constraint packs, training traces, certificates, proof maturity, or Casimir
adapter contracts.

## Verdict

G1 is **live accepted** for the exact canonical lifecycle authority and
poisoned-projection-resistance surface described above. G2 is unlocked. This
audit does not accept A0/A1/B Minecraft parity, persistent viability, live-mail
wake integration, durable goals, concurrent reasoning, or domain transfer.
