Program gate: G8
Workstream: CS1-CS4 prerequisite onboarding and binding repair
Capability or component: accepted durable exact-chat MCP steering transport
Lifecycle stage: tool admission; evidence re-entry; presentation
Reaction timescale: active-task polling, independent finite human pairing lifetime
Authority owner: human approves exact-chat scope; authenticated destination accepts; Helix revalidates every operation
Current maturity: specified
Target maturity: deterministically verified and separately evidenced packaged rehearsal
Required evidence: accepted-grant polling without checkpoint publication; wrong identity, expiry, revocation, stale presence and duplicate negatives; real handler and packaged pickup/ack evidence
Explicit non-goals: no checkpoint capability declaration, idle task wake, automatic provider delivery claim, human consent automation, gameplay permission, private runtime, ET6 substitution or NAV1 promotion
Downstream gate unlocked: none; parent CS1-CS4 acceptance remains required

# Durable MCP polling independent of public checkpoints

This repair continues the [onboarding packet](eh-g8-cs-onboarding-pairing-plan-v1.md)
under the [environment work program](../helix-environment-harness-work-program-v1.md).
It can proceed within the current CS prerequisite scope and does not depend on
qualified movement. Classification: tool admission, evidence re-entry and
presentation. Codex retains reasoning and invocation ownership.

The native startup rehearsal succeeded. The next inspected boundary is an
accepted durable `exact_chat_steering` grant being projected as unavailable solely
because presence declares `tool_activity_only`. The current authenticated task
can call steering read/ack, but its catalog has no public checkpoint publisher.
Declaring a higher observability level would overstate available capabilities.

Freeze these boundaries before editing:

1. Only an existing, finite, human-approved, authenticated-destination-accepted
   durable grant establishes this polling transport. Registration, heartbeat,
   tool visibility and a browser assertion alone do not establish it.
2. Polling means the exact authenticated task may request queued steering. It
   does not prove automatic delivery, current model activity, wake-up support or
   public checkpoint publication. Preserve the actual observability declaration.
3. Revalidate the encrypted grant, destination, installation and trust on every
   durable operation. Preserve exact task/chat/run/epoch checks, expiration,
   independent revocation, stale rejection and idempotent records.
4. Agent-origin prompt ingress and Ready up association still require fresh
   current-service exact-task presence. An idle pairing may persist and hold
   queued work without falsely passing this active-operation check.
5. Existing legacy claim/continuation admission remains unchanged. Do not turn
   all tool-activity-only clients into automatically reachable tasks.
6. Test the real store/access/public-handler paths with isolated principals and
   storage before requesting live pairing consent. Distinguish queued, picked
   up, acknowledged and visible delivery. Automatic provider delivery remains
   separately unqualified.

Run focused pairing/steering/Ready up regressions, discipline full for this
continuation change, docs audit, relevant build and native package checks.
Record a new requirement-by-requirement CS5 supplement. This packet cannot close
O3 automatic delivery, O6 integrated workflow or original CS acceptance by itself.

The packaged pre-consent review must also retain the last offered exact run when
presence expires before the human selects its checkbox. Existing tests retain an
already-selected run only; they do not cover this earlier review boundary. Freeze
the new presentation regression before editing: removing presence-derived props
must retain the unselected candidate with an explicit revalidation label, keep
consent unchecked, perform no write and clear it on owner/chat changes. The
authenticated invitation handler must still validate current run ownership and
expiry; a retained display candidate is never authority.

The native review also exposed a presentation defect: the human checked both
boxes but could not identify the unstyled approval button. Give the existing
approval/reconciliation action a visible boundary, contrasting fill, adequate
pointer target and keyboard focus indication. This is presentation only; its
handler, disabled-state checks, consent scope and invitation identity remain
unchanged. Component and isolated browser tests do not substitute for verifying
the new styling in the packaged renderer.
