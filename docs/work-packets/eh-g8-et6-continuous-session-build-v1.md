# ET6 prerequisite: continuous environment-session build

Program gate: G8 — environment-harness release evaluation.
Workstream: ET6 prerequisite workflow repair, not a replacement capacity acceptance.
Capability or component: provider-neutral session readiness, prompt ingress and measured rolling execution; Minecraft reference adapter.
Lifecycle stage: source admission; secondary tool admission, execution, evidence re-entry and presentation.
Reaction timescale: bounded session preparation; semantic planning; adapter-native continuous execution and interruption.
Authority owner: Runtime Codex chooses plans; Helix validates identity, consent, evidence and effects; the resident executor advances admitted work only.
Current maturity: specified for this integrated prerequisite workflow; existing component evidence retains its exact scope.
Target maturity: deterministically verified prerequisite contracts plus a packaged development rehearsal, before original ET6 live acceptance resumes.
Required evidence: readiness/recovery matrix, exact prompt ingress trace, real resident movement and successor timings, interruption/revocation negatives, artifact identity and packaged UI/MCP parity.
Explicit non-goals: no ET6 completion by relabeling, NAV1 implementation, private model loop, synthetic provider principal, human-only consent bypass, indefinite authority, raw reasoning capture or receipt answer authority.
Downstream gate unlocked: resumption of original ET6 acceptance attempts only; the separate NAV-EQ direct-MCP lane governs the current NAV1 prerequisite.

## Control and rationale

The sole stage/status authority is
`docs/helix-environment-harness-work-program-v1.md`. Original acceptance remains
in `docs/work-packets/eh-g8-et-environment-time-receding-horizon-v1.md`.
This prerequisite is permitted inside G8 because it repairs infrastructure ET6
needs; it does not assume ET6 passed or advance navigation prerequisites.

Current dependency clarification (2026-09-08): NAV-EQ is the separate parallel
G8 direct-MCP execution qualification described in
`docs/work-packets/eh-g8-nav-direct-mcp-execution-qualification-v1.md`.
It may qualify NAV development without closing CS1–CS5 or full ET6. Retained
checkpoint statements below about an unchanged ET6/NAV1 gate describe the
policy at their recording time; this clarification supersedes only that
NAV prerequisite. Exact room-driven prompt delivery retains every applicable
binding, provenance and acknowledgement requirement.

Current recovery work (2026-09-14): the user has supplied finite four-capability
player authority. The accepted exact task/chat/run pairing is retained; human
gameplay consent is no longer the pending boundary described by older evidence.
The [repeated-read repair](eh-g8-cs-ready-up-read-freshness-v1.md) has three
successful delayed packaged MCP Ready up requests with unchanged goal and
authority. Subsequent idle player credential expiry exposed a missing goal
recovery transition after same-authority epoch rotation. Follow the
[idle epoch recovery packet](eh-g8-cs-idle-epoch-recovery-v1.md) for that first
reproduced boundary. Its component and packaged results remain separately
recorded; no full CS stage, original ET6 exit or NAV1 qualification is closed.

On 2026-09-07 the user requested replacing repeated live acceptance attempts
with a build-first approach. Preserve all previous positive and negative
evidence. Do not erase the goal's missing requirements or present this packet
as evidence that any component is already integrated accepted.

Recent diagnostic records:

- `reports/helix-minecraft/et6-live-route-20260907-2130.md`: a short completed
  walk supplied zero rolling extensions; ordered event delivery lag prompted
  a batch transport repair. Tests and packaging are not a live throughput pass.
- `reports/helix-minecraft/et6-identity-recovery-20260907-2332.md`: an online
  source and ready controller coexisted with stale player identity. Recovery
  required source, subject, authority and goal reconciliation. EXE automation
  failed separately; a user-submitted natural prompt did reach the exact task.

## Ordered build stages and exit tests

### Development versus live-testing blocker (2026-09-08)

The external task's missing `helix_environment_session_ready_up` tool blocks
live MCP preparation and the integrated EXE/MCP rehearsal, not all source
development. The connected app's refreshed published action list contains the
tool, but the current task's callable catalog did not expose it at the last
check. Publication, callable delivery and successful authorized invocation are
three separate claims. This observation does not prove a Codex internal defect.

Continue dependency-ordered implementation, deterministic regressions and
packaging that do not require that external capability. In particular, finish
first-session objective/goal bootstrap integration rather than waiting on the
catalog or treating the tested goal-create primitive as a complete workflow.
Do not declare a stage complete while its live or integrated exit evidence is
missing, and do not claim simulated execution as live capacity proof.

Before returning to live testing:

1. Confirm the running EXE matches the intended built artifacts.
2. Inspect the same task's callable catalog for Ready up; an app settings
   listing or a successful in-process catalog test is insufficient.
3. If still absent, record this as an external client-catalog delivery blocker.
   Use only supported refresh/reconnect mechanisms, with a specific expected
   catalog change; do not repeat reconnects without new evidence. CasimirBot
   code changes cannot be assumed to force an active Codex task's tool reload.
4. Once callable, refresh same-task presence, inspect current setup state and
   reconcile any expired preparation intent before testing. Do not reuse old
   claim handles or infer that queued preparation created a verified run.
5. Prove authorized inbox pickup and the shared Ready up workflow before
   proceeding to integrated movement, interruption and revocation testing.

No replacement task, impersonated provider call, human-only consent automation
or weaker authority gate is an acceptable workaround. Preserve the original
CS1–CS4 requirements and the subsequent unchanged ET6 acceptance/NAV1 gate.

Live-testing handoff clarification (2026-09-08): Codex can inspect and repair
CasimirBot source, tool registration, contracts, tests and packaged artifacts.
That does not establish control over Codex's own active-task catalog delivery.
If a published tool remains absent from this task, isolate that boundary using
supported diagnostics; do not promise a repository patch will reload it. A
fresh callable-catalog check for `helix_environment_session_ready_up` still
returned no matching tool when this clarification was recorded.

Complete the artifact and callable-tool preflight above before asking the user
to generate another binding claim. Record the exact missing capability and
repair owner if preflight fails, continue safe build work where possible, and
request only the remaining necessary user action when the live path is ready.
Do not restart the binding/reconnect cycle merely because time has elapsed.
This is a handoff requirement, not new live evidence or a maturity promotion.

### CS0 inspected baseline (2026-09-07)

This inventory selects CS1's first failing contract. It does not assert that
new CS1 functionality exists or that all later defects are diagnosed.

| Boundary | Existing implementation to reuse | Observed gap / next evidence |
| --- | --- | --- |
| Controller readiness | `server/services/environment-connectors/actions/authority-store.ts`, `projectEnvironmentActionConnectorReadiness` | Inputs cover authority status, manifest and heartbeat; no observation-subject epoch or goal identity. Correct as a connector projection, insufficient as session readiness. |
| Player identity | `server/services/environment-connectors/subjects/subject-binding-store.ts`; `server/mcp/__tests__/helix-mcp-environment-subject.test.ts` | Stale epoch rejection and authenticated self-selection already exist. Compose them; do not recreate or weaken them. |
| Goal recovery | `server/services/environment-connectors/goals/durable-goal-store.ts` | Revision-checked recovery/rebound/checkpoint/resume succeeded in the retained trace, but orchestration was manual tool-by-tool work. Preserve causal evidence and optimistic revision checks. |
| Exact task binding | `server/routes/agent-connections.ts`; registered claim/read/ack MCP tools in `server/mcp/helix-mcp-server.ts` | Typed browser steering reached the exact binding. No new provider task or replacement binding is needed for ordinary recovery. |
| Prompt ingress | Browser steering routes in `server/routes/agent-connections.ts`; `client/src/lib/helix/ask-prompt-launch.ts` | Current callable MCP catalog has pickup/ack but no exact-chat prompt submission. Browser `/steering/current` can fall back to latest when chat is omitted; CS2 must require explicit exact chat, never inherit that fallback. |
| UI readiness | `client/src/components/agent-access/AgentConnectionSetup.tsx` and its tests | UI explicitly says connection checks do not prepare Minecraft. Preserve that truth until shared session readiness is implemented. |
| Temporal admission | `server/services/environment-connectors/temporal-plans/temporal-plan-preflight.ts` and `__tests__/temporal-plan-preflight.test.ts` | Existing tests reject wrong task/run, blocked capability, wrong clock, expired frontier and revoke-during-read. Do not replace these with a single ready boolean. |
| Moving runway | `minecraft-environment-time-compiler.ts` in the same temporal directory; `shared/helix-minecraft-player-capabilities.ts` | Serial actions use existing player primitives; walk has 50–10,000 ms duration, navigate finishes on arrival. A large plan ceiling is not sustained motion. CS3 must measure the actual useful motion envelope before claiming a missing primitive or a capacity pass. |
| Input tooling | Native click/set_value errors in the retained recovery report | Tooling failure, not proven harness UI failure. A later user prompt succeeded. Do not patch Helix admission to compensate for desktop automation errors. |

Frozen first CS1 regression specification: given an active exact chat/run
binding, online source, active movement authority and fresh controller heartbeat,
but a stale observation-subject epoch and old goal identity, session readiness
must be false and name subject re-verification before goal recovery. It must
not request another binding or silently rotate a healthy source. With existing
consent, recovery must reuse the exact subject and run, refresh causal evidence,
and become ready only after every required check succeeds. Calling Ready up
three more times must preserve all valid identities and perform zero rotations.
Add wrong-profile and revoked-authority variants before implementing repairs.

There is currently no combined readiness implementation identified by the scoped
source/catalog inventory, so this is a new composition contract, not an assertion
that the connector's scoped `ready_for_actions` result violated its own schema.

Baseline verification: `helix-mcp-environment-subject.test.ts` (2 tests) and
`temporal-plan-preflight.test.ts` (9 tests) passed together, 11/11, with one fork.
Documentation audit and scoped diff checks passed. These verify existing
boundaries only, not the new Ready up contract. CS0's inventory exit is satisfied;
CS1 begins by implementing the frozen failing regression above.

### CS0 — Freeze the first-divergence baseline

Inventory existing implementations and tests before adding another facade.
Map profile, service, client, task/chat/run, source epoch, subject, action epoch,
lease, goal revision, probe, plan, delivery and re-entry identities. Classify
each observed failure as product, tooling, timing, or required operator consent;
do not infer product failure from computer-control capture errors.

Exit: a reproducible failing fixture or exact retained runtime trace for each
selected defect, an implementation/reuse map, and a frozen next failing test.
Choose one primary failing boundary for the next patch.

### CS1 — One idempotent Ready up workflow

Expose one shared preparation contract to MCP and the EXE environment workflow.
Return per-layer status, remaining duration, exact identities, stable blockers,
safe repair actions and human-only approval requirements. Reuse valid state.
Do not rotate healthy credentials, replace a valid binding or create another
run just to refresh presence. Repair authorized identity/epoch dependencies in
order and stop at genuine consent boundaries. This is deterministic setup
orchestration, not a private model/tool execution loop.

Separate presence freshness, one-time claim TTL, binding-session policy,
source credential lifetime, action lease and goal budget. Expose a server-bounded
test duration; the user-authorized eight-hour development window is not a new
production default. Renewal must not resurrect revoked authority. Short task
presence is agent-maintained while active, not a request to the user to rebind.

Exit: three repeated Ready up calls preserve valid identities and cause zero
unnecessary rotations; expiry/revoke/restart/wrong-profile/stale-subject tests
either recover within existing consent or expose exactly the missing approval.
No broad ready state is returned until a current player probe and applicable
controller/goal checks pass. UI and MCP agree on the same revision.

CS1 partial implementation checkpoint (2026-09-07):
`shared/helix-environment-session-readiness.ts` now provides a pure, ordered
readiness projection over trusted normalized server checks. The focused
`shared/__tests__/helix-environment-session-readiness.spec.ts` suite passed
27/27 tests. It covers stale subject/goal despite a healthy controller, missing
layers, mismatched context, expired checks, invalid clocks, duplicate checks,
missing evidence, revocation retention and non-mutating repeated projections.
The projection grants no execution or answer authority. Its input context is
not itself authentication proof: the server collector must establish it.

This is not yet a Ready up endpoint or packaged feature. No server collector,
consented repair orchestration or UI/MCP integration is proved by these tests.
Repeated pure projections do not satisfy the three repeated Ready up calls
exit test. CS1 remains active; next work is authoritative collection and
dependency-ordered recovery using existing stores before exposing the facade.

The subsequent CS1 server checkpoint extracts
`server/services/environment-connectors/session/bound-session-evidence.ts`
and uses it in existing temporal-plan preflight. It reuses the binding store
and goal/catalog/perception resolver, requires a matching non-null environment
run, and checks the binding both before and after asynchronous evidence reads.
Temporal preflight retains its final binding check after frontier compilation.
Focused verification passed 42/42: readiness projection (27), bound-session
evidence composition (6), and existing temporal preflight (9). The six new
composition fixtures mock underlying stores; they do not prove integrated
database collection, recovery, lease renewal or EXE readiness. Static discipline
checks passed against the dirty checkout; their scan also reports unrelated
changed surfaces. Patch classification: source admission and evidence
normalization. No live-source identity format or continuation protocol changed.
The combined readiness projection is still not wired to an endpoint. Next is
collecting independent layer failures (especially stale subject before stale
goal) and implementing consent-preserving recovery; CS1 remains active.

Independent collection checkpoint: `session/session-readiness.ts` now composes
the existing membership, exact task, environment/subject projection, authority,
controller, goal and perception readers. It rejects a foreign participant or
task before environment reads, reports stale subject and goal together, requires
a finite action lease, checks exact selected identities, sanitizes unknown
errors and rechecks binding before returning. It does not infer new consent
requirements merely from failure. The focused battery passed 59/59, including
17 collector fixtures with injected readers. These prove composition only;
actual database-backed collector behavior, concurrency and end-to-end readiness
remain unverified. No UI or MCP endpoint exposes it yet, and it is not a cached
grant or a lease renewal mechanism.

Recovery implementation note from source inspection: `bindSubject` currently
revokes the existing active subject binding and inserts a new one on selection.
The Ready up orchestrator must not call selection for a healthy subject merely
to refresh status. Any stale-epoch repair must account for the resulting binding
identity and dependent authority/goal recovery. Do not assume the existing
selection operation is idempotent or silently widen it into lease renewal.

Subject verification checkpoint: added `ensureOwnRoomEnvironmentSubject` and
wired healthy-subject collection through it. It uses the existing transaction,
membership, online roster and conflict checks, then returns only the existing
exact unexpired current-epoch row. It never creates a binding, changes an expiry,
or revives stale/revoked state. Ordinary explicit selection retains replacement
semantics. The existing pg-mem-backed room subject route test passed with three
consecutive ensures preserving the complete subject rows and command-grant
binding, plus wrong-player, expiry, restart and revoke rejection checks. This
is database-backed component evidence, not a PostgreSQL concurrency or packaged
workflow proof. Collector fixtures also verify a changed subject during the read
blocks only that layer. Actual stale-state recovery and consent presentation
remain to be implemented; CS1 is not closed.

Epoch-recovery checkpoint: `refreshOwnRoomEnvironmentSubjectEpoch` now verifies
the same existing active, unexpired subject against the admitted current roster
and updates only its observation producer epoch and confirmation timestamp.
It checks the expected previous epoch and exact binding ID, preserves original
verification and expiry, and records one audit event when the epoch changes.
The pg-mem room route test passed with three calls producing one refresh event;
wrong binding/previous epoch are rejected and explicit selection remains a
separate replacement operation. Production concurrency is not proven by pg-mem.

`session/ready-up-session.ts` now contains the first bounded reconciliation
step: inspect, attempt only a known stale subject-epoch repair with current
task binding, then inspect again. It deliberately leaves other blockers open.
Its new composition tests are pending execution after the full discipline
battery, which is running serially following this identity-recovery change.
No MCP/UI entry point or packaged claim is made by this checkpoint.

Recovery dependency inspection found that `resolveTemporalPerceptionContext`
requires an active goal before reading perception, while goal recovery itself
requires fresh perception. Its exact source-credential/epoch/probe evidence read
has therefore been extracted as `readExactEnvironmentPerceptionEvidence` for
internal recovery use. Normal temporal admission still checks active goal and
current action catalog first. The extracted read retains source status,
credential expiry, exact player identity, prior-turn and observation-age checks;
it is not an authenticated endpoint and callers must establish membership,
exact task and current authority before using it. Focused recovery-perception
fixtures are pending behind the serial full discipline run. Goal recovery
orchestration has not yet been connected to this read.

Subsequent source checkpoint (verification pending): `recover-session-goal.ts`
now uses that read for fixed rebound/checkpoint/resume steps and is called by
Ready up only after source, subject, authority and controller checks pass.
Only restart/disconnect recovery reasons are eligible; manual override,
emergency stop, revocation, death and paused goals are not auto-resumed. Each
step rechecks evidence freshness and exact binding, and appends using the
freshly inspected optimistic revision. Identity-only checkpoints claim no
milestone or postcondition completion. A failed multi-step recovery reports
unknown partial change rather than falsely reporting zero changes. New
orchestration fixtures are queued behind the running full discipline suite;
this source checkpoint does not establish integration, concurrency or EXE proof.

Verification update: the full discipline command completed successfully,
including live-source continuation (26 tests), identity audit (9 tests) and
server build. Four duplicate-key/case warnings were reported in unrelated
demonstration, solar-derived and starsim files. The focused session battery then
passed 82/82 across seven files, including the queued Ready up, goal-recovery
and recovery-perception fixtures. Goal orchestration remains mock-backed;
this is not yet full database-backed recovery proof.

MCP integration checkpoint: added `helix_environment_session_ready_up` with
the shared exact-selector request schema and both OAuth catalog mappings.
It derives profile/client/session/participant from authenticated server state,
requires action-write scopes and verifies the exact task/chat/run before
delegating to the shared service. Its description explicitly excludes launch,
selection, permission renewal and gameplay. The local-supervisor MCP suite
passed 24/24, including raw catalog discovery on both surfaces and read-only
scope rejection before invocation. Successful end-to-end MCP invocation,
UI integration, lease-duration presentation and packaged verification remain
open; do not ask for an EXE switch or reconnect based on catalog tests alone.

Ledger integration update: the durable-goal suite passed 22/22 with a new
Ready up recovery test using the actual pg-mem ledger, reducer, hash checks and
optimistic appends. Three later calls leave every event row unchanged and no
milestone postconditions are completed. Binding, connector identity and probe
readers remain fixture ports; this is not a full live-chain test.

The successful MCP-call fixture now verifies server-derived profile/participant
and exact task/chat forwarding, and rejection of another chat before the
preparation executor is invoked. The executor is substituted in this transport
test. Its initial presence fixture was malformed (missing declared checkpoint
support), failed before preparation, and passed after fixture correction. This
failure is test-setup evidence, not a reported product runtime defect.

### CS2 — Supported exact-chat prompt ingress

Reuse normal Helix prompt dispatch. Add a scoped, idempotent MCP submission
surface only if CS0 confirms it is missing. Bind to the explicitly selected
Helix conversation and current task/run/epoch; do not write directly into the
steering store, fabricate a user principal, change destination, or invoke a
provider-private API. Identify agent-submitted versus user-typed origin.
Show the submitted prompt and delivery state in that same EXE chat.

Exit: a natural prompt appears once, reaches the exact bound task and is
acknowledged once; wrong chat/run/epoch, duplicate request, expired/revoked
binding and missing scope have adversarial tests. Pickup/ack is advisory only.
Polling-only idle tasks are described truthfully; no unsupported wake promise.

### CS3 — Instrument and establish continuous moving runway

Use the real compiler, broker, outbox and resident executor with fixed bounded
development courses. Prepare caller-authored candidate plans before timing a
trial; the harness must not invent navigation strategy. Measure actual movement
duration, not maximum timeout. Do not pad a finished route with idle waits,
wall contact, pointless route loops or three independent walks to pass.

Measure observation age, admission latency, durable receipt latency, delivery,
activation, remaining moving runway, stalls and control release on correlated
clocks. Distinguish scheduled motion, admitted work and performed motion.
Freeze timing budgets before rerunning; missed budgets remain negative evidence.
If current primitives cannot supply useful bounded moving runway, write and
test the missing primitive contract before another live capacity attempt.

Exit: real resident execution consumes three linked successors before motion
stops, under a declared measured latency envelope; changed-affordance,
lost/reordered acknowledgement, backpressure and reconnect fixtures prove no
duplicate effects. Simulated timing is prerequisite evidence only. A bounded
Minecraft development trial verifies the mechanics without claiming ET6.

### CS4 — Interruption, re-entry and packaged rehearsal

Prove manual override and emergency stop release controls and invalidate
incompatible queued work. Revoke rejects stale plans without subsequent motion.
Re-enter fresh exact observations into the bound run; receipts never become an
answer. Keep injected interruption separate from genuine operator-input proof.

Build only after focused source checks pass. Record renderer/service/companion
hashes and launch the exact tested EXE. From the ordinary destination setup and
environment workflow, demonstrate Ready up, approvals, prompt submission,
visible result and in-place recovery. Browser diagnostics are not EXE UI proof.
Retain rollback until the replacement verifies; recycle only authorized builds.

Exit: one artifact-qualified rehearsal passes without ad hoc configuration,
code edits, repeated authentication, provider-task replacement or UI hunting.
Any human-only step is deliberate, correctly highlighted and state-resolving.

### CS5 — Resume original ET6, unchanged

Reconcile every original work-packet criterion and evidence field before the
trial, including controlled/unknown course coverage, three rolling successors,
timing percentiles, evidence volume, fresh re-entry, real interruption,
reconnect, revocation and stale rejection. Keep unavailable measurements null.
Only its full acceptance artifact can close ET6 and unlock NAV1.

## Development loop and stopping rules

### CS1 scoped typecheck and duration reuse audit (2026-09-07)

A no-emit compiler pass over the readiness/request contracts, preparation
service, binding store and Ready up control found no diagnostics in those
targets or session-service files. A wider route/setup pass found the existing
route dependency type omitted `inspectCurrent` and `inspectLatest`; that type
now includes the methods actually called. Including the real desktop bridge
declaration (`RuntimeSurfaceProvider.tsx`) removed narrowed-harness ambient
errors. The repeated route/setup check has zero targeted diagnostics and 132
dependency-graph diagnostics: this is not a clean repository-wide typecheck.

Duration reuse inspection confirms Player Embodiment already offers finite
1/2/8/24-hour and longer lease selections, a capability acknowledgment and an
expiry notice. Ready up must direct genuine approvals to that existing boundary,
not add an independent lease-grant mechanism or infer consent from a stale check.
Its navigation/approval integration remains open; no consent was activated.

### CS1 typed recovery presentation and build checkpoint (2026-09-07)

Environment preparation now preserves typed goal/subject/authority failure
codes instead of reporting them as unavailable reasoning bindings. Private
exception messages are not returned. UI recovery copy distinguishes ambiguous
goals, missing goals, stale player evidence and unavailable snapshots from
task-presence failures. Route/UI tests passed 17/17.

`npm run build` passed with the current expiry and recovery UI included (client
39.49 seconds; server bundle passed). Existing browser externalization/eval/
chunk warnings and four server duplicate-key/case warnings remain. This is not
packaging or live proof. Packaging preflight found about 708 MiB free on C:;
retained unpacked builds are about 587 MiB each, with `release` about 796 MiB.
No build was deleted or replaced. Revalidate space and exact active artifacts
before packaging; source-side CS1 work can continue independently.

### CS1 readiness display lifetime checkpoint (2026-09-07)

Production client build passed before the following display-lifetime change,
with browser-externalization, dependency-eval and large-chunk warnings. It is
not an updated packaged EXE or proof of the subsequent change.

Readiness projections now include `valid_until_ms`, bounded by the earliest
checked lease or observation-freshness deadline; blocked projections return
null. Agent Access stops displaying cached readiness after that time and
directs the user to Ready up, explicitly distinguishing evidence freshness
from binding/permission expiry. The focused projection/UI tests passed 32/32;
all six session service suites passed 60/60. Rebuild the client after this
change before any packaged rehearsal. No session lease was extended.

### CS1 automatic browser preparation and control (2026-09-07)

Follow-up integration checks: the automatic HTTP branch maps the authenticated
owner and exact selection to its service; preparation arguments pass the real
snapshot descriptor's input schema and non-snapshot observations are rejected.
An internal guard rechecks binding validity immediately before probe dispatch,
after asynchronous admission. Its revocation fixture performs zero dispatch.
The route/gateway/preparation run passed 61 tests; the subsequently strengthened
preparation suite passed 7/7. These remain fixture-backed checks, not EXE/live
evidence. The automatic endpoint still needs a real-transport rehearsal.

The browser route now also accepts the exact binding/run selection plus a
request ID, without goal/probe/source IDs. `prepare-browser-session.ts` resolves
the verified run association, membership, unique goal and same subject, checks
the account context, requests a first-party snapshot through the existing
gateway, and passes its observation into shared Ready up. Stable request-derived
probe identities enable broker deduplication. This is not an external-provider
execution, and no lease or consent is renewed. Missing prerequisites fail rather
than creating a run, selecting a different player or manufacturing evidence.

Agent Access exposes `EnvironmentSessionReadyUp` on an active binding. The
control distinguishes blocked checks from HTTP success and disables chat-only
preparation. Backend route/orchestration tests passed 18/18; UI tests passed
39/39 after updating an assertion about the superseded static readiness text.
The backend automatic orchestration uses injected readers; end-to-end automatic
route, real snapshot transport, simultaneous-request behavior and packaged EXE
verification remain pending. CS1 is not complete. Source/run bootstrap, finite
lease recovery and actionable approval presentation still need integration.

### CS1 setup observation constraint checkpoint (2026-09-07)

Reuse inspection found an existing first-party browser observation path in
`workstation-tool-gateway/environment-probe.ts`. Its new optional, internal
`expectedEnvironmentIdentity` constrains room, source, world, connector and
resolved player before dispatch. Missing exact connectors do not trigger
materialization or a fallback. The field is not part of model-authored arguments;
ordinary calls without it retain existing behavior. Existing browser account,
room consent and read-grant checks remain in the path.

The gateway suite passed 42/42, including exact first-party dispatch and four
wrong-target negatives with zero dispatch/materialization. Static discipline
passed with source-admission classification. These are mocked connector tests;
automatic Ready up observation acquisition and packaged execution remain pending.
The broader prompt benchmark and full continuation battery have not been rerun
for this new optional constraint; do not treat the focused result as those gates.

### CS1 exact goal discovery checkpoint (2026-09-07)

The goal store's `findForSession` locates the exact participant/profile/room/run
against the latest ledger event, then revalidates the reduced goal. It returns
no match rather than substituting another run, excludes terminal goals, preserves
paused goals as paused and rejects multiple unfinished matches explicitly.
The database-backed goal suite passed 23/23, including this discovery path.
This removes the need for a future UI adapter to guess the newest goal, but
automatic probe discovery/request and the UI adapter are still pending. No
readiness, recovery or movement authority follows from discovering a goal.

### CS1 owner HTTP preparation checkpoint (2026-09-07)

`POST /api/account/session/agent-connections/environment-session/ready-up`
now invokes the same preparation service as MCP. It authenticates the browser
owner, requires active account linkage, resolves the exact existing task target
internally and derives room participation server-side. Provider continuation
fields from the browser are rejected; the internal target is not returned.
The route does not claim, acknowledge, grant authority or dispatch gameplay.

The agent-connections route suite passed 12/12 with an injected preparation
service, covering target mapping, wrong chat, injected provider identity,
missing membership, revoked account linkage and revoked binding. The server
bundle built successfully with four unrelated existing duplicate-key/case
warnings. This is HTTP/component evidence, not a packaged UI rehearsal.
Next: automatic exact session-selector discovery and the ordinary EXE control;
the new endpoint alone does not make UI preparation complete. CS1 stays open.

### CS1 recovery-orchestration checkpoint (2026-09-07)

CS1 remains active and incomplete. The shared Ready up service now has 12
passing focused orchestration tests in
`server/services/environment-connectors/session/__tests__/ready-up-session.test.ts`.
They cover healthy no-ops, exact stale-subject recovery, binding checks,
fresh inspection using the recovered goal revision, blocked prerequisite
layers, typed partial-recovery conflicts without retry, and propagation of
unexpected failures. A recovery receipt alone never establishes readiness.
These injected-reader tests are component evidence, not packaged or live proof.

Next integration boundary: expose the shared preparation workflow through the
authenticated EXE setup path while distinguishing the browser owner from the
bound external AI task. Do not supply browser-posted provider identity as if it
were authenticated MCP identity. Ordinary UI preparation, complete prerequisite
reconciliation and packaged parity remain unproven; no ET6 trial is unlocked.

The binding store now provides an internal `resolveOwnedPreparationTarget`
operation for that boundary. It resolves only an active, exact owner/chat/run/
mission/epoch association with live matching task presence; it does not select
the latest task, mutate a binding or confer provider authority. Its raw result
must stay server-side. The binding-store suite passed 9/9, including no-op
resolution and wrong-owner, wrong-target, revoked and expired-presence negatives.
This is source-admission component evidence only: no browser preparation endpoint
or EXE integration is claimed by this checkpoint. Static discipline and scoped
diff checks passed; full continuation verification has not been rerun for this
new method, which does not change existing pickup or acknowledgement paths.

Work one failing boundary: reproduce, patch, focused regression, bounded
component rehearsal, then update evidence. Do not repeatedly run the full
acceptance journey to discover missing setup features. After the same typed
failure recurs without a changed implementation or relevant external state,
stop that experiment and repair or explicitly request the missing dependency.
No reconnect/rebind/build loop without evidence that it addresses the failure.

CS1 automatic transport checkpoint: MCP Ready up now accepts the same exact
binding/chat/run/request selection as the browser, plus its authenticated task
continuation. The old explicit-evidence input remains supported. Both automatic
paths use the same preparation service; MCP additionally verifies that the
authenticated task matches the internally resolved target before observation.
The gateway receives the authenticated account context, not a manufactured
external reasoning run. Focused MCP and preparation tests passed 33/33, including
wrong-task rejection before another observation. These tests mock preparation
at the MCP boundary and mock service dependencies separately; they are not an
integrated broker or packaged parity proof.

Ready up also returns its exact room selection for navigation. The UI can open
that room's existing settings only when its controller is present and not closed;
otherwise it reports unavailable controls without opening another room. The two
focused UI suites passed 11/11. No consent control is activated. Automatic
scrolling to a specific missing approval, full lease reconciliation and the
ordinary packaged workflow remain unverified. CS1 remains active and incomplete;
ET6 and NAV1 status are unchanged.

Follow-up integration inspection found two gaps hidden by the mocked adapter
test: this MCP SDK publishes an empty input object for a top-level Zod union,
and the supervisor coordination session ID is not the gateway account session
ID. Ready up now publishes an object schema, validates the two strict command
forms inside the handler, and passes the principal's actual account session to
preparation. The MCP test checks the advertised fields and account-session
mapping, not only a successful call. The same 33-test battery passed after these
repairs. This reinforces that component success alone does not close CS1.
The browser-route and strict request-shape suites also passed 19/19, rejecting
mixed automatic/manual arguments and caller-supplied identity. Documentation
audit passed. A bounded TypeScript program including the MCP server completed
but reported 2,059 diagnostics across its dependency graph, so it is not a clean
typecheck or release gate. No EXE was packaged or live session changed here.

Combined preparation checkpoint: new fixtures compose automatic preparation,
the actual readiness collector/projection, Ready up orchestration and goal
recovery. Storage and physical observation are still injected. The restart
case checks subject refresh before observation, three ordered recovery writes,
and three subsequent calls without another repair or revision change. Revoked
action authority and manual override remain blocked without goal recovery writes.
This exposed a lost repair in the response: subject refresh before observation
was absent from the final receipt. Preparation now preserves that repair alongside
the later goal result. The receipt remains non-authoritative; this is not a live
gameplay trial or database concurrency proof. Mid-request failures after a repair
still need an explicit partial-outcome presentation audit.

Partial-outcome presentation checkpoint: Ready up now renders the returned
subject/goal repairs separately from its readiness status, including uncertain
partial recovery, and a transport failure explicitly warns that setup steps
may already have completed. Eight focused UI tests passed. This does not solve
structured partial effects for an exception after pre-observation subject
repair: that server error path still needs evidence-preserving transport across
both adapters. No rollback or broad session-readiness claim is inferred from
a missing response.

Structured partial-failure checkpoint: automatic preparation now wraps failures
after repair entry in a sanitized session error containing confirmed repairs,
an explicit unknown-partial-effects flag and no readiness/answer authority.
Both browser and MCP adapters preserve this same projection. Unknown exception
messages are not exported. Fifty focused service/HTTP/MCP tests passed, including
failure after subject refresh, uncertain later preparation and identical error
projection at both adapters. UI consumes repairs even on a failed HTTP response.
The earlier structured-error gap is repaired for automatic preparation; this
does not prove database transaction or live connector behavior. CS1 is incomplete.

Run-lifetime checkpoint: automatic preparation revalidates the database-backed
run/room association before subject repair, after observation and after recovery.
A run expiring during observation blocks recovery; a changed association during
recovery blocks the readiness response while retaining reported repairs. Fifteen
preparation tests passed, including these two asynchronous race fixtures. The
association reader is injected in these tests: no live database race proof is
claimed. Finite lease display/renewal and remaining integrated CS1 criteria remain
open; repeated task-presence checks are not a substitute for those requirements.

Run-deadline projection: the existing association query now returns the stored
run expiry and rejects invalid/past values. Automatic Ready up exposes that
deadline separately and bounds readiness validity by it. The UI labels it as
an environment run deadline, not chat-binding or gameplay permission duration;
no renewal is performed. The four association tests include pg-mem query execution.
Full source/action/goal-budget reconciliation and consent-preserving renewal
remain open. This deadline display does not itself close CS1.

Lease-renewal inventory checkpoint: the existing MCP operation
`helix_environment_action_authority_extend` delegates to
`extendEnvironmentActionAuthorityLease`. The MCP input bounds the requested
deadline to one minute through seven days, and the store checks exact owner,
room, environment and stored active status. The inspected store selection does
not independently require the prior deadline to remain in the future; its
credential update likewise selects stored active status. Therefore this is not
yet evidence of consent-preserving automatic renewal. Before composing it into
Ready up, add store-level tests for expired-but-stored-active authority and
credentials, invalid deadlines, revoked state, and concurrent expiry/revocation.
Separate explicit owner extension from automatic reuse of a previously
consented session budget. Do not introduce an unconditional sliding seven-day
lease. No renewal was invoked during this inventory; CS1 remains incomplete.

Lease-store regression checkpoint: three new cases failed before repair because
invalid requested expiry and expired/invalid stored-active prior expiry reached
the update path. The store now validates finite deadlines after the row lock,
guards the update with the database wall clock, and does not extend expired or
revoked connector credentials. A failed guarded authority update stops before
credential updates. Thirteen focused lease/supersession tests passed, including
pg-mem execution of the actual SQL with identity/policy preservation and
expired/revoked credential negatives. The guarded-update failure is injected;
pg-mem is not concurrent production PostgreSQL evidence. This repair does not
implement automatic renewal or decide a consented session renewal budget.
Ready up integration, packaged proof and CS1 completion remain outstanding.

Gameplay-deadline presentation checkpoint: Ready up now displays the authority
check's finite deadline separately from the run deadline and short readiness
freshness. Expired/revoked permission points to Player Embodiment while retaining
the chat binding; a blocked controller is described separately and does not
automatically demand consent. Twelve focused UI tests passed, including expired
and revoked variants with zero additional requests. This is JSdom presentation
evidence, not EXE navigation proof. Automatic renewal remains unimplemented;
the generic exact-room settings action still needs targeted control navigation.

Targeted-navigation checkpoint: the Ready up settings request now includes its
returned exact environment ID. The matching room dialog reveals and focuses
that environment's player-settings region after it renders, without clicking
controls. Unknown targets remain explicitly pending and closable, never falling
back to a different environment. Twenty-two focused UI/dialog tests passed
after supplying the required public-results array in an older room fixture.
These tests include target/missing-target focus and exact-room rejection; they
do not prove real EXE layout or network-loaded room availability. No consent or
lease was changed. CS1's automatic renewal and packaged workflow remain open.

Accumulated-source build checkpoint: `npm run build` passed after lease expiry,
deadline presentation and targeted navigation changes (3,327 client modules;
client 42.91 seconds; server bundle 39.5 MB). Browser externalization/eval,
mixed imports and server duplicate-key/case warnings remain. This is bundling,
not a clean TypeScript check or packaged EXE acceptance. SHA-256:

- `dist/index.js`: `73475B45D1F6E581AD2B97A685313A9FC56FCFDE3BD4459BD984C4BC7354B902`
- `dist/public/index.html`: `BA0C78A281248E5334BE579AD81CF140BC89D11C948CF2917ECA24A2ADAAADA1`

The existing Player Embodiment duration selector includes eight hours with
explicit acknowledgement; reuse that finite development window rather than
invent an indefinite sliding renewal. No separate renewable consent budget was
found in the inspected shared environment/Minecraft contracts. This inventory
does not establish server/UI duration parity or remove the remaining CS1 lease
reconciliation requirement. Disk free after the build was 701,054,976 bytes;
no EXE was packaged, replaced or deleted at this checkpoint.

Duration-parity inventory: the Player Embodiment selector sends `leaseMs` to
action authority settings and connector pairing. The separate agent run uses
`request.budget.expires_in_seconds`, whose default is one hour and maximum is
seven days. Pairing permits at most thirty days; its bootstrap store clamps to
that same maximum. Thus selecting eight-hour gameplay does not establish an
eight-hour run. Two cross-contract tests passed for explicit eight-hour values
and finite ceiling rejection, preserving the unchanged one-hour run default.
This is schema evidence only, not coordinated session duration or store proof.
Next integration must carry a deliberate workflow duration to the existing run
start request and the separate consented action/pairing requests, or report the
shorter already-existing run deadline. Do not extend or replace an existing run
merely to align clocks, or claim that a gameplay selector already does this.

Play-workflow handoff checkpoint: the existing Play button delegates scoped
run/goal setup through bound steering rather than directly creating a run. Its
instruction now carries the existing binding's run ID, requires preserving that
run, and requests an explicit remaining finite budget only when a new run is
necessary and authorized. Displayed deadlines require server revalidation; no
automatic grant/renewal or invented association is permitted. Eleven focused
journey/player-panel tests passed, including bound-run and chat-only guidance.
This is prompt-handoff evidence, not proof that the receiving model creates a
correctly budgeted run or that CS1 duration reconciliation is complete. The
previous bundle hashes precede this source change and are not updated EXE proof.

Approval-projection checkpoint: the authority store's explicit new-grant rule
is now reused by readiness for an exact expired/revoked authority. It sets the
existing human-approval flag, and the UI names room-owner review without
activating any control. Unknown identities, invalid timestamps and controller
failures do not imply new consent. Forty-one preparation/collector/lease tests
passed, followed by 32 collector/UI tests including two additional invalid-date
and wrong-player negatives. This replaces the earlier unconditional false flag
for this specific governing boundary; other consent layers remain unqualified.
No automatic renewal, live permission change or EXE proof is claimed.

Rejected-probe recovery checkpoint: automatic preparation now performs read-only
diagnostic collection after a returned probe rejection, preserving exact-room
settings navigation and independent approval blockers. Perception is forced
blocked and goal recovery is not invoked. The run association is revalidated
before and after diagnostics. Seventeen preparation tests passed, including
revoked authority plus rejected observation with no goal recovery. Thrown
transport exceptions retain the separate sanitized partial-failure path. This
is injected-service evidence, not a real connector failure or EXE acceptance.

Combined transport checkpoint: 78 preparation, Ready up, browser-route, MCP and
UI tests passed together. Strengthened HTTP/MCP assertions preserve blocked
readiness, the approval flag, exact settings selection, run deadline and
non-authority flags. The initial MCP assertion incorrectly expected a flat
payload; its established envelope nests these facts under `receipt`. Corrected
assertions passed (HTTP 12; MCP 25). Services remain injected at these transport
boundaries, so this is transport preservation, not end-to-end database/connector
parity or a CS1 exit claim.

Desktop-host build checkpoint: process inspection found no CasimirBot/Electron
process and no listener on the previously used 1522/59485 ports. The current
callable catalog did not expose `helix_environment_session_ready_up`; no old
claim was reused or reconnect attempted. After inspecting the packaging scripts,
`npm --prefix apps/desktop run build:host` passed, including pinned tunnel-client
verification and the service dependency manifest. Duplicate-key/case warnings
remain. SHA-256: `apps/desktop/dist/service.mjs` =
`E1F12C788FD1CAFE0D0F313BFA6E05E5C3C13CF914279298EB75217C4B9A172B`;
`apps/desktop/dist/main.cjs` =
`04F15D251266B65E28607990428F50C786BF1A4ED27A6EEFAF66E86CB657848C`.
Client rebuild, runtime staging, packaging and ordinary EXE verification remain.
No existing packaged build was deleted or launched.

Client/staging checkpoint: the current client rebuilt in 37.47 seconds. Runtime
staging passed dependency closure, data-asset and pinned tunnel checks. An
independent relative-path/SHA-256 comparison found 635 built client files and
635 staged files with zero differences. Staged runtime manifest SHA-256:
`963B6815F5E12D143E6072A06E61E2B96E0A9C9800EE5C6435DC0934013572D7`.
Only the generated staging tree was replaced, reproducible from the build;
packaged rollback builds were untouched. Disk free was 365,207,552 bytes, so a
new package was not attempted. Full built/staged/packaged verification and EXE
workflow evidence remain unproven; staged parity does not substitute for them.

Packaging-space checkpoint: under the user's earlier recycle-only authorization,
three verified obsolete desktop output folders were moved to the Windows Recycle
Bin: `release-et6-chunked-snapshot-20260907`, `release-et6-expiry-ui-20260907`, and
`release-et6-snapshot-batch-20260907` (1,845,084,596 bytes total). Exact child paths
and absence of reparse points were checked first; no CasimirBot/Electron process
was running. The newer `release-et6-event-batch-20260907` rollback was preserved.
These folders remain recoverable until the user empties the bin; recycling alone
does not reclaim disk capacity. Source, evidence and current staging are intact.

Post-space packaging checkpoint (2026-09-08 UTC): disk free was verified at
25,774,362,624 bytes. Two new binding-switch UI regressions first failed, then
passed after keying Ready up state to the complete binding identity/status;
the UI suite passed 14/14. Binding changes now unmount/abort the old request
and clear its cached readiness rather than displaying another binding's facts.
The client rebuilt in 39.80 seconds and runtime staging passed. A separate
development package was created at
`apps/desktop/release-cs1-ready-up-20260908/win-unpacked/CasimirBot.exe`;
the prior event-batch rollback was preserved. No package was published.
Independent SHA-256 comparisons found all 645 staged/packed runtime files
identical, all 635 built/staged renderer files identical, and the packaged
ASAR main/preload/service identical to their desktop build outputs.
EXE SHA-256: `bd025acb0379389825e02986a7f32a085ffbda69800880e830e42a9621d69c16`.
Runtime manifest SHA-256:
`a475cd4a376561bc93746a200430bb14665be2a50bbe1094f3f2a30213a1584c`.
This is packaging parity, not signed-release certification or ordinary EXE
workflow proof. The EXE has not been launched in this checkpoint. CS1 remains
incomplete; CS2-CS4, live ET6 capacity and the NAV1 gate remain unchanged.

Packaged navigation rehearsal (2026-09-08 UTC): launched the new CS1 EXE by
its exact path. The initial computer-control launch reported no targetable
window, but the original processes remained live; a later fresh window listing
found the workspace without restarting. The service ready receipt reported
ready at 02:58:37Z. Actual Windows accessibility/screenshot inspection and clicks
proved Destination -> Set up connection -> confirmation -> Agent Access. This
is native EXE evidence, not browser substitution. No authentication or consent
control was activated.

The EXE initially waited for task presence. MCP presence registration from this
exact continuation succeeded at 03:02:08Z after correcting two rejected request
arguments (180-second maximum; continuation declaration requires checkpoint
support/policy). Recheck and subsequent observation showed AI app connected,
current catalog/chat attachment and polling continuation. The run checkbox was
disabled with the explicit no-verified-run explanation. No claim was generated.
This proves the connection-navigation path only: it does not prove Ready up,
environment authority or movement. Next CS1 boundary is run/goal bootstrap or
restoration before run-associated binding; current automatic preparation requires
an already bound run and an existing matching goal, so it cannot close this
ordinary setup path on its own. Preserve this distinction rather than requesting
another chat-only claim. Startup logs also showed profile snapshot HTTP 413 and
a reasoning-theater config fallback; neither is yet established as causal here.

Run-preparation discovery checkpoint (2026-09-08 UTC): source inventory found
idempotent `HelixAgentApiService.startRun` and explicit run/room binding, but no
room-scoped preparation lookup. Added read-only
`SharedLiveRoomBindingStore.listRunPreparationCandidates`, returning at most two
owner/room/participant-scoped active candidates so a caller can detect ambiguity
without selecting the newest. It checks the complete run owner tuple, current
membership role/consent identity, active room binding, room status, finite run
expiry and cancellation. Three repeated reads preserve all binding rows.
The existing binding-store suite passed 8/8 with the new pg-mem-backed case
covering foreign owner fields/room/participant, changed consent, revoke,
cancellation, expiry, departed membership, closed room and invalid clock.
This is component evidence only: task/objective compatibility, ambiguity tests,
bootstrap orchestration, transport wiring and packaged re-verification remain.
No live state was mutated and the running EXE does not include this new lookup.
Patch classification: source admission / evidence normalization. The disabled
run checkbox is not fixed by this component alone; CS1 remains incomplete.

Discovery budget regression (2026-09-08 UTC): a new database-backed fixture
created two distinct owner-scoped runs using the real run store, bound both to
the same room, and confirmed discovery preserves ambiguity. Exhausting one
run's step budget initially left it in the candidate list (1 failed, 8 passed).
The query now also requires remaining steps and no completion timestamp.
The rerun passed 9/9, including exclusion of a completed second candidate.
This repair does not extend a budget, resume a run, or select a candidate.
Discovery is still not wired to the pre-binding Ready up workflow; no new
EXE or integrated-session claim follows from this regression.

Pre-binding MCP discovery integration (2026-09-08 UTC): the existing
`helix_environment_session_ready_up` accepts the strict `discover_runs`
operation with exact room and continuation selectors, without requiring a task
binding. It authenticates the client, enforces its existing scopes plus run-read,
resolves the participant through the room service, and calls the owner-scoped
candidate query. It does not create/select a run, bind a chat, or run repairs.
The receipt explicitly retains ready=false, task_binding_verified=false,
revalidation_required=true and non-authority flags. Two candidates mean the
candidate limit was reached, not a claim that additional records exist.
The published SDK schema remains an object; strict command-union validation
rejects mixed preparation/discovery fields and caller-supplied owner identities.
Schema/MCP tests passed 33/33; the MCP suite then passed 25/25 after adding a
run-read-scope denial assertion proving discovery was not called again.
These transport tests inject the candidate reader; the separate database tests
prove its query. Browser entry, run creation/restoration, exact task/objective
compatibility and packaged catalog/runtime verification remain open. The running
EXE has not been rebuilt with discovery, and CS1 is still incomplete.

### CS1 pre-binding preparation handoff boundary

Authorization inspection (2026-09-08 UTC) found that browser Ready up authenticates
a cookie-owned account while run creation uses a separately verified agent API
principal (tenant, issuer, subject, profile and scopes). Native MCP authentication
also requires its protected desktop/session delegation. A browser click must not
manufacture that principal, copy the external client's scope set, or create a
second unrelated run owner merely to make the run visible.

Implement the remaining pre-binding path as a bounded setup handoff:

1. EXE Ready up records an explicitly requested structured preparation intent
   for one current authenticated task presence, exact local chat, selected room
   and finite requested duration. Derive account and target client identity on
   the server; do not accept caller-supplied owner identity. This is setup intent,
   not task-binding consent, chat-history access, a game grant or a model prompt.
2. The same Ready up MCP entry point reads only intents addressed to its own
   authenticated client/session/continuation. Runtime Codex handles the intent
   using its own supported run discovery/start/room-binding tools. Reuse a
   compatible verified run; never select the newest ambiguous candidate.
   Preserve one request identity across uncertain delivery/retry so a partially
   created run can be recovered with the existing run API idempotency key.
   Do not implement a server-side model loop or claim to wake an idle provider.
3. Publish the exact resulting run through existing supervisor presence and
   revalidate its room association. Only then reveal the run checkbox for the
   user's separate binding decision. Completion of the setup handoff means
   run preparation only; it must not mark player/controller/goal readiness true.

Required first regression for this integration: a browser intent cannot be read
or completed by another profile, client, continuation or expired target presence;
three retries return the same intent with no duplicate run-start instruction.
An acknowledged intent without a current verified run remains incomplete and
does not enable run binding. After service restart, stale handoff completion
cannot revive a run association. Pending setup remains distinguishable from
actual human approval and from a provider task that is not polling.

This boundary is a specification, not implemented handoff evidence. Reuse the
existing coordination stores where their owner/identity semantics fit; do not
forge a browser sender as an MCP client to reuse a relay API. Keep the current
post-binding Ready up repair service as the next stage of this same workflow.

Preparation-intent component checkpoint (2026-09-08 UTC): added
`session/preparation-intent-store.ts`. Browser selection verification is a
required injected port, and current exact profile/client/session/continuation
presence is checked before and after that verification. Requests carry only
structured room/chat/duration selectors; their origin is browser setup, not
external reasoning. Same-ID retries preserve one intent; changed payloads,
foreign readers, stale presence, expired intents and changed service identity
fail closed. Public projections are cloned and explicitly non-authoritative.
The store has a ten-minute delivery TTL distinct from the requested session
duration and a 128-record fail-closed capacity bound; it does not evict dedupe
records to create duplicate instructions. Capacity lifecycle is not yet
production-qualified. It contains no run-start, consent, model or completion
operation. Nine focused fixtures passed, including required selection rejection
and presence changing during its asynchronous verification. Selection validation
is mocked here; real route/store composition and browser/MCP wiring remain the
next work. No EXE-ready or CS1-complete claim is made.

Preparation transport checkpoint (2026-09-08 UTC): browser
`environment-session/prepare-request` now authenticates the session and active
account link, rejects caller owner fields, and submits structured selectors to
the mailbox. The default mailbox verifies current room membership. Source
inspection clarified that these Helix chat IDs are local browser references,
not `agi_chat_sessions` records: the receipt labels their basis
`browser_declared_profile_scoped`; it does not claim server-verified chat
ownership or read chat history. This refines the selection-validator wording
above without broadening binding or execution authority.

The same coordination-store instance keys a service-local mailbox shared with
Ready up's new `read_preparation` MCP operation. The MCP caller's profile,
client/session and continuation are derived through its existing authentication;
the read returns pending intents only, never readiness or completion. Focused
existing regressions passed 54/54, then HTTP/MCP tests passed 38/38 after adding
direct browser owner derivation, forged-owner/unlinked rejection, and pre-binding
MCP read checks. The HTTP enqueue is injected in that transport test; real room
query plus mailbox plus both transports is not yet one integrated fixture.
UI request creation, task-side preparation, completion observation and EXE
verification remain open. No active runtime was rebuilt or restarted here.

Preparation UI checkpoint (2026-09-08 UTC): the no-verified-run branch now
offers `Ready up this room` when the exact task and local chat are known. It
lists available rooms, requires selection when ambiguous, and submits only a
structured preparation request with a requested finite duration. Pending is
explicitly not ready, not an idle-task wake, and not binding or game authority.
Three focused component fixtures passed: ambiguous room selection and exact
payload, uncertain-delivery retry identity and authority-response rejection,
and abort on target replacement. The parent keys the component to profile,
service, client session, task and chat. These are component tests, not EXE proof.
Remount-safe request persistence, expiry/recovery presentation, task-side
fulfillment and integrated runtime qualification remain open. The running EXE
does not yet contain this UI; CS1 remains incomplete and ET6 remains unproven.

Preparation retry checkpoint (2026-09-08 UTC): request identity is now written
to window session storage before delivery and reused after panel remount,
scoped to profile, service epoch, client session, exact task/chat, room and
requested duration. Storage failure prevents delivery instead of risking an
unrecoverable retry identity. This retains IDs, not credentials or authority.
The UI displays the server preparation-delivery deadline separately from the
requested game-session duration and does not silently renew an expired request.
Five preparation UI tests plus fourteen existing Ready up UI tests passed.
The added fixtures cover remount replay, service isolation and storage failure.
This does not yet provide cross-window deduplication or expired-intent recovery;
task-side fulfillment and integrated EXE qualification remain open. No runtime
was restarted, and the packaged EXE is unchanged.

Preparation cross-window checkpoint (2026-09-08 UTC): the service mailbox now
coalesces concurrently submitted identical pending selections even when browser
windows use different request IDs. Each ID remains an immutable retry alias;
aliases do not extend expiry, duplicate inbox entries or allow payload changes.
Different chat/room/duration selections remain distinct, and expired aliases
cannot resurrect when a new explicit request is accepted. Twelve mailbox tests
and fourteen HTTP tests passed (26 total). The new HTTP fixture uses the real
mailbox to prove three requests produce one inbox intent and stale presence
rejects both replay and read. Browser-session and room-validation ports remain
fixtures; this is not a real database/MCP/EXE end-to-end qualification.
Current callable tool discovery includes run start and room binding but no
environment-session Ready up inbox tool. No run was started without a selected
intent, and no reconnect or replacement task was requested. Task-side
fulfillment, expiry recovery and coherent package qualification remain open.

Existing run-setup composition checkpoint (2026-09-08 UTC): production
`HelixAgentApiService.startRun`/`inspectRun`, `HelixAgentRunStore` and
`SharedLiveRoomBindingStore.bindRunToRoom`/candidate discovery were composed
against pg-mem with real migrations. Three repeated start/attach/inspect calls
retain one run and one room binding, preserve the eight-hour budget deadline
and zero used steps, and never invoke the injected turn executor. A different
principal subject cannot inspect the run. The ten binding-store tests pass.
This establishes that setup can reuse existing run APIs rather than introducing
another execution loop. It uses a declared test principal and seeded room
membership, not browser-principal promotion or live provider authentication.
The pre-binding inbox-to-task handoff and verified association publication are
not covered by this fixture; task-side completion/recovery and packaged UI
qualification remain next. CS1 is still incomplete.

Preparation retirement checkpoint (2026-09-08 UTC): Ready up now supports
`acknowledge_preparation` for the authenticated exact task, intent and run.
Its production verifier checks the selected run using the owner/room/member/
consent/lifecycle/budget query, with an exact run filter rather than a truncated
discovery list. Only successful verification retires pending pickup as
`run_prepared`; aliases preserve that result. Retries revalidate the run,
different-run replacement fails, and presence loss during validation prevents
retirement. The receipt remains non-authoritative and `ready: false`; publishing
fresh presence and human review of run association remain separate steps.
The browser handles this replay state without claiming gameplay readiness.
Mailbox/store/schema regressions passed 32/32 and MCP/UI regressions 30/30.
MCP coverage here proves nonexistent-intent rejection; positive acknowledgement
is a store-level fixture with an injected verifier, not integrated live proof.
EXE rebuild, positive full-path qualification and expired-intent recovery remain
open. CS1 is not complete; no ET6 or NAV1 advancement is claimed.

Browser/MCP transport and package checkpoint (2026-09-08 UTC): one combined
fixture now sends the browser HTTP request into the same production mailbox
instance read by MCP, acknowledges its exact run, verifies inbox retirement and
browser replay, then rejects acknowledgement when run verification fails.
The 26 MCP coordination tests pass. Room/account/run verification remain
injected fixtures, so this is not live authentication or EXE acceptance.
Renderer, host/service, staging and directory packaging succeeded; all 645
staged runtime files and three ASAR entrypoints match their build outputs.
Artifact hashes and limitations are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-preparation-package.json`.
The new package has not been launched; the prior running EXE is unchanged.
Native workflow rehearsal, expiry recovery and CS1 exit qualification remain
open. No signed-release, ET6 or NAV1 advancement is asserted.

Native preparation checkpoint (2026-09-08 UTC): the verified replacement EXE
was launched after normal shutdown of the old build. Ordinary destination setup
opened Agent Access; this same task's authenticated presence refresh advanced
the panel automatically to the new preparation controls. Selecting the existing
Shared GPT Live room and requesting preparation produced a visible pending
receipt and delivery countdown. No binding, trust or gameplay consent was
activated. Detailed positive/negative observations are in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-native-preparation-request.json`.
Current callable catalog still lacks `helix_environment_session_ready_up`, so
native request creation is proven but live task pickup/acknowledgement is not.
No reconnect loop was requested. Catalog capability alignment and expiry
recovery remain development work; CS1 is not closed.

Transport recovery checkpoint (2026-09-08 UTC): the advertised authorization
status tool initially returned not-found. The supported tunnel request/execute
path reused existing device trust; a 300-second delegation restored full
transport, and the subsequent g2-action authorization check passed with no
missing scopes. The rejected eight-hour transport request is retained as
negative evidence: that connector schema caps this distinct delegation at 300
seconds. No user consent click, reconnect, task replacement or gameplay grant
was used. Current callable discovery still lacks Ready up, distinguishing the
remaining catalog-availability gap from restored transport/scope readiness.
See `docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-transport-recovery.json`.
This is not live inbox pickup or CS1 completion.

Preparation expiry recovery checkpoint (2026-09-08 UTC): the UI now offers
`Check preparation` after enqueue, reusing the same request identity to observe
pending or run-prepared replay. A local countdown alone cannot replace an
intent. Only the server's typed `preparation_intent_expired` response reveals
an explicit `Request preparation again` action. Its new ID is persisted before
delivery and retained after uncertain delivery. A stale task-presence response
instead requests same-task presence refresh, not rebind/restart or replacement.
Seven UI and fourteen mailbox tests passed (21 total), including no automatic
replacement from a past local deadline and stable IDs after replacement failure.
This change is source-only after the recorded native package; the running EXE
does not yet contain expiry recovery. Native/live closure remains open.

Binding-preview budget consistency checkpoint (2026-09-08 UTC): the query
behind verified run association accepted a waiting run whose step budget was
exhausted, while preparation candidate discovery already rejected it. A pg-mem
query regression reproduced that mismatch before the fix. Association
revalidation now also requires unused steps and no completion timestamp.
The association and binding-store suites pass 14/14, including exact-budget,
over-budget and completed-timestamp negatives. This is source-admission
component evidence, not proof of the current user's missing-run cause or live
preparation fulfillment. The running EXE predates this patch; CS1 remains open.

Task-side run preparation checkpoint (2026-09-08 UTC): Ready up now exposes
`prepare_run` for an exact pending browser intent. The caller explicitly reuses
an inspected run, or supplies an objective for bounded creation when discovery
finds no current candidates. Existing run creation and room-binding APIs are
composed without a model continuation. Concurrent/repeated requests retain
choice and partial run identity; lost presence stops attachment, and revoked
room bindings cannot be restored by this preparation path. Requested duration
sets only the run budget, never game permission or binding consent.
The focused mailbox/schema/binding-store/MCP battery passed 62 tests. Evidence
and source hashes are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-run-preparation-composition.json`.
The MCP transport fixture uses injected run/account/room ports; this is not
live principal or packaged workflow proof. Verified presence publication,
catalog availability, native rehearsal and full CS1 exit qualification remain
open. No running service was restarted and the EXE is unchanged.

Preparation-to-presence checkpoint (2026-09-08 UTC): `prepare_run` now reuses
the existing authenticated presence verifier to publish the exact prepared
room/run for this same task. No claim or environment approval is generated.
The shared presence implementation also continues serving the original MCP
presence tool. The 26-test coordination suite passes, including exact identity
publication and failed-publication retry with no additional run creation.
Renderer, host and runtime staging completed. The replacement directory package
`apps/desktop/release-cs1-run-handoff-20260908/win-unpacked` has 645 runtime
files matching staging and all three ASAR entrypoints matching host outputs.
The packager process handle was missing on later polling, so package contents,
not an assumed packager exit code, establish this artifact check. Native launch
verification is still in progress; this is not CS1 closure or live acceptance.

Replacement native checkpoint (2026-09-08 UTC): the verified run-handoff EXE
replaced the normally closed prior build. Initial window discovery failed while
startup processes were live; no duplicate launch was issued. Service readiness
and a targetable native window then appeared. Ordinary destination setup reaches
Agent Access and, after same-task presence refresh, the preparation controls.
The current callable catalog still lacks Ready up, so no live preparation call
or new binding was attempted. Artifact hashes, startup evidence and limitations
are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-run-handoff-package.json`.
The replacement remains running; current-session live pickup is still unproven.

Heartbeat selection regression checkpoint (2026-09-08 UTC): the transport
fixture reproduced an ordinary heartbeat clearing the room/run just published
by `prepare_run`. Omitted selection fields now retain this same authenticated
task's selectors and re-run current store verification; explicit nulls or a
resource-claim array still replace them. No cached proof is promoted. The
26-test coordination suite passed with failed-verification, other-task and
explicit-clear negatives. A further expired-presence/disconnect fixture was
added afterward and remains pending while the serial full discipline battery
runs. Its prelude and first adversarial shard passed; the whole battery is not
yet a pass. This source change is newer than the running run-handoff EXE and
must be packaged before claiming native behavior. CS1 remains incomplete.

Verification queue checkpoint (2026-09-08 UTC): the existing pg-mem run-setup
fixture now invokes production `prepareRun` around the real run service and
room-binding store, rather than manually sequencing their calls. It checks
three preparations, one creation, stable run/binding/budget, zero executor
calls and refusal to restore a revoked binding. This revised fixture and the
new expired-presence fixture await execution after the current single-worker
full discipline run. They are not passing evidence yet. The full run has
passed all four prompt-solving shards and the fixed API-parity contracts;
scenario shards, continuation/identity checks and final server build remain.

Verification and disabled-checkbox checkpoint (2026-09-08 04:59 UTC): the
full discipline battery exited zero, including all API shards, 26 continuation
routing tests, 9 identity tests and the server build (four build warnings).
The queued focused suites passed 37/37: 27 MCP coordination and 10 real-store
binding tests. These are in-process regressions, not live capacity evidence.
Native EXE accessibility inspection confirms the run checkbox is disabled,
the preparation room is unselected, and no verified run is published. A fresh
same-task MCP presence succeeded but still returned null room/run/runtime
identities. Presence refresh alone cannot prepare that missing run. Ready up
remains absent from this task's callable catalog; no new claim or consent was
attempted. The heartbeat fix remains source-only pending packaging. CS1 is
incomplete and ET6 remains unproven.

Presence-retention packaging checkpoint (2026-09-08 UTC): host, staging and
directory packaging exited zero. All 645 runtime files and three ASAR
entrypoints match their staged/built inputs in
`release-cs1-presence-retention-20260908`. Artifact hashes and catalog negative
evidence are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-presence-retention-package.json`.
The prior EXE remains running; replacement launch and native regression are
not yet verified. No binding, consent or runtime capability was renewed.

Guidance regression checkpoint (2026-09-08 UTC): native replacement launch and
ordinary destination navigation succeeded, as recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-presence-retention-native.json`.
The guide prematurely requested binding while run verification was absent.
A failing panel fixture reproduced this. Popup and target labels now share
one state-derived message: Minecraft preparation before verification, binding
review afterward, with optional chat-only access preserved. Classification:
presentation; no admission or consent changes. The new regression passes, but
the full 36-test panel suite reproducibly has three failures (33 pass): pending
claim disappearance, claim-deadline reconciliation and expired-presence error
presentation. These failures require diagnosis before another renderer/package
build; they are not dismissed as unrelated. Current EXE does not contain this
source-only guidance change. CS1 remains incomplete.

Panel fixture diagnosis checkpoint (2026-09-08 UTC): the three claim/expiry
failures used call-order fetch mocks; the preparation component's room-list
read consumed responses intended for binding calls. Endpoint-specific mocks
(including the readiness query string) preserve the original assertions and
all 36 panel tests now pass. No production claim/expiry guard was weakened.
The guidance regression remains included. Renderer build is the next packaging
step; native behavior of this presentation patch is not yet verified.

Transport diagnosis checkpoint (2026-09-08 UTC): coordination-only early return
explains the authorization-status tool-not-found result; that result alone did
not establish a stale catalog. Supported transition under existing device trust
restored Full Harness without user consent automation or reconnect. The same
authorization-status tool then returned g2-action ready with no missing scopes.
Ready up remains absent from task enumeration after catalog notification; live
preparation remains unproven. See
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-guide-transport-restoration.json`.
Do not repeat transport restoration while this scope is healthy or relabel
transport readiness as run, player, lease or goal readiness.

Pre-probe association checkpoint (2026-09-08 UTC): a new CS1 fixture withdrew
the verified run association during environment lookup. Preparation dispatched
one probe before rejecting the stale association afterward. The orchestrator
now revalidates the association immediately before probe admission, retaining
existing post-observation checks. Classification: source admission. All 18
preparation fixtures pass, including zero probe/repair calls for this negative.
This is injected-dependency evidence, not an atomic dispatch transaction or
live Minecraft proof. The patch is source-only; current EXE is unchanged.

Combined session verification checkpoint (2026-09-08 UTC): all seven session
suites passed, 90/90 (preparation 18, intents 17, readiness 20, Ready up 12,
goal recovery 13, bound evidence 6, recovery perception 4). This includes the
pre-probe association regression and keeps source/build evidence separate from
native execution. Current prerequisite gaps remain: live current-client Ready
up invocation, repeated real-session identity preservation, full source/lease/
goal deadline presentation, and matching native readiness. CS2 exact-chat
ingress and CS3/CS4 execution/rehearsal exit requirements are not satisfied by
this battery. No gate or maturity promotion is made.

Catalog-boundary revalidation (2026-09-08 05:45 UTC): the disabled-checkbox
report was checked against fresh same-task presence: room, run and verified
retained runtime were all null. Ready up is absent from this task's callable
tool enumeration. The suspected missing transition-shadow registration is not
a demonstrated fix: when the reasoning binding store is supplied, the normal
registration already publishes Ready up on both coordination and full surfaces.
The targeted raw OAuth catalog and browser-to-MCP preparation mailbox tests
passed 2/2 (25 unselected), using in-process transports and injected identities.
This disproves a general current-source registration omission; it does not
prove delivery of the current catalog to this external client. Do not add a
duplicate shadow registration, impersonate this client through direct HTTP,
or request another claim to compensate. Live catalog delivery remains a
separate unresolved boundary; no run was prepared by this diagnostic.

Source credential deadline checkpoint (2026-09-08 UTC): the readiness collector
now reuses the owner/room-scoped credential-free device status reader, matches
exact environment/source/world identity and requires one unambiguous active
credential with a finite deadline. That deadline is attached to the source
check and caps snapshot validity independently of action permission and run
budget. Expired, revoked, missing/invalid deadline, foreign and ambiguous rows
fail closed without inferred human approval. Reader failure remains a source
diagnostic and does not suppress independent subject verification. The EXE
component displays the returned source deadline separately and clears it on
binding identity changes. Classification: source admission, evidence
normalization and presentation; no credential mutation, renewal or new grant.
Focused tests passed 61/61: collector 28, preparation 18, Ready up UI 15.
These use injected readers/jsdom, not live database credential expiry or native
rehearsal. The static discipline classifier passed against the dirty checkout;
it is not a scoped integration proof. Changes remain source-only, and CS1,
live catalog delivery and subsequent CS2–CS4 requirements remain open.

Source-deadline packaging checkpoint (2026-09-08 UTC): renderer, host, staging
and directory packaging completed successfully. All 645 staged runtime files
and three ASAR entrypoints match the new package. Hashes are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-source-deadline-package.json`.
The package includes both source-expiry handling and the earlier pre-probe
association revalidation. The prior running EXE was preserved; this replacement
has not been launched or rehearsed. No live catalog or readiness claim follows
from packaging, and CS1 remains incomplete.

Native source-deadline launch checkpoint (2026-09-08 UTC): the old EXE closed
normally and process absence was checked before one replacement launch. Native
window identity matches the verified new package. Ordinary Destination setup
navigation reached Agent Access, and fresh same-task presence with an explicit
continuation bridge advanced it to AI app connected. No run is verified and
the run checkbox remains disabled. Ready up remains absent from task tool
enumeration, so no preparation, expiry display or complete session rehearsal
is claimed. The lower status line retained an unavailable-continuation message
despite current readiness above; this additional presentation inconsistency
needs reproduction before repair. Evidence:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-source-deadline-native.json`.
No permission or binding control was activated. CS1 remains incomplete.

Continuation diagnostic repair checkpoint (2026-09-08 UTC): a new panel test
reproduced the native contradiction: after Start Harness, a later fresh
continuation check left the lower unavailable message in place. That message
was stored as a one-time string. Connection guidance now derives from the
current readiness state, while operation receipts remain independent text.
The full panel suite passed 37/37; an added reverse transition assertion also
passed in isolation, withdrawing the ready message when continuation becomes
unavailable again without another tunnel start. Classification: presentation.
No heartbeat, binding, consent or admission rule changed. This follow-up is
source-only, newer than the running source-deadline package. It does not fix
the missing live Ready up tool or establish a verified environment run.

Unconfirmed preparation retry checkpoint (2026-09-08 UTC): inspection confirmed
that a missing durable goal is an explicit prerequisite failure, not a reason
to invent a default objective. Separately, a failing UI fixture demonstrated
that retrying after a lost response and panel remount minted a different probe
request ID. The post-binding Ready up component now persists the request ID
before delivery, scoped to exact binding/epoch/chat/mission/run, and retains it
for unconfirmed outcomes. A confirmed successful response releases that retry
identity so a subsequent check can obtain fresh evidence. Storage failure sends
nothing. All 18 Ready up UI tests pass, including unknown-response remount,
fresh request after confirmation and storage failure. Classification:
presentation/request identity; the existing broker still owns deduplication.
No private retry or execution loop was added. This is source-only browser
fixture evidence, not live zero-duplicate-effect proof; CS1 remains open.

Published catalog refresh checkpoint (2026-09-08 UTC): the connected Device
Check v2 app's complete Actions accessibility tree did not contain Ready up.
One Information / Refresh action added `helix_environment_session_ready_up`,
including the current pre-binding preparation modes, without reconnecting,
changing permissions or restarting the EXE. Its row has no reconnect-needed
label, although other action rows do. Current-task callable enumeration still
omits Ready up. The first divergence is therefore narrowed to saved publication
and subsequent task catalog delivery, not a demonstrated missing server
registration. Evidence is in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-published-catalog-refresh.json`.
Recheck delivery to this same task before any preparation invocation; do not
infer invocation or authorized scopes from the settings listing. No run,
binding or environment action was created. CS1 remains incomplete.

Final probe admission checkpoint (2026-09-08 UTC): Ready up remains absent
from this task's callable enumeration after the published metadata refresh.
Separately, a failing preparation fixture proved that loss of the run
association during the gateway's asynchronous reads still reached dispatch:
the final callback checked only task binding. That callback now reuses the
existing current-association validator, including run deadline, room/run
versions and task binding. Preparation tests pass 19/19. The real gateway's
focused exact-environment battery passes 7/7 (37 unselected), including a new
asynchronously rejected final check with zero dispatch. This is source
admission composition evidence, not an atomic transaction or live expiry
test. It changes neither permissions nor connector contracts. The running
EXE does not yet contain this patch; CS1 and later stages remain incomplete.

Dispatch/retry package checkpoint (2026-09-08 UTC): the accumulated current-state
guidance, unknown-outcome retry identity and final probe association validation
are now included in a successfully built directory package. All 645 staged
runtime files and three built ASAR entrypoints match. Identity evidence:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-dispatch-retry-package.json`.
The previous EXE remains running; this package has not been launched. Packaging
does not repair external task catalog delivery or prove the disabled-checkbox
workflow. Native rehearsal and all unsatisfied CS1–CS4 exit tests remain open.

Dispatch/retry native checkpoint (2026-09-08 UTC): the prior package exited
normally and process absence was checked before one replacement launch.
The initial unavailable window resolved without another launch. The returned
native window matches the verified dispatch/retry package. Ordinary Destination
setup navigation reached Agent Access; same-task MCP presence was accepted by
the new service. The no-run checkbox remains disabled and room options are
visible. Selecting the existing test room failed with a native child-window
targeting error; this is not evidence of a product selection defect. No Ready
up submission, permission action, binding or gameplay occurred. Evidence:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-dispatch-retry-native.json`.
The replacement remains running; full native rehearsal and CS1 are incomplete.

Native preparation submission checkpoint (2026-09-08 06:43 UTC): fresh native
state showed the existing Shared GPT Live room highlighted in the selector.
Keyboard Return completed selection after the child-window click error, and
Ready up this room became enabled. One ordinary button submission returned
Preparation requested, disabled the room/duration selectors and changed the
button to Check preparation, with 588 seconds remaining in its delivery
window at observation. This is browser intent acceptance, not a prepared run
or gameplay consent. The same task's presence was refreshed before submission;
its callable catalog still omits Ready up, preventing supported inbox pickup.
No binding code or human-only control was used. The prior native snapshot
remains immutable; this checkpoint supersedes only its incomplete selection
attempt. Do not mint repeated intents or treat the queued intent as readiness.

First-session goal creation checkpoint (2026-09-08 UTC): inspection found a
remaining first-use gap: prepare_run creates the bounded run, while post-binding
Ready up requires an existing environment goal. Do not hide that gap with an
invented default objective. The existing goal-create store now accepts an
optional owner-scoped idempotency key, backed by goal-row uniqueness and the
creation ledger. It revalidates current identity before replay, rejects changed
objective/identity, and returns current paused/canceled state without recreating
participant grants. The existing MCP goal-create operation forwards that key;
legacy unkeyed calls remain non-idempotent. Classification: request identity
and source/tool admission. A red fixture reproduced duplicate creation before
the patch; the full store suite now passes 24/24, including three retries across
store instances with one creation row/event. The focused in-process MCP test
passes 1/1 (4 unselected), confirming key forwarding under authenticated room
identity. These are pg-mem and injected-identity tests, not production PostgreSQL
concurrency or live setup proof. Shared first-use objective selection and goal
bootstrap integration remain to be implemented and verified. This patch is
source-only, newer than the running dispatch/retry EXE; CS1 is incomplete.

Explicit first-goal preparation integration checkpoint (2026-09-08 UTC): the
shared automatic preparation request accepts an optional strict goal_bootstrap
containing an explicit objective and exact environment/subject/authority
selectors. Both authenticated browser and MCP adapters forward it to the same
preparation function. No bootstrap still returns the existing missing-goal
failure. Missing-goal creation verifies the selected active self subject and
current run association, then delegates to the existing identity-validating,
keyed goal store. An existing matching goal is reused; conflicting objective,
subject or authority is rejected. Creation failure is reported with partial
effects uncertainty rather than hidden behind a fresh attempt. No objective,
player or permission is inferred, and no gameplay is dispatched by creation.
Classification: source/tool admission and request composition. Preparation
fixtures pass 23/23; complete browser route and coordination MCP suites pass
41/41, including bootstrap forwarding. These use injected preparation/identity
ports; the existing store tests supply separate ledger evidence, not a live
combined first-session trial. The ordinary UI still needs an explicit input
path for this data, and real database concurrency/native acceptance remain
open. Changes are source-only and CS1 remains incomplete.

First-goal UI handoff checkpoint (2026-09-08 UTC): inspection found an existing
plain-language Minecraft play objective in Player Embodiment. It queues a
request to the exact bound task for goal/monitor setup rather than compiling
user prose into invented milestone contracts. The missing-goal Ready up error
previously discarded room navigation, making its instruction to start that
workflow unreachable from the readiness result. Preparation now revalidates
the run association after goal lookup and returns only the authenticated room
as navigation context for this typed failure. The UI offers Review environment
settings without automatically opening controls, selecting an environment,
granting permission or claiming readiness. Other failures cannot use this
handoff; stale association suppresses it. Classification: presentation and
evidence normalization. Focused preparation and UI regressions pass 45/45,
including missing-goal navigation and stale-association negatives. This reuses
the existing objective entry path; it does not prove the combined goal setup
journey or provide a direct UI editor for the strict goal_bootstrap payload.
Native first-session, real database concurrency and packaging remain open.
Source-only changes are newer than the running EXE; CS1 remains incomplete.

Packaged goal-handoff checkpoint (2026-09-08 UTC): client, host, runtime staging
and directory packaging passed for release-cs1-goal-handoff-20260908. All 645
staged runtime files matched the package; bundled main/preload/service matched
their build outputs. Hashes and bounded native startup observations are in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-goal-handoff-package.json`.
The prior EXE closed normally and the replacement launched once. Its native
destination controls expose Set up connection; the displayed governed-turn
ready state is not an exact-task or environment readiness pass. The current
task still lacks callable Ready up. No integrated first-session or movement
trial occurred, no consent was automated, and CS1 remains incomplete.

First-goal ledger composition checkpoint (2026-09-08 UTC): a regression now
calls the actual browser/MCP preparation function with real durable-goal
creation/discovery and migration 059 against pg-mem. Creation commits and then
its response is deliberately lost. Three retries discover the same goal using
fresh store instances: one goal and one ledger event remain. A conflicting
objective is rejected before another observation probe, with no additional
ledger effects. The initial uncertain outcome is retained as such; rejected
observations do not become readiness and do not invoke recovery. The complete
durable-goal suite passes 25/25. This is composed component evidence with
simulated identity, binding, observation and readiness ports; pg-mem does not
prove production PostgreSQL concurrency or live capacity. No runtime code was
changed in this checkpoint, so the preceding package remains current for it.
CS1 integrated first-session acceptance remains open.

Terminal retry checkpoint (2026-09-08 UTC): the composed first-session ledger
test now also cancels the goal after commit but before returning the lost
response. Normal discovery omits this terminal goal; nevertheless three
same-key creation retries return the existing canceled ledger, not a new goal.
Exactly one goal and two events (creation and cancellation) remain. A changed
objective conflicts with the original request and adds no events. Both focused
cases pass (2/2; 24 unrelated cases skipped). Observations remain simulated
rejections, so this demonstrates terminal ledger preservation, not live
readiness, successful recovery or execution. No runtime repair was needed.

Ordinary native navigation checkpoint (2026-09-08 UTC): refreshed the same
continuation through authenticated presence on the goal-handoff EXE. Presence
returned no verified room/environment/run. Following Destination → Set up
connection → Open Agent Access reached AI app connected / Review task binding,
with current chat attachment and polling continuation. The environment-run
checkbox was disabled with the explicit no-verified-run explanation; the
preparation room selector was reachable. No claim or consent was submitted.
This verifies ordinary navigation rather than an MCP-opened panel shortcut.
It does not establish environment preparation: the task's missing callable
Ready up remains the delivery prerequisite for consuming the preparation
intent. Do not request another chat-only claim as a workaround.

Repair-result presentation checkpoint (2026-09-08 UTC): reproduced the UI
mislabeling goal_creation_or_replay_verified with changed=null as an uncertain
partial result. The UI now distinguishes verified creation/replay with an
unspecified change count from genuinely uncertain outcomes; unknown reason
codes retain the uncertainty warning. Blocked readiness remains blocked.
The regression failed before the presentation fix; the full Ready up UI suite
then passed 22/22. Classification: presentation only. This source-only change
postdates the goal-handoff EXE and needs the next renderer package refresh.
It does not advance CS1 maturity or supply live evidence.

Combined CS1 regression checkpoint (2026-09-08 UTC): all seven session suites,
the durable-goal ledger suite and Ready up UI suite pass together, 153/153 across
nine files with one worker. Counts and exact command are retained in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-combined-regression.json`.
This checks accumulated component compatibility, not the missing packaged
MCP/EXE integrated trial or production database concurrency. The latest UI
presentation fix remains newer than the running package. CS1 stays incomplete.

Exact-chat prerequisite repair (2026-09-08 UTC): inspection for CS2 found the
existing composer could use inspectLatestReasoningBinding after selected-chat
submission failed, then dispatch through another chat's active binding. A
focused regression failed before repair. Fallback selection now verifies the
explicit candidate chat on current and remembered binding lookups; the latest
foreign binding cannot substitute. All seven bound-destination tests pass,
including typed/finalized-voice parity and the new foreign fallback negative.
Classification: source admission / destination selection. This narrowly repairs
an existing prerequisite reused by both CS1's play-objective entry and CS2;
it does not depend on or claim completion of CS1's still-open live trial.
No new MCP ingress, scope contract, complete idempotency or wake behavior is
claimed. The source change is newer than the running EXE. Further exact
run/epoch and absent-chat boundaries remain part of CS2 verification.

Absent-chat submission checkpoint (2026-09-08 UTC): the existing current-chat
submission route accepted an omitted conversation ID and chose the latest
binding (reproduced 202 instead of expected 400). It now requires an explicit,
nonblank conversation ID; the typed client request requires that field and the
composer declines submission without a selected chat. Read-only latest-binding
inspection remains available. Existing explicit-chat callers retain their
behavior; callers relying on implicit latest submission must supply a chat.
Full connection-route and destination UI suites pass 21/21, covering omitted,
empty, whitespace, foreign and valid chat targets. Classification: source
admission / API target validation. This repairs the same existing prerequisite
as the preceding checkpoint, not a new CS2 MCP surface or CS1 live pass.
Changes remain source-only until the next packaged build.

Submission target pinning checkpoint (2026-09-08 UTC): current-chat POST now
requires binding ID, epoch and nullable run ID as well as explicit chat. The
server compares these against the owned current binding before dispatch;
mismatched binding/epoch/run returns 409. The composer resolves the target
read-only, submits that exact tuple once and no longer retries a failed write
through the compatibility dispatch route. Existing explicit browser callers
must send the tuple; the rebuilt package is required before live use. Complete
connection-route and destination suites pass 21/21, including typed/voice
behavior and target mismatch negatives. Classification: source admission and
API target validation. This narrows existing dispatch, not a new MCP ingress
surface, durable client retry envelope or live CS1/CS2 acceptance. The changes
are source-only and subsequent run/epoch concurrency evidence remains open.

Visible prompt replay checkpoint (2026-09-08 UTC): replaying the same accepted
steering request through the existing Minecraft play bridge produced two user
messages in the exact chat. The regression failed with two messages before the
fix. The composer now reuses the chat store's appendMessageOnce operation with
a binding/epoch/client-event key. All eight destination tests pass. This is
presentation idempotency for the existing submission path, not proof of server
ingress idempotency, durable cross-restart retry, new agent-origin support or
zero gameplay effects. The shared store is reused rather than a second message
ledger. Classification: presentation; source-only, not yet packaged.

Prompt replay compatibility checkpoint (2026-09-08 UTC): the previous
append-once change exposed a first-message title regression (New chat remained
unchanged). A regression reproduced it before repair. appendMessageOnce now
applies the existing user-first-message title rule only for default titles;
custom titles and conflicting replays remain unchanged. The composer replay
test now unmounts/remounts between requests and still shows one prompt. The
destination, append-once and concurrent-session suites pass 18/18. This is
presentation/store compatibility evidence, not browser-process restart or
server idempotency proof. Source-only; no maturity or live gate advancement.

Steering request conflict checkpoint (2026-09-08 UTC): the store previously
returned the original receipt when a client-event ID was reused with different
text. A regression reproduced silent acceptance of the conflict. Replay now
requires equal normalized instruction hash, origin and effective lifetime;
conflicts return reasoning_steering_request_conflict (409) without replacing
the original event. Identical retries retain the original receipt and deadline.
Store and connection-route suites pass 23/23, including exact-task pickup and
acknowledgement assertions. This intentionally replaces the older permissive
replay expectation. Classification: source admission / request identity.
Source-only; durable restart replay and the new scoped MCP ingress remain open.

Expired replay checkpoint (2026-09-08): a deadline-boundary regression showed
dispatch replay returning pending while read/inspection returned expired for
the same event. Replay now uses existing inspectEvent projection; it retains
the event identity, cursor and original deadline without adding an event.
Both unacknowledged expiry and previously acknowledged history are covered;
late acknowledgement remains rejected. Store and connection-route suites pass
25/25 after the reproduced failure. Classification: evidence normalization.
The quick discipline static check passed (classification warning retained).
This is source-only component evidence, not a packaged trial, durable restart
proof or CS1/CS2 completion. Scoped MCP ingress and integrated exits remain open.

Dispatch receipt presentation checkpoint (2026-09-08): the composer ignored an
already expired or acknowledged dispatch receipt and initially claimed queued
pickup. Both regression cases failed before repair. It now presents the returned
delivery state immediately, publishes the corresponding existing play-bridge
result and cancels the previous pickup poll; acknowledged transport remains
explicitly not task completion. All ten bound-destination tests pass, including
typed/voice and exact-chat replay cases. Classification: presentation / evidence
normalization. Source-only UI evidence; no EXE rebuild or live pickup trial is
claimed by this checkpoint.

Packaging checkpoint (2026-09-08): accumulated source changes built successfully
through client, host, runtime staging and electron-builder into
`apps/desktop/release-cs1-replay-20260908/win-unpacked`. Commands, hashes and
warnings are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-replay-package.json`.
The running EXE was not replaced. Subsequent SHA-256 comparison verified all 645
staged runtime files against the packaged counterparts, with zero mismatches;
main.cjs, preload.cjs and service.mjs extracted from app.asar also match the
built files. Launch and ordinary workflow rehearsal remain unverified; build
and content equality do not advance CS1–CS4. The same-task callable catalog was
checked again and still lacks Ready up; no new binding was requested.

MCP Ready up admission checkpoint (2026-09-08): expanded the authenticated
in-process MCP regression with foreign binding ID, stale epoch, foreign run,
mission and chat variants. All reject before either preparation implementation
is called, and the original active binding/run remains unchanged. The complete
local-supervisor MCP suite passes 27/27. This is test-only source-admission
coverage using mocked preparation dependencies, not real recovery or callable
delivery to this external task. No new defect was reproduced at this boundary;
the next integrated evidence must exercise real preparation dependencies rather
than treating this transport test as CS1 completion.

Final readiness freshness checkpoint (2026-09-08): reproduced ready=true with
an expired validity deadline after final asynchronous association validation.
The shared browser/MCP preparation function now rejects expired/nonfinite or
missing ready deadlines with environment_session_readiness_expired while
retaining completed repair facts; it does not retry recovery or renew authority.
All 26 preparation tests pass after the red regression. Classification: evidence
normalization. This fixture uses injected dependencies and a controlled clock,
not live integration. This source repair is newer than release-cs1-replay-20260908
and requires a subsequent build before its EXE behavior can be claimed.

Post-expiry combined regression (2026-09-08): all nine CS1 session, goal-ledger
and Ready up UI suites pass together, 154/154. Exact command, counts and limits
are retained in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-post-expiry-regression.json`.
Recovery/repetition coverage remains component composition with injected ports;
it does not close the missing external MCP/packaged EXE rehearsal. CS1 remains
incomplete and later stage exits retain their original requirements.

Normal-dispatch reuse checkpoint (2026-09-08): extracted the existing current
chat route's strict schema and exact binding/epoch/run validation into
`server/services/local-supervisor/exact-chat-steering-dispatch.ts`; the browser
route calls it with the authenticated profile, never a request-selected profile.
This preserves the existing dispatch policy so subsequent scoped MCP ingress
can reuse normal dispatch rather than independently writing to the steering
store. Route and store regressions pass 25/25. Classification: source admission
refactor. It repairs no newly claimed defect and adds no MCP tool, agent-origin
support or live evidence. This source prerequisite is independent of CS1's open
external rehearsal; CS1 and CS2 remain incomplete. It is newer than the package.

Agent-origin prerequisite checkpoint (2026-09-08): shared exact-chat dispatch
now has an agent entry that verifies the authenticated task association and
matches requested binding/chat/run/epoch before reusing normal dispatch. It
assigns agent_submitted internally and labels delivered content as agent
advisory; browser schemas remain typed/finalized-voice only. The real binding
store regression covers origin injection, foreign task/target, identical replay,
single delivery and revoke rejection. Route/store suites pass 26/26.
Classification: source admission / evidence normalization. No MCP adapter is
registered yet; that adapter must enforce scope and construct the authenticated
actor rather than accepting a caller principal. EXE message projection and
end-to-end acknowledgement remain open. Source-only, independent prerequisite
work; no CS1/CS2 maturity advancement or live acceptance is claimed.

Scoped MCP ingress checkpoint (2026-09-08): registered
helix_reasoning_prompt_submit with supervisor-write authorization and both
catalog metadata maps. The adapter derives profile/client/session identity
from authentication, verifies the bound continuation and invokes shared normal
dispatch with forced agent origin. The in-memory MCP transport test proves
same-request replay, conflict/target/origin rejection, one agent-labeled pickup,
acknowledgement and missing-scope rejection. Full coordination suite: 27/27.
Classification: source/tool admission. This is source-only; no published external
catalog availability, packaged EXE visibility or live gameplay is proven.
The tool explicitly does not claim EXE display, idle wake or completion. CS2
still requires visible exact-chat projection and remaining adversarial/live
evidence; no CS1 exit or subsequent ET6/NAV1 gate is advanced.

Owner display read checkpoint (2026-09-08): added an authenticated browser
chat-prompts read and a separate store owner-display entry. Exact profile,
binding/epoch/chat/run are checked; bounded steering projection is reused
without constructing a provider session or acknowledging pickup. Tests cover
owner display, foreign targets, absent session, cursor and revoke rejection,
and unchanged pending status. Route/store suites pass 26/26. A first absent
session test failed because its resolver mock always authenticated; the fixture
now explicitly returns no session for that case. Classification: source
admission / presentation. No client polling/rendering is connected yet and no
visible EXE evidence is claimed. Source-only; CS2 remains incomplete.

Agent prompt display checkpoint (2026-09-08): selected exact active chats now
mount a bounded agent-origin display using the authenticated read endpoint.
The owner display returns the recent 50 events (task pickup retains oldest-first
cursor semantics). The component validates event schema/binding/epoch, dedupes
IDs, polls serially and aborts on unmount. It labels delivery as last-checked
transport state, never an answer, and submits/acknowledges nothing. Display and
existing destination tests pass 12/12. Classification: presentation / evidence
normalization. This is jsdom source evidence, not a packaged EXE trial; restart
persistence, rapid switching, complete adversarial coverage and integrated CS2
acceptance remain open. The display is a bounded section, not a provider reply.

Display switching checkpoint (2026-09-08): reproduced the previous run's prompt
remaining visible while a new run read was pending. Display snapshots now carry
an exact binding/epoch/chat/run/service context key and render only for that key;
the existing cancellation guard rejects late old responses. The regression
switches twice with an unresolved response, then resolves it and verifies no
old prompt appears. Display/destination suites pass 13/13. Classification:
presentation / evidence identity. Source-only jsdom proof, not packaged or live
acceptance; remaining CS1–CS4 gates are unchanged.

Ingress/display composition checkpoint (2026-09-08): the same real binding
store now backs an in-memory MCP submission/pickup/ack test and authenticated
HTTP browser-display route. Exact event and text match before acknowledgement;
after acknowledgement the same single displayed event reports acknowledged.
The new tool's OAuth metadata is checked in both existing catalog projections.
Coordination suite passes 27/27. Test-only integration evidence with injected
browser authentication, not packaged rendering, live external-client delivery,
cross-process durability or gameplay. CS2 remains incomplete.

Combined ingress package checkpoint (2026-09-08): client/host/staging/packaging
passed into release-cs2-ingress-20260908. All 645 staged runtime files and three
core bundles match the packaged artifacts. Hashes and limits are retained in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs2-ingress-package.json`.
This includes the readiness deadline repair and agent submission/display work.
It has not been launched; the running EXE was preserved. No CS1–CS4 exit or
ET6/NAV1 advancement follows from this build evidence.

Native launch checkpoint (2026-09-08): release-cs2-ingress-20260908 now runs
after normal closure and process-absence verification of the old package.
The launch helper initially found no window, but the new processes remained
alive and later exposed the window; no duplicate launch was attempted.
Ordinary composer Set up connection -> Open Agent Access navigation reached
Waiting for your AI task with linked-account and device-trust presentation.
No consent control was automated. Current callable discovery still lacks
Ready up and prompt submission, so no new binding was requested. Exact scope
and artifact hash are recorded in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs2-ingress-launch.json`.
This supersedes only the prior package's not-launched observation, not its
unverified integrated workflow status. CS1–CS4 remain incomplete.

External catalog diagnostic (2026-09-08): same-task presence refresh succeeded
after the package switch, but the advertised authorization-status tool returned
Unknown tool (-32001). The signed-in app settings page lists Ready up with an
older schema lacking current source's goal_bootstrap field; prompt submission
was not present in the observed reasoning section. Neither required tool is
callable in this task. These are distinct publication/adoption observations,
not proof of missing consent or a Codex internal defect. The local request
schema suite passes 9/9; it does not verify external publication. Retained scope:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-external-catalog-boundary.json`.
The next catalog refresh has a concrete expected change: current Ready up
schema and prompt submission, followed by separate callable verification.
No reconnect, new binding, permission change or stage advancement occurred.

Official refresh guidance checkpoint (2026-09-08): the user reported refreshing
the app actions; subsequent callable discovery still found neither required
tool. OpenAI's developer-mode metadata procedure says to refresh, confirm the
advertised changes, then start a new conversation for affected tests:
https://developers.openai.com/plugins/deploy/connect-chatgpt#refresh-metadata
(retrieved 2026-09-08). This is general plugin guidance, not evidence that this
exact Codex task supports in-place catalog adoption or that a new task would
necessarily resolve the mismatch. Do not promise that Refresh alone repairs
the existing task. The goal's no-replacement-task constraint remains in force;
this citation does not authorize a new task or principal substitution. A
separately authorized diagnostic conversation could check discovery only, but
cannot inherit this task's binding or count as its integrated acceptance.

### Live-test handoff: repair ownership and callable tools

Planning clarification (2026-09-08; documentation only): Development Codex can
repair CasimirBot source, catalog registration, authorization metadata, UI and
packaging, and run bounded local regressions. Those repairs do not establish
that the external Codex task has received the updated MCP catalog. Do not assume
an EXE rebuild, a connected-plugin badge, or an in-process MCP test resolves
external-client tool availability.

Before requesting another binding or starting the live trial:

1. Verify the running EXE is the intended package and identify its current
   service instance without exposing credentials.
2. Discover the required tools in this exact task's callable catalog, including
   `helix_environment_session_ready_up`, `helix_reasoning_prompt_submit`, and
   the binding claim, steering read and acknowledgement tools. Record missing
   tools separately from authentication, binding and environment-readiness
   failures. Revalidate availability at test time; historical absence is not a
   permanent diagnosis.
3. If a tool is missing, locate the first failing boundary: source registration,
   packaged service, authorized catalog publication, or external-client catalog
   adoption. Repair repository-owned defects here. Do not claim control over
   Codex's internal catalog refresh or use Codex UI puppeteering, a fabricated
   provider session, or repeated replacement tasks as a workaround.
4. If recovery requires a supported external-client user action, describe the
   exact action and evidence needed once. Do not request repeated rebinding or
   reconnects without new diagnostic evidence. Continue independent source and
   deterministic work while the live-client prerequisite remains unresolved.
5. Once availability is verified, refresh this same task's presence, validate
   the exact chat/run association and finite authority, and use the normal
   Ready up and prompt path. Keep human-only approvals explicit. Record visible
   prompt delivery, pickup and acknowledgement separately from admitted action,
   observation re-entry and task completion.

This is a live-test prerequisite, not a new goal or a passed gate. Local fixes
and simulations cannot substitute for the original ET6 three rolling
extensions, observation re-entry, interruption and revoke/stale rejection proof.
CS1–CS4 exit criteria and the NAV1 dependency remain unchanged.

Record runtime artifacts and failures without secrets. Run the applicable
focused tests and `npm run helix:environment-harness:docs-audit` for plan/status
changes. Run discipline checks when their scoped lifecycle contracts change.
This plan does not itself pass any runtime or capacity gate.

Authorized continuation checkpoint (2026-09-08): the single user-authorized
project-chat handoff verified the refreshed v2 callable tools and registered
its own exact authenticated presence. The ordinary EXE room preparation
request reached that continuation, and MCP prepared a finite run. Supported
opaque read-source repair and trusted-device Minecraft launch succeeded; no
human-only consent or gameplay was automated. Two readiness presentation
defects were reproduced: polling stopped before run preparation appeared, and
the strict status schema rejected the real resolver's run deadline. Both
repairs pass the combined 67-test UI/route/resolver/schema battery. Documentation
audit and quick discipline static checks pass; the latter includes existing
dirty-checkout classifications beyond this presentation/evidence-normalization
patch. Repackaging and integrated review remain pending at this checkpoint.
Exact observations and limitations are retained in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-continuation-preflight.md`.
The requirement reconciliation in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs5-requirement-reconciliation.md`
is explicitly incomplete, with unavailable ET6 measurements null. CS1–CS4
remain incomplete; this does not close CS5, original ET6 or the NAV1 gate.

Packaged recovery checkpoint (2026-09-08): the patched readiness package
launched with the existing authenticated profile. Ordinary trusted Full Harness
recovery restored the transport scope; explicit same-task resource claims
revalidated the retained run without replacing it. Run discovery exposed a
separate indexed pg-mem OR-filter failure, reproduced with 18 fixture runs.
The parameterized optional-filter repair passes 29 focused binding, route and
resolver tests. Repackaging this repair and native binding review remain pending.
See `docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-indexed-discovery-boundary.md`
and `docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-review-retry-package.json`.
No CS1–CS4 completion, CS5 closure, ET6 pass or NAV1 advancement is claimed.

Exact bound transport checkpoint (2026-09-08): human-approved task/chat/run
binding and MCP agent-origin submission, identical replay, pickup and one
acknowledgement succeeded in the discovery-repair EXE. Native visible delivery
failed because the display was included only in the alternate renderer. The
source repair now covers the default renderer and passes 14 focused UI tests;
it is not yet packaged. The ordinary bound-room settings navigation also needs
recovery when its controller displays another room. The binding remains active;
no replacement was requested. See
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-default-renderer-ingress-boundary.md`
and `docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs2-bound-transport-rehearsal.json`.
All full CS1–CS4 exits and CS5 reconciliation remain open; ET6/NAV1 unchanged.

Exact-room review repair (2026-09-08): the ordinary settings action now reads
the explicitly bound room when the controller displays another one. It rejects
wrong/closed responses, cancels superseded selection and opens the dialog only
for the requested room. It never joins or changes consent. The combined
navigation/default-display battery passes 32 tests; packaging and live review
remain pending. Evidence:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-exact-room-navigation-repair.md`.
All integrated exits and original ET6/NAV1 status remain unchanged.

First-goal epoch recovery checkpoint (2026-09-08): a live explicit bootstrap
request rejected the existing selected player solely because its epoch was
stale. Source now performs the same-subject fresh-directory repair before goal
creation, revalidates exact association and leaves finite authority enforcement
unchanged. The setup battery passes 43 tests; full discipline, host packaging and
live proof remain pending. See
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-bootstrap-epoch-repair.md`.
No stage is closed and original ET6/NAV1 status is unchanged.

Consolidated recovery package checkpoint (2026-09-08): full discipline passed
101 executed tests across the prompt, API, continuation and identity checks,
plus the server build. Shard-filter skips are not additional executed tests.
The focused UI battery (32) and preparation battery (43), renderer and desktop
host builds passed. The new session-recovery package completed with exit 0;
all 645 staged runtime files and the three host/service bundles match. Native
rehearsal remains pending in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-cs1-session-recovery-package.json`.
The old package separately exhibited intermittent HTTP 522 responses while its
local service remained running. One supported trusted transport refresh restored
authorization and exact presence without replacing the binding, but a later
inspection still failed. Retain this partial recovery and expired run evidence in
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-transport-recovery-checkpoint.json`.
No integrated CS1–CS4 exit, completed CS5 handoff, ET6 pass or NAV1 advance is
claimed by these build and component results.

Native recovery checkpoint (2026-09-08): the consolidated package launched
and the ordinary EXE setup requested an eight-hour run budget. After current
discovery returned no eligible run, the same preparation intent produced one
verified run. The user supplied its new exact binding claim. Agent-origin
submission, identical replay, exact pickup, acknowledgement and one visible
default-renderer prompt were observed. UI and MCP both reported the missing
goal; the repaired review action opened the exact bound room. Explicit bootstrap
then repaired the same selected player's epoch and stopped at expired action
authority. Human gameplay approval remains pending. The misleading initial
Play binding projection, preparation-form reset on lost presence, and navigation
past historical environment cards remain recorded limitations, not reasons to
replace a valid binding. See
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-session-recovery-native-checkpoint.json`.
These scoped packaged observations do not close any full CS1–CS4 stage or CS5
handoff and do not pass ET6 or unlock NAV1.

Play status presentation checkpoint (2026-09-08): the source-only correction
uses the latest exact-chat binding projection before Play instead of initially
requesting a binding that is already active. Activation's server inspection is
unchanged. Twelve focused panel/journey tests and quick static discipline pass.
The running EXE is preserved while genuine finite player consent is pending;
no further binding or restart is requested for this label. Evidence:
`docs/evidence/eh-g8-et6-continuous-session-build-v1/2026-09-08-play-binding-presentation-repair.md`.
Packaged verification and the full stage exits remain outstanding.
