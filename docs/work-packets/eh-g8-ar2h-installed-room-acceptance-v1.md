Program gate: G8 — release evaluation, CFP-1 active
Workstream: AR-2H — installed owner/guest room acceptance
Capability or component: AR-2G installed mission selection, attributed dispatch and shared result explanation
Lifecycle stage: Admission, execution and recovery
Reaction timescale: Explicit operator review and short semantic replanning
Authority owner: Authenticated developer owner, separately consenting guest and selected task owner
Current maturity: deterministically verified
Target maturity: live accepted
Required evidence: Exact artifact/runtime identity, authenticated account and shared-room topology, task binding, attributed instruction, pickup/result/explanation and revoke/reconnect trace
Explicit non-goals: Production deployment, signing, commercial closure, three-member acceptance or unapproved provider spending
Downstream gate unlocked: Controlled multi-member interpretation evaluation after its separate capacity and cost admission

# Goal

Continue from AR-2G's exact developer package. Establish supported browser,
runtime, identity and room transport readiness, then execute the installed
owner/guest sequence recorded in its qualification. Record the first divergent
boundary and repair an admitted defect only with focused evidence. Do not count
isolated startup or deterministic fixtures as authenticated live acceptance.

This is the admitted AR parallel lane, independent of commercial closure. The
inherited implementation is deterministically verified; this packet's live
target has not passed. Codex retains model/runtime ownership. No change to
provider cost scope or permission boundaries is authorized by an access retry.

## Current checkpoint — accepted pairing and refreshed connector

The installed AR-2G owner session now has an accepted one-hour chat-only pairing
for this exact task. The installed UI confirms an active binding and recognizes
the paired task in the room's mission panel. Refreshing the existing ChatGPT
connector fixed stale steering schemas; an authenticated read with fresh task
presence succeeds with an empty queue. These are prerequisites, not completed
instruction delivery or owner/guest live acceptance.

The owner authorized continuation and the installed room confirms mission
selection, revision 1, for the paired chat. Independent guest access to
the same room authority, attributed dispatch, result disclosure and the
separately admitted voice journey remain open. Pairing expires at
`2026-09-25T13:30:35.074Z`; refreshing presence does not extend consent.
See the accepted-pairing and connector-refresh checkpoints below.

Transport preflight now identifies the missing OAuth invitation-join path.
The synthetic OAuth/account baseline and catalog checks pass after fixture and
evaluator repairs, but the release-evaluation patch still requires Casimir
verification: the attempted installed adapter call returned
`401 desktop_session_required`. No certificate or integrity result exists.

## Initial access checkpoint — 2026-09-25 UTC (historical)

Both supported control entry points again failed before window/tab inventory:
`cua.getState()` and Node REPL initialization of `@oai/sky` returned
`failed to write kernel assets: The system cannot find the path specified.
(os error 3)`. No CasimirBot processes or listeners owned by the inspected
Node/CasimirBot processes were detected. This does not establish account or key
validity. No launcher, credential store, browser profile or provider session was
inspected or changed.

Next access step: restart Codex and retry supported control initialization.
This is a proposed recovery, not a verified fix. If the keyed runtime remains
absent, use the existing skill's opaque-launch authorization contract; never
substitute an ad hoc server or inspect launcher contents.

## Topology preflight discovered in source

Do not conflate Friends & Parties with assisted Shared Live Rooms:

| Surface | Source-backed boundary | Required installed evidence |
| --- | --- | --- |
| Friends & Parties | `apps/desktop/src/friends-parties-coordination-broker.ts` restricts its proxy to `/api/agi/friends-parties`. | Its own domain session and broker readiness, when testing that product surface. |
| Assisted Shared Live Rooms | `SharedLiveRoomApi.ts` uses relative `/api/agi/realtime/rooms` requests; `room-store/database.ts` reads the runtime database through `getPool()`. | Both independently authenticated participants reach the same authoritative room service and room identity through supported transport. |
| Mission selection/dispatch | `RoomMissionOwnerApi.ts` uses same-origin `/api/account/session/agent-connections/room-missions`; owner-options rechecks membership and task authority. | Room membership, captured handoff, selected task and returned evidence resolve on the qualified authority path. |

Merely enabling the Friends & Parties broker does not forward either assisted
room or mission APIs. A shared database alone is also not sufficient evidence
for process-local live session/handoff/result state. Before spending on voice,
record the actual origin/topology and verify guest room visibility, owner task
admission and the intended evidence path. Do not widen the broker allowlist or
copy credentials to bridge these surfaces. If the desired installed topology
cannot satisfy the existing boundaries, admit a transport implementation slice
with its own account/session isolation tests before continuing live acceptance.

A two-profile same-service demonstration is useful but must be labeled as such;
it does not qualify two independent EXEs or EXE-to-domain collaboration.

## Pending acceptance

- [x] Supported browser control and exact AR-2G artifact/runtime identified (resumed checkpoint below).
- [x] Exact owner-approved chat pairing accepted; installed room candidate visible and empty steering queue read succeeds.
- [x] Installed room mission selection confirmed for the exact paired chat, revision 1.
- [ ] Independently authenticated owner/guest and supported shared-room topology verified.
- [ ] Owner's current paired task selected; guest denied owner-only controls.
- [ ] Separately admitted voice session captures the correct speaker and exact instruction.
- [ ] Explicit dispatch, task pickup and actual result distinguished and observed.
- [ ] Explicit result disclosure produces an evidence-supported shared explanation.
- [ ] Revoke, consent loss, uncertain retry and reconnect boundaries observed.
- [ ] Live evidence and remaining limits recorded without promoting full AR-2/G8.

The initial checkpoint below supplied only source preflight. The later pairing
and connector checks establish bounded installed prerequisites; the unchecked
journey steps still require live evidence. AR-2G remains the latest verified
package; no new build, deployment or paid call was made.

Evidence: [access preflight](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/access-preflight.json)
and [passing canonical documentation audit](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/docs-audit.txt).

### Second access checkpoint — 2026-09-25 UTC

[Recheck evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/access-recheck-2.json):
both tool sessions reset successfully, but initialization still failed with
the same kernel-assets path error. No CasimirBot process or inspected Node/
CasimirBot listener was present. No signed-in or live step could run.

A fresh public domain fingerprint returned HTTP 200 and identified compiled
production commit `42c5b19b4be2a95d2d2de937ef9f550df8b6f2b4`, built
`2026-09-01T19:18:38.201Z`. It is not the AR-2G artifact's dirty source identity;
domain parity remains unqualified. This check made no deployment or account
mutation. Browser recovery and an approved active runtime remain prerequisites.

### Third access checkpoint — goal blocked

[Third recheck](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/access-recheck-3.json)
again found both control entry points failing with the same kernel-assets error
and no inspected local runtime listener. The same access blocker has persisted
across three consecutive AR-2H goal turns, including the original continuation.
The prior turn added fresh domain evidence but did not recover access. No live
process or job is being awaited. Further duplicate probes or package rebuilds
cannot establish authenticated installed acceptance.

Goal disposition: blocked pending external access recovery. Restart Codex and
resume this task to retry supported control; if initialization still fails,
the tool runtime needs repair before this walkthrough. The goal is not complete,
all live acceptance items remain pending, and no capability maturity is promoted.

### Resumed checkpoint — control restored, owner trust pending

The owner resumed AR-2H. Browser inventory and native Windows control now work.
The [readiness record](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-1-readiness.json)
records fresh EXE/ASAR hash matches to AR-2G and startup of that exact package.
Its normal saved profile opens with an active local password account, unverified
email and an expired historical task pairing. This is observed installed state,
not proof of hosted identity or a working provider call.

Authenticated MCP presence for this exact Codex task succeeded under the same
profile shown in the EXE. The UI then displayed current catalog/chat attachment
and continuation readiness. Finite task registration was rejected with
`pairing_registration_device_trust_required`. The visible native control is
**Trust this device for Full Harness**. It remembers short logged tunnel leases
and permission to start/stop installed local environment apps on that profile
and device; environment effects remain separately authorized. The owner was
asked to review and operate this security permission. No trust, binding or
environment grant was fabricated or bypassed.

Chrome's domain session is not signed in. The account panel reports Google
sign-in unconfigured; following its Auth0 sign-in link returns
`auth0_account=auth0_not_configured`. Domain sign-in readiness therefore fails
independently of restored computer control. No domain settings were changed.

Goal work has resumed; the earlier blocked checkpoints remain historical.
Next: observe owner trust approval, register this exact task, review its finite
pairing and the shared-room topology, then establish a separately authenticated
guest. Do not start a paid voice session before those prerequisites and its
cost scope are admitted. This checkpoint does not complete AR-2H or full AR-2.

### Owner trust attempt — device registration prerequisite

The owner reported completing the trust action. The UI instead displayed the
typed `device_not_registered` recovery message; retrying the same finite task
registration still returned `pairing_registration_device_trust_required`.
Navigating the read-only recovery panel established the narrower prerequisite:
**Device & Security** reports native supervisor and credential isolation ready,
MFA/step-up available, the current profile session active, and this Windows
device **unregistered**. The enabled **Register this device with MFA** control
is now visible for the owner. See [registration readiness](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-2-device-registration.json).

The owner must complete native confirmation and browser MFA, then return to
Full Harness trust. This replaces the earlier request to repeat the trust
button. Local MFA availability is distinct from the separately observed domain
`auth0_not_configured` failure. No authentication dialog or security permission
was automated and no registered-device state was forged. Installed pairing,
guest admission and provider acceptance remain pending.

### Owner MFA completed — registered device active

The owner completed the Auth0 one-time-password ceremony. The browser reached
the credential-free loopback `/complete` page; subsequently the native security
panel changed this exact Windows device from **unregistered** to **active** and
reported **Fresh MFA was verified and the exact security operation completed.**
The [completion observation](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-4-mfa-completed.json)
records this result without codes, tokens or receipt material. The historical
September 13 registration activity was not used as proof of this completion.

Device registration is now observed in the installed package. The separate
Full Harness trust approval, task registration/pairing, guest admission and
live interpretation journey remain pending. Neither the MFA result nor the
loopback page grants room or environment effects.

### Full Harness trust confirmed — exact task review prepared

The installed Agent Access panel now reports **This device is trusted for Full
Harness**. Authenticated finite registration for this exact task succeeded at
`2026-09-25T05:39:17.203Z`, expiring at `2026-09-25T06:09:17.203Z`.
Registration is an authenticated client declaration, not pairing consent or
execution authority. See [sanitized readiness evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-5-trust-and-task-review.json).

The September 14 invitation was expired. A fresh review now selects this task,
uses a one-hour pairing duration and a 15-minute invitation deadline, and
excludes the old environment run. The approval checkbox remains unchecked;
no invitation has been issued or accepted by this checkpoint. The owner has
been asked to approve this exact review and use Copy invitation for an opaque
handoff, without placing credential material in conversation or evidence.

Automatic invitation delivery is unavailable in the installed UI. Source
inspection confirms that `pairing-delivery-service.ts` specifies a trusted
adapter but has no production adapter enabling it. This is a distinct delivery
limitation, not a reason to repeat MFA or device trust. Current registration and
pairing durations must not be conflated with the short presence heartbeat.

Next: accept the owner-issued finite invitation through the authenticated exact
task tool, verify the restored binding, then establish independently
authenticated guest access on the supported room authority path. The last room
observation still has one owner, no guest and no voice consent. No provider
call, environment action, deployment or capability maturity promotion occurred.

### Guest access preflight — native origin is not a browser guest surface

While awaiting owner pairing approval, authenticated room inspection still
returned exactly one participant, no voice consent and an idle runtime. Two
credential-free HTTP probes to the installed origin returned **401** for both
`/api/account/session` and `/api/agi/realtime/rooms`. This is expected native
isolation, not evidence of a broken account or a missing room.

`apps/desktop/src/main.ts` injects the per-launch desktop header only into the
native Electron session's requests to the exact service origin;
`server/index.ts` mounts the desktop session guard before these routes. Opening
that origin in ordinary Chrome therefore does not provide an independent guest
sign-in path. Do not copy the native secret or disable the guard to manufacture
guest acceptance. A second independent EXE likewise does not establish access
to the owner's room authority merely by running the same artifact.

Together with the existing broker and domain findings, this rules out the
assumption that an ordinary second browser can join this installed session as
currently configured. Task pairing can still be completed independently. The
next guest step must establish a supported shared authority transport; otherwise
record the missing transport as the first installed collaboration divergence
and qualify a bounded transport implementation before spending on voice.

### Owner returned — expired registration and submitted-request recovery

At `2026-09-25T10:35:24Z`, after the owner reported copying, the installed UI
showed a submitted request with **Reconcile invitation** and the generic
unconfirmed-request message. No invitation field or confirmed pairing was
visible. The clipboard did not contain a valid JSON pairing invitation; its
contents were not disclosed or retained and no acceptance call was made.

Replaying the original registration request returned the typed
`pairing_registration_expired` error. Its original deadline was
`2026-09-25T06:09:17.203Z`. The original invitation POST's response was not
captured, so expiry is a verified current blocker, not a recovered log of that
POST. Authenticated presence still succeeds on the same service and profile;
Full Harness trust remains visible. Repeating MFA is not the recovery step.

A new registration for the same destination digest expires at
`2026-09-26T10:37:02.576Z`. This 24-hour registration is task discovery metadata;
it neither extends any existing pairing nor grants environment or room control.
The owner was asked to cancel the uncertain request using its existing
cancellation fence, refresh registered tasks, and review/approve the same
one-hour, chat-only scope. No pending request was silently retargeted.

Admitted narrow repair, classified **presentation**: preserve the server's
allowlisted `pairing_registration_expired` code in `durablePairing.ts` and show
fixed recovery guidance in `DurableTaskPairing.tsx`. The existing regression
fixture now covers HTTP 409 expiry, exclusion of server-private message text,
frozen submitted controls and identical request replay. This changes neither
consent nor expiry/retry semantics and does not depend on guest transport.
The currently running AR-2G package remains unchanged; source verification is
recorded separately from installed acceptance. Full AR-2H remains incomplete.

Verification for this repair: all **46** tests in
`client/src/components/agent-access/__tests__/DurableTaskPairing.spec.tsx` pass
with one Vitest fork; changed-code `git diff --check` passes.
`helix:ask:discipline:quick` exits successfully, with warnings on existing
unrelated dirty Helix surfaces, and `helix:environment-harness:docs-audit`
passes. No package rebuild or live provider test was performed. Casimir
verification is outside this presentation-only patch's scope.

### Accepted pairing and connector refresh — 2026-09-25 UTC

The owner cancelled the uncertain request, approved a fresh one-hour chat-only
pairing and copied its invitation. Authenticated acceptance succeeded at
`2026-09-25T12:31:10.764Z`. The installed panel confirms **Pairing: accepted**
and **Chat binding: active**. The invitation was consumed opaquely and matching
clipboard contents cleared; no secret is included in the
[pairing checkpoint](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-6-pairing-accepted.json).
Consent expires at `2026-09-25T13:30:35.074Z`; neither the 24-hour registration
nor the short presence heartbeat extends it.

Room candidate discovery succeeded after refreshing this task's presence and
the installed room's task options. The room still reports no selected mission.
Selecting this paired task is a separate owner-confirmed room operation; it
does not start voice, provider inference or environment actions.

Initial steering reads failed because the connector's cached tool definitions
omitted the server-required `helix_conversation_id`, `mission_id` and `run_id`.
Using **Refresh** on the existing ChatGPT developer-mode connection visibly
updated read/acknowledge schemas and listed the room-result submission tool.
No server validation was weakened. A post-refresh call passed schema validation
but correctly rejected inactive task presence. After an authenticated heartbeat,
the same exact binding read returned `ok: true` and `deliveries: []` in this
task, without requiring a new conversation or renewed pairing. See
[connector-refresh evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-7-connector-refresh.json).

An empty successful read proves this narrow authenticated path only. It does
not prove instruction pickup, acknowledgement, result submission, shared
explanation, guest isolation or restart/revocation behavior. No provider call,
guest admission, game action, package rebuild or deployment occurred. Continue
with owner mission selection and the supported shared-authority transport
prerequisite; keep the full AR-2H acceptance checklist open.

### Room selected and stale results display repaired

The owner requested autonomous continuation after the concrete room-selection
review. The installed UI now reports **Mission: Selected · revision 1**, the
exact paired chat and **Room mission selected. Review a captured instruction
before sending it.** No current captured instructions are available. See
[selection evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-8-mission-selected.json).

The adjacent results panel still displayed its earlier no-mission catalog.
This admitted **presentation** repair synchronizes the two owner panels through
`SharedLiveRoomOwnerWorkflow.tsx`. Validated selection/revocation or refreshed
mission identity changes remount the results panel, abort its old request,
discard report/disclosure state and fetch its own authorized catalog. It never
uses the selection receipt as report or answer authority, and does not initiate
an explanation. The existing owner/presence/closed-room visibility fence stays
in `SharedLiveRoomActivePanel.tsx`.

The 24 existing mission/results tests pass; all three new cross-panel tests
pass for selection refresh, revocation clearing and a delayed old read. Five
surrounding dialog/navigation tests also pass, for 32 passing targeted tests.
The documentation audit and scoped diff check pass. The
quick discipline check passes with warnings on unrelated existing dirty
surfaces. This source repair is not installed acceptance: the running AR-2G
package is unchanged. Casimir verification is outside this presentation scope.

### Next implementation boundary — shared room authority transport

Continue this packet rather than repeating device setup or opening another
roadmap. The narrow next sequence is:

1. Define one authoritative service for the private owner/guest demonstration.
   Record where account membership, live runtime, handoff, mission binding and
   results live. `room-store/database.ts` uses the runtime pool, whereas
   `runtime-registry/state.ts` and `local-supervisor/reasoning-task-binding-store.ts`
   retain process-local state. A common database cannot join those runtimes.
2. Specify and qualify separate guest authentication and the owner's connection
   to that service. The current `friends-parties-coordination-broker.ts` grants
   only its named API; do not widen it into a general proxy or export the native
   desktop header. Cross-service task association needs an explicit scoped
   transport and identity check, not copied local binding IDs.
3. Implement the smallest admitted transport with two independent test
   principals. Verify wrong room/account/task, expired/revoked session,
   disconnect/restart, duplicate dispatch and private-result isolation before
   packaging. Preserve the ordinary personal MCP path and developer access.
4. Qualify the matching EXE/service pair with both participants reaching the
   same authoritative room and current owner task. Only then admit the separate
   voice/cost test. Label any two-profile same-service rehearsal as such; it
   does not qualify two independent EXEs or production domain collaboration.

The transport implementation is a prerequisite repair within AR-2H's admitted
AR lane. Production deployment, authentication-provider changes, new exposure
and paid inference still need their own concrete admission. This does not open
unadmitted commercial child stages or promote AR-2/G8 maturity.

### OAuth transport baseline — prerequisite repair, verification incomplete

Fresh authenticated inspection at this checkpoint still finds one present
owner, no guest, all media consents false, an idle runtime and zero public
results. [Transport-baseline evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-9-oauth-transport-baseline.json)
separates that installed observation from synthetic test results.

`server/routes/helix-shared-live-rooms.ts` already exposes OAuth-protected room
list, inspect and create control at `/api/v1/rooms`, through the existing shared
control service. At that baseline, invitation joining existed only on the browser-session route
in `server/routes/agi.realtime-room/room-lifecycle-routes.ts`. Neither that REST
OAuth router nor the room MCP tool catalog provided invitation joining then.
Use this existing principal/account boundary for the next transport slice;
do not exchange room-scoped credentials for an unrestricted browser session.

Baseline replay exposed two defects in verification, not production OAuth:

- The OAuth fixture signed `tenant_id` but inherited a workstation-specific
  tenant-claim selector. It now explicitly configures and restores its own
  claim selector; correctly signed unlinked identities reach account-link
  denial instead of failing at tenant verification.
- The live-acceptance catalog evaluator required an older command-error schema.
  It now requires the server's `content_role`, `reentry_required`,
  `answer_authority`, `assistant_answer` and `terminal_eligible` fields and exact
  literals. Negative tests reject omission or forgery of each field. This is
  **evidence normalization / release evaluation**, with no change to server
  access, model sampling, tool execution or terminal authority.

All **13** targeted tests pass: twelve acceptance-script tests and the synthetic
signed-JWT/account REST/MCP chain, including wrong signatures, unlinked account,
read parity and revoked account-link denial. Scoped diff checks pass. The
synthetic chain's room list is mocked; it does not prove two real members can
join, nor any room mutation, media session or installed transport.

Because release-evaluation code changed, the repository's Casimir gate applies.
The existing CLI adapter request to the installed service returned
`401 desktop_session_required` before evaluation. No trace was exported; no
PASS verdict, certificate hash or integrity status is claimed. This patch
remains pending that verification, and no desktop credential was extracted,
guard weakened, replacement keyed server started or deployment made.

The user requested autonomous continuation. Independent source development of
OAuth invitation joining is admitted while the release-evaluator Casimir check
remains open; neither source work nor its tests may close that check. This
slice does not expose the installed service, alter provider configuration or
depend on a paid voice session. Classification: **source admission / tool
admission**. Codex runtime and terminal ownership remain unchanged.

The admitted implementation slice is invitation joining through the existing OAuth control service:
derive the joining profile from the verified principal, require exact room and
one-time invitation, preserve the profile admission lock and personal-session
conflict check, and qualify uncertain-retry/revocation behavior without storing
invitation plaintext in receipts. Prove this with two independent accounts
before wiring native transport. Native room routing, same-service runtime
ownership, cross-service association of the owner's local task and live guest
acceptance remain separate unfinished work; adding a join endpoint alone does
not complete the journey.

### OAuth invitation join — source implementation checkpoint, 2026-09-25

Current maturity: `implemented` for the REST invitation-join slice only. The
installed AR-2G EXE is unchanged; AR-2H live acceptance remains open.

`POST /api/v1/rooms/join` now accepts an exact room ID and one-time invitation
through the existing verified OAuth principal and linked account. Both room
read and manage scopes are required. The strict request cannot supply another
profile, grant consent or acquire owner authority. No browser cookie or native
desktop credential is issued. The MCP catalog is unchanged.

The control service holds the existing profile admission lock and shares the
browser join's personal-Realtime conflict check. Profile reference generation
was extracted into a dependency-free module without changing its hash format;
this removes an import cycle found during testing. The lock and admitted-session
registry remain process-local: this does not qualify multi-worker exclusion.

Durable idempotency is scoped to the OAuth owner tuple and hashes the invitation
for request identity. Its receipt stores only the room and participant reference.
A retry reads current membership; closed rooms, departed members and replacement
participant identities are rejected. An uncertain join or receipt-save outcome
blocks automatic redemption retries. Invitation strings now receive the existing
secret redaction treatment; invitation handling is exempted from the REST
credential-input filter only for the strict purpose-bound join body.

Verification includes service, REST admission, shared personal-session exclusion,
redaction and the browser room lifecycle suites. A two-account deterministic
test uses real room persistence and concurrent service joins: one membership is
created, the second request replays, the guest remains a participant with media
consents false, and creating invitations is denied to that guest. The OAuth
principal is simulated in these tests; no real remote account, JWT-to-join live
journey, installed guest UI or production database was exercised.

[Source checkpoint evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-10-oauth-invitation-join.json)
records **67 passing tests** across six suites, including the profile-reference
regression checks. The discipline quick check and harness documentation audit
pass. No full repository typecheck was run.

The broader transport suite also exposed an existing exact MCP catalog assertion
that expects an older tool list. Targeted REST checks pass; the unrelated catalog
assertion remains open and is not counted as passing. The earlier release-evaluator
Casimir check still has no PASS, certificate or integrity result.

Next: qualify the verified OAuth/account chain together with this real join path,
then implement the narrowly scoped native guest transport to the same room
authority. Guest mission denial, owner task delivery, results, media and reconnect
still require their recorded live acceptance. The last installed pairing expired
at `2026-09-25T13:30:35.074Z`; its earlier acceptance is historical evidence, not
current permission. Renew only when the installed journey is ready to use it.

### Verified OAuth join chain and installed transport boundary — 2026-09-25

The signed-token integration fixture now exercises the real REST authentication,
durable account-link resolution, shared control service and room database join
together. Its guest has a distinct linked profile and subject. Wrong signatures,
unlinked subjects, missing room-manage scope and supplied profile identity are
rejected before membership changes. Successful joining and same-key retry leave
exactly two members, with the guest a participant, all tested media consents false
and runtime idle. Revoking the guest's account link blocks receipt replay through
that token; it does not silently delete the account-owned room membership.

These are synthetic HS256 tokens in an isolated in-process test database, not
production Auth0/JWKS or installed remote guest acceptance. The existing signed
REST/MCP read-parity test remains in the suite. The desktop guard is included in
a separate request path and rejects even a valid room token without a native
desktop session. No exception was added to that guard.

[Verified OAuth join-chain evidence](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2h/resume-11-verified-oauth-join-chain.json)
records both integration tests passing and the fresh installed/domain checks.

Fresh authenticated MCP inspection still finds the installed room waiting for a
guest: one present owner, idle/unbound model runtime, no media consent and zero
public results. A fresh browser read of the domain fingerprint still identifies
commit `42c5b19b4be2a95d2d2de937ef9f550df8b6f2b4`, built
`2026-09-01T19:18:38.201Z`; the new join code is not deployed there.

The next connection implementation must address all three source-level gaps
together, rather than treating native sign-in as room readiness:

1. `auth0-native-account-link.ts` defaults to `openid profile` plus the Friends
   & Parties coordination scope. Shared-room read/manage scopes require their
   own explicit authorization/configuration; existing MFA does not prove them.
2. Native account linking writes to the local account-link store. Friends &
   Parties bootstraps a domain cookie separately; neither that cookie nor local
   binding IDs prove a room OAuth subject is linked on another authoritative
   service. Establish and verify that mapping on the selected room authority.
3. The installed desktop guard rejects general room REST access. Provide a
   narrowly scoped room transport that preserves its boundary and reaches the
   same runtime, mission and task authority. Do not widen Friends & Parties,
   distribute the desktop header or assume a shared database joins local tasks.

Native transport implementation remains admitted source work. Actual new network
exposure, provider configuration and production deployment remain outside this
packet's current execution scope. AR-2H stays active with its full installed
acceptance checklist open; the earlier Casimir release-evaluator check is still
unresolved.
