Program gate: G8.
Workstream: CS1 and O1 ordinary Ready up failure localization.
Capability or component: sanitized environment-session preparation failures.
Lifecycle stage: evidence normalization; source admission.
Reaction timescale: bounded explicit preparation calls; existing freshness and lease limits remain unchanged.
Authority owner: the authenticated exact task and existing finite human grants.
Current maturity: specified for this diagnostic repair.
Target maturity: deterministically verified failure localization, followed by packaged reproduction and repair of the identified boundary.
Required evidence: retained generic packaged failure; deterministic failures at each preparation phase; no private exception data; preservation of typed rejection and partial repair facts; focused regressions, documentation audit and ordinary package verification.
Explicit non-goals: no automatic consent, replacement pairing, deadline extension, private execution loop, automatic retry, gameplay or CS/ET6/NAV1 acceptance claim.
Downstream gate unlocked: diagnosis of the current shared Ready up boundary only.

# Locate preparation failures without exposing private data

Follow the [work program](../helix-environment-harness-work-program-v1.md),
[continuous-session packet](eh-g8-et6-continuous-session-build-v1.md), and
[onboarding packet](eh-g8-cs-onboarding-pairing-plan-v1.md).

After actual human permission renewal, the same player connector became ready
and the same durable goal recovered from revision 18 to 22 through supported
expiry, rebound, checkpoint and resume events. Automatic Ready up still returned
`internal_error` on the read-cost package. This does not identify a failed
credential, account, run query or persistence barrier.

Add a finite server-owned preparation phase to sanitized failures. Cover the
initial account, binding, run and membership reads as well as later observation
and recovery. Preserve existing typed rejection codes, partial repair facts and
fail-closed admission. Never project exception messages, stacks, SQL, argument
values, credentials or caller-supplied phase labels. No diagnostic phase grants
readiness or suggests a reconnect. Reproduce through the packaged MCP path
before assigning a cause or changing authority behavior.

The first diagnostic package localizes both automatic and exact-selector calls
to task-binding verification. Add bounded substeps for destination authorization,
repository initialization, durability confirmation, record read and accepted
record validation. Preserve recognized policy rejections; classify unknown
exceptions only with a finite vocabulary, never their raw text or stack. The
isolated native encrypted repository plus MCP recovery/verification test passes
and therefore does not establish the cause of the packaged failure.

The second diagnostic package reports `durability / snapshot_changed`. A
deterministic interleaving reproduces the same rejection without a second data
mutation: while confirmation writes its snapshot, another reader requests a
strict save; that queued save marks the table dirty before the first reader
receives its successful persistence acknowledgement. Separate forced collection
from mutation tracking. Required tables must still be freshly collected for
each strict batch. Actual concurrent mutations, omitted required tables and
failed writes must still reject confirmation. Retain the failed fixture and
verify the repair through the same packaged session before claiming resolution.
