Program gate: G8.
Workstream: CS4 ordinary packaged environment recovery.
Capability or component: prepared Minecraft launcher profile observation during startup.
Lifecycle stage: presentation; evidence re-entry.
Reaction timescale: the existing bounded 30-second launcher observation window.
Authority owner: the existing authenticated lifecycle admission; the launcher must still display the exact saved profile before Play.
Current maturity: specified.
Target maturity: deterministically verified classification and separately evidenced packaged launch.
Required evidence: retained native window-only observation followed by the exact visible profile, isolated real profile-guard regression, wrong-profile/process/version rejection, existing lifecycle tests, documentation audit and qualified package.
Explicit non-goals: no profile selection, credential access, extra launch, permission renewal, relaxed exact-profile match, enlarged timeout, private model loop, gameplay or ET6/NAV1 qualification.
Downstream gate unlocked: further CS4 rehearsal only.

# Launcher loading recovery

The [work program](../helix-environment-harness-work-program-v1.md) remains the
sole roadmap; preserve the [continuous-session exits](eh-g8-et6-continuous-session-build-v1.md).

At 2026-09-14T11:23:45Z the real packaged lifecycle helper saw only three
window buttons and zero profile candidates, then returned
`minecraft_fabric_profile_selection_required`. Native inspection subsequently
showed the expected `helix-combat-c0-isolated fabric-loader-0.18.4-1.21.8`
profile without changing it. One inspected retry launched client 23960 and
joined the prepared loopback server with the qualified companion.

Distinguish an observation containing only standard window-frame buttons from
a populated page with the wrong profile. The former may be re-observed only
within the existing launch deadline. It never authorizes clicking Play.
Exact profile, version, process and enabled/visible control checks remain.
Empty or failed observations remain unavailable, and a populated wrong-profile
page still rejects immediately. Keep the initial failure as negative evidence;
the successful inspected retry is not proof of a one-call cold launch.
