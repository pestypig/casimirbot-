Program gate: G8 — release evaluation; CFP-1 active
Workstream: AR-2A — deterministic preparation within AR-2
Capability or component: Three-human assisted-room fixture and existing boundary checks
Lifecycle stage: Offline evaluation preparation
Reaction timescale: Ordered human turns and worker results; no latency claim
Authority owner: Canonical work program; Codex reasoning lifecycle; program owner for effects
Current maturity: specified (integrated three-member scenario)
Target maturity: specified (scenario), with explicitly scoped deterministic component evidence
Required evidence: Synthetic scenario, assertions against existing components, routing triage, source hashes, test report and next prerequisite
Explicit non-goals: Production edits, DB capacity changes, API calls, live credentials, native effects, deployment, managed offer or full AR-2 acceptance
Downstream gate unlocked: Reviewable bounded implementation prerequisite; live AR-2 remains conditional

# AR-2A goal prompt

Model: GPT-5.6 Sol, medium; Astra escalation for unresolved authority questions.
The owner confirmed this selection during execution. AR-2's guide envelope is
700 development credits, with review at 525; this preparation should use only a
small portion. Shared account balance observed near start: 918.22485 credits.
Account balance changes are not task-attributable. API budget for this slice: zero.

> Follow the [launch execution guide](eh-g8-casimirbot-platform-market-launch-execution-v1.md)
> and [AR-1 audit](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-1/contract-source-audit.md).
> Build a reproducible synthetic Dan/Sam/Alex scenario with one Live participant,
> one mission principal, attributed disagreement and correction, read-only
> workers, owner grants, interruption, revoke/reconnect and cross-room denials.
> Exercise existing production boundary functions without a provider, database,
> native actuator or new runtime loop. Distinguish observed behavior from
> unimplemented expectations. Triage the named-document precedence mismatch
> without changing production routing or weakening the existing assertion.
> Preserve free personal BYO-provider use, shared Helix Ask/Home connection-menu
> semantics, separate hosted eligibility and the selected no-model launch offer.
> Record exact checks, failures and hashes, then identify the smallest next
> implementation prerequisite. Do not claim three-user live acceptance.

## Scope and admission rationale

This is an offline evidence fixture, not a new production implementation lane.
It can inspect existing components with synthetic identities without depending
on signed distribution, commercial action rights, model access or expanded room
capacity. It does not modify the G1-T1 allowlist or admit CFP-2/3. Production
code, runtime schemas, routes, grants and database constraints remain unchanged.
Patch classification: evidence normalization audit and source-admission test
triage only; no change to either authority mechanism.

Deliverables are confined to this packet, the linked
[AR-2A evidence directory](../evidence/eh-g8-codex-first-paid-product-delivery-v1/ar-2a/),
and a progress reference in the launch guide. A runnable evidence probe imports
existing code and uses a mock read driver; it does not authenticate real users,
create a room, perform reasoning or execute effects.

## Completion

- Store the fixed scenario with explicit expected-only steps.
- Run a focused existing-component probe and relevant existing tests.
- Preserve the failing routing assertion and explain its first divergence.
- Report which identity checks exist and which must be supplied by callers.
- Record source hashes and documentation validation.
- Leave the full AR-2 checkbox unchecked and identify the next admitted-work
  decision before any live execution.

The Configure connection target is carried as acceptance expectations:
non-subscriber personal BYO works on a qualified path, visible provider/payer,
no silent managed fallback, protected credentials, separate hosted entitlement.
This fixture cannot prove UI, secret custody, billing or actual model access.

