Program gate: G8 — environment-harness release evaluation.
Workstream: CS1 shared Ready up retry repair within the continuous-session prerequisite.
Capability or component: browser/MCP preparation observation correlation.
Lifecycle stage: evidence re-entry.
Reaction timescale: explicit readiness check with a five-second observation freshness bound.
Authority owner: Codex invokes preparation; Helix validates exact task/run/player identity and finite human consent; the resident executor receives no gameplay in this repair.
Current maturity: specified for this repair; existing component evidence retains its exact scope.
Target maturity: deterministically verified retry freshness and separately recorded packaged development rehearsal.
Required evidence: red/green real-broker composition across three delayed identical requests; unchanged goal and authority identity; broker replay and stale/wrong-identity rejection; focused regressions, discipline checks, documentation audit and package identity.
Explicit non-goals: no consent renewal, healthy credential rotation, private retry/model loop, age-gate relaxation, gameplay, ET6 completion, NAV1 qualification or receipt answer authority.
Downstream gate unlocked: further CS1-CS4 development rehearsal only.

# Ready up repeated-read freshness repair

The sole roadmap remains [the work program](../helix-environment-harness-work-program-v1.md).
Preserve every exit in [CS1-CS5](eh-g8-et6-continuous-session-build-v1.md)
and [O1-O6](eh-g8-cs-onboarding-pairing-plan-v1.md).

## Frozen failure and change contract

The authenticated packaged call first returned ready and created one goal. An
identical call approximately 30 seconds later returned a perception blocker
`durable_goal_evidence_identity_mismatch`. A distinct request ID, with current
presence, returned ready against the same goal. See
[the immutable pre-repair evidence](../evidence/eh-g8-et6-continuous-session-build-v1/2026-09-14-ready-up-retry-before.json).

The preparation turn and tool-call IDs were both derived solely from the stable
setup request. The durable broker therefore returned the original completed
observation on every repeat. Its downstream five-second age check correctly
rejected that evidence. This is an evidence re-entry defect, not a new permission
requirement.

Keep stable setup/goal idempotency and exact association checks. Each explicit
Ready up invocation acquires one independently correlated read through the
existing gateway and broker. Retransmission of that individual read retains
broker deduplication. Do not retry internally, extend the age bound, relabel old
observations, create another goal or change human authority.

## Deterministic verification

Before changing production code, compose real preparation with real durable
probe dispatch, lease, result submission and exact evidence read in isolated
pg-mem storage. Account/task/run/goal projections and sensor payloads are
fixtures. Advance the clock beyond five seconds between three identical Ready
up requests. All three must acquire fresh exact evidence; the first old
observation must still reject. Retain negative scope and broker duplicate
checks. This proves orchestration, not physical sensing or live movement.

After green focused regressions, run the relevant discipline checks and docs
audit. Rebuild, compare packaged content, launch normally and repeat the exact
MCP request three times without new consent or identity rotation. Report native
UI parity separately. Any new failure stops progression to movement and remains
in evidence. No integrated exit is qualified by component results alone.

