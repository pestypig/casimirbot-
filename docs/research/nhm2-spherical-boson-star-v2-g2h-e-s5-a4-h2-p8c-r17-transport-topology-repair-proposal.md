Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: S5-A/A4 candidate-neutral H2 evidence retrieval
Capability or component: H2-P8C-R17 stopped-attachment topology repair plus transport-independent retrieval
Current maturity: frozen executable successor; independent static audit 24/24 PASS
Target maturity: authenticated local P8C archive and unchanged frozen result audit
Required frozen inputs: terminal R16 result, exact R16 controller, retained read-only clone, active charter, and exact three-user stopped topology
Required evidence: three stopped attachment identities, two exact detaches, sole stopped R15 postcondition, generated R17 controller identity, helper lifecycle, exact archive bytes/hash, source unmount, and zero running VM closure
Stop/fail criteria: first R17 failure terminal; no R16/R17 retry, substitution, source write, numerical execution, or missing terminal receipt
Explicit non-goals: SSH trust mutation, candidate evaluation, positive sampling, retuning, evidence deletion, Rust/G3/SI/metric/lane work, or authority promotion
Downstream gate unlocked: H2-P8D causal classification only after authenticated local capture and unchanged frozen P8C audit

# H2-P8C-R17 transport-topology repair proposal

Status date: August 30, 2026.

Status: **FROZEN / CHARTER-ELIGIBLE / AUDITED 24/24 PASS**.

R16 terminated before resource creation because the retained clone has three
stopped helper users rather than the frozen sole R15 user. R17 changes only
that topology precondition. It authenticates the exact R13/R14/R15 stopped
attachment set, detaches the clone from stopped R13 and R14, requires stopped
R15 as the sole user, and generates a byte-authenticated R17 controller from
the frozen R16 controller by changing only the helper, Cloud Shell archive and
controller-evidence names.

The inherited startup procedure, read-only mount rules, expected 16,443-byte
archive `9535ce13...bd4d`, four bounded guest chunks, one `e2-small`, 10 GB
boot disk, 1,800-second runtime ceiling, `$1.00` cost ceiling, source unmount,
helper stop and final source detach remain unchanged. The guest namespace
remains `nhm2-r16` because the inherited startup procedure is byte-identical;
the R17 helper, archive and evidence paths are disjoint.

R17 performs no SSH/SCP operation, source write, numerical process, candidate
evaluation, retune, evidence deletion, or authority promotion. First failure
is terminal and does not authorize retry.

Frozen proposal SHA-256: `133cf34692ea0dd6a02b9e50d15d73001a07979c83cf53b0cb361c12ca87da7f`.
The independent static audit passes **24/24**; the wrapper is 4,538 bytes at
`80cb0386...fd22`, and Git Bash syntax validation passes.
