Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R28 forward-slash isolated host-key path successor
Current maturity: R27 native SCP proven; receipt path guard failed before SSH; retained VM stopped; calibration unexecuted
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R28 record
Required frozen inputs: R27 result, stopped retained VM/disk and R27 partial archive, exact native OpenSSH keypair, unchanged outer archive, guest ledger, executable and auditor
Required evidence: exact R27-to-R28 semantic diff, one restart, one new-name SCP, forward-path isolated host-key receipt, one SSH handoff, serial evidence, stop and P8Q audit
Stop/fail criteria: first identity, start, transfer, receipt, guest, build, execution, recovery or audit failure terminal; no retry or fallback
Explicit non-goals: use/overwrite/delete R27 partial archive, new VM, metadata/IAM/user mutation, retuning, candidate evaluation, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R28 forward-slash host-key path successor

Status date: September 3, 2026.

Status: **FROZEN PREEXECUTION PROPOSAL / RETAINED VM REMAINS TERMINATED**.

Change classification: exact local path normalization and fresh evidence/remote
names only. Scientific semantics and authority remain unchanged.

## Evidence-selected correction

The immutable R27 result is SHA-256
`d919944dc84839649be0801400b1df9ab33230ef07de6cda1f41e06681b63e41`
and independently audits 16/16. Its native SCP returned success and transferred
the authenticated archive, proving the key, `dan` identity, network and native
SCP path. The next guard failed only because Windows OpenSSH did not create the
isolated receipt at the backslash-form `UserKnownHostsFile` value. The VM was
stopped before SSH handoff; no calculation ran.

R28 changes the immutable R27 controller only as follows:

1. use a new R28 local evidence root, isolated receipt name, remote archive
   name, guest stage, service name and markers;
2. derive the OpenSSH option path with exact PowerShell
   `$KnownHosts.Replace('\', '/')` semantics, yielding an absolute
   forward-slash path matching `^[A-Za-z]:/`;
3. pass that derived value to both native SCP and SSH.

All other controller content is byte-for-byte derived from R27. The retained
`/home/dan/h2-p8p-r27-upload-v1.tar` remains immutable and unused. R28 uses only
`/home/dan/h2-p8p-r28-upload-v1.tar` and a new local evidence root.

## Frozen identities and resource boundary

| Item | Frozen value |
| --- | --- |
| R28 controller | `tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r28_forward_path_controller_v1.ps1`; 14,541 bytes; SHA-256 `479e59f066d6cba8367b4955a08c1c830e98c9eee68f1333c5e653cc7ccd6f50`; parser PASS |
| retained VM | `nhm2-h2-p8p-r26-c2d-32-20260903`, ID `4290604153416687194`, `us-east1-c`, initially `TERMINATED` |
| machine/image/disk | unchanged `c2d-standard-32`, Debian `v20260817`, 30 GB `pd-standard` |
| native SSH/SCP | unchanged SHA-256 `6250fd52163fe99a0dc49403ed1b4bbef9b764bdb7bada017a93d057d9376a42` / `63b7118d8e1a8a84398cf4ce1584dc6b146606092fe9c68bbaf110bbdcfb480a` |
| keypair | unchanged private SHA-256 `37e1a9dab99f498aa6d01e335e5351088247cb307175b60ee71f9f37c88b2b95`; public SHA-256 `d5035122b18833ab736834cc388af852317573913804a32d233326afd2bb5bc7` |
| outer archive | unchanged 236,640,768 bytes; SHA-256 `3c697fa3e238398d3b4c30a6c379fea8d6d545b2228d0c699dd66594a23670b5` |
| executable | unchanged SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718` |
| restart/runtime/cost | exactly one restart / 18,000 seconds / `$9.00` total ceiling |

The new evidence root is
`artifacts/nhm2/g2h-e-s5/candidate-neutral/h2-p8p-r28-native-openssh-execution-v1-20260903`
and must be initially absent. The exact VM and R27 partial evidence remain
retained; no resource is created or deleted.

## Bounded execution

R28 repeats the already frozen retained-resource, account, key and disk guards,
then restarts the exact VM once and waits 180 seconds. It performs exactly one
native SCP as `dan` to the new R28 remote filename. The SCP may accept one host
key into only the forward-slash-form R28 evidence receipt. That receipt must be
a regular file and is hashed immediately. Exactly one SSH handoff then requires
`StrictHostKeyChecking=yes` against the same receipt.

The guest must require the preexisting `pestypig` user, authenticate all four
members, require the unchanged ledger targets absent, and stage only the base,
overlay and ledger. The unchanged ledger conditionally installs Debian
`docker.io` only if absent, performs the offline build, verifies the executable
and runs exactly one candidate-neutral P=1024 calibration with 32 CPUs and a
14,400-second timeout. All outcomes are preserved. The VM stops automatically
or through the bounded failure handler. Only the serial evidence archive is
recovered and evaluated by the unchanged P8Q auditor.

First failure is terminal and consumes R28. No retry, second restart, transfer,
host-key acceptance, SSH, ledger, build or numerical process is permitted. No
new VM, resource substitution, alternate path/identity/key/transport, metadata
or IAM mutation, guest-user creation, additional upload, retune or evidence
deletion is allowed.

## Scientific and authority boundary

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false. R28 cannot authorize P=65,536. It can only produce the frozen P8Q
resource-bounded yes/no decision.

## Exact authorization text

> I authorize exactly one H2-P8P-R28 forward-slash host-key-path retained-VM P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Restart only the existing stopped VM `nhm2-h2-p8p-r26-c2d-32-20260903`, instance ID `4290604153416687194`, in project `dark-stratum-455714-h4`, zone `us-east1-c`, under the frozen 18,000-second and `$9.00` ceilings. Execute only the 14,541-byte R28 controller SHA-256 `479e59f066d6cba8367b4955a08c1c830e98c9eee68f1333c5e653cc7ccd6f50`. Preserve and do not use, replace or delete the retained R27 remote archive and evidence. Use the unchanged hash-bound native OpenSSH executables, keypair, guest identity `dan` and scientific archive; create only one new isolated R28 `known_hosts` receipt using its authenticated forward-slash absolute path; perform exactly one SCP to the new R28 remote filename and one SSH handoff; require preexisting `pestypig`; execute one unchanged offline build and exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout; preserve every outcome, stop the exact VM, recover and independently audit serial evidence, and apply only the frozen P8Q rule. First failure is terminal. I do not authorize retry, a second restart, transfer, host-key acceptance, SSH, ledger, build or numerical process; a new resource; metadata, IAM or guest-user mutation; fallback, substitution, alternate path, identity, key, transport, zone, machine or disk; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or authority promotion.
