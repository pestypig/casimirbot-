Program gate: G2H-E-S5-A4 / P8P observer progress and turnaround calibration
Workstream: Candidate-neutral R39 evidence recovery
Capability or component: V4-R1 Stage-A R44 retained-helper retrieval
Current maturity: One consumed R44 attempt, independently audited R44_FAILED; no R40 archive or P=1024 result
Target maturity: New, separately authorized candidate-neutral evidence-recovery path after the failure and helper-state defects are resolved
Required frozen inputs: V4-R1 proposal SHA-256 5a33865e7240f4d33f27300c967e8ac44e7f996f518ed68ee1bf144d2544dc49; attempt 2516da22e4cf4a8775cabc69f90771cc0f093aa3549a89a2afd1e570e13fec62; script SHA-256 f4e278347f335cda7d75565adc17ab7136de81b34b37258517f5b015bdaa89a5
Required evidence: Preserve the 8c4e6fecce0c68434238a439fb32cfac70b8a33b333538bce8f11a7e722336bf reservation and 53f0e6ee27e8d3bdcd885921b166a516099de22d912e7449bddb9070659217c1 terminal receipt, full journals and HTTP responses, serial diagnostic, provider status and residual scheduling
Stop/fail criteria: This attempt is terminal; no retry, restart, alternate root, build, numerical process or claimed restoration from partial state
Explicit non-goals: Frozen-candidate evaluation, mathematics retuning, P=65,536, G3/SI/metric/lane work, evidence deletion or scientific/physical/propulsion/transport authority promotion
Downstream gate unlocked: Candidate-neutral failure diagnosis and a new proposal only; Stage B and P=1024 remain closed

# V4-R1 Stage-A terminal result — September 20, 2026

The user approved the exact V4-R1 charter with its $12 planning ceiling, 27,600-second cumulative VM limit, temporary helper service-account/startup-metadata effects, evidence preservation and first-failure stops. Action-time read-only checks found the exact original and helper VMs stopped, zero project VMs running, the expected disk identities/lineage, direct unconditional worker bucket grants, an absent target object and an absent frozen local evidence root. The exact 31-file source closure passed; the selected Node and Python batteries passed 113/113 and 40/40. These checks admitted **one** Stage-A reservation and helper start, not a scientific run.

The frozen evidence root is `C:\NHM2-P8P-Workflow-Review\v4-r44-evidence`. Its create-once reservation SHA-256 is `8c4e6fecce0c68434238a439fb32cfac70b8a33b333538bce8f11a7e722336bf`. The controller installed and read back provider STOP for `2026-09-20T14:16:51Z` before starting only helper `nhm2-h2-p8p-r39-rescue-e2-small-20260904`, ID `7129462452423922626`. The original R32 build VM was not started.

The operator invocation used `p8p_v4r1_stage_a_dispatch_20260920.mjs`, SHA-256 `32ca97b269969282ced441dacf7f2f0e62d43486fb2c9dfb04f7b05ce2fed9d6`. It was locally syntax-checked and bound to the approved proposal, attempt and script, but it was written after the 31-file source-disjoint closure and was **not** part of that independent review. The closure claim above applies to the pinned production modules, not this late operator wrapper. This additional limitation reinforces that the failed attempt cannot be promoted to a retrieval PASS.

Read-only serial output from this boot shows the Google metadata startup-script runner failed to create `/tmp/metadata-scripts...` with `read-only file system` at about `14:02:57Z`; later `containerd` writes under `/var/lib` failed the same way. This is a diagnostic observation outside the frozen HTTP receipt chain. No `R44_EXPORT_READY` marker was observed. Because the guest path could not export, one operator safety STOP was requested under the charter's exceptional cleanup allowance and completed. That STOP is **external** to the runner's HTTP journal; the terminal field `extraSafetyStops: 0` counts only the runner's internal safety requests and must not be read as zero total operator STOPs.

The controller's terminal receipt SHA-256 is `53f0e6ee27e8d3bdcd885921b166a516099de22d912e7449bddb9070659217c1`: status `R44_FAILED`, errors `r44_object_not_observed` and `r44_restore_final_schedule`, archive `null`, scientific authority `false`. Independent `audit_p8p_r44_attempt_v1.py` replay accepted the evidence classification as `R44_FAILED`, **not** retrieval PASS. The exact GCS object still returned 404 and no R40 archive was captured locally.

After the controller ended, an independent Compute read found the helper `TERMINATED` with its original default service account and scopes and no startup metadata. Scheduling was **not fully restored**: `automaticRestart` was back to `true`, but the old absolute `terminationTime` and `instanceTerminationAction: STOP` remained. The controller had issued one restore-scheduling request and then failed its final baseline assertion. The original R32 VM remained `TERMINATED`; the project list showed zero running VMs. Leave the helper stopped and unchanged. Do not call this restoration PASS.

The immediate candidate-neutral defects are distinct: this retained helper's boot filesystem was read-only during startup, and the restore request omitted fields that the provider retained. Google's current [`set-scheduling` CLI reference](https://docs.cloud.google.com/sdk/gcloud/reference/compute/instances/set-scheduling) exposes explicit `--clear-termination-time` and `--clear-instance-termination-action` flags; this documents a possible separate cleanup method, **not** authority to invoke it under the consumed V4-R1 attempt. A new frozen proposal must resolve the residual stopped-helper schedule, choose a trustworthy writable execution environment for recovering the old archive, and independently review its safety and evidence path before any new mutation. Stage B, P=1024 and P8Q promotion remain blocked.

For this status-document change, `math:validate` passed 323 entries and all 18 required warp/GR test files passed 179/179. The adapter's repository convergence check returned `PASS`, `firstFail: null`, empty deltas, certificate hash `6e84f965957f63aad452981d2ede72e62f706d32e0a5b6b469899884e12a4e45`, `integrityOk: true`, `GREEN`. That certificate verifies its constraint-pack run only; it does **not** certify the failed R44 attempt, a boson-star state, or any physical viability claim. The existing dirty generated math-report files were preserved rather than overwritten by a new `math:report` run.
