Program gate: G2H-E-S — versioned primary successor execution
Workstream: authenticated classical and quantum control branch
Capability or component: sole primary-v2 invocation and immutable first-failure attribution
Current maturity: authorized invocation complete; immutable pre-mathematics partial result
Target maturity: authenticated primary Tolman-VII mathematical decision
Required frozen inputs: proposal `65093d00...cfdd`, authorization `452537ef...cc10`, contract `30de966d...343d`, image `sha256:8334e977...159ab`, executable `666ba126...69028` and checkpoint `8de58e8a...20f5`
Required evidence: invocation/log/result ledger, retained container, exclusive-root chronology, exact C parser reproduction and producer-independent audit
Stop/fail criteria: any persisted invocation, partial output or first failure; no retry, retune, deletion or alternate root
Explicit non-goals: reinterpretation as a star failure, same-proposal retry, independent execution, G3, SI, lanes, lamp or physical claims
Downstream gate unlocked: candidate-neutral authorization-schema disposition only

This packet records runtime and receipt evidence. It does not change
mathematical semantics, the candidate, any proof duty or claim authority.

# G2H-E-S primary result: immutable pre-math partial execution

## Decision

`PARTIAL_EXECUTION_PRE_MATH_AUTHORIZATION_SCHEMA_VERSION_SKEW`.

The exact authorized checkpoint ran once. Its outer process returned `1` after
the retained Docker container returned `66`. The invocation marker, empty
stdout, exact stderr and process result were persisted. Proposal
`65093d00...cfdd` is exhausted and no retry is eligible.

This is not a Tolman-VII mathematical `FAIL`. The output root is absent, so no
preflight record, surface-germ result or scientific duty was produced.

## Immutable evidence

| Evidence | Identity |
| --- | --- |
| Authorization | 380 bytes, `452537ef...cc10` |
| Invocation | 525 bytes, `2b6e57dc...cdd2` |
| Process result | 300 bytes, `0d398769...79c9` |
| Standard error | 60 bytes, `8a51fa06...2643` |
| Standard output | empty, `e3b0c442...b855` |
| Retained container | `6ffbd649062a...a9644b`, exited 66, not OOM-killed |

The exact stderr is:

```text
digest, source, runtime or authorization preflight rejected
```

## First-failure attribution

The v2 checkpoint and frozen seven-line record require:

```text
schema=nhm2.g2h_execution_authorization.v2
```

The exact primary-v2 C source still admits only:

```c
strcmp(value, "nhm2.g2h_execution_authorization.v1") == 0
```

The actual-runtime candidate-neutral C fixture independently establishes:

```json
{"candidate_entrypoint_invocations":0,"v1_admitted":true,"v2_rejected":true}
```

All other frozen identities match: contract and seven source files, token
digest, primary executable, image, build binding and output root. Therefore the
typed first failure is:

```text
AUTHORIZATION_SCHEMA_VERSION_SKEW_V2_CHECKPOINT_V1_BINARY
```

The read-only partial audit passes `16/16`. It binds the v2 ledger and retained
container, preserves every v1 byte, reproduces the exact C parser behavior,
proves the v2 and independent roots absent, and records zero candidate
evaluations.

## Chronology and authority

- primary-v2 candidate-capable entrypoint invocations: exactly 1;
- candidate evaluations: 0;
- primary-v2 output root and proof manifest: absent;
- surface theorem-assumption gate and all G2G duties: not reached;
- independent authorization, invocation and root: absent;
- retry, retune, deletion and alternate root: forbidden;
- candidate admission, proof, G3, SI, 68-file lanes, lamp, physical viability,
  propulsion and transport authority: false.

The only eligible next work is a candidate-neutral disposition of the schema
skew and, if justified, a separately versioned successor proposal requiring a
new explicit authorization. The current image, proposal, authorization, ledger
and retained container may not be changed or rerun.

## Verification

Current-head verification passes:

- math report/validation: `318` entries, `PASS`;
- every required WARP file: `18/18`, `179/179` tests;
- Casimir adapter run `2468`: `PASS`, first failure `null`, deltas empty;
- certificate `6e84f965...4e45`: `GREEN`, integrity true.

The verification receipt is
[`nhm2-spherical-boson-star-v2-g2h-e-s-primary-verification-receipt.v1.json`](./nhm2-spherical-boson-star-v2-g2h-e-s-primary-verification-receipt.v1.json).
It certifies only the integrity of this partial-result classification. It does
not certify a Tolman-VII proof or physical viability.
