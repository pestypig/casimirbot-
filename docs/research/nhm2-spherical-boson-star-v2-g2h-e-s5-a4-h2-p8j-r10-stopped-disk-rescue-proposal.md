Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: S5-A/A4 candidate-neutral H2 continuation diagnosis
Capability or component: H2-P8J-R10 stopped-disk terminal-evidence rescue
Current maturity: R9 allocation, transfer and controller handoff authenticated; original VM stopped; controller result unread
Target maturity: authenticated read-only stopped-disk archive and immutable R9 causal classification
Required frozen inputs: stopped VM/disk `nhm2-h2-p8j-r9-c2d-32-20260831`, R9 proposal `f652f5af...109a`, controller `4b8f5722...76e6`, Cloud Shell archive `ffe56577...f665`, and frozen P8J result-only auditor
Required evidence: stopped original, untouched source disk, derivative snapshot/clone identities, read-only guest device and mount, deterministic rescue archive, local hash agreement, independent audit, and stopped helper
Stop/fail criteria: original restart/mutation, ambiguous partition/filesystem, writable clone/device/mount, evidence mutation, Docker/numerical start, retry/retune, candidate ingress, budget breach, or authority promotion
Explicit non-goals: repairing or repeating R9, creating another scientific VM, frozen-candidate evaluation, positive sampling, Rust/G3/SI/metric/lane work, deletion, or authority promotion
Downstream gate unlocked: authenticated P8J result-only classification and the smallest separately versioned evidence-selected lead; no execution authority

# H2-P8J-R10 stopped-disk rescue proposal

Status: **FROZEN PREPARATION / NO RECOVERY RESOURCE CREATED**.

Frozen local identities:

- rescue procedure: 2,674 bytes, SHA-256
  `387637b21e78971daa25011bd19d73275fb99ae873cea59e381683ee74bf85cf`;
- Cloud Shell rescue orchestrator: 9,017 bytes, SHA-256
  `a7a824fedee5cf32db43b37608b5e71a282565373f4dca83239259dda51f2c4d`;
- exact 41-command staging ledger: SHA-256
  `30ae0c68a047fe2f7dd891dbb06b0abbf27a82c76c9cc5af6f9ceb28d2611f35`,
  with 38 ordered chunks and definition audit **12/12 PASS**;
- independent proposal audit source: 4,826 bytes, SHA-256
  `969d04d7de8ebff3342e86e10752c1a4e2554bc51d11d42c1b62d75519b0f9f5`;
- independent proposal audit: **14/14 PASS**.

The proposal raw SHA-256 is reported externally after these frozen child
identities are present; it is not self-embedded.

This packet changes evidence transport and receipt semantics only. It changes no
mathematical semantics, scientific runtime authority, candidate identity or
physical claim.

## Authenticated R9 boundary

The locally preserved 9,037-byte Cloud Shell archive hashes to
`ffe56577eab1b7a6b0b51964d8e33e5b79ae3bfe7d7a628656d8f0095049f665`.
It proves `R9_PREEXECUTION_PASS`, exact allocation in `us-east1-c`, successful
SCP, Docker installation, outer exit `3`, and fail-stop of the exact VM. The
remote setup emitted no `R9_REMOTE_CONTROLLER_ACTIVE` marker. Because exit `3`
is observed only after the systemd handoff and five-second active check, the
controller may have created terminal evidence before becoming inactive. No
scientific result is inferred until that stopped-disk evidence is recovered.

## Frozen recovery resources

- project `dark-stratum-455714-h4`, original zone `us-east1-c`;
- standard snapshot `nhm2-h2-p8j-r9-evidence-snapshot-20260901` from the
  retained 30 GB `pd-standard` boot disk;
- 30 GB `pd-standard` snapshot-derived disk
  `nhm2-h2-p8j-r9-evidence-clone-20260901`;
- temporary on-demand `e2-small` helper
  `nhm2-h2-p8j-r10-rescue-e2-small-20260901` in `us-east1-c`;
- exact image `projects/debian-cloud/global/images/debian-12-bookworm-v20260817`
  and 10 GB `pd-standard` helper boot disk;
- helper boot before clone attachment; clone attached as device
  `nhm2-h2-p8j-r9-evidence-clone` in Compute Engine `READ_ONLY` mode;
- 3,600-second helper ceiling and `$0.50` combined compute/prorated-storage
  ceiling through 24 hours after creation.

All derivative names must be absent. First failure is terminal; no resource
substitution or second creation attempt is permitted.

## Frozen read-only procedure

The exact procedure is
`tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8j_r10_stopped_disk_rescue_v1.sh`.
It requires a read-only guest block device, exactly one supported filesystem,
and mounts only ext4 with `ro,noload` or xfs with `ro,norecovery`. It reads only
the existing P8J evidence/export, three Docker logs, controller source identity,
persistent controller journal when present and bounded filesystem metadata.
It creates one deterministic archive on the helper boot disk, which must be
hash-verified through Cloud Shell and in an initially absent local capture.
The clone is unmounted and the helper stopped. The original VM is never started.

The exact Cloud Shell chronology is frozen in
`h2_p8j_r10_cloudshell_rescue_orchestrator_v1.sh`. Its only resource-creating
operations are the named snapshot, helper and clone; it performs one read-only
attachment, one rescue execution and one archive retrieval. Atomic staging is
bound by the 41-command ledger before the orchestrator is invoked once.

The snapshot, clone, stopped helper, original VM/disk and evidence remain
retained pending a separate cleanup decision. This packet authorizes no deletion.

## Result boundary

Only the frozen P8J result auditor may classify recovered terminal evidence.
The rescue itself authorizes no numerical execution, rerun, candidate
evaluation, positive sampling, retuning, handler/root creation, Rust,
G3/SI/metric/lane work, or candidate, proof, geometry/state, lane, lamp,
physical, propulsion or transport authority promotion.
