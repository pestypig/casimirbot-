# NHM2 spherical boson-star v2 G2B-B4-R2 integrated four-grid successor

Program gate: G2B — replacement classical proof attempt  
Workstream: authenticated classical branch closure  
Capability or component: fresh-output four-grid execution bound to corrected initializer  
Current maturity: preregistered additive successor to immutable B4  
Target maturity: authenticated first-failure receipt or bounded four-grid `PASS`  
Required frozen inputs: B4-R1 receipt, unchanged B4 runner, branch policy, solver/evaluator sources and admitted Linux runtime  
Required evidence: sealed additive wrapper, host/image tests, one exclusive invocation, per-level/pair chronology and independent terminal audit  
Stop/fail criteria: first source/runtime/initializer/grid/solve/pair/persistence mismatch; no retry or retune  
Explicit non-goals: changing immutable B4, thresholds, grids, solver algorithms, proof duties, candidate admission, lane/lamp/physical/propulsion/transport authority  
Downstream gate unlocked: vacuum-continuation execution packet only after exact four-level/three-pair `PASS`

## Authorized delta

This successor changes exactly the parent bindings needed to consume the audited B4-R1 initializer. It does not copy or edit the immutable B4 runner. An additive wrapper must import that byte-pinned runner and replace only:

- packet, test, checkpoint, and wrapper identity bindings;
- initializer root and its persistence receipt binding;
- scalar payload raw SHA-256 from `da88f738...` to `47f2858a...`;
- output root from `g2b-b4-four-grid-v1` to `g2b-b4-r2-four-grid-v1`;
- execution token name/value;
- successor receipt validation needed for B4-R1's authority-neutral schema.

All other five payload bindings, source hashes, runtime checks, MPFR/fenv policy, amplitude schedule, node counts `64/96/128/256`, independent full-solve rule, Newton/Armijo policy, convergence thresholds, pair ordering, output schemas, first-failure chronology, and authority locks remain those of the immutable B4 implementation.

The wrapper must bind both its own bytes and the immutable B4 runner bytes transitively. It may not import or invoke M5 or either B1 materializer. It may call the unchanged B4 execution spine only after independently validating B4-R1 receipt self hash `15c73b1e1ad1583dddf85f2276f661b3016704b113d02a26a995a49863d7e682`, raw receipt hash `fb7b5a8e344289756f5c622994bb6d53e01187236322eac6c0559319e4c06590`, exact seven-file inventory, corrected scalar hash, five unchanged payload hashes, `fourGridExecutionAuthorized=false`, `successorPacketPreparationUnlocked=true`, and every authority lock false.

## One-shot rule

The sole output root is:

```text
artifacts/nhm2-spherical-boson-star-v2-g2/g2b-b4-r2-four-grid-v1/
```

It must be absent before execution and must never be deleted, reused, or retried by this packet. The admitted image remains `sha256:715f6ea4674ace8cd1758954b09fda66e7f4ca9373a28af7a796fe1f9926dfd1`, offline. A normal mathematical failure is terminal evidence, not authority to diagnose during the invocation.

If any level fails, later levels and every pair requiring them remain unexecuted. If any adjacent pair fails, later proof duties remain locked. Only an exact terminal status with four completed levels and all three adjacent comparisons passing may unlock preparation of the separately sealed vacuum-continuation duty.

Candidate admission, vacuum connection, no-fold, positivity, boundary proof, joint geometry/state, 68-file execution, replay, pair agreement, Theory Graph lamp, physical viability, propulsion, and transport authority remain false regardless of this result.
