Program gate: G2H-E-S4-R1 — mini-boson-star proof-definition completeness review
Workstream: authenticated classical and quantum control branch
Capability or component: executable proof semantics required before source/runtime-disjoint implementation
Current maturity: selected/preregistered scientific member with proof duties, but under-specified proof ABI
Target maturity: additive versioned exact-definition contract independently reviewable before any proof implementation
Required frozen inputs: R2 protocol `f2fec60d...a6cb2`, matrix `7f7b7ac8...10d03`, contract `041c406c...ed12a`, R1 `e86dcd10...ec0a`, and immutable Tolman result `4248b29a...392d7`
Required evidence: mechanical completeness audit, gap classification, immutable R2 binding, absent candidate roots, no evaluation, and current math/WARP/Casimir PASS at closure
Stop/fail criteria: modifying R2, inventing a scientific choice inside implementation, importing an unsealed legacy ABI, evaluating the member, creating a candidate root, authorization, retuning, or authority promotion
Explicit non-goals: proof-program implementation, candidate execution, Rust scientific execution, G3, SI/metric lanes, lamp, physical viability, propulsion, or transport
Downstream gate unlocked: resumed G2H-E-S4 implementation only after an additive exact-definition contract closes every hard gap

# G2H-E-S4-R1 definition-completeness review

## Trigger and disposition

The first ordered S4 definition audit stops implementation. The immutable R2
contract uniquely selects the ordinary nodeless mini-boson-star family member,
equations, broad proof duties, arithmetic ceilings, grids, failure order and
authority locks. It does not uniquely define all numerical operators, state
encodings and observables required for two independent proof programs to
produce reproducible, byte-comparable evidence.

This is the exact self-correction path required by the S4 gate rule: preserve
R2 byte-for-byte and return to a separately versioned definition review.

Disposition:

`RETURN_TO_VERSIONED_DEFINITION_REVIEW_BEFORE_IMPLEMENTATION`

This is a safe scientific result, not a candidate FAIL. No candidate value was
computed, neither future root exists, and no execution or proof authority is
created.

## Hard definition gaps

| Ordinal | Exact definition still required | Why a prose duty is insufficient |
|---:|---|---|
| 1 | Unknown vector, field transforms, coefficient order and canonical codec | Independent programs cannot decode the same state uniquely. |
| 2 | One exact analytic seed for every frozen grid | R2 references a preregistered seed but does not specify it. |
| 3 | Node formulas, core/tail maps, component weights and every frozen norm | Grid sizes and a basis label do not define cross-grid defects. |
| 4 | Nonlinear operator, derivative, approximate inverse and validation map | Residual and correction thresholds do not specify how bounds are assembled. |
| 5 | Continuation tubes, `Y,Z0,Z1,Z2`, radii and strict predicates | Counts alone do not determine a radii-polynomial proof. |
| 6 | Overlap, orientation, kernel, transversality and endpoint maps | The no-fold/continuous-cover duty has no executable predicate. |
| 7 | Geometrized mass convention and exact ADM, Komar and energy maps | `b^-2=1-2m/r` plus `mbar_P=1` does not bind all three reported observables. |
| 8 | Radial perturbation operator, gauge reduction, domain, boundary conditions and eigenvalue normalization | “Positive fundamental mode” is a conclusion, not the frozen spectral problem. |
| 9 | Classical manifests, records, first-failure payloads and hash domains | Immutable evidence and replay require a canonical wire ABI. |
| 10 | Quantum mode/observable ABI, admissible smearing inventory and RSET/noise algorithms | The mathematical state definition does not select finite proof products or their comparison rules. |

The older vacuum-continuation ABI cannot fill these gaps: it identifies its own
scientific choices as null, reports `exactScientificDefinitionsComplete=false`,
and is not a dependency of the R2 contract. The earlier diagnostic isotropic
branch code also uses different variables, gauge and source parameter, so it
cannot silently become the selected areal-coordinate proof evaluator.

## Primary-source recovery map

The first source pass narrows the repair rather than authorizing an
implementation:

| Gap | Primary-source finding | R1 disposition |
|---|---|---|
| Mass normalization | Saffin--Xie equations (24)--(25) define `b^-2=1-2G*M_phys/r` and `M_phys=int 4*pi*r^2*rho dr`; with frozen `mbar_P=1`, `G=1/(8*pi)`. | Additively define the R2 symbol `m` as geometrized mass `G*M_phys`, so `m'=r^2*rho/2`; compare geometrized ADM, Komar and integrated-energy intervals. This is a source-derived convention repair, not a new solve. |
| Static state variables | Saffin--Xie equations (16)--(23) directly use the frozen `b,s,sigma,omega` gauge and `shat_0=s(0)*omega`. | Suitable scientific source for background variables, boundary normalization and origin parameter; it still does not supply a validated coefficient ABI. |
| Radial stability | Kain equations (28)--(39) give a coupled pulsation problem and boundary conditions; Alcubierre et al. equations (20)--(33) give a transformed two-channel formally self-adjoint operator and domain behavior. | Source support exists, but R1 must select one equivalent formulation, derive the exact `ell=0` map into frozen `b,s,sigma`, and prove equivalence before sealing. A turning-point comparator alone is insufficient. |
| Quantum modes | Saffin--Xie equations (42)--(44) supply spherical mode separation and normalization in the same metric gauge. | Comparator/source input only. Its finite spherical wall, Pauli--Villars ghosts, modal extrapolation and smoothing are explicitly forbidden by R2 and cannot be imported. |
| Hadamard state | Junker proves Hadamard behavior for ground/KMS states on a class of static spacetimes. | Supports the theorem-level state duty after the geometry hypotheses are proved; does not define a finite numerical proof product. |
| Mean RSET | Décanini--Folacci provide the local Hadamard renormalization framework and ambiguity structure in four dimensions. | Supports the frozen scheme, but the parametrix coefficient recursion, subtraction order, remainder bounds and output inventory remain to be frozen. |
| Connected noise | Phillips--Hu derive the point-separated noise bi-tensor framework for scalar fields. | Supports separated-point structure; R1 must still preregister compact test tensors, smearing quadrature, positivity witnesses and comparison records. |
| Validated continuation | No selected primary source in R2 defines the required 1024-cell interval/radii construction for this areal-coordinate EKG problem. The repository's older ABI deliberately leaves these choices null. | New derivation and independent mathematical review required. Counts and thresholds remain frozen; implementation cannot begin from a method label. |

Primary sources used for this mapping:

- [Saffin and Xie, *Quantum fields in boson star spacetime*](https://arxiv.org/abs/2601.05129)
- [Kain, *Boson stars and their radial oscillations*](https://arxiv.org/abs/2106.01740)
- [Alcubierre et al., radial stability of ell-boson stars](https://arxiv.org/abs/2103.15012)
- [Junker, static ground/KMS Hadamard states](https://arxiv.org/abs/hep-th/9507097)
- [Décanini and Folacci, Hadamard RSET](https://arxiv.org/abs/gr-qc/0512118)
- [Phillips and Hu, noise kernel](https://arxiv.org/abs/gr-qc/0010019)

The key planning result is that the mass convention and source-gauge map are
recoverable now; the stability formulation required an explicit equivalence
derivation; and the finite quantum products required a complete half-line
spectral-measure construction rather than an assumed finite bound-mode list.

## Additive repair boundary

The next contract version must classify each addition before sealing:

- **representational:** canonical number/interval codec, key order, state and
  coefficient packing, hash domains, record routes and failure payloads;
- **numerical-proof semantic:** maps, norms, seed construction, interval
  operators, radii bounds, continuation tubes and spectral discretization;
- **scientific:** mass convention, stability operator/domain, ground-state mode
  construction, renormalized observables and smearing inventory.

Representational details may be frozen mechanically. Numerical-proof and
scientific choices require derivation and independent source/theory review;
they may not be improvised while writing C++ or Rust.

## Mechanical audit

Run:

```powershell
python scripts/nhm2_g2h_e_s4_definition_completeness_audit.py
```

The expected successful audit disposition is the fail-closed return to
versioned definition review. A zero exit code means the frozen identities,
locks and absent roots were verified and the incompleteness was reproduced; it
does not mean the proof ABI or candidate passed.

All candidate, geometry/state, SI, metric, lane, replay, lamp, physical,
propulsion and transport authority remains false.

## Additive endpoint-definition progress

The versioned SP endpoint contract now fixes the exact normalized
Schrodinger--Poisson differential and integral operators, their Frechet
derivative, the scaling zero mode, the member-row border and the strict
radii-polynomial predicates.  The member row removes the unbordered scaling
mode because its derivative along that mode is exactly
`2*(nu0-Vc)>0`; together with `eta=lambda/[5*(nu0-Vc)]`, this fixes the
continuation orientation to `d(shat_0)/d(lambda)=1/5` without a positive
parameter sample.

Run:

```powershell
python scripts/nhm2_g2h_e_s4_sp_endpoint_definition_audit.py
```

The current definition-only replay passes 24/24 symbolic and manufactured
checks.  It does not execute or prove the endpoint.  The corrected coefficient
realization, component weights, approximate inverse, numerical `Y/Z` bounds,
endpoint ball, first-cell overlap and independent definition audit remain open.

The corrected v2 state/grid draft also resolves the four defects that
invalidated v1 at the representation level.  Each frozen `N` is now a node
count per patch; even core variables are functions of `r^2` and the physical
scalar derivative is reconstructed as an odd field; DCT-I output is converted
to canonical Chebyshev coefficients before prolongation; and the exact
formal-germ-plus-flat tail functions use the polynomial weight `(1+k)^8`
instead of an inadmissible exponential analyticity weight.  The positive and
vacuum charts have separately square counts `8*N+2` and `8*N+1`, with the
vacuum mass limit defined as a dependent integral observable.  The internal
definition replay passes 63/63, including exact flat-index coverage, the
separate scaled vacuum interface, and coefficient derivative-loss constants:

```powershell
python scripts/nhm2_g2h_e_s4_state_grid_v2_definition_audit.py
```

This is not yet an independent theory audit or a sealed packing.  The
derivative-loss constants, flat-remainder envelopes and approximate-inverse
block class are now separately defined and internally replayed, while each
producer's deterministic construction algorithm remains a preexecution binding.

A source-language-disjoint Node/BigInt replay additionally passes 52/52.  It
checks both square layouts, every flat-index cover, canonical prolongation,
polynomial weights, chart-scaling exponents, strict synthetic radii behavior,
hash bindings and absent roots:

```powershell
node scripts/nhm2_g2h_e_s4_r1_independent_definition_replay.mjs
```

That replay is an independent fixture implementation, not an independent
theory acceptance and not a scientific proof.

The next three additive contracts now define the previously prose-only proof
machinery without evaluating it:

- the flat-carrier ABI fixes 78 mixed derivative envelopes, endpoint flatness,
  formal-germ subtraction and the weighted infinite-coefficient bound; its
  symbolic/manufactured audit passes 27/27;
- the validated-inverse ABI fixes exact dyadic finite matrices, infinite-tail
  action, bounded-bijection evidence, pre-bound chronology and strict
  `Y,Z0,Z1,Z2`/73-radius decisions; its audit passes 25/25;
- the continuation ABI fixes all 1,024 cells, second-order left predictors,
  symmetric strict containment, the scaled chart handoff, exact orientation,
  no-fold semantics, first failure and terminal N=256 containment; its audit
  passes 35/35 with zero scientific cells.

These are still internal definition fixtures.  Producer-specific construction
algorithms, independent theory/cross-language replay of these newest
contracts, and every scientific bound remain open.

## Additive mass and radial-stability progress

The common-unit mass ABI now fixes `m=G*M_phys`, the Misner--Sharp/ADM boundary
map, the coordinate-energy integral selected by the exact mass equation, and
both Komar boundary and volume forms.  It expressly prevents the proper-volume
matter diagnostic from being substituted for the R2-C09 equality mass.  Four
mass balls must have one common intersection, and all three identity residuals
must contain zero within the frozen width.  The manufactured definition audit
passes 16/16:

```powershell
python scripts/nhm2_g2h_e_s4_mass_observable_definition_audit.py
```

The radial-stability ABI now specializes the Alcubierre et al. two-channel
operator exactly to the frozen `ell=0` normalization and chooses its half-line
Friedrichs realization.  It does not import the source paper's numerical
shooting result or its unproved nodal hypotheses.  Instead, it requires two
independent proof-carrying inequalities:

- a global symmetric matrix-Riccati factorization proves `H_T>=L*I` with an
  exact dyadic `L>0`, excluding zero and every lower spectral value;
- a compactly supported co-rotating trial vector proves a Rayleigh upper bound
  `U` strictly below the essential threshold `(1-omega)^2`.

The min-max principle then forces a discrete fundamental radial eigenvalue in
`[L,U]`.  Separately, strict `dM/dlambda>0` on all 1,024 continuation cells
establishes the pre-first-turning-side comparator without giving it eigenvalue
authority.  Exact origin rates, tail limits, relative-compactness duties,
record fields and first-failure precedence are all frozen.  The internal
symbolic/manufactured audit passes 37/37:

```powershell
python scripts/nhm2_g2h_e_s4_radial_stability_definition_audit.py
```

This result evaluates no selected background or radial eigenvalue.  The
contract remains unsealed pending independent theory review and producer
builder bindings.

## Additive quantum finite-product progress

The quantum ABI now fixes the distinct real `m=1`, `xi=0` Klein--Gordon
operator on `L2(Sigma,alpha^-1 dvol_h)`, its positive Friedrichs form and the
exact spherical radial measure `b^2*s*r^2 dr`.  It defines the static ground
state through the full projection-valued spectral measure.  In particular, it
does not assume a finite set of bound poles: atoms accumulating at the massive
threshold are retained by the Weyl--Titchmarsh measure, and numerical products
use certified negative-axis resolvents without a finite wall.

The same `G_plus` supplies the four-dimensional Hadamard subtraction, the
minimal-coupling conserved RSET with `ell_H=1` and zero ambiguity vector, and
the connected noise.  Sixty-four compact spacetime bumps, each separated from
the origin and infinity, are crossed with four spherical tensor channels.  The
finite products are therefore a `64 x 4` mean array and a `256 x 256` noise
matrix.  Noise positivity is a two-particle Fock Gram identity; unsmeared
coincidence, eigenvalue clipping and post-hoc zero fill are forbidden.  Every
discarded angular, spectral, radial and subtraction sector requires a directed
tail enclosure.

Run:

```powershell
python scripts/nhm2_g2h_e_s4_quantum_definition_audit.py
node scripts/nhm2_g2h_e_s4_r1_independent_definition_replay.mjs
```

The internal quantum audit passes 49/49.  The separate Node/BigInt replay now
also binds the quantum contract and its finite-product invariants.  Neither is
an independent theory acceptance or a selected-member calculation.  All ten
definition gaps now have additive drafts, but every gap remains open until the
combined independent theory/ABI audit accepts the exact definitions and the
producer-specific construction builders are frozen.

## Additive disjoint-builder progress

The last implementation-time choice surface is now a versioned builder ABI.
It fixes 512-bit arithmetic, a common `2^-448` exact-certificate projection,
tie rules, persistence order and terminal budget exhaustion.  The primary lane
uses complete-pivot LU, Chebyshev Riccati certificates and Arb radial
collocation.  The independent lane uses Householder QR, Bernstein/Taylor
certificates and pure-Rust limb balls.  Continuation, stability, angular,
spectral, Hadamard and noise selectors are all finite and non-null.  A failed
budget cannot switch algorithms, raise precision, subdivide or extend itself.

Run:

```powershell
python scripts/nhm2_g2h_e_s4_builder_definition_audit.py
```

The exact/synthetic audit passes 40/40.  No inverse, stability certificate or
quantum product was constructed from the selected member.  All ten inventory
gaps are now draft-complete pending the combined independent theory/ABI audit;
none is promoted to accepted merely by this internal fixture result.

## Closure and handoff

The combined definition seal is
`728d8c9a807d27356a6d9f33e897feb73331abb12e6a76435dbce099d9c025ca`.
Its audit rehashes 15 bound definition artifacts and replays 18 exact,
manufactured and cross-language suites; it passes 42/42.  Current-tree gates
then pass at math 318, WARP 179/179 and Casimir run 2473 `PASS/GREEN` with
certificate integrity true.  This closes only exact-definition completeness
and returns the active gate to G2H-E-S4 fixture implementation.  It does not
prove or execute the selected star, authorize either lane, create a future
root, or promote geometry/state, lane, lamp or physical authority.
