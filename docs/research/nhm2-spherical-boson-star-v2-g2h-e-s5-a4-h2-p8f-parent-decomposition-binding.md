Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: S5-A/A4 candidate-neutral H2 continuation diagnosis
Capability or component: H2-P8F read-only next-selector decomposition binding
Current maturity: P8E selector decomposition fixture 13/13 twice and independent audit 52/52 PASS
Target maturity: exact parent-input extraction with no parent mutation, bounded fixture replay and independently audited representative-run definition
Required frozen inputs: P8E identities, P8C terminal source chronology, H2/P2 16-thread binding, source-prefix digests, P=65,536, degree 3, ordered jet 9, 512-bit precision and `2^-180` width rail
Required evidence: ordinary initialization equality, source-prefix authentication, absent parent mutation, one candidate-only selector call, exact P8E reconstruction, deterministic serialization, zero candidate activity and independent audit
Stop/fail criteria: parent model/publication/terminal-state mutation, selector schedule execution, more than one representative candidate call, target drift, output/result drift, retry/retune, frozen-candidate ingress, root/token creation or authority promotion
Explicit non-goals: rerunning P8C, invoking first-passing selection, changing the 17-member schedule or threshold, evaluating the frozen member, positive sampling, accepting H2, creating scientific output, linking a handler, beginning G3/SI/metric/lane work, or promoting any candidate, proof, geometry/state, lamp, physical, propulsion or transport authority
Downstream gate unlocked: one hash-bound candidate-neutral P=65,536 representative decomposition execution under the standing charter; no frozen-candidate execution authority

# H2-P8F parent decomposition binding

Status date: August 30, 2026.

Status: **IMPLEMENTED / FIXTURE AND INDEPENDENT AUDIT PASS / REPRESENTATIVE RUN NOT STARTED**.

P8E proves that the selector can decompose an already constructed input without
changing its ordinary result. P8F binds that surface to the exact next H2
source ordinal while leaving the H2 parent untouched.

## Read-only parent contract

The additive H2 entrypoint must:

1. require an initialized, nonterminal H2 context;
2. validate the complete scalar-ledger inventory and exact identities;
3. rehash every already admitted scalar-prefix model against the stored digest;
4. require exactly one new source ordinal beyond the existing H2 prefix;
5. construct the same `B`/`V` selector views, target interval/order and
   `B(0)` 13-jet inventory used by ordinary `extend_impl`;
6. call exactly one `evaluate_prepared_candidate_decomposition` for the named
   dyadic panel count, degree and ordered jet; and
7. return the selector output/result and bounded P8E observation without
   translating, publishing, appending a model, setting terminal failure, or
   changing the H2 context.

The ordinary `initialize`, `extend`, `initialize_diagnostic` and
`extend_diagnostic` entrypoints remain unchanged. The new surface is a
read-only diagnostic, not a parent execution and not a selector decision.

## Representative target

P8D freezes the sole representative target before implementation:

```text
panel_count = 65,536
target_degree = 3
target_jet = second_jet(1,2) = 9
thread_count = 16
```

This is the terminal fixed-schedule member with the smallest observed P8C
first-failure ratio, `1.0258985158...`. P8F may not change the target after
seeing decomposition output. A fixture may use `panel_count=2` solely to prove
the parent binding and ordinary invariants; that fixture cannot choose or
predict the representative result.

## Decision table

The representative observation will support only this causal split:

| Observation | Permitted inference |
| --- | --- |
| final radius is materially explained by summed elementary radii | the current analytic constituent enclosures, not outer selector rounding, dominate |
| final radius materially exceeds summed elementary radii | outer ordered accumulation/rounding is a concrete optimization lead |
| one ordered slot dominates | inspect that exact bivariate factor pairing and its upstream model/source-hull bounds |
| boundary panel dominates | inspect the once-applied `F(t)G(0)` boundary contribution |
| nonboundary panels dominate | inspect distributed Volterra panel enclosures and contraction behavior |

No row authorizes threshold relaxation or a scientific retry. Any later repair
must be independently derived, versioned and equivalence-tested before a new
irreversible candidate decision.

P8C remains immutable numerical failure. All candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority
remains false.

## Implementation closure

The read-only parent binding is implemented under these exact identities:

| Member | SHA-256 |
| --- | --- |
| H2 header | `95b9edfe39367b882dc96ff1bced3c74fa0bb75263bc5c519d1bf1df9cf1c838` |
| H2 source | `d00603a418ed0507248f9019b35887a8e453c09d0336c9e1464b0bcb2f8f224c` |
| parent-binding fixture | `0e85c5b0d530ded10fadff279e3bff83fd76006e986dc123bb443269da61fc1b` |
| pinned fixture Dockerfile | `8f353a4a99933ad592090726c6a60023370800f7c418fd0eebaca1d2b81f2285` |
| fixture executable | `c7a6f1e5c8f8b636407517561fe3db22ac0f5eaef86bee594b89e73e9254fcf8` |

Two hardened fixture executions pass 11/11 with byte-identical reports. The
H2 publication is exactly unchanged before, between and after two diagnostic
calls; both calls produce identical selector Result, Arb Output and bounded
decomposition observation. Each call visits one two-panel candidate, performs
zero width checks and creates no candidate activity or authority.

The independent source/build/runtime audit passes **47/47**. Its immutable
receipt hashes to
`2fb073ba1dc27eda0e4eef171736f89a5f1f7479939aea3678e8cb7cfbac5218`.
The representative executable has been built but not invoked; its SHA-256 is
`12aa0158d56340a7fb7a545c4d2a5bc918c76148ba37548de2988cb968790d20`.

The cloud resource inventory then found the standing charter's 60 GB new-
storage ceiling fully occupied by the retained R13/R14/R15/R17 helper lineage.
No P8F VM or disk was created. A cloud launch requires a separately confirmed
deletion of fully retained-equivalent stale helper resources or a revised
storage ceiling. The candidate-neutral local pinned runtime remains eligible
without either action.
