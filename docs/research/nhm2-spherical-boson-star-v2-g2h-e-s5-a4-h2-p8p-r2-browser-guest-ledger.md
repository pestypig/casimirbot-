Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R2 exact browser-SSH guest execution ledger
Current maturity: R2 proposal independently cleared; no R2 resource or process created
Target maturity: one byte-bound, fail-fast guest sequence with no transport improvisation
Required frozen inputs: R2 proposal `0068d40d...bde483e`, exact base/overlay archives, source manifest, controller and three initially absent roots
Required evidence: syntax-valid exact ledger, identity/absence guards, exact extraction and manifest replay, narrow Docker admission and one controller invocation
Stop/fail criteria: ledger drift, wrong guest/archive/root, package broadening, second controller invocation, fallback, candidate ingress or authority change
Explicit non-goals: VM creation, upload, Docker installation, numerical execution, candidate evaluation, retuning, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: exact R2 execution after separate authorization and action-time Docker confirmation only

# H2-P8P-R2 browser guest ledger

Status date: September 1, 2026.

Status: **FROZEN ADDITIVE TRANSPORT LEDGER / NOT EXECUTED**.

## Binding

The governing R2 capacity successor remains raw SHA-256
`0068d40da22fa8f3e9b8dc8a2924ae08c18fcf7c0f726bde8a8ab40f9bde483e`.
This ledger does not change the proposal. It freezes the exact guest-side
sequence so browser terminal execution cannot improvise archive checks,
extraction order, manifest replay, Docker admission or controller invocation.

The source ledger is:

- path
  `tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r2_browser_guest_sequence_v1.sh`;
- exactly 2,845 bytes and 79 lines;
- SHA-256
  `d7f98f542ee776eec9f6d24173031f2b671bb54e59fd74d44d5b637990f536a6`;
- local syntax check: exact Git Bash `bash -n`, exit `0`.

The file is a local command ledger, not a third upload. R2 still permits only
the two frozen tar archives. After those two browser uploads and the required
action-time confirmation for possible `docker.io` installation, the ledger
bytes may be entered once into the authenticated guest shell through a single
literal `bash -s` heredoc envelope. The source bytes between the delimiter
lines must equal the file above exactly. No line may be edited, omitted,
duplicated or appended.

## Fail-fast sequence

The ledger:

1. binds all three output/source roots and both input archives to exact paths;
2. requires guest identity `pestypig`;
3. requires both archives to be regular non-symlinks with exact bytes and
   SHA-256 identities;
4. requires source, evidence and export paths all absent before mutation;
5. installs only Debian `docker.io` if the `docker` command is absent, then
   requires the Docker service and client/daemon handshake;
6. creates only the source root and extracts base first, overlay second;
7. requires the 2,820-byte source manifest at exact SHA-256
   `c6ee88481ae7842b176d4c8a2001601c38cc317132c446a086c92b75ddef5aa0`;
8. independently replays all 11 manifest entry byte counts and hashes, the
   candidate-neutral flag, P=1024 width, 32-thread count and all-false authority
   object;
9. requires the 4,498-byte controller at SHA-256
   `5af4b629336e166d07a277ae59b0f9776ac9e86b728e762030f27237ed1c8f5b`
   and passes `bash -n`; and
10. invokes exactly one `sudo bash "$CONTROLLER"` as the terminal statement.

The controller remains responsible for the offline build, executable identity,
sole P=1024 process, timeout, evidence export and shutdown. A dropped browser
session before controller completion is partial transport evidence; it is not
permission to invoke the ledger or controller again.

## Confirmation boundary

This packet creates no resource and authorizes no execution. The existing R2
authorization decision remains required before VM creation. Browser policy
also requires a separate action-time confirmation immediately before entering
the ledger because it may install Debian `docker.io` on the exact R2 VM.

No candidate, proof, geometry/state, lane, lamp, physical, propulsion or
transport authority is promoted.
