Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R29 no-space isolated host-key receipt successor
Current maturity: native SCP twice proven; spaced receipt path twice failed before SSH; VM stopped; calibration unexecuted
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R29 record
Required frozen inputs: R28 result, stopped retained VM/disk and two partial archives, exact native OpenSSH/keypair, unchanged outer archive, ledger, executable and auditor
Required evidence: exact R28-to-R29 diff, local OpenSSH option parse, one restart, new-name SCP, no-space receipt plus evidence copy, one SSH handoff, serial evidence, stop and P8Q audit
Stop/fail criteria: first identity, start, transfer, receipt, guest, build, execution, recovery or audit failure terminal; no retry or fallback
Explicit non-goals: use/overwrite/delete R27/R28 partials, new VM, metadata/IAM/user mutation, retuning, candidate evaluation, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R29 no-space host-key receipt successor

Status date: September 3, 2026.

Status: **FROZEN PREEXECUTION PROPOSAL / RETAINED VM REMAINS TERMINATED**.

Change classification: no-space local receipt placement and fresh
evidence/remote identities only. Scientific semantics remain unchanged.

## Evidence-selected correction

The immutable R28 result is SHA-256
`0eecabbb9c2d763e0e9c082b23101d47fa1a87119791d053640d223029a41fb9`
and independently audits 16/16. Both R27 and R28 proved successful native SCP,
but OpenSSH did not create the isolated receipt beneath the space-containing
repository path. Both stopped before SSH and numerical work.

R29 derives from R28 with only these changes:

- new R29 evidence, remote archive, guest stage, service and marker identities;
- one initially absent no-space receipt path:
  `C:\Users\dan\AppData\Local\NHM2\p8p-r29-known-hosts`;
- exact forward form:
  `C:/Users/dan/AppData/Local/NHM2/p8p-r29-known-hosts`;
- after creation, hash the receipt and copy it once into the R29 evidence root,
  requiring exact hash equality before SSH.

Local `ssh -G` parsing returns exactly the frozen forward-form
`userknownhostsfile` value. The R27 and R28 remote archives remain retained,
unused and unmodified. R29 uses only the new path
`/home/dan/h2-p8p-r29-upload-v1.tar`.

## Frozen identities and procedure

| Item | Frozen value |
| --- | --- |
| R29 controller | `tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r29_no_space_hostkey_controller_v1.ps1`; 14,891 bytes; SHA-256 `e7163c720f6850fae9dd4a42499e0ee9f9d880823fd575d80384dec27de88e8e`; parser PASS |
| retained VM | `nhm2-h2-p8p-r26-c2d-32-20260903`; ID `4290604153416687194`; `us-east1-c`; initially `TERMINATED` |
| machine/image/disk | unchanged `c2d-standard-32`; Debian `v20260817`; 30 GB `pd-standard` |
| native SSH/SCP and keypair | unchanged hash-bound R28 identities |
| outer archive | unchanged 236,640,768 bytes; SHA-256 `3c697fa3e238398d3b4c30a6c379fea8d6d545b2228d0c699dd66594a23670b5` |
| executable | unchanged SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718` |
| restart/runtime/cost | exactly one restart / 18,000 seconds / `$9.00` total ceiling |

R29 creates no cloud resource. It repeats the frozen retained-resource,
account, key and disk guards, restarts the exact VM once, waits 180 seconds and
performs exactly one native SCP as `dan` to the new R29 remote filename. Only
the no-space R29 receipt may accept the host key. It must exist as a regular
file, be hashed, copied once into the new immutable evidence root and match
exactly. One SSH handoff then uses `StrictHostKeyChecking=yes` against the same
receipt.

The guest requires preexisting `pestypig`, authenticates all four members and
stages only the unchanged base, overlay and ledger. The unchanged ledger may
conditionally install Debian `docker.io`, perform the offline build, verify the
executable and run exactly one candidate-neutral P=1024 calibration with 32
CPUs and a 14,400-second timeout. All outcomes are preserved; the exact VM is
stopped automatically or by the failure handler; only the serial evidence is
recovered and passed to the unchanged P8Q auditor.

First failure is terminal and consumes R29. No second restart, transfer,
receipt, SSH, ledger, build or numerical process is permitted. No new resource,
fallback, substitution, metadata/IAM/user mutation, alternate path/identity/
key/transport, retune, additional upload or evidence deletion is allowed.

## Scientific and authority boundary

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false. R29 cannot authorize P=65,536; it can only produce the frozen P8Q
resource-bounded yes/no decision.

## Exact authorization text

> I authorize exactly one H2-P8P-R29 no-space host-key-receipt retained-VM P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Restart only the existing stopped VM `nhm2-h2-p8p-r26-c2d-32-20260903`, instance ID `4290604153416687194`, in project `dark-stratum-455714-h4`, zone `us-east1-c`, under the frozen 18,000-second and `$9.00` ceilings. Execute only the 14,891-byte R29 controller SHA-256 `e7163c720f6850fae9dd4a42499e0ee9f9d880823fd575d80384dec27de88e8e`. Preserve and do not use, replace or delete the retained R27/R28 remote archives or evidence. Use only the unchanged hash-bound native OpenSSH executables, keypair, guest identity `dan` and scientific archive; create only the initially absent no-space R29 host-key receipt, hash it and copy it once into the new evidence root; perform exactly one SCP to the new R29 remote filename and one SSH handoff; require preexisting `pestypig`; execute one unchanged offline build and exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout; preserve every outcome, stop the exact VM, recover and independently audit serial evidence, and apply only the frozen P8Q rule. First failure is terminal. I do not authorize retry, a second restart, transfer, receipt, SSH, ledger, build or numerical process; a new resource; metadata, IAM or guest-user mutation; fallback, substitution, alternate path, identity, key, transport, zone, machine or disk; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or authority promotion.
