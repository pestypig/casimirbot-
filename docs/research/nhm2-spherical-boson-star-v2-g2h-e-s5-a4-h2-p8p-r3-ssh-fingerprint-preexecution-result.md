Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R3 deterministic chunk ingress / browser-SSH metadata-version preexecution result
Current maturity: terminal instance-metadata optimistic-lock failure; exact VM stopped; zero upload, guest command or numerical process
Target maturity: immutable fail-closed R3 evidence and one separately versioned fresh-metadata successor decision
Required frozen inputs: R3 proposal `b85d4db9...2d61e5c`, exact 15-part manifest, unchanged overlay, exact VM/image/machine/disk bindings and first-failure rule
Required evidence: local 16-file materialization, one exact VM creation, first SSH observation, zero-transfer chronology and terminal VM state
Stop/fail criteria: fingerprint mismatch is terminal; no Retry, Troubleshoot, upload, Docker installation, build, calibration or P8Q promotion
Explicit non-goals: scientific interpretation, candidate evaluation, retuning, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: separately frozen fresh-page metadata successor only; no calibration or R3 retry authority

# H2-P8P-R3 SSH-fingerprint preexecution result

Status date: September 1, 2026.

Status: **BLOCKED_PREEXECUTION_INSTANCE_METADATA_FINGERPRINT_CONFLICT / R3 EXHAUSTED**.

Change classification: receipt semantics only. Mathematical semantics and
runtime authority are unchanged.

## Authorized boundary

The operator authorized exactly one H2-P8P-R3 deterministic chunked
browser-ingress P=1024 turnaround calibration under proposal SHA-256
`b85d4db90cb42a232e64d6cc285fcc4da82ad25a04bb84271b5c85c4e2d61e5c`.
The operator then separately confirmed the billable Create action.

The authenticated resource was:

- project `dark-stratum-455714-h4`;
- VM `nhm2-h2-p8p-r3-c2d-32-20260901`;
- instance ID `637527339076077505`;
- zone `us-east1-d`;
- on-demand machine `c2d-standard-32` with 32 vCPUs and 128 GB memory;
- exact image
  `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`;
- 30 GB auto-delete `pd-standard` boot disk;
- no backup schedule and no Ops Agent;
- 18,000-second aggregate runtime ceiling and $9.00 total ceiling.

First failure was terminal. Browser Retry, troubleshooting as a retry, fallback,
a second upload batch, resource substitution, retuning, candidate evaluation
and authority promotion were not authorized.

## Observed chronology

1. The authorized local materializer ran exactly once against the initially
   absent R3 staging directory. It produced exactly fifteen ordered base parts
   plus the unchanged overlay and reported `P8P_R3_INGRESS_MATERIALIZED 16/16`.
2. A separate verification-only invocation authenticated the same sixteen
   regular files and reported `P8P_R3_INGRESS_VERIFIED 16/16`. The fifteen
   parts total 236,492,800 bytes and remain bound by manifest SHA-256
   `ec1c115461442e1ddba9ccc635aebdd722be5440f23e06c797406ce8cde6b52d`.
3. The exact VM name was absent before Create. The one exact creation request
   succeeded at September 1, 2026 9:17:34 PM America/New_York. The authenticated
   Compute Engine detail bound the instance ID, zone, image, machine, disk and
   standard provisioning listed above.
4. The first authenticated Compute Engine SSH-in-browser surface terminated
   with `Connection Failed` and
   `Supplied fingerprint does not match current metadata fingerprint.` It
   exposed `Retry` and `Troubleshoot` actions.
5. Neither action was taken. No file chooser or upload was invoked, no staged
   byte crossed the cloud boundary, and no guest command or ledger character
   was entered.
6. The VM was stopped under the frozen fail-stop and cost boundary. A refreshed
   authenticated Compute Engine list reported `Stopped. This instance is
   stopped` for the exact R3 VM in `us-east1-d`.

## Terminal inference

R3 passed deterministic local chunk materialization, exact resource allocation
and resource-identity checks. It failed while the browser-SSH surface attempted
an instance-metadata update before archive ingress. Google Compute Engine
defines this exact message as an optimistic-lock conflict: the request carried
an instance-metadata fingerprint older than the current metadata version. The
metadata fingerprint is a hash/version of the metadata contents; it is not an
SSH host-key fingerprint. This record therefore does not establish any guest
host-key mismatch and authorizes no metadata or key mutation.

The sixteen-file batch was not selected or transmitted. The guest ledger was
not entered. Debian `docker.io` was not installed. Reconstruction, manifest
replay, offline build, executable authentication, controller invocation, the
sole P=1024 process, serial evidence export and independent turnaround-result
audit did not run.

The preregistered result is therefore
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. This is infrastructure/preexecution
evidence, not a numerical result and not the P8Q yes/no decision about a full
P=65,536 observation. R3 is exhausted. A successor may separately freeze a
freshly loaded console-detail and one-attempt browser-SSH metadata transaction,
the first console resolution recommended by Google, while preserving this
failure; this record grants no R3 retry, restart or alternate transport.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.
