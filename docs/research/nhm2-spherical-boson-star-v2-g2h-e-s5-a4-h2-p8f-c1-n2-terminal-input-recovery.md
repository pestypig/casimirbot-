Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: S5-A/A4 candidate-neutral H2 continuation diagnosis
Capability or component: H2-P8F-C1 N2 terminal-input incident recovery
Current maturity: unintended same-specification VM `instances` stopped before setup, upload, build or numerical execution
Target maturity: delete only the inert accidental resource and create the exact previously approved N2 identity once
Required frozen inputs: N2 correction `be5696c...103b`, archive `c40fda6b...24640`, binary `14140897...1bad6`, controller `940ee74a...db8b2`, and unchanged 24-hour/$40 ceilings
Required evidence: accidental-resource chronology, zero scientific actions, exact deletion target, exact replacement identity, pre-entry line clear, rendered-command identity check, one creation, and terminal resource inventory
Stop/fail criteria: accidental resource not terminated, any non-boot data disk, any evidence on the accidental disk, command-render mismatch, replacement conflict, quota/capacity rejection, numerical start, retry, retune, evidence deletion or authority promotion
Explicit non-goals: reusing or renaming the accidental VM, deleting any evidence resource, changing scientific/runtime bindings, candidate evaluation, Rust/G3/SI/metric/lane work, or any authority promotion
Downstream gate unlocked: unchanged P8F-C1 environment setup on the exact approved VM; no scientific authority

# H2-P8F-C1 N2 terminal-input recovery

This packet changes receipt and cloud-resource identity only. It changes no
mathematical semantics, runtime executable, result receipt semantics or
scientific authority.

## Immutable incident

After Cloud Shell reconnected, an attempted sequential key dispatch timed out
after inserting a partial prefix. The subsequent terminal fill appended the
approved creation command to that prefix instead of replacing the shell line.
The shell therefore received a malformed command containing two `gcloud
compute instances create` argument sequences.

Google created one unintended VM named `instances`, then rejected the second
resource request on the global 32-vCPU quota. Read-only inventory and chronology
bind the unintended resource as:

- project `dark-stratum-455714-h4`, zone `us-central1-a`;
- instance name `instances`, ID `4056482909131436405`;
- machine `n2-standard-32`;
- creation `2026-08-31T02:55:38.250-07:00`;
- start `2026-08-31T02:55:53.909-07:00`;
- stop `2026-08-31T02:57:21.051-07:00`;
- current status `TERMINATED`;
- one boot disk only, named `instances`, 30 GB `pd-balanced`, auto-delete true.

No archive was copied to this VM. Docker was not installed or started. No
source was extracted, no image was loaded or built, no executable was invoked,
no output or evidence root was created, and no candidate or positive sample was
read. The immediate stop capped compute exposure at approximately 88 seconds.

The incident is a transport/input defect, not a numerical result and not a
quota-compatible C1 execution.

## Narrow recovery

Recovery requires separate authority for exactly two cloud mutations:

1. permanently delete only the terminated accidental VM `instances` and its
   attached auto-delete boot disk `instances`; and
2. after read-only absence verification, create exactly one replacement named
   `nhm2-h2-p8f-c1-n2-32-20260831` with the unchanged approved N2 resource.

Before the replacement command is submitted, the Cloud Shell terminal line
must receive `Ctrl+U`, be observed empty at a live prompt, then receive the
exact creation command once. The rendered line must contain exactly one
`gcloud compute instances create` token sequence and the exact replacement
name before Enter. Any ambiguity fails closed without another mutation.

The replacement remains:

- on-demand `n2-standard-32` in `us-central1-a`;
- exact Debian image `projects/debian-cloud/global/images/debian-12-bookworm-v20260817`;
- exactly 30 GB `pd-balanced` boot storage;
- 86,400-second aggregate runtime ceiling and `$40.00` total ceiling;
- unchanged candidate-neutral archive, offline images, build, binary,
  controller, single process, 32 threads, P=65,536 surface, progress evidence,
  automatic stop and every scientific/authority lock.

Deletion is limited to the accidental inert resource. The already uploaded
Cloud Shell archive, every NHM2 evidence disk/snapshot/clone, stopped historical
VM, local evidence, manifest and receipt remain protected.
