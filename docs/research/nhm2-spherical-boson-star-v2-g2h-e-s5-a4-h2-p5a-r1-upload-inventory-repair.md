Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: candidate-neutral C08 implementation and performance equivalence
Capability or component: H2-P5A-R1 additive upload-inventory repair and offline build replay
Current maturity: repaired 37-entry archive producer-audited 15/15 and independently audited 28/28; cloud upload and numerical execution unauthorized
Target maturity: exact separately authorized representative-width cloud execution under the unchanged five-run decision
Required frozen inputs: predecessor archive `5a4f6f98...c321d`, predecessor manifest `7c56923d...a907`, blocker receipt `1f7715b8...83cf`, Dockerfile `bf45b37b...de9c`, and required binary `aa37562f...13b7`
Required evidence: byte-identical preservation of all 36 predecessor members, exactly one additive Dockerfile, deterministic archive/manifest/proposal hashes, clean offline extraction, pinned-image load, offline build, binary-hash reproduction, independent audit, and false authority locks
Stop/fail criteria: any predecessor-byte drift, missing or extra archive entry, Dockerfile drift, binary mismatch, candidate or timing invocation, upload/VM creation without separate authorization, retry/retune, or authority promotion
Explicit non-goals: changing arithmetic, equations, order, width, run sequence, threshold, candidate, selector, runtime authority, frozen-candidate evaluation, full selector, roots, handlers, G3, SI/metric, lanes, lamp, physical viability, propulsion, or transport
Downstream gate unlocked: one separate exact H2-P5A-R1 cloud authorization decision only

# H2-P5A-R1 upload-inventory repair

Status date: August 27, 2026.

Status: **PASS INERT / AWAITING SEPARATE EXPLICIT AUTHORIZATION**.

This packet changes only upload representation and its receipt bindings. It
changes no mathematical semantics, candidate identity, numerical parameter,
runtime authority, proof maturity, or scientific claim.

## Additive repair

The exhausted corrected P5A attempt proved that its 36-entry archive omitted
the already frozen Dockerfile. R1 reconstructs a new archive from the immutable
predecessor archive bytes rather than from current working-tree source files.
All 36 predecessor members retain their exact path, order, byte length, and
SHA-256. R1 appends exactly one member:

`tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/Dockerfile.primary.mini-boson-c08-h2-p5a-width-calibration.v1`

Its SHA-256 remains:

`bf45b37bcac9d10d1d86215e82dc1dd09cb6d200934e48d3cfccdb385058de9c`.

No manifest, proposal, script, candidate data, positive sample, authorization
record, token, or output root is carried inside the upload archive.

## Frozen identities

- repaired source manifest SHA-256:
  `dc33dec0fac549e4f72591b23d44b3355f6aee9b759fbbe1ee7086875709de91`;
- repaired 37-entry archive SHA-256:
  `a8b660522087c820aa23f7e11737aa55b944b7f6a048f867cabdeb4d8ccb6422`;
- repaired archive size: `236257280` bytes;
- execution proposal SHA-256:
  `73fd408fd4d30f843c445e3930ddc7843eac3ac2594948bc714871e39e5936d1`;
- producer preflight SHA-256:
  `ca89e06392db0e8cbb76f8d0392e2725f290fa4dadc877cceb5e15a0085414da`;
- required rebuilt binary SHA-256:
  `aa37562fe73ecf48b0177b6875aea48a259a0439fdbc24abc0525624acb013b7`.

The proposal uses a distinct future VM name,
`nhm2-h2-p5a-r1-c4-16-20260827`, so it cannot restart, overwrite, or reinterpret
the terminated predecessor VM.

## Audit chronology

The producer preflight passes **15/15** with zero uploads, zero VMs, and zero
numerical runs.

The first independent audit correctly built the repaired context and reproduced
the binary hash, but its own two predicates rejected Docker's standard
legacy-builder deprecation warning and tested `sha256sum` as an exact argv list
element instead of the actual `/usr/bin/sha256sum`. That audit is preserved as
26/28 `FAIL` at SHA-256:

`388daa6836cb3c080eac11cd970f3a8eb42c71ee9a31617c1e8bf5a7f479dd74`.

The versioned audit correction changes only those two representational checks.
It preserves the first failure, re-extracts the 37-entry archive in a fresh
temporary directory, reloads the pinned base images, rebuilds with networking
disabled, and invokes only `/usr/bin/sha256sum`. The v2 audit passes **28/28**
at SHA-256:

`d805b68387922cd4dda3dd7791f6d3d4bccfe60bf875ed1578e847c8e02a711e`.

No calibration entrypoint, candidate, timing surface, selector, or positive
sample was invoked during either audit.

Current-head repository verification passes: math registry `323/323`, the
required 18-file WARP battery `179/179`, and Casimir adapter run `2550`
`PASS/GREEN` with first failure `null`, certificate
`6e84f965957f63aad452981d2ede72e62f706d32e0a5b6b469899884e12a4e45`,
and integrity true. This verifies preexecution gate integrity only and promotes
no scientific or physical authority.

## Unchanged future numerical boundary

If separately authorized, the only allowed numerical sequence remains:

1. `--threads 1`;
2. `--threads 4`;
3. `--threads 8`;
4. `--threads 16`;
5. one repeated `--threads 16`.

Each process performs exactly one candidate-neutral `P=1024` call with a
3,600-second external timeout. All semantic hashes must agree, stderr must be
empty, and the slower repeated-16 result must be no more than `337502` ms.
PASS, FAIL, timeout, or partial output is terminal evidence. There is no
numerical retry or retune.

## Authority state

- uploads performed: 0;
- VM created or started: false;
- numerical runs: 0;
- frozen-candidate evaluations and positive samples: 0;
- authorization record or token: absent;
- candidate/output roots and handlers: absent;
- every proof, geometry/state, lane, lamp, physical, propulsion, and transport
  authority: false.

## Exact authorization text

> I authorize creation of exactly one temporary on-demand `c4-standard-16` Google Compute Engine VM named `nhm2-h2-p5a-r1-c4-16-20260827` in `us-central1-a` with exactly 30 GB `hyperdisk-balanced` storage, at the planning compute rate of approximately `$0.79068/hour`, a 7,200-second aggregate VM runtime ceiling, and a `$2.00` total cost ceiling, under H2-P5A-R1 proposal SHA-256 `73fd408fd4d30f843c445e3930ddc7843eac3ac2594948bc714871e39e5936d1`. Upload only the repaired candidate-neutral 37-entry archive `h2-p5a-r1-upload-v1.tar` with SHA-256 `a8b660522087c820aa23f7e11737aa55b944b7f6a048f867cabdeb4d8ccb6422`, bound by source manifest SHA-256 `dc33dec0fac549e4f72591b23d44b3355f6aee9b759fbbe1ee7086875709de91`; upload no additional files. Build the pinned image and require binary SHA-256 `aa37562fe73ecf48b0177b6875aea48a259a0439fdbc24abc0525624acb013b7` before timing. Execute exactly one candidate-neutral `P=1024` calibration at 1, 4, 8, and 16 threads plus exactly one repeated 16-thread calibration, each with a 60-minute external timeout. Preserve PASS, FAIL, timeout, or partial output as immutable evidence, independently audit it, and stop the VM afterward without deleting its disk, logs, or evidence. I do not authorize restarting or changing the exhausted predecessor VM, any other machine, zone, disk type or capacity, additional upload, smaller-width calibration, full selector, numerical retry or retuning, frozen-candidate evaluation, positive sampling, candidate/output-root or handler creation, G3/SI/metric/lane work, or any candidate, proof, geometry/state, lane, lamp, physical, propulsion, or transport authority promotion.
