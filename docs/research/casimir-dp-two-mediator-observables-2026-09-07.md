# Two distinct coherence observables and mediator range

Exploratory observable audit, 2026-09-07. This corrects overly restrictive wording in recent progress records that treated a nonzero boundary contrast as necessary for every shared scattering signal.

The frozen stage4-2r configuration explicitly separates a primary held-out coherence contraction from a four-cell boundary cross-ratio. The frozen Diósi comparator itself has no boundary-state dependence. Therefore a shared scattering model may predict a measurable primary contraction and a null boundary interaction. That is a falsifiable pair of predictions, not automatically a failed bridge. It would not identify the primary loss as Diósi–Penrose collapse; mass, separation, time, environmental controls and other distinguishing tests remain essential.

For boundary label b and separated/compact history s, suppose C_bs = A_b exp(-D_s). Then

    log R4 = log C_AS - log C_AC - log C_RS + log C_RC = 0,

while D_separated-D_compact need not vanish. This identity requires factorization and does not presume it for real apparatus. The sibling script checks it numerically using a demonstration exponent equal to the frozen comparator. That example is not a computed scattering rate. Earlier partial diamond event-rate proxies remain insufficient to supply the actual primary branch-history contraction or a qualified measurable residual.

## Direct static range check

The lightest trial mediator is 1 keV, with reduced Compton range hbar c/m = 1.97327e-10 m. The configured 10 micrometre gap is about 50,677 such ranges. A direct static Yukawa propagator across that distance carries exp(-50677), approximately 10^-22009. Heavier trial mediators are shorter range. Thus a direct static mediator exchange across the configured gap is not a promising mechanism for a macroscopic boundary-state response in this family.

This is a range diagnostic, not a complete boundary calculation or a total-rate bound. Actual source-to-sphere distance requires the as-built geometry; the configured gap alone does not specify every distance. Real particles can cross the apparatus and be scattered or absorbed, changing local flux without the same static gap factor. In-medium collective response and ordinary electromagnetic environmental noise also require their own treatment. Do not use the static result to suppress all scattering or dismiss the primary observable.

## Corrected next requirements

Keep both observables in the shared model. First obtain a physically supplied local phase-space distribution and the primary branch-history decoherence prediction with the frozen apparatus and a qualified uncertainty budget. Predict a null boundary cross-ratio only when factorization is justified; otherwise calculate its departure. A nonzero boundary observation would discriminate against a truly factorized candidate even if its primary contraction matched. This preserves the user's measurable-signal objective without silently adding a mandatory nonzero control-channel signal.

The script asserts the frozen apparatus hash and records its two estimands verbatim. No frozen configuration was changed. Research-only documentation and diagnostic, without a physical viability or certificate claim; no Casimir server verification applies.
