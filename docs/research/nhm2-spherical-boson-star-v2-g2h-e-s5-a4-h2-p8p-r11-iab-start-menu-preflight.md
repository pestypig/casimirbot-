Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R11 in-app-browser Start-menu semantic preflight
Current maturity: frozen read-only menu inspection; R10 authenticated exact stopped resource and located one collapsed More-actions control
Target maturity: determine whether that exact menu exposes exactly one enabled semantic Start / Resume item without activating it
Required frozen inputs: R10 result, retained R10 tab, exact stopped resource, exact collapsed More-actions accessible control
Required evidence: exact resource remains stopped, one semantic menu expansion, one post-expansion accessibility read and Start-control count/state
Stop/fail criteria: stale tab, changed identity/state, expansion ambiguity/failure or absent/duplicate/disabled Start item terminal; no menu-item activation, retry, refresh or fallback
Explicit non-goals: VM start, confirmation, SSH, upload, Docker, build, calibration, candidate evaluation, retuning, P=65,536, deletion, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: one separately frozen and action-time-authorized IAB start successor if and only if exactly one enabled Start / Resume item is observed; P8Q remains stopped

# H2-P8P-R11 in-app-browser Start-menu preflight

Status date: September 2, 2026.

Status: **FROZEN READ-ONLY MENU PREFLIGHT / ZERO BILLABLE ACTION**.

R10 result SHA-256
`e1d47f7a1182cf1cff572308eb01a61bff45c0c34542b6bad91af9852cebb273`
independently audits 13/13. It proves the in-app-browser session is
authenticated, the exact retained VM is stopped, and its toolbar contains one
collapsed accessible More-actions control but no directly exposed Start item.

R11 may reuse only that exact retained tab. Before input, it must still show
VM `nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID
`1920090043510946854`, project `dark-stratum-455714-h4`, zone `us-east1-c`
and state `Stopped`. It may click exactly once on the uniquely observed
semantic button named `More actions nhm2-h2-p8j-r9-c2d-32-20260831`, then
perform exactly one accessibility read.

R11 may report the count, enabled state and accessible name of any resulting
`Start / Resume` menu item. It must not click or activate any menu item, accept
a confirmation, refresh, navigate, type, retry or use a fallback. The expansion
is local presentation state only and submits no Google Compute request.

Only observation of exactly one enabled semantic `Start / Resume` item may
unlock a separately frozen billable start successor. Every VM, ingress,
controller, scientific, evidence, ceiling, automatic-stop, P8Q and authority
binding remains unchanged.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false. Only `P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED` applies.
