Program gate: G2H-E-S-R1 — primary-v2 authorization-schema version-skew disposition
Workstream: authenticated classical and quantum control branch
Capability or component: candidate-neutral primary-v3 schema alignment and preexecution proposal
Current maturity: independently audited implementation/preexecution PASS; no candidate execution
Target maturity: separately authorized primary-v3 Tolman-VII mathematical decision
Required frozen inputs: exhausted v1/v2 evidence, contract `30de966d...343d`, unchanged v2 source `2d205130...9740` and new v3 binding `5c9546b1...cc164`
Required evidence: exact source delta, Linux C authorization fixtures, runtime/executable bindings, exclusive chronology and producer audit
Stop/fail criteria: any old-byte mutation, candidate entrypoint invocation, future root/ledger/container creation, fixture disagreement or authority promotion
Explicit non-goals: primary-v3 execution, Rust execution, candidate/duty retuning, G3, lanes, lamp or physical claims
Downstream gate unlocked: G2H-E-S3 exact primary-v3 authorization decision

This packet changes runtime preflight semantics only by aligning the C parser to
the already-frozen authorization v2 schema. It does not change mathematical
semantics, receipt authority, the candidate, any proof duty or claim status.

# G2H-E-S-R1 result: primary-v3 preexecution PASS

## Decision

`PASS_VERSIONED_PRIMARY_V3_SCHEMA_ALIGNMENT_PREEXECUTION_ONLY`.

The primary-v3 source differs from immutable primary-v2 only in four bounded
identity/preflight changes: a fresh token digest, a fresh v3 output root, the
C authorization schema expectation from v1 to v2, and explicit absence checks
for both exhausted v1 and v2 roots. Scientific equations, arithmetic, proof
duties, thresholds and failure precedence are unchanged.

## Evidence

- primary-v3 source: `37ffaba2...c2e68`;
- image: `sha256:5a790295...7ada`;
- executable: `1764b6be...46e1`;
- build binding: `5c9546b1...cc164`;
- checkpoint: `c7266e34...60532`, inert without `--execute` and the exact record;
- candidate-neutral actual-Linux C fixture: 10/10 cases;
- combined audit: 31/31;
- candidate entrypoint invocations and candidate evaluations: zero.

The fixture admits exactly the seven-line schema-v2 record and rejects schema
v1, wrong decision, wrong lane, each wrong digest/root, a wrong plaintext token
and a wrong runtime self-hash. This agrees with the Python checkpoint contract.

## Chronology and authority

Both exhausted proposals, authorizations, ledgers, 60-byte stderr records,
empty stdout records, sources and retained exit-66 containers remain intact.
The v1, v2, v3 and independent roots are absent. The primary-v3 authorization,
ledger and fixed container are absent. Rust remains unauthorized.

Proposal `af0a0394a16f58e7c7e4dff30a0f5e2fb97562baefc1c77f6a47d76499667384`
is inert and requires a new exact user authorization. Candidate admission,
proof, G3, SI, lane, lamp, physical viability, propulsion and transport
authority all remain false.
