# NHM2 Spherical Boson-Star v2 Lambda-Zero Core Residual Witness

Program gate: G2 — classical branch proof and terminal state

Workstream: lambda-zero limiting-ground-state proof closure

Capability or component: exact rational first-duty counterexample witness

Current maturity: admitted immutable binary64 proof center; no directed core
residual decision

Target maturity: one content-addressed exact decision for the first unresolved
core duty at one preregistered point

Required frozen inputs: proof-center admission raw SHA-256
`ff07124e88673fee04f9ca7e3e7c4b6545a1ee37fb70bda43a140e56bf582645`
/ 2,158 and self-hash
`ff37f9eebebcaf49a5d3fd88d749c62071e33cc5f58b3af6f069700a88a530df`;
current directed-proof architecture raw and semantic bindings; current
directed-proof operator raw and semantic bindings; sealed lambda-zero proof
definition

Required evidence: exact little-endian binary64-to-rational injection; exact
Chebyshev and derivative recurrences; exact compact-coordinate chain rule;
exact normalized Schrödinger residual; exact comparison to `1/10^10`; exclusive
canonical receipt; all authority false

Stop/fail criteria: any dependency or payload drift; malformed, nonfinite, or
negative-zero word; exact arithmetic budget exhaustion; non-strict witness;
output collision

Explicit non-goals: fitting, changing, or retrying the center; searching for a
more favorable point after exact evaluation; completing later core, exterior,
kernel, tangent, first-tube, or branch duties; candidate, lamp, physical,
propulsion, or transport promotion

Downstream gate unlocked: either an exact G2 first-failure receipt or resumption
of the full directed core cover if this witness is non-strict

Change class: exact authority-neutral falsification of a frozen numerical duty

## Preregistered point and formula

A non-authoritative dense binary64 diagnostic identified the first core rails
as likely failures. Before exact evaluation, this packet freezes the simple
dyadic point

```text
x = 1/128
rho = x/(1+x) = 1/129
t = 2*rho-1 = -127/129.
```

No exact result at this point was used to select it. The point is inside the
closed frozen duty domain `[2^-8,32]`.

Each core payload word is decoded to its exact binary64 rational. For
`p(t)=sum_n c_n*T_n(t)`, exact derivative coefficient recurrences produce
`p_t` and `p_tt`. The frozen chain rule is applied exactly:

```text
p_rho = 2*p_t
p_rhorho = 4*p_tt
p_x = (1-rho)^2*p_rho
p_xx = (1-rho)^4*p_rhorho - 2*(1-rho)^3*p_rho.
```

The witness evaluates

```text
R_S = -(1/2)*(u_xx + 2*u_x/x) + (V-nu0)*u
D_S = 1 + abs(u_xx/2) + abs(u_x/x) + abs(V*u) + abs(nu0*u)
q_S = abs(R_S)/D_S.
```

The frozen duty fails exactly when `q_S > 1/10^10`. The receipt stores the
reduced numerator and denominator of every reported rational plus the strict
margin `q_S-1/10^10`. It does not use decimal or binary floating arithmetic to
decide the result.

## One-shot rule

The sole output is
`artifacts/nhm2-spherical-boson-star-v2-g2/lambda-zero-core-residual-witness-v1.json`.
It must be absent before execution. If the exact inequality is strict, the
first-failure code is
`core_normalized_schrodinger_point_counterexample` and the frozen G2 center
fails without retry, retuning, or evaluation of later duties. If the inequality
is not strict, the result is `WITNESS_NOT_ESTABLISHED` and the complete frozen
interval cover remains the next step.

## Authority boundary

An exact counterexample is evidence only about the frozen core residual duty.
It cannot establish another candidate, alter the branch policy, admit a
candidate, light a diagnostic lamp, or support physical, propulsion, or
transport claims. All such authority remains false.
