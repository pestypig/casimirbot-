Program gate: G2B — replacement classical proof attempt
Workstream: authenticated classical branch closure
Capability or component: B4-R11-R1 diagnostic module-identity repair
Current maturity: versioned implementation repair
Target maturity: one authenticated terminal-equivalence diagnosis
Required frozen inputs: immutable R11 source/result and unchanged evidence set
Required evidence: sole loader delta, tests, fresh root and exact receipt
Stop/fail criteria: any non-loader semantic delta, mismatch, partial output/error
Explicit non-goals: R11 retry, B4-R10 retry, Newton/continuation or retune
Downstream gate unlocked: branch closure or one supported successor class

# B4-R11-R1 canonical module-identity repair

R11 failed before scientific evaluation because identical Python class sources
were loaded under different module identities. R11-R1 makes exactly one harness
change: branch modules are imported by their canonical file-stem names after
the frozen branch directory is placed on `sys.path`. The R9 formulation remains
loaded from its exact frozen path.

The R11 source is not edited or rerun. R11-R1 imports it as a frozen library,
replaces only its `_load` helper for the five branch-module paths, then calls the
unchanged `_diagnose` function. It writes to the fresh exclusive root
`artifacts/nhm2-spherical-boson-star-v2-g2/g2b-b4-r11-r1-terminal-equivalence-diagnosis-v1`.

Every input binding, residual/Jacobian evaluation, single correction
reconstruction, 25 domain-only trials, comparison, decision predicate, and
authority lock remains unchanged. Trial residuals and Armijo merits remain
forbidden. Any further failure is terminal for this diagnostic family.
