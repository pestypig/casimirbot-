Program gate: G2H-E-S5-A4 — P8P observer progress and turnaround calibration
Workstream: candidate-neutral H2 turnaround preexecution
Capability or component: P8P-R37 newline-free literal guard controller and retained-R32 build-only fixture
Current maturity: inert successor proposal; R36 exhausted at transported multiline guard before file tests
Target maturity: one authenticated complete archive ingress followed by the unchanged build-only R32 fixture
Required frozen inputs: R36 result, R35 hard link, exact stopped R32 VM/disk, authenticated remote R31 fixture/R32 wrapper, and exact parse-tested newline-free R37 controller
Required evidence: controller identity/parser/newline audit, hard-link identity, stopped resource identity, exactly one restart/guard/SCP/handoff, remote hashes, unchanged wrapper evidence, terminal chronology, independent audit, and automatic stop
Stop/fail criteria: any identity mismatch, controller drift/newline, second restart/guard/SCP/handoff, retry/fallback, archive overwrite, resource substitution, Docker/build failure, numerical invocation, candidate ingress, retune, evidence deletion, or authority promotion
Explicit non-goals: retrying R36, changing or copying the source archive, another link, new VM/disk, P=1024 or P=65,536 execution, frozen-candidate evaluation, positive sampling, G3, SI/metric/lane work, lamp, physical viability, propulsion, or transport
Downstream gate unlocked: only an authenticated build-only fixture PASS may make a corrected P=1024 calibration proposal eligible

# H2-P8P-R37 newline-free controller proposal

Status date: September 4, 2026.

Status: **FROZEN INERT / SEPARATE BILLABLE AUTHORIZATION REQUIRED**.

R36 proved that Windows gcloud/PuTTY transformed a multiline `--command`
argument into a remote attempt to execute `C:\WINDOWS\system32\cmd.exe`. R37
changes only the local representation of the two remote command arguments.

The exact 7,701-byte controller
`tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r37_single_line_controller_v1.ps1`
has SHA-256
`dd93e4a5be713770bd96e5bc9d4f8890586134b66d41144bdb95d1ee67ee6b6c`
and zero PowerShell parse errors. Both remote commands are literal single-line
strings, and an executable local guard rejects either carriage-return or
newline before any cloud action. Static inspection binds exactly one start,
one read-only guard SSH, one SCP, one post-transfer SSH, and one cleanup stop
path. No R37 controller execution has occurred.

R37 reuses only authenticated `C:\NHM2-R35\p8p.tar`, exactly 236,640,768 bytes
with SHA-256
`3c697fa3e238398d3b4c30a6c379fea8d6d545b2228d0c699dd66594a23670b5`,
and stopped VM `nhm2-h2-p8p-r32-e2-4-20260904`, instance ID
`1893159507643031574`, project `dark-stratum-455714-h4`, zone
`us-east1-b`, with its existing 30 GB `pd-standard` disk unchanged.

After exactly one restart and one 120-second wait, exactly one read-only SSH
guard must authenticate the existing 4,024-byte R31 fixture SHA-256
`97c0209284fa67ee33e259a75abfef7947f1f8ce8971af3e7f39a3421ce07c79`,
the 3,129-byte R32 wrapper SHA-256
`f66d2f72649c36f88c3e03134150967aadfba639f59781facfaa3ed6ccde9a19`,
and remote archive absence. Exactly one SCP may transfer only the R35 hard link
to the unchanged remote path. Exactly one post-transfer SSH handoff must
authenticate the archive and invoke the unchanged R32 build-only wrapper once.

The VM has a 3,600-second aggregate restart ceiling and `$1.00` total ceiling.
Complete or partial evidence is immutable. The controller waits for guest
shutdown, reads serial evidence, and stops the VM after any earlier failure.
First failure is terminal.

R37 permits no numerical execution and changes no scientific source, image,
Dockerfile, fixture, binary, equation, threshold, schedule, precision,
selector, or candidate data. P8Q remains
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`; all scientific and physical authority
remains false unless and until the build-only fixture independently passes.
