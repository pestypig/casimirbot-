Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R7 retained-P8J environment reuse successor
Current maturity: frozen transport/environment successor; R6 exhausted on zone capacity before VM start
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R7 record
Required frozen inputs: R6 terminal result, retained stopped P8J-R9 VM and disk, retained authenticated base archive, unchanged P8P overlay/controller/executable/auditor, R7 guest ledger
Required evidence: exact retained-resource preflight, one restart, stable authenticated browser SSH, two-file native-picker ingress, retained-base and P8P-manifest authentication, one controller invocation, serial evidence recovery, independent audit and P8Q classification
Stop/fail criteria: first identity, capacity, restart, SSH, marker, guard, picker, transfer, retained-base, Docker, build, execution or evidence failure terminal; no retry, fallback, second session/dialog/batch/process or scientific change
Explicit non-goals: new VM or disk, P8J evidence mutation, SSH trust or metadata mutation, frozen-candidate evaluation, retuning, P=65,536 execution, G3/SI/metric/lane work, evidence deletion or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R7 retained-P8J environment successor

Status date: September 2, 2026.

Status: **FROZEN ENVIRONMENT-REUSE SUCCESSOR / AWAITING SEPARATE BILLABLE-ACTION CONFIRMATION**.

Change classification: resource selection, transport procedure and receipt
semantics only. Mathematical semantics and runtime authority are unchanged.

## Narrow causal basis

The sole R6 restart request failed before allocation because a
`c2d-standard-32` instance was unavailable in `us-east1-d`. No guest boot,
SSH session, file selection, uploaded byte, Docker action, build or numerical
process occurred. R6 is exhausted.

A read-only September 2 console inspection establishes that the earlier P8J-R9
environment still exists as a stopped resource and already has the exact
machine family, sufficient memory, compatible Debian generation, Docker-era
source payload and a retained 30 GB disk. P8J-R9 previously completed the
candidate-neutral representative observation and was stopped after evidence
recovery. Reusing that stopped environment avoids another scarce-capacity
creation request and avoids retransmitting the 236,492,800-byte authenticated
base archive.

R7 changes no scientific source. It uploads only the unchanged 134,656-byte
P8P overlay and the 2,971-byte R7 environment-reuse ledger. The ledger fails
closed unless the retained base archive is still a regular non-symlink with
the original exact bytes and SHA-256. All P8P work uses new P8P paths; retained
P8J source and evidence paths are neither read as scientific inputs nor
modified.

## Immutable predecessor and scientific bindings

R7 binds:

- R6 proposal SHA-256
  `5b2304763bd1198fdb71db2d6cdb273a6d2288438b0bcb4bc9a1908be2dc2e2e`;
- R6 result SHA-256
  `6d9dafe5658d6580a604fdc1a1879e7d304256146c7130cf65cdd8833ea7826f`;
- R6 result auditor SHA-256
  `978ec22741ceaf6e467b38ff5d12f8a3405253998ef5082c6d0b29f7db3e6269`,
  passing 23/23;
- original base archive: 236,492,800 bytes, SHA-256
  `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`;
- unchanged P8P overlay: 134,656 bytes, SHA-256
  `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`;
- P8P manifest: 2,820 bytes, SHA-256
  `c6ee88481ae7842b176d4c8a2001601c38cc317132c446a086c92b75ddef5aa0`;
- unchanged controller: 4,498 bytes, SHA-256
  `5af4b629336e166d07a277ae59b0f9776ac9e86b728e762030f27237ed1c8f5b`;
- R7 guest ledger: 2,971 bytes, SHA-256
  `9af4180a714d9dfd24dd6cfe6d3952a73d75c932a3f05f5e3484f79467c94a5b`;
- executable SHA-256
  `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`;
- unchanged result auditor SHA-256
  `cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`;
- exactly one candidate-neutral P=1024 controller process on 32 CPUs with a
  14,400-second external timeout;
- phase timing, at most 64 progress lines, all 514 degree buckets, six
  aggregate totals, P8I equality, chronology, neutrality, authority locks and
  the preregistered P8Q trinary classifier.

No candidate-dependent byte, numerical threshold, selector schedule,
precision, equation, observer semantics, reduction order or acceptance rule
changes.

## Exact retained resource and ceilings

Only this resource may be used:

| Field | Frozen value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM and boot disk | `nhm2-h2-p8j-r9-c2d-32-20260831` |
| instance ID | `1920090043510946854` |
| zone | `us-east1-c` |
| state at freeze | `Stopped` |
| creation time | August 31, 2026, 11:13:13 PM UTC-04:00 |
| machine | on-demand `c2d-standard-32`, 32 vCPUs, 128 GB |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260817` |
| boot disk | retained 30 GB `pd-standard`, read/write; no replacement or resize |
| aggregate restart ceiling | 18,000 seconds |
| total R7 ceiling | $9.00 compute plus prorated retained storage |

Before restart, require the VM stopped and every binding exact. Require the
local overlay and ledger exact. Authorize one restart only. After one fixed
180-second boot wait, freshly load the exact instance Details page and open
exactly one authenticated browser-SSH surface as `pestypig`.

## SSH, guard and two-file ingress

At the first stable prompt, enter one harmless command and require its exact
response:

```sh
printf '%s\n' R7_SSH_ROUNDTRIP_READY
```

Wait 30 passive seconds. Then run exactly one fail-closed guard requiring:

- `/home/pestypig/h2-p8f-c2-r1-cloud-upload-v1.tar` to be a regular
  non-symlink with the frozen base size and SHA-256;
- the R7 overlay and ledger destination paths, P8P root, P8P evidence root and
  P8P export path all absent;
- `nhm2-h2-p8j-r9.service` and `nhm2-h2-p8j-r13.service` inactive;
- no `mini-boson-star` process and no running Docker container.

Require exact marker `R7_REMOTE_GUARD_READY`. Only then activate Upload once
and use one native Windows file-open dialog to select exactly these two files:

1. `h2-p8p-overlay-upload-v1.tar`;
2. `h2_p8p_r7_retained_p8j_guest_sequence_v1.sh`.

Require exactly two successful transfers. Authenticate both remote files by
regular-file, non-symlink, byte-count and SHA-256 checks. Invoke the R7 ledger
once through `bash`; do not edit or chmod it.

Docker must already exist. Its absence is terminal rather than an installation
authorization. The ledger authenticates the retained base, extracts base then
overlay into the initially absent P8P root, validates all 11 manifest entries,
starts Docker, performs the unchanged offline build with no pull and no
network, authenticates the executable, and invokes the unchanged controller
once. The controller exports evidence to the serial console and shuts the VM
down after PASS, FAIL, timeout or partial result.

## Evidence, P8Q and authority boundary

Recover only the one hash-bound P8P evidence archive from serial output into a
new local R7 capture. Run the unchanged independent result audit. Retain all
P8J evidence and resources. First failure is terminal; R7 has no retry,
fallback, rescue or second process.

Only a complete authenticated calibration may produce
`P8Q_YES_PROPOSAL_READY` or `P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`. Any failure,
timeout, missing archive or incomplete audit produces
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. No result authorizes P=65,536.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.

## Exact authorization text

> I authorize exactly one H2-P8P-R7 retained-P8J environment-reuse P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Restart only the existing stopped Google Compute Engine VM `nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID `1920090043510946854`, in project `dark-stratum-455714-h4`, zone `us-east1-c`, preserving its exact `c2d-standard-32` machine, Debian image and 30 GB `pd-standard` disk, under an 18,000-second aggregate restart ceiling and a $9.00 total ceiling. Require the exact resource, local 134,656-byte P8P overlay SHA-256 `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`, and 2,971-byte R7 guest ledger SHA-256 `9af4180a714d9dfd24dd6cfe6d3952a73d75c932a3f05f5e3484f79467c94a5b`. Open exactly one fresh authenticated browser-SSH surface as `pestypig`; require exactly one `R7_SSH_ROUNDTRIP_READY` response, exactly 30 passive seconds, and exactly one remote guard proving the retained 236,492,800-byte base archive SHA-256 `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`, P8P path absence, inactive P8J services and no scientific or container process before returning `R7_REMOTE_GUARD_READY`. Only then use exactly one native Windows file-open dialog to upload exactly the overlay and ledger, require two successful transfers and authenticate both. Invoke the ledger once through `bash`; require Docker already present, validate the P8P manifest, build with no pull and no network, require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`, and execute exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout. Preserve all outcomes, automatically stop the VM, recover and audit the one serial evidence archive and apply only the frozen P8Q rule. First failure is terminal. I do not authorize retrying R6 or R7; a second SSH surface, restart, marker, guard, dialog, upload batch, VM or numerical process; new resource creation; resource replacement, resize or rescue; Docker installation; mutation or deletion of any P8J source, evidence, archive, disk or resource; alternate transport; SSH trust, metadata, IAM, firewall, Chrome or security-setting mutation; additional upload; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or any authority promotion.
