# NHM2 Spherical Boson-Star v2 Initializer Production Runtime Repair

Status: **active repair packet after BLOCKED numerical decision**.

Program gate: **G1-R1 — initializer production runtime repair**

Workstream: source seed to frozen six-payload initializer boundary

Capability or component: authenticated production orchestration, fixed native
arena ownership, shared private continuation composition, and initializer input
publication

Current maturity: authenticated source-manifest resource preflight, retained
one-shot native arenas, abandonment-safe finalization, and the complete frozen
fixed-index MPFR/GMP primitive dictionary; exact held-source/loaded-module
identity, the frozen N=64 spectral slot graph, and the fixed analytic L0
initializer graph are present; the retained lease also owns and exactly restores
the frozen Windows UCRT binary64 environment on its acquiring thread; the exact
core residual/Jacobian/LU/Newton graph is present and reproducibly reaches the
frozen Armijo first failure, so no projected core state, continuation/tail graph,
six-payload instance, runtime closure, preseal, or evidence issuer exists

Target maturity: one authority-neutral, authenticated six-payload initializer
instance that can enter the frozen evaluator without changing its graph

Required frozen inputs:

- candidate identity
  `nhm2.semiclassical_v2.spherical_boson_star_1s_weak_field_control/v1`;
- final branch-selection semantic
  `221af0c6b9f858d20ca2f89c5e4eedf14a0c64ede9ff39e60077b79f08ad9aaa`
  / 41,280 bytes;
- initializer/evaluator semantic
  `2253cea43e7b0abc99aaebd19ced18994eba4605b65fe674febb03d9945cdbc5`
  / 24,711 bytes;
- the exact source/dependency pins already frozen by the seed producer;
- arena cardinalities `mpfr=65,536`, `binary64=262,144`,
  `permutation=257`;
- the six-payload order and 2,664-byte aggregate frozen by the evaluator.

Required evidence:

1. A server-owned, no-caller-path/hash/provider production entry that obtains
   one fixed arena set before any candidate numeric read.
2. One authenticated private loader that injects the same core-quadrature
   module instance through core orchestration, tail initialization, and the
   combined tail operator; token rebinding and core-integral recomputation stay
   forbidden.
3. Exact source, dependency, toolchain, executable, loaded-runtime, command,
   input, monotonic chronology, and scientific-preseal bindings.
4. Successful reverse cleanup, context restoration, descriptor close, and
   sticky poison on incomplete cleanup.
5. One source-seed receipt and six exact payload bindings, including the
   domain-separated supplemental `U,U1,V,V1` join-barrier trace.
6. Independent exact-byte reread and validation against the frozen initializer
   evaluator before any N=64 solver call.
7. Hostile tests proving caller values are not traversed before trust/runtime
   admission and that copied, spliced, stale, or resealed-in-isolation payloads
   cannot cross the boundary.
8. Focused tests, static/type checks, independent audit, math validation, and
   applicable Casimir certificate integrity.

Stop/fail criteria:

- Any required frozen source or semantic binding drifts.
- The implementation requires retuning, alternate grids, alternate solver
  rules, core-integral recomputation, or continuation-token rebinding.
- The same authenticated private module identity cannot be retained across
  stages.
- Fixed arenas cannot be allocated and proven within the frozen preflight.
- Runtime/toolchain/executable identity or scientific preseal remains absent.
- Cleanup, restoration, or post-close identity proof fails.
- Any proposed receipt promotes candidate, proof, replay, lamp, physical,
  propulsion, or transport authority.

Explicit non-goals:

- Running the N=64 candidate before this packet closes.
- Changing the frozen initializer operation graph or synthesizing missing
  payload bytes.
- Running N=96, N=128, or N=256.
- Claiming vacuum, no-fold, positivity, or boundary proofs.
- Producing joint geometry/state, metric inputs, mean/noise, constraints, lanes,
  replay agreement, or a Theory Graph lamp.
- Claiming physical viability, propulsion, or transport.

Downstream gate unlocked: a fresh G1 numerical-decision attempt packet for the
same frozen `N=64, A=2^-16` checkpoint. This repair does not itself authorize or
constitute that attempt.

## First implementation boundary

The first implementation decision is a narrow `GO/NO-GO` design audit of the
existing seed producer's private module lifecycle and fixed-arena ABI. Do not add
another schema-only blocker. The audit must identify one concrete server entry,
one owner for the native arenas and shared module instance, the exact consumer
surface needed by the frozen six-payload evaluator, and every cleanup/poison
transition. If those cannot be defined without changing frozen numerics, record
`NO-GO` and escalate the exact definition conflict instead of coding around it.

## August 21 design and resource-preflight checkpoint

Design verdict: **GO for one Node/native-addon owner; NO-GO for promoting the
existing Python container graph**.

The frozen resource policy requires the JavaScript typed arenas and native MPFR
descriptor arena to coexist under one owner. The current Python producer cannot
be wrapped as production because its lists, tuples, gmpy2 objects, and separately
loaded module instances do not satisfy the fixed slot/lifetime graph.

An authority-neutral native feasibility component now executes the exact
resource preflight in one Node process:

- native source:
  `tools/nhm2-spherical-boson-star-v2-initializer-runtime/native_arena_preflight.rs`,
  SHA-256
  `282271256ff58ffa08f85b46fdfdb228295aa7d0f6131b51a60a0470af4f9853`,
  159,743 bytes;
- focused test:
  `tools/nhm2-spherical-boson-star-v2-initializer-runtime/test_native_arena_preflight.mjs`,
  SHA-256
  `96cf01ecbca4ee87bce15f0d7db71b281b3fadf9471edbe82c621b7685827553`,
  34,884 bytes;
- observed MPFR library: raw SHA-256
  `95b280f52d24a1fe1e024877ee325a629c3424e12961d27f84daec73d02c4bd8`,
  version 4.2.2;
- observed GMP library: raw SHA-256
  `829adcf025d22e641c6816b431fbe5b226a39b390c7205192d480151646fe9c9`,
  version 6.3.0;
- observed exact counts: MPFR 65,536, binary64 262,144, permutation
  257;
- every MPFR slot was initialized in increasing order at precision 256,
  verified as positive zero, and cleared in decreasing order;
- MPFR exponent bounds were restored; both typed arenas were exact-length and
  positive-zero/zero initialized;
- candidate numeric reads and every execution, candidate, lamp, physical,
  propulsion, and transport authority remained false.
- a zero-argument build-manifest path independently checks the exact 904,297
  byte MPFR DLL and its SHA-256 before load; a same-size one-byte mutation and a
  build with no manifest both fail before allocation.
- the same zero-argument boundary now retains the MPFR arena and exact typed
  arrays behind one opaque, process-local, one-shot lease; concurrent
  acquisition, post-close reuse, and reacquisition all fail closed;
- explicit close clears MPFR slots in reverse order, restores the exponent
  range, unloads the module, and returns an authority-false close receipt;
  forced cleanup failure latches a sticky poison that prevents reuse.
- abandonment invokes the same cleanup path through a native finalizer; a
  finalizer cleanup failure also latches sticky poison;
- lease identity is exact: detached/copied operation methods and close methods
  cannot act on the native arena;
- the exact RNDN dictionary is available only by validated arena indices:
  positive-zero, set-ui, set-si, set-d, set, GMP-backed set-z-2exp, const-pi,
  add, subtract, multiply, divide, negate, cosine, square root, exponential,
  logarithm, get-d, and comparison. Each primitive clears and reads MPFR flags,
  canonicalizes zero, rejects invalid alias/domain/resource inputs, and latches
  native postcondition failure.
- GMP and MPFR source files are opened without write/delete sharing before the
  production-shaped load and retained through lease cleanup; their loaded
  module volume/file identities match the held sources, both sources are
  rehashed after load, and hostile write-open attempts fail while the lease is
  live and succeed only after close.
- before the lease exposes any numerical method, it captures the caller's exact
  Windows UCRT 8-byte floating-point environment and masked control state,
  installs the frozen `windows_amd64_ucrt_full_fenv` controls, and binds all
  numerical callbacks and cleanup to the acquiring Windows thread;
- every numerical callback revalidates the live controls. Explicit close and
  abandonment cleanup retain that environment until MPFR clear/range restore
  and module unload finish, then exactly restore and reread the caller state.
  Forced restoration mismatch returns the typed first failure and latches the
  process-local runtime poison. The bound source policy is raw SHA-256
  `8d452abdfa6d9b3e0cf92aa7d8682202b588f1fe8b0fe0772c6d003d2d12f1a4`
  / 14,980 bytes;
- the zero-argument retained-lease method now executes the exact frozen N=64
  Lobatto node, barycentric-weight, unrounded MPFR D, and unrounded MPFR D2
  graph in the prescribed current-level slots; it performs 823,988 fixed-index
  operations and writes only the N=64 prefixes of the binary64 rho, D, and D2
  surfaces;
- the complete binary64 surfaces independently match the established raw
  f64le SHA-256 values
  `1f42876204af11c7eebab8bba8cbcd8694270e106f19479bbbd74fc47521ecab`
  for rho,
  `16f67212db733eade1b09c0dcaf21f4c817472c7fd5f311701aaed0fce564c70`
  for D, and
  `708ca1b0c4033c4873403000beb6027d22682ba2f5432af07cac9b2bed7d7d76`
  for D2; the retained MPFR weights match
  `a28c4a929a2ec377ca6743afd3c15d0817a7cd8398e610acef054bda57f9820d`
  when observed by the diagnostic test;
- the graph requires a pristine lease and exact live ArrayBuffer backing
  identity, rejects replaced or transferred/detached views, rejects repeat or
  detached-method invocation, preserves every unused capacity suffix as
  positive zero, and leaves candidate reads and all authority false.
- the same lease then executes the frozen analytic N=64 L0 initializer only
  after rehashing the rho, D, and D2 binary64 surfaces; it evaluates the exact
  kg/nu, interior integral, origin, and infinity chronology in the named MPFR
  scratch range and writes only the 129-word current-state prefix;
- the initializer reaches the fixed cumulative operation count 837,901, the
  packed state rehashes exactly to
  `cdac4932d5f11808a7a443fe8cb40e56c69418396f28409e9094e354722b95c5`,
  kg has bits `3feef30abf082e7f`, nu has bits `bfddeeea11683f4a`, and the
  unused current-state suffix remains positive zero;
- initializer-before-spectral, repeat invocation, changed spectral bytes,
  non-pristine output storage, detached/replaced backing identity, and copied
  method identity all fail closed; this analytic initializer still performs no
  candidate numeric read and grants no solve, execution, or acceptance
  authority.
- the same lease now evaluates the exact N=64 binary64 core residual and
  analytic Jacobian in the frozen arena, factors the row-major Jacobian with
  lowest-row-tie partial pivoting, performs exactly three MPFR256 residual
  refinements through the retained native module, and follows the frozen
  48-update/25-trial Armijo schedule without fallback or retuning;
- two independently executed implementations agree on the first failure: nine
  accepted updates with alpha exponents `0,0,0,0,0,1,3,6,8`, followed by
  `armijo_schedule_exhausted_without_retry` on the tenth solve after 52 total
  trial evaluations. The preserved current equation norm is
  `6.052214285290347e-11`, above `2^-40`; the scaled last accepted step is
  `3.043268818520606e-17`;
- the native current-state raw f64le SHA-256 is
  `601af0c0de01be4bb5a2abc0dc743cae57397a50c9406720856ae396c7325e50`
  and its preserved current-residual raw f64le SHA-256 is
  `13418bbf6f97925754b7dd999b1e70e2d2495d2efb4993f61ee98cf4be62dc17`;
  both match the frozen Python graph bit-for-bit. No endpoint projection,
  continuation, tail work, payload materialization, candidate read, or authority
  promotion occurs after this first failure.

This checkpoint does **not** close G1-R1. The public diagnostic path still
accepts a caller-selected DLL solely for negative/feasibility coverage, while
the production-shaped lease path is zero-argument and build-manifest-bound.
The complete primitive surface, held-source loader identity, frozen spectral
graph, analytic L0 initializer, and core Newton graph are present, but no
complete source/dependency/toolchain/executable/runtime/command/preseal receipt
exists. More importantly, the frozen core graph has now reached its literal
first-failure disposition before projection, continuation, tail, quadrature, or
payload materialization. Therefore the lease remains deliberately
`productionRuntimeReady:false` and cannot read candidate numerics. The next
action must classify this exact first failure under the no-retuning rule and
activate only a separately approved smallest causal repair; downstream graph
implementation cannot make the missing projected state exist.

Checkpoint verification:

- focused native lease test: PASS, including missing/corrupt manifest,
  active/stale/consumed lease, cleanup poison, exact binary64 caller-state
  restoration, and forced restoration-failure poison regressions;
- math-stage validation: PASS, 318 entries;
- Casimir `repo-convergence`: PASS / GREEN, run 2420, trace
  `adapter:a61653bd-eea0-4f0d-87fc-84952f85de01`, certificate
  `6e84f965957f63aad452981d2ede72e62f706d32e0a5b6b469899884e12a4e45`,
  integrity true.
