Program gate: G2B — replacement classical proof attempt
Workstream: authenticated classical branch closure
Capability or component: B4-R11-R1 terminal-equivalence diagnosis result
Current maturity: independently audited diagnostic `PASS`, branch closed
Target maturity: immutable scientific stop and replacement-research handoff
Required frozen inputs: R4/R5/R6/R7, R9/R10 and R11/R11-R1 evidence
Required evidence: direction hash, 25 domain trials, comparison and audit
Stop/fail criteria: any unique-successor claim absent exact recorded support
Explicit non-goals: retry, retune, vacuum proof, candidate/proof/lamp authority
Downstream gate unlocked: G2C replacement-candidate research only

# B4-R11-R1 result record

## Decision

```text
status = PASS
decision = CLOSE_FROZEN_BRANCH_NO_SUPPORTED_SUCCESSOR
```

The receipt self-hash is
`3724e8eb50e49beee6354f205b4a0593538706484a9a38f0b6d50d654727a164`.
The raw receipt SHA-256 is
`cec0415c440ba921c437237e539a2ce6b68cd0614d48b63c630f4e06a0ed79bc`.

## Evidence

The fixed correction reconstruction exactly matches all eight hashes in R10
trace ordinal 29. Its frequency component is positive,
`3.311179596954873e-5`. Every frozen trial exponent 0..24 is finite, but every
trial has `w >= 1`; the first is `1.0000331117943924` and the last is still
`1.0000000000003963`. Therefore all 25 are rejected by the unchanged domain
before any trial residual or merit can be evaluated.

R4 and R10 have exactly the same 29 accepted-alpha exponents. Their maximum
state difference is `2.324568760137674e-14`; their residual, scaled-step and
unused-constraint ratios are respectively `0.9999995178430278`,
`0.9999999990642421`, and `0.9999999999506171`. The R9 power-of-two
equilibration therefore left the terminal chronology scientifically unchanged.

R7 supplied no unique proposal class, and R10 falsified the sole intervention
supported by R8/R9. The recorded bytes identify the immediate `w<1` obstruction
but do not distinguish a new coordinate, globalization, discretization,
equation, threshold, or candidate. Selecting one now would be post-result
retuning. This frozen candidate branch is closed without a GO.

## Implementation chronology and audit

The first R11 harness invocation is preserved as implementation `BLOCKED`: it
stopped before evaluation because identical grid classes had different Python
module identities. R11-R1 changed only that loader identity and used a fresh
output root. It did not retry R11 or B4-R10.

The producer-independent audit imports no producer, evaluator, LU, Newton, or
continuation code. Host and admitted Linux pass 6/6. It rehashes all 21 inputs,
the canonical receipt and self-hash, independently reproduces every trial
frequency/domain decision and every R4/R10 comparison, and verifies all false
authority locks.

No candidate solve, Newton, continuation, trial residual, Armijo merit, state
update, retry, or retune occurred. Vacuum continuation and all later proof
duties are ineligible for this branch. Candidate, proof, execution, replay,
lane, pair, joint-state, lamp, Theory Graph, physical, propulsion, and transport
authority remain false.

## Repository verification

- math registry: 318 entries valid;
- required WARP regression: 18/18 files and 179/179 tests pass;
- Casimir adapter: run `2449`, `PASS` / `GREEN`, first failure null;
- certificate SHA-256:
  `6e84f965957f63aad452981d2ede72e62f706d32e0a5b6b469899884e12a4e45`;
- certificate integrity: true;
- training trace:
  `artifacts/training-trace-g2b-b4-r11-r1-branch-closure.jsonl`.

The adapter PASS cannot convert branch closure into candidate, proof, or
physical authority.
