Program gate: G2H-E-S5-A4 — P8P observer progress and turnaround calibration
Workstream: candidate-neutral H2 turnaround preexecution
Capability or component: P8P-R37 newline-free guard transport
Current maturity: immutable local-command-interception BLOCKED result; R37 exhausted
Target maturity: separately versioned cmd-metacharacter-free controller successor
Required frozen inputs: R37 proposal/controller, authenticated R35 hard link, exact retained VM, guard evidence, and cleanup chronology
Required evidence: one restart/wait/guard, exact guard output/exit, absence of SCP/build/numerical execution, automatic stop, and independent audit
Stop/fail criteria: retrying R37, another R37 SSH/upload/build action, changing evidence or hard link, leaving VM running, numerical invocation, candidate ingress, retune, evidence deletion, or authority promotion
Explicit non-goals: retry/fallback, second restart, SCP, Docker/build work, P=1024 or P=65,536 execution, frozen-candidate evaluation, G3, SI/metric/lane work, lamp, physical viability, propulsion, or transport
Downstream gate unlocked: one separately frozen cmd-metacharacter-free controller proposal; no execution authority

# H2-P8P-R37 cmd-pipe interception result

Status date: September 4, 2026.

Status: **BLOCKED AT READ-ONLY GUARD TRANSPORT / R37 EXHAUSTED**.

R37 authenticated its exact proposal, 7,701-byte controller, R35 hard link,
fresh evidence root, and exact stopped retained VM. It restarted the VM exactly
once and completed the exact 120-second wait.

The one newline-free read-only SSH guard then returned exit 255 with exact
output:

```text
'cut' is not recognized as an internal or external command,
operable program or batch file.
```

This proves Windows `cmd.exe` intercepted the guard's pipe operator before the
remote shell could execute the file tests. The failure is distinct from R36's
newline transformation. First failure was terminal. No SCP, archive upload,
post-transfer SSH, guest wrapper, Docker action, build, fixture, or numerical
process occurred.

Cleanup stopped the exact VM, now authenticated `TERMINATED` with last-stop
timestamp `2026-09-04T11:12:22.833-07:00`. The R35 hard link remains authentic
and unchanged.

The next eligible controller must reject Windows command metacharacters in both
remote arguments before cloud action. It may retain shell command substitution
but may contain no pipe, ampersand, input redirection, output redirection,
carriage return, or newline.

This is transport/preexecution evidence only. P8Q remains
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`; all scientific and physical authority
remains false.
