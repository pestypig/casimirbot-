Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R4 fresh-metadata browser-SSH and browser-filechooser preexecution result
Current maturity: terminal browser-filechooser failure; exact VM stopped; zero transferred bytes, guest ledger or numerical process
Target maturity: immutable fail-closed R4 evidence and one separately versioned transport-admission successor decision
Required frozen inputs: R4 proposal `5202340a...69055e`, stopped exact R3 VM, verified 16-file local staging, unchanged guest ledger/controller/executable/auditor
Required evidence: exact resource preflight, one restart, fresh-details reload, one successful browser-SSH surface, remote absence guard, first filechooser observation and terminal VM state
Stop/fail criteria: unavailable chooser is terminal; no upload retry, fallback, second SSH surface, Docker installation, build, calibration or P8Q promotion
Explicit non-goals: scientific interpretation, candidate evaluation, retuning, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: separately frozen transport-admission successor only; no calibration or R4 retry authority

# H2-P8P-R4 browser-filechooser preexecution result

Status date: September 1, 2026.

Status: **BLOCKED_PREEXECUTION_BROWSER_FILECHOOSER_UNAVAILABLE / R4 EXHAUSTED**.

Change classification: receipt semantics only. Mathematical semantics and
runtime authority are unchanged.

## Authorized boundary

The operator authorized exactly one H2-P8P-R4 execution under proposal
SHA-256
`5202340ab57083b88fa874b9f2c2ec5b4dd5196b2da86716e33f15a9e169055e`,
including one restart of the existing stopped R3 VM under the frozen $9.00 and
18,000-second ceilings, one freshly reloaded browser-SSH attempt and the
unchanged bounded ingress/calibration sequence only if SSH succeeded.

The authenticated resource remained:

- project `dark-stratum-455714-h4`;
- VM `nhm2-h2-p8p-r3-c2d-32-20260901`;
- instance ID `637527339076077505`;
- zone `us-east1-d`;
- on-demand `c2d-standard-32` with 32 vCPUs and 128 GB memory;
- exact image
  `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`;
- retained 30 GB `pd-standard` boot disk;
- 18,000-second aggregate restart ceiling and $9.00 total ceiling.

First failure was terminal. R4 did not authorize retrying its upload, selecting
an alternate transport, opening another SSH surface, starting a second VM or
process, retuning, evaluating a candidate or promoting authority.

## Observed chronology

1. The independent R4 proposal audit passed `20/20`. The verification-only R3
   ingress materializer authenticated exactly fifteen ordered base chunks plus
   the unchanged overlay and reported `P8P_R3_INGRESS_VERIFIED 16/16`.
2. The exact VM was authenticated `Stopped` before the one restart. The
   instance ID, `us-east1-d` placement, machine, image and retained disk matched
   the frozen table.
3. The one authorized restart succeeded. The exact VM reported `Running` with
   external IP `34.26.4.67`. The exact instance Details page was freshly
   reloaded once and reauthenticated the same instance ID and resource
   bindings.
4. Exactly one new Compute Engine browser-SSH surface was opened. Its ordinary
   transient metadata authorization succeeded and the guest prompt authenticated
   user `pestypig`. The R3 metadata optimistic-lock conflict did not recur.
5. The exact remote absence guard ran once and returned
   `R4_REMOTE_ABSENT_16`, proving all sixteen destination paths absent before
   file selection.
6. The authorized single-batch selection was then attempted through the SSH
   page's visible Upload control. No filechooser event appeared. Inspection
   confirmed one multiple-file input, but activating that same input also
   produced no chooser before the bounded wait expired. No path was selected,
   no transfer began and zero bytes crossed the cloud boundary.
7. The missing chooser was treated as the first terminal R4 failure. No Retry,
   alternate transport, additional selection, guest ledger, Docker
   installation, build, executable invocation, calibration or evidence-export
   process occurred.
8. The exact VM accepted the authorized stop request, first reported
   `Updating. This instance is being stopped`, and then reported
   `Stopped. This instance is stopped` in the authenticated Compute Engine
   instance list.

## Terminal inference

R4 resolves the R3 metadata transaction problem: a fresh page and a single new
browser-SSH surface successfully reached the authenticated guest. The next
failure is narrower and local to browser file-selection admission. Chrome
exposed the expected multiple-file input but did not deliver a filechooser to
the automation surface. This is not archive corruption, guest rejection,
Docker failure or a numerical result.

The base chunks and overlay remain local and unchanged. They were not uploaded
or reconstructed. The guest ledger was not entered. Debian `docker.io` was not
installed. Manifest replay, offline build, executable authentication, the sole
P=1024 controller process, serial evidence export and the turnaround-result
audit did not run.

The preregistered classification is therefore
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. This is not the P8Q yes/no decision
about one full P=65,536 observation. R4 is exhausted. The only eligible
successor is a separately frozen transport-admission packet after the Chrome
extension's file-upload permission is verified; this record grants no R4
retry, restart or fallback.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.
