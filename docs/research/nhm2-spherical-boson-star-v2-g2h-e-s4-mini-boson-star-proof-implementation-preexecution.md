Program gate: G2H-E-S4 — mini-boson-star proof implementation and preexecution closure
Workstream: authenticated classical and quantum control branch
Capability or component: source/runtime-disjoint proof producers, fixtures, build bindings, guards, and inert execution proposal
Current maturity: closed independently audited implementation/preexecution PASS; no candidate execution or proof
Target maturity: achieved with absent candidate roots and no execution authority
Required frozen inputs: R2 protocol `f2fec60d...a6cb2`, matrix `7f7b7ac8...10d03`, contract `041c406c...ed12a`, R1 `e86dcd10...ec0a`, and immutable Tolman result `4248b29a...392d7`
Required evidence: two disjoint implementations, exact/synthetic fixtures, digest-pinned offline runtimes, build manifests, guarded first-failure receipts, independent audit, absent future roots, and current math/WARP/Casimir PASS
Stop/fail criteria: contract drift, shared scientific/runtime lineage, failed fixture, unbound executable/runtime, future root creation, candidate evaluation, authorization, threshold change, or authority promotion
Explicit non-goals: executing the star, user authorization, retuning, precision escalation, Rust scientific execution, G3, SI/metric lanes, lamp, physical viability, propulsion, or transport
Downstream gate unlocked: G2H-E-S5 inert primary execution-preflight decision only

# G2H-E-S4 mini-boson-star proof implementation and preexecution

## Current disposition

The additive `G2H-E-S4-R1` review is closed. Definition seal
`728d8c9a807d27356a6d9f33e897feb73331abb12e6a76435dbce099d9c025ca`
binds all ten exact-definition areas and both finite producer-specific builder
algorithms. The combined audit passes 42/42 across 18 suites, the independent
Node replay passes 100/100, math validation reports 318 entries, all required
WARP tests pass 179/179, and Casimir run 2473 is `PASS/GREEN` with integrity
true. S4 fixture-only implementation may resume. R2 remains immutable, no
candidate was evaluated, both candidate roots remain absent, and no execution
or downstream authority exists.

The common implementation layer is closed at fixture-runtime maturity. Build
binding `17753923c7b75884efc6779929ce02e9a84ed4971fdf5a22da024fa5df3fdf4d`
authenticates a C++ Arb/FLINT/GMP/MPFR fixture executable and a source/runtime-
disjoint pure-Rust scratch-musl executable. Their expanded manufactured suites
pass 28/28 and 27/27. Independent runtime audit passes 67/67, including exact
image/executable replay, candidate-interface and corrupt-seal rejection,
ephemeral forbidden-root rejection, and exclusive five-record first-failure
streams that reject overwrite with exit 67. Receipt
`nhm2.g2h_e_s4.fixture_runtime_verification_receipt.v1` preserves the earlier
fixture slice; the current C04 closure is preserved by its versioned successor
receipt.

Source-disjoint wire modules additionally enforce the complete sealed ingress
and scalar rejection set, including decoded duplicate keys and arbitrary-width
rational normalization, so `C02` is complete.

The complete common layer now includes exact, producer-specific finite budget
tables for all 29 primary and 32 Rust counters frozen by the builder contract.
Every counter is exercised through its exact limit and one terminal excess;
the typed exhaustion is persisted as the first failure and later work remains
ineligible. This does not close scientific implementation. The fail-closed
producer matrix passes its consistency audit 8/8 while reporting 4/4 common
roles complete and 3/13 frozen constructor roles complete in each scientific
lane. `P01` provides reusable 512-bit Arb operations and exact-midpoint
ties-even projection with a directed error ball. Source/runtime-disjoint `R01`
provides native signed eight-limb balls, sixteen-limb products/division and the
same exact projection semantics without C, FFI or dynamic libraries; its exact
1024-bit endpoint accumulation now also covers a valid 513-bit midpoint sum.

The bound `P02/P03` primary source performs deterministic complete-pivot Arb LU,
all canonical inverse-column solves, exact `2^-448` projection, FLINT
fraction-free Bareiss determinant certificates and finite `Z0` verification.
The source/runtime-disjoint `R02/R03` path performs column-pivoted Householder
QR, all canonical solves, pure-Rust ascending-61-bit-prime CRT determinant
reconstruction under a conservative Hadamard bound, and direct finite `Z0`
verification. The primary suite passes 29/29; the independent suite passes
29/29, including arithmetic 8/8, inverse 8/8 and determinant/`Z0` 6/6. Binding
`de5fce1ca1f0906d7cb36c003b7874e5a7fa39a5d63a43d918f47bcc2af8e290`
and its digest-pinned offline images pass runtime/source/guard audit 72/72 and
inverse audit 19/19. The next roles are `P04/P05` and `R04/R05` continuation
builders; an inert future proposal remains forbidden until every matrix role is
directly implemented and exercised.

The continuation pair is now also closed at manufactured-fixture maturity.
`P04/P05` evaluates the frozen second-order predictor, scans all 73 radii in
order across a synthetic 1,024-cell chain, enforces strict containment and
chronology, and computes Arb polynomial-convolution majorants with exact FLINT
`(1+k)^8` weights plus the flat-carrier envelope. `R04/R05` independently
re-evaluates the Taylor tube, converts Chebyshev coefficients to Bernstein
enclosures by recurrence and degree elevation, performs native exact binomial
convolution through degree 512, and returns a directed conversion-width
remainder. Primary continuation fixtures pass 10/10; independent fixtures pass
9/9; runtime audit passes 75/75 and continuation audit passes 22/22 under
binding `e57766fe330922f8ffc915937eb7688c11be6b48f71274ae2aa22f208ecd31e3`.
The matrix is now 5/13 in each scientific lane. The next frozen roles are
`P06/P07` and `R06/R07` stability builders. No scientific continuation cell was
executed and proposal eligibility remains false.

The radial-stability pair is now closed at manufactured-fixture maturity.
`P06/P07` binds the degree-12, 256-cell constant Riccati fixture through every
32-step implicit-midpoint cell, exactly 16 strict damped-Newton decreases and
one `2^-448` projection; verifies the symmetric residual, strict principal
minors and one least-lattice jump repair; and exercises 64 inverse iterations,
the degree-16 fixed C1 cutoff, `0<L<=U<E_ess` with `L=2^-96`, and 1,024
positive manufactured mass derivatives. `R06/R07` independently exercises
both GL2 stages through all 256 by 40 steps in degree-14 Bernstein form, exactly
20 fixed 96-vector Newton-Krylov sweeps, the full 65,537-ordinal jump-search
pass/touch/exhaustion behavior, and 80 block-Lanczos steps for a degree-18 trial
whose invariant least Ritz value is separated from the manufactured chain by
an exact Gershgorin bound. Both stability suites pass 9/9. Runtime/source/guard
audit passes 78/78 and the independent stability audit passes 26/26 under
binding `215078e444fd4ff30c393b03daa2c70ef093fcc5719cf802d387f56d6c123515`.
The fail-closed matrix is now 7/13 in each scientific lane; `P08-P13` and
`R08-R13` quantum constructors remain. No scientific stability operator or
eigenvalue was evaluated, both candidate roots remain absent, and proposal
eligibility remains false.

The first P08 source pass triggered the gate's semantic self-correction
rule. Builder v1 fixes the broad quantum methods and coarse counts but not a
total algorithm: among other omissions, the Rust `kappa=sinh(t)` route has no
frozen `t` endpoint or panel formula, both energy routes lack a bound energy
coordinate and extrapolation stencil, and the radial, Hadamard and noise paths
lack finite correction/projection work selectors. The versioned 12-class gap
inventory reproduces this stop 23/23 without candidate ingress. Additive
successor `2989373624362e7f591ca0f00b76d1b01e2aa861f01eaf53f9b62c666f2862fc`
now fixes all twelve classes while preserving every inherited selector. Its
exact audit passes 49/49, source-language-disjoint Node replay passes 29/29 and
closure receipt `9928c20bb46cd177a70b9f10681467ce260bcde0cc0da4ac1e3cd9116e8a5bf1`
passes 13/13. The repair explicitly uses the weighted Richardson/Poisson
remainder, obtains state-dependent `W_DF` from the ground-state spectral product
rather than geometric transport, and extends the frozen wire-v1 envelope.
`G2H-E-S4-R2` is closed and parent S4 fixture implementation may resume at
P08/R08. Completed P01-P07 and R01-R07 evidence remains unchanged. No candidate,
quantum product or root was evaluated or created, and all authority remains
false.

The radial-resolvent pair is now closed at manufactured-fixture maturity.
`P08` constructs all 25 physical-order CGL nodes and solves a full 50-unknown
two-component manufactured degree-24 collocation system with the bound
complete-pivot Arb path, eight strict defect reductions, degree-48 defect
enclosure, both endpoint directions, one projection and a nonzero Wronskian.
`R08` independently exercises every 256 by 48 fixed degree-28 Taylor step,
ascending convolution, the least power-of-two seed, exactly twelve strict
Picard contractions, native rectangular complex-ball division, endpoint
chronology and one projection per cell. Both radial suites pass 9/9 and both
full harnesses pass 32/32. Binding `69568e54...b337` and runtime audit 26/26
also reject a mutated total-builder contract at exit 65. Receipt
`d848c1e1...969c7` advances the fail-closed matrix to 8/13 per lane. No selected
background byte, positive parameter sample, candidate root or quantum product
was consumed; the next roles are `P09/R09`.

The angular-subtraction pair is now closed at manufactured-fixture maturity.
`P09` solves and back-substitutes all 21 order-20 coefficients of a positive-root
manufactured Drach recurrence, enumerates `ell=0..255` with all 65,536
degeneracy ordinals, evaluates exactly 21 Arb Hurwitz-zeta tails at `q=511/2`,
and enforces eight fixed majorant iterations, threshold-first chronology, one
projection and strict `2^-132` touch rejection. `R09` independently derives the
order-22 flat DeWitt recurrence, enumerates `ell=0..287`, generates exact
Bernoulli numbers through `B_128` by a native fixed-limb Akiyama-Tanigawa
recurrence, and exercises all 64 Euler-Maclaurin corrections from `nu=577/2`
plus twelve majorant iterations. Both angular suites pass 9/9 and both full
harnesses pass 33/33. Binding `28931caf...19647`, runtime audit 34/34 and matrix
audit 10/10 are preserved by receipt `652914c2...b2c22`, advancing the matrix to
9/13 per lane. Candidate interfaces still reject at exit 64, corrupt contracts
at exit 65, both candidate roots remain absent, and the next roles are
`P10/R10`.

The negative-axis spectral pair is now closed at manufactured-fixture maturity.
`P10` constructs the 32 GL roots from the exact 8,192-interval mesh, performs
600 exact rational bisections and 16 Arb interval-Newton steps per root, forms
positive weights, covers all 1,024 exact panels, validates degree 63, and closes
the order-20 tail after eight majorant iterations. `R10` independently encloses
Machin pi with 160 terms per atan, evaluates degree-256 sinh/cosh/tanh models,
constructs the 24 half-shift tanh-sinh nodes on all 1,536 panels, validates
degree 47 and closes the order-22 tail after twelve iterations. Both sub-suites
pass 9/9 and both harnesses 34/34. Binding `e5b1abda...6e913`, runtime audit
34/34, matrix audit 11/11 and receipt `7b77ecbf...25c2b` advance the fail-closed
matrix to 10/13 per lane. No candidate byte or scientific output was consumed;
the next roles are `P11/R11`.

The physical-measure and smearing pair is now closed at manufactured-fixture
maturity. `P11` splits all 2,048 panels at the exact manufactured threshold,
solves and replays the nine-level order-8 epsilon system with fraction-free
FLINT rationals, preserves the threshold atom, certifies the weighted
ninth-order Poisson remainder below `2^-132`, closes the order-20 tail and
normalizes 128x32 GL32 smearing. `R11` independently performs the corresponding
2,304-panel split, a native 4,096-bit fraction-free Bareiss solve with exact
moment replay, correlation-preserving Poisson enclosure, order-22 tail and
192x24 TS24 smearing. Both sub-suites pass 9/9 and both harnesses 35/35.
Binding `476fabc7...14ca`, runtime audit 36/36, matrix audit 12/12 and receipt
`72f87903...f9344` advance the fail-closed matrix to 11/13 per lane. Candidate
ingress remains zero, both roots remain absent and all authority remains false;
the next roles are `P12/R12`.

The Hadamard/RSET pair is now closed at manufactured-fixture maturity. `P12`
enumerates the 70 graded four-dimensional multiindices, instantiates flat
`P=Box-1`, solves and exactly replays `V_0..V_20`, exercises complete-pivot Arb
inversion plus eight strict correction sweeps, derives `W_DF` from the
state-product difference, projects once, recomputes the 64x4 zero RSET and
conservation cells and closes the degree-21 remainder. `R12` independently
builds the native flat metric/connection, replays `V_0..V_22`, performs twelve
interval Picard passes and closes its degree-23 remainder. Both sub-suites pass
9/9 and both harnesses 36/36. Binding `598ca3d3...336e`, runtime audit 34/34,
matrix audit 13/13 and receipt `c9ac27cd...dfccf` advance the matrix to 12/13
per lane. Candidate ingress and authority remain zero/false; the final roles
are `P13/R13`.

The noise pair is now closed at manufactured-fixture maturity. `P13` projects
all 256 manufactured vectors, forms all 65,536 direct interval Gram entries,
performs one unpivoted 32,896-entry lower Arb Cholesky plus two strict factor
corrections, reconstructs the full Gram and certifies row residuals and tails.
`R13` independently performs unpivoted two-pass MGS, projects its factor, forms
all 65,536 original-vector direct dot products and independently reconstructs
the factor Gram. Exact-zero and nonexact-zero-containing residual semantics,
phase, mutation and `2^-132` touch chronology are exercised. Both sub-suites
pass 9/9 and both harnesses 37/37. Binding `a2bcab1a...760e`, runtime audit
34/34, matrix audit 14/14 and receipt `9db3999d...67e12` establish 13/13 role
completion per lane.

## Final S4 closure

The complete producer audit passes 15/15 and the sole inert-proposal audit
passes 12/12. Closure receipt `539a4ac5...a191` binds the frozen definitions,
4/4 common roles, 13/13 scientific roles per lane, all nine fixture classes,
source/runtime disjointness, and absent candidate roots. The current-head
verification receipt `b5ab2249...c816` records math registry 318 with validation
`PASS`, all 18 required WARP files and 179/179 tests passing, and Casimir
`PASS/GREEN` with certificate `6e84f965...4e45` and integrity true.

The only future proposal hashes to `a95a63df...f4d9` and remains inert. It has
no candidate-capable executable, checkpoint, command, token, authorization,
invocation, or result. Candidate evaluations and positive samples remain zero,
both roots remain absent, Rust scientific execution remains locked, G3 has not
begun, and every scientific and physical authority remains false. S4 is closed;
only the separate G2H-E-S5 preflight decision is eligible.

## Gate rule

S4 may build and test proof machinery only on exact identities, manufactured
solutions, synthetic pass/fail fixtures, and source-tabulated comparison data.
It may not evaluate the frozen `shat(0)=6/5` candidate. Candidate roots remain
absent, and no authorization token or decision may be created in this gate.

The first semantic disagreement with the R2 contract is a stop, not permission
to revise the member, equations, grids, thresholds, arithmetic, ordering, or
failure semantics. A genuine contract defect returns to a separately versioned
definition review while preserving R2 unchanged.

## Ordered implementation closure

1. Reproduce the R2 selector, contract hashes, equations, origin recurrence,
   infinity variables, compactification and all ordered duties in a read-only
   definition audit.
2. Implement the primary C++ proof producer in a digest-pinned offline Linux
   image using the frozen Arb/FLINT/GMP/MPFR arithmetic lineage.
3. Implement an independent pure-Rust producer with no primary scientific
   source, C library, GMP, MPFR, FLINT, Arb, image, cache, or output-root reuse.
4. Exercise both implementations only on exact/Minkowski, manufactured ODE,
   origin, infinity, no-fold, positivity, radii-polynomial, chronology and
   corruption fixtures. Every hard failure must terminate at its frozen first
   ordinal and persist an exclusive receipt.
5. Bind source digests, compiler/toolchain identities, dependency trees,
   runtime-image digests, executable digests, commands, environment allowlists,
   expected absent roots and immutable failure-output grammar.
6. Independently replay fixtures and build manifests, verify runtime
   disjointness and prove that the two future candidate roots remain absent.
7. Run the required math registry/validation, all WARP tests, and Casimir adapter
   verification with certificate integrity.
8. If and only if every prior item passes, write an inert versioned primary
   execution proposal. It is not authorization and may not create a token.

## Required fixture classes

- exact Minkowski/zero-field solution and `shat(0)->1` continuation endpoint;
- manufactured smooth nodeless profiles with closed residuals;
- origin even-series accept/reject and conical-singularity rejection;
- infinity mass/tail accept/reject and artificial finite-wall rejection;
- lapse positivity, no-fold, completeness and strict-sign boundary fixtures;
- continuation overlap, orientation, simple-kernel, transversality, terminal
  containment and first-failure fixtures;
- KG mass-gap, self-adjoint-domain and Hadamard-hypothesis fixtures;
- RSET conservation/ambiguity and smeared-noise symmetry/positivity fixtures;
- hash, mutation, path, chronology, stdout/stderr truncation, root-existence and
  alternate-root rejection fixtures.

Candidate-derived values are forbidden fixture inputs.

## Execution boundary

S4 closes only with implementations and evidence ready but inert. A candidate
run requires a later proposal digest, exact checkpoint command, absent-root
proof, explicit user authorization, and a new gate. Primary authorization would
not authorize Rust; independent execution always requires its own later
decision.

All diagnostic, physical, propulsion and transport authority remains false.
