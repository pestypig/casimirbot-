Program gate: G2H-E-S5-A4 — P8P observer progress and turnaround calibration
Workstream: candidate-neutral existing-evidence recovery
Capability or component: bounded guest-attribute readiness observation
Current maturity: local infrastructure correction; no successor execution
Target maturity: independently reviewed readiness handling for a separately bounded recovery
Required frozen inputs: preserved direct-recovery record000050 and immutable terminal evidence
Required evidence: exact-error replay, adversarial tests, composed binding test, independent review
Stop/fail criteria: unrelated API error, missing evidence persistence, deadline exhaustion, unauthorized resource action
Explicit non-goals: numerical execution, candidate evaluation, retuning, evidence deletion, G3/SI/metric/lane work, authority promotion
Downstream gate unlocked: recovery successor preparation only; no scientific gate

# Missing guest-attribute namespace: transport-design correction

The consumed direct-recovery attempt preserved56 hash-linked receipts under
C:/NHM2-CV2-Recovery-v1/evidence. Its first fixture query returned the installed
SDK's exact namespace-not-found HTTP404. The helper was independently observed
TERMINATED afterward; the clone was never attached and no archive was published.
This is not evidence of a guest fixture failure or successful guest execution.

The prior poller admitted successful empty arrays as pending but rejected this
actual SDK representation before its300-second readiness window could elapse.
The successor adapter recognizes only the exact fixture/archive namespace,
account, process_exit_1, exitCode1, confirmed termination and empty stdout.
It requires preserved failure evidence, records an additional pending observation,
and uses the unchanged phase deadline. It does not manufacture a receipt or PASS.
Other404s,403s, malformed responses, process timeouts and persistence failures
remain terminal. General resource reads retain their original behavior.

Independent review found that the first version's default adapter was bypassed
by the complete operation binding. The corrected poller wraps the injected raw
reader explicitly; a composed recoveryOperations test covers that path.
Local tests:40/40 across pending-reader, API-reader, receipt-poller and operation
composition suites. A local replay of the preserved record50 stderr also passed;
its exitCode1 was reconstructed from the recorded process_exit_1 message, not
claimed as a separately recorded field. These are local tests, not cloud success.

The consumed manifest and evidence remain unchanged. No restart, resource
creation, attachment or new dispatch is granted by this document. The approved
one-shot resource exception has been consumed. A successor using the newly
retained helper requires explicit resource/attempt authority; the remaining
aggregate budget alone does not grant that authority. Preparation may continue
locally, but old startup behavior and exclusive guest paths must be examined
before proposing reuse; do not blindly restart the helper.
