# NHM2 Tile-Effective Counterpart Surface Follow-Up

This patch is validation-hardening and blocker retirement work. It is not a validation claim, not a certified transport claim, and not a physical warp-drive promotion.

Permitted claim posture:

```text
NHM2 now has a red-team regional source-closure evidence backbone and is adding tile-effective counterpart surface producers so source-to-geometry divergence can be audited per region.
```

Forbidden claim posture:

```text
NHM2 is validated.
NHM2 has a full warp passing solve.
NHM2 proves a physical GR + quantum transport mechanism.
Casimir negative energy validates the warp metric.
External holography or wormhole papers validate NHM2.
Certificate GREEN overrides review or fail source-closure gates.
```

## Patch Scope

The new tile-effective counterpart artifact attempts to normalize the tile-side tensor surface that must be compared against the metric-required stress tensor:

```text
Delta T_ab(region) = T_ab_metric_required(region) - T_ab_tile_effective_counterpart(region)
```

The controlled regions are:

```text
global
hull
wall
exterior_shell
```

Each region carries tensor authority, source-model provenance, region mask, chart, units, aggregation mode, normalization basis, QEI linkage, and conservation diagnostics. A region can pass only when it is a real `tile_effective_counterpart`, not a `gr_matter_channel_observation`, not a diagonal proxy, and not a metric echo.

## Expected First Outcome

The desired first output may still be blocked:

```text
global: same-basis candidate but diagonal/proxy authority review
hull: tile_effective_counterpart_missing
wall: tile_effective_counterpart_missing
exterior_shell: tile_effective_counterpart_missing
overallState: fail
claimEffect: blocked
```

That is useful progress if it identifies the first divergence boundary precisely. The next source-closure blocker should become one of:

```text
counterpart_missing
metric_echo
basis_mismatch
tensor_authority_insufficient
qei_missing
conservation_missing
residual_exceeded
```

## Research Boundary

External theory papers remain context and guardrails only. They do not validate NHM2, they do not establish the tile-effective source, and they do not permit physical-mechanism language without repository-emitted source, observer, QEI, conservation, and reproducibility evidence.
