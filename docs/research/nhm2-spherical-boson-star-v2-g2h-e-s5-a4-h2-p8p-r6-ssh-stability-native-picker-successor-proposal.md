Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R6 SSH-stability-gated native Windows file-picker successor
Current maturity: frozen transport-only successor; R5 exhausted on SSH loss before guard authentication; exact VM stopped
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R6 record
Required frozen inputs: R5 terminal result, stopped exact R3 VM, verified 16-file staging inventory, unchanged guest ledger/controller/executable/auditor
Required evidence: one restart and browser-SSH surface, bounded SSH round-trip/stability gate, authenticated absence guard, one native picker transaction, exact ingress/reconstruction, one controller invocation, evidence audit and P8Q classification
Stop/fail criteria: first identity, restart, SSH, marker, guard, picker, selection, transfer, reconstruction, build, execution or evidence failure terminal; no retry, fallback, second session/dialog/batch/process or scientific change
Explicit non-goals: SSH trust or metadata mutation, alternate transport, frozen-candidate evaluation, retuning, P=65,536 execution, G3/SI/metric/lane work, evidence deletion or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R6 SSH-stability-gated native-picker successor

Status date: September 1, 2026.

Status: **FROZEN TRANSPORT-ONLY SUCCESSOR / AWAITING SEPARATE BILLABLE-ACTION AUTHORIZATION**.

Change classification: transport procedure and receipt semantics only.
Mathematical semantics and runtime authority are unchanged.

## Narrow causal basis

R5 reached the authenticated `pestypig` prompt but the sole SSH connection
dropped while the prerequisite remote absence guard was being submitted. No
guard marker was observed, so remote destination-path state remains
unauthenticated. The Upload control was not activated and the native file
picker was not tested.

R6 changes only the pre-upload terminal interaction. After one fresh
browser-SSH prompt, it uses one consistent native UI keyboard-control path from
the start, with no preliminary browser accessibility/API typing attempt. It
requires one harmless exact round-trip marker, a fixed 30-second passive
stability interval and then the unchanged remote absence guard. Only both
authenticated responses admit the unchanged R5 native-picker transaction.

This is a transport-stability discriminator, not an optimization and not a
scientific retry. A connection loss at any point remains terminal.

## Preserved authenticated bindings

R6 preserves without change:

- R5 proposal SHA-256
  `650806a579fdf41dbe7bd6731439bf1872644a2f15091c95388e7ec86e6e3b03`;
- R5 terminal result SHA-256
  `f078b95a667a7e54168c5526bf330beef23dd0fb9b2c465542e8e48dd049ef34`;
- corrected R5 result auditor SHA-256
  `33120f4da9b27749870cfd0f4786c70a56c22a1927f62c2bff77157bf27b3f02`;
- R3 chunk manifest SHA-256
  `ec1c115461442e1ddba9ccc635aebdd722be5440f23e06c797406ce8cde6b52d`;
- exactly fifteen ordered base parts totalling 236,492,800 bytes and one
  unchanged 134,656-byte overlay in the verified local staging root;
- original base SHA-256
  `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`;
- overlay SHA-256
  `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`;
- guest ledger SHA-256
  `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96`;
- executable SHA-256
  `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`;
- exactly one candidate-neutral P=1024 controller process on 32 CPUs with a
  14,400-second external timeout;
- phase timing, at most 64 progress lines, all 514 degree buckets, six
  aggregate totals, P8I equality, chronology, neutrality and authority locks;
- unchanged result auditor SHA-256
  `cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`;
- preregistered P8Q trinary classifier.

No candidate-dependent byte, numerical threshold, selector schedule,
precision, equation, observer semantics, reduction order or acceptance rule
changes.

## Exact retained resource and ceilings

R6 may use only:

| Field | Frozen value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM | `nhm2-h2-p8p-r3-c2d-32-20260901` |
| instance ID | `637527339076077505` |
| zone | `us-east1-d` |
| machine | on-demand `c2d-standard-32`, 32 CPUs, 128 GB |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260826` |
| boot disk | retained 30 GB `pd-standard`; no replacement or resize |
| aggregate restart ceiling | 18,000 seconds |
| total R6 ceiling | $9.00 compute plus prorated retained storage |

Before restart, require the exact VM stopped, every resource binding exact, all
sixteen local files unchanged and the R6 evidence root absent. Authorize one
restart only. After running, freshly load the exact instance Details page and
open exactly one new authenticated browser-SSH surface.

## Frozen SSH-stability gate

After the `pestypig` prompt appears:

1. Use only the native UI keyboard-control path for terminal entry. Do not
   first invoke a browser accessibility/API typing action.
2. Enter exactly once:

   ```sh
   printf '%s\n' R6_SSH_ROUNDTRIP_READY
   ```

   Require exact output `R6_SSH_ROUNDTRIP_READY` and the returned prompt.
3. Wait exactly 30 passive seconds without terminal or cloud action.
4. Enter the unchanged absence guard exactly once:

   ```sh
   for f in "$HOME"/h2-p8f-c2-r1-cloud-upload-v1.tar.part{01..15} "$HOME"/h2-p8p-overlay-upload-v1.tar; do test ! -e "$f" || exit 91; done; echo R4_REMOTE_ABSENT_16
   ```

   Require exact output `R4_REMOTE_ABSENT_16` and the returned prompt.

Any missing/duplicate marker, connection change, timeout, prompt ambiguity or
nonzero guard outcome is terminal. No Retry, Troubleshoot, reconnect, second
SSH surface or fallback is permitted.

## Unchanged picker, guest and stop boundary

Only after both stability-gate responses may R6 activate the visible Upload
control once and use one native Windows file-open dialog. It must navigate only
to the exact R3 staging directory, require exactly the sixteen manifest-bound
regular files, select them once and activate Open once. Require exactly sixteen
successful transfer items and exact reconstruction of the original archive.

The one R6 authorization may also install Debian `docker.io` on this temporary
guest if and only if the unchanged ledger's exact presence check proves it
absent. This avoids a second operator stop while changing no source, image,
scientific input or acceptance rule. Load only the archived pinned base images;
build with no pull and no network; authenticate the executable; invoke the
controller once; preserve every outcome; export and audit the one evidence
archive; and stop the VM automatically.

First failure is terminal. No resource creation, second restart, second picker,
additional upload, retry, rescue, evidence deletion or alternate transport is
authorized.

## P8Q and authority boundary

Only a complete authenticated calibration may produce
`P8Q_YES_PROPOSAL_READY` or `P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`. Any failure,
timeout, missing archive or incomplete audit produces
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. No result authorizes P=65,536.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.

## Exact authorization text

> I authorize exactly one H2-P8P-R6 SSH-stability-gated native Windows file-picker P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Restart only the existing stopped Google Compute Engine VM `nhm2-h2-p8p-r3-c2d-32-20260901`, instance ID `637527339076077505`, in project `dark-stratum-455714-h4`, zone `us-east1-d`, preserving its exact `c2d-standard-32` machine, Debian image and 30 GB `pd-standard` disk, under an 18,000-second aggregate restart ceiling and a $9.00 total ceiling. Require the exact resource and sixteen local staged files unchanged and the R6 evidence root absent. Open exactly one fresh authenticated browser-SSH surface as `pestypig`; use only native UI keyboard control; require exactly one `R6_SSH_ROUNDTRIP_READY` command and response, exactly 30 passive seconds, and exactly one unchanged absence guard returning `R4_REMOTE_ABSENT_16`. Only then use exactly one native Windows file-open dialog to upload exactly the sixteen authenticated candidate-neutral staging files in one batch and require sixteen successful transfers plus unchanged archive reconstruction. If and only if the unchanged guest ledger proves Debian `docker.io` absent, install it once; then run unchanged guest ledger SHA-256 `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96`, require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`, and execute exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout. Preserve all outcomes, automatically stop the VM, audit the evidence and apply only the frozen P8Q rule. First failure is terminal. I do not authorize retrying R5 or R6; a second SSH surface, restart, marker, guard, dialog, upload batch, VM or numerical process; browser API typing before the native command path; alternate transport; resource creation, replacement, resize or rescue; SSH trust, metadata, IAM, firewall, Chrome or security-setting mutation; additional upload; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or any authority promotion.
