Program gate: G2H-E-S5-A4 — P8P observer progress and turnaround calibration
Workstream: candidate-neutral R39 evidence recovery
Capability or component: observable selected-device discovery diagnostic
Current maturity: local pure diagnostic core tested; live executor not implemented or authorized
Target maturity: reviewed diagnosis plan, then separately frozen bounded read-only execution
Required frozen inputs: consumed v2 manifest/88 receipts, exact helper and attached clone, unchanged partition selector
Required evidence: exact command/version/inventory bytes, closed failure codes, resource and kernel read-only identity, verified stop
Stop/fail criteria: missing provenance, substituted device, non-read-only state, command or output limit, uncertain stop
Explicit non-goals: VM action during preparation, mounts, archive reads, retries, new resources, candidate/numerical work, retuning, deletion, G3/SI/metric/lanes, authority promotion
Downstream gate unlocked: evidence-backed discovery diagnosis only

# Goal and place in the larger plan

This is local preparation following the consumed retained-v2 recovery. It does
not replace the canonical work program or make a new scientific gate. The path
remains recovery of R39 evidence, build-only fixture closure, then the one
authorized P=1024 calibration and unchanged P8Q decision. No P=65536 observation
or selected mini-boson-star evaluation is admitted here.

The immediate aim is to stop losing actionable error information. The last
guest failure only reported ValueError before mount. Synthetic local evidence
shows that a flat disk list is rejected by the existing nested partition
selector; it does not establish that the guest actually returned that list.

# Prepared local core

tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_discovery_diagnostic.py
has no command execution, cloud access, mount, file-writing or CLI behavior.
It plans three distinct read-only observations of one already-resolved device:
lsblk version, the exact consumed inventory command, and the same command with
explicit --tree. The comparison is planned, not a retry or relaxed acceptance.

It preserves each inventory's exact bytes/base64, byte count, SHA-256, argv and
specific selection failure code using the unchanged consumed select_partition.
Each inventory is capped at8192bytes, version at256bytes, total encoded receipt
at24576bytes. Duplicate JSON keys, malformed encoding/nonfinite constants,
invalid shapes, wrong devices, writable disks/partitions and ambiguous supported
filesystems cannot become parser acceptance. Parser acceptance is explicitly
not mount, recovery or scientific authorization.

Nine local tests pass. A first deep-nesting test expected a decoder error but
the installed Python parsed it and rejected its top-level shape instead. The
test was corrected to require rejection under either bounded failure path;
the classifier was not loosened. These tests do not run lsblk on Linux.

# Required live-executor contract — not yet execution-ready

Use only the retained helper2570241336417567358 in us-east1-b and exact
attached read-only clone6517758864936518301. Do not use the old detached-clone
preflight unchanged. Fresh read-only API observations must confirm stopped
helper, exact boot/snapshot/clone provenance and attachment mode. Original
source VMs/disks remain untouched. Account/project must match the dedicated SDK.

Before any restart, the executor must bind a diagnostic-only startup that
cannot launch the previous recovery code. Startup replacement and one helper
restart require a separate bounded authorization; none is granted by this
plan. No attachment, detachment, mount, archive read, Docker start, new resource
or filesystem repair is part of this diagnostic.

Before and after the three observations, require the fixed alias to resolve
to the same actual block device, authenticate device numbers and verify kernel
read-only status using read-only descriptors/ioctl. Require all partitions
and relevant mountpoints unmounted. Do not infer guest device identity solely
from a plausible /dev path or cloud READ_ONLY label.

Use exact argv without shell interpolation, per-command output/time limits and
an aggregate deadline with cleanup reserve. Preserve exit status, bounded
stdout/stderr, truncation/timeout flags, invocation order and partial failures.
First execution error is terminal; do not run a later observation after it.
A baseline parser rejection is diagnostic data, not a command error; the
preplanned explicit-tree comparison may follow a successful bounded baseline
command. If the receipt cap is exceeded, preserve the failure, not a silently
truncated JSON inventory. The pure core does not itself enforce these executor
requirements or authenticate cloud observations.

Use a fresh attempt-bound receipt namespace and exclusive local capture.
Authenticate API helper identity, attempt, chronology and raw byte hashes;
test full maximum-size export against the chosen transport before dispatch.
Guest-attribute transport is not independent scientific replay. Preserve
failure-shaped receipts as failures without passing them to the archive-success
validator. Confirm helper stop independently on every outcome.

# Review the rest of recovery before requesting another full recovery

1. Device discovery: actual lsblk version/shape, bounded device readiness,
   supported unique partition and stable kernel block identity/read-only state.
2. Mountpoint: use a fresh attempt-bound path; the old shared
   /mnt/nhm2-hostkey-attestation path must not be blindly reused or deleted.
3. Mount: retain exact ro,noload or ro,norecovery, no external filesystem helper,
   stable device identity, and matching kernel mountinfo recovery-disabled flags.
4. Read: unchanged no-follow descriptor traversal and exact12122-byte archive
   SHA-25673029fde08f14f9fcd01490c4e5d5bc188213eaa2f9a8d857eb5e456b86d0922.
5. Cleanup/export: exact unmount verification, bounded authenticated receipt,
   exclusive local publication and independently confirmed stop on all paths.

Do not fold these mount/read operations into the diagnostic. Establish which
discovery defect the data supports, then review the minimal recovery correction
as a whole. Do not alter scientific code or weaken a safety check to get PASS.

# Authority and remaining work

Current conservative charter allowance is10.40USD/9600seconds, not a renewed
execution grant or actual billing figure. No new cost reservation was made.
The next preparation is a complete minimal executor/export/receiver binding
with local failure-path review, followed by one consolidated authorization
request. Do not present this pure diagnostic module as an executable packet.

Independent review authenticated the88-record predecessor chain, reran all9
new tests and cleared the pure core, final caps and this plan for the local
preparation milestone. All8 existing Linux-parser regression tests also pass.
No critical issue was identified for this local milestone. This does not
establish live guest behavior or clear the unimplemented execution binding.
No cloud operation or scientific change occurred during this preparation.

## Local controller and receiver checkpoint — September 7

The diagnostic lifecycle core is now implemented in
h2_p8p_discovery_controller.mjs, with19 local synthetic lifecycle tests passing.
It has no live SDK binding or CLI. Its closed phases contain no creation,
attachment, mount, build or recovery operation. It requires the pinned helper
and clone, startup verification, a running-state observation, diagnostic
validation and independently confirmed stop. Adapters remain responsible for
actual authenticated observations, cancellation and process deadlines.

Independent review identified and prompted fixes for late-result acceptance,
expired cleanup deadlines and loss of confirmed-stop evidence on persistence
failure. Emergency cleanup is bounded to60seconds and always marks an expired
attempt failed; it is safety handling, not an extension for ordinary work.
The terminal event is explicitly provisional and cannot certify completion.
A durable outer binding must separately accept the returned status and its
timing after persistence; that binding is not yet implemented.

The pure h2_p8p_discovery_receiver.py passes6 tests. It decodes and rehashes
exact bytes, reconstructs commands and classifications with the unchanged
parser, and binds expected helper/attempt/resolved-device identity. It preserves
rejected partition selections as diagnostics while rejecting altered fields,
false permissions, duplicate keys, forged hashes and noncanonical encodings.
It is not independent scientific replay and does not itself authenticate API
provenance. Partial command-failure export still needs its execution binding.

Remaining work is the real guest device/command adapter, bounded export, fresh
attached-clone cloud preflight/startup binding and durable outer acceptance.
These must be reviewed together before any frozen execution proposal. Existing
recovery controllers and frozen packages remain untouched. No cloud calls,
new costs, numerical work or live restart occurred at this checkpoint.

## Guest adapter checkpoint — September 7

h2_p8p_discovery_guest.py now implements an unbound read-only Linux adapter:
fixed-alias whole-disk and partition identity checks, BLKROGET checks, capped
sysfs/mountinfo reads, and a before/after device-state comparison. It exposes
no CLI or automatic startup. It never mounts, reads an archive or launches
Docker/numerical code. Cloud and instance provenance must still be supplied
by the future authenticated outer binding.

Command capture admits only the three planned lsblk commands, uses a private
process group with a10second work deadline and bounded cleanup, caps stdout
at256/8192bytes and stderr at512bytes, and preserves partial output, exit code,
failure and reap/group state. Later commands do not run after failure.
The outer worker must enforce aggregate cancellation, including potentially
blocking device probes; no such bound is claimed from a synchronous function
alone. Actual Linux ioctl, selector and process-group behavior remains untested
on this Windows host.

Ten injected local tests pass. Review prompted adding the final read-only
device check even on command failure, with separate post-probe failure codes
that do not overwrite the original failure. A maximum-output test establishes
that the full envelope exceeds24KiB; it now has an explicit64KiB local cap.
It must not be passed to the old24KiB transport unchanged. The initial export
plan proposed segmentation; the verified platform limit below permits a simpler
single-value successor instead. No export or cloud execution has occurred.
The diagnostic core's24KiB cap still applies
to its smaller nested receipt; it is not a bound on the full guest envelope.

Remaining execution readiness: actual guest smoke/failure-path validation
under an authorized bounded environment, aggregate worker/export supervision,
transport receiver binding, attached-clone cloud preflight and durable outer
acceptance. Do not request a live recovery on the basis of injected tests.

## Selected single-value transport — September 7

Official Google documentation was checked during preparation:
https://docs.cloud.google.com/compute/docs/metadata/manage-guest-attributes
It specifies256KiB per guest-attribute value,128bytes per key,10 queries per
minute and a burst limit of3 queries per second. Therefore the old24KiB local
receipt cap is not a Google platform maximum. No frozen older cap was changed.

The selected h2_p8p_discovery_transport.py wraps at most65536 envelope bytes
in one canonical hash-bound value capped at90112bytes, below256KiB. It binds
the full attempt namespace, pinned helper, raw byte count, SHA-256 and canonical
base64. The receiver reconstructs these fields exactly and rejects missing,
duplicate, stale, altered or oversized data. Seven local tests pass, including
injected guest-to-transport-to-nested-receiver integration and partial failure
preservation. Independent review cleared the initial five-test core.

The earlier pure segmentation prototype and its seven tests are retained as
local exploratory work; no segmented publisher is selected or executed. The
single-value choice avoids eleven-chunk writes and partial-segment recovery.

This is still pure transport integrity, not acceptance of all guest assertions.
The live binding must authenticate API resource/attempt/chronology, use a fresh
namespace, cap the full API JSON response with sufficient overhead for the
roughly88KiB value, and supervise one bounded publication. Schedule queries
below the documented rate limits rather than copying the older5second poll.
Any process on the guest can write guest attributes; they do not supply
independent scientific or application-user authentication. Full envelope
semantics, original resource provenance and confirmed stop remain mandatory.
No HTTP publisher, cloud query, restart or numerical execution ran in this work.

## Continuing full-goal implementation — September 7

The full goal remains active through a reviewed execution package, separate
cloud authorization, bounded live diagnostic and evidence-based next recovery
decision. Component tests do not close that goal.

h2_p8p_discovery_publish.py now provides an explicit single-use publication
worker. It validates the envelope, reads only the fixed metadata instance ID,
then permits one PUT to the attempt-bound namespace. Proxy use and redirects
are disabled; status, response headers and response size are checked. First
failure consumes the worker. Six injected tests PASS and independent review
cleared this scoped interface. It has not made a real network request. Socket
inactivity timeouts do not replace the still-required outer wall-clock kill/reap
supervisor, which must close stdin and preserve ambiguous publication failure.

h2_p8p_discovery_accept.py now checks full envelope consistency: pinned attempt,
known failure phases/codes, read-only device-family observations, command order
and output bounds, no commands after failure, and stable before/after state for
success. It cross-checks nested diagnostic bytes against actual command records.
Eight local tests PASS after review identified and prompted a cross-link from
each accepted partition path/device number to the guarded kernel family,
excluding the whole disk. A complete observation can legitimately contain parser
rejection; guestSucceeded means the diagnostic procedure completed, not that
partition selection, build, recovery or science passed. API provenance and
durable acceptance remain separate requirements.

Remaining end-state requirements, all still open:

1. Supervise the diagnostic and publication workers with aggregate deadlines,
   bounded stdin/stdout/stderr, private process cleanup and partial-failure
   persistence; verify the real installed runtime behavior where available.
2. Bind the existing attached read-only clone and latest stopped helper to
   fresh cloud preflight, diagnostic-only startup and automatic/independent stop.
3. Bind the larger single-value API capture, rate-limited observation, full
   envelope acceptance and exclusive durable evidence store.
4. Independently review and freeze the complete source/command/resource/budget
   package; request one consolidated bounded authorization. No restart yet.
5. Execute only after authorization, verify native Linux behavior and preserve
   all results, including failure, before deciding the next recovery step.

No milestone above is satisfied merely by this document. No cloud action or
new cost reservation has occurred during this implementation turn.

## Worker supervision implementation — September 7

Added the explicit observation entrypoint and h2_p8p_discovery_worker.py. Its
closed roles run only the observation child or single-use publication child.
The POSIX engine drains capped stdout/stderr while writing bounded stdin
nonblocking, closes input, enforces60/20second work deadlines and a5second
cleanup wait, and records partial output and cleanup uncertainty. Seven mocked
I/O tests PASS; independent review cleared the worker interface only. Initial
test harness syntax and missing Windows SIGKILL mocking were corrected. No
native Linux behavior was tested or inferred from those mocks.

DiscoverySupervisor connects exactly one observation to evidence recording,
full-envelope consistency validation and exactly one publication with exact
acknowledgment checking. Its100second local budget refuses a worker when its
complete work/cleanup window will not fit. Six injected composition tests PASS;
review prompted rechecking that window after intent persistence as well as
before it, so slow recording cannot admit a worker without its cleanup reserve.
An observation timeout/nonzero/cap error preserves captured output and prevents
publication; local/serial capture binding is still required for those failures.
Its provisional terminal event is not durable outer acceptance.

Critical containment prerequisite: the diagnostic's lsblk child creates its
own session. A parent process-group kill is NOT whole-tree cleanup. Before
execution, the final startup must bind a verified transient systemd service
with a fixed runtime limit and KillMode=control-group (or another separately
reviewed equivalent). Debian Bookworm documentation supports the proposed
systemd-run --wait/--pipe, exec service type and control-group kill semantics:
https://manpages.debian.org/bookworm/systemd/systemd-run.1.en.html
https://manpages.debian.org/bookworm/systemd/systemd.kill.5.en.html
No service was created. Exact unit/startup commands, native containment checks,
bounded local/serial receipt capture, cloud resource binding and VM shutdown
are unfinished. This is a tracked safety gap, not a new completed milestone.
The full goal remains active; no new authorization has been requested yet.

## Exclusive guest journal preparation — September 7

h2_p8p_discovery_journal.py adds a bounded supervisor-event recorder:
16 exclusive records, 128KiB per record and 512KiB aggregate. Records bind
sequence and predecessor SHA-256, flush and fsync before returning, and sync
the directory on POSIX. Write failures poison the writer and preserve partial
files without retry or overwrite. Four local tests pass. Windows tests do not
establish native Linux durability. Independent review and startup integration
remain open, as do serial export, whole-tree containment and cloud lifecycle.
No cloud or numerical execution occurred. The full goal remains active.

The journal is now connected to the supervisor by the explicit run_session
function. It validates the attempt before creating the exclusive capture,
persists begin/end records, and distinguishes journal completion from successful
publication or authority. Two new disk-backed/injected-worker integration tests
pass, alongside six existing supervisor tests: successful publication capture
and durable partial timeout output with no publisher dispatch. Reusing the
capture path fails before another worker runs. Independent review is requested.
This remains an unbound callable, not an executable cloud package; containment,
serial export, startup and independent VM stop still require implementation.

## Containment policy binding preparation — September 7

Independent journal review identified a missing parent-directory sync after
exclusive root creation. The constructor now syncs the trusted parent before
accepting records; a constructor-sync failure leaves the root preserved and
unusable for retry. Journal tests now5PASS.

The new pure containment module fixes an attempt-bound transient service with
120second runtime, 10second stop timeout and control-group SIGKILL policy. Its
observed-property guard requires exact unit/main PID and unified cgroup-v2
membership, rejecting foreign membership or substituted service settings.
Three synthetic tests pass. The guarded entrypoint and actual service dispatch
remain unfinished; no Linux service behavior is inferred from these tests.
Observed containment policy explicitly does not mean cleanup was confirmed.
Independent review of this binding is requested. The complete discovery test
discovery reports82PASS, including six duplicated imported supervisor tests.
No cloud action or additional authorization has occurred.

## Guarded entry implementation — September 7

Added the explicit guest entrypoint that captures a bounded systemctl observation,
checks its own cgroup-v2 membership and exact main PID, and only then admits
the session. Failed guard output remains in its bounded report; no diagnostic
worker follows a failed guard. Three injected tests pass. Independent review
also prompted explicit Restart=no in both service creation and observation.
The entire local discovery suite now reports79 unique tests PASS after removing
duplicate imported-TestCase collection. No actual service or cloud operation
ran. The outer startup must still capture this report and journal, verify
service termination and stop the VM; none of those duties is implied by a
successful containment-policy observation. Complete package review remains open.

## Raw failure-export framing — September 7

Added bounded raw export encoding/decoding for at most16 journal records,
512KiB total record bytes and a32KiB guest report. The canonical base64/hash
wrapper fits its768KiB cap even at maximum input. Four tests pass, including
preservation of incomplete JSON records, maximum-size round trip and rejection
of altered names/attempts. This preserves failure bytes without treating them
as valid journal receipts. File collection, serial framing/capture and service
termination binding remain unfinished; no actual serial/cloud export occurred.
Independent review cleared the preceding guarded-entry and Restart=no checks
for their local scope, not native containment or whole-package execution.

## Outer lifecycle and serial composition — September 7

Added fixed-path POSIX journal collection with no-follow descriptors, regular
file/size/stability checks and bounded inventory. Native collection is not yet
tested. Added attempt-bound numbered serial frames with exact reconstruction;
missing, duplicate, reordered or altered frames fail validation. Maximum export
plus frame overhead fits below1MiB in the local test, not a guarantee that an
unobserved cloud serial buffer retains it alongside other output.

Startup composition now requires unit absence before dispatch, stops the service
before collecting journal bytes, preserves ambiguous dispatch failure, and calls
poweroff even after export failure. Its native deadline, serial and shutdown
adapters remain unbound. The outer base64 report requires64KiB rather than32KiB;
the unfrozen export cap is now800KiB, with maximum-input roundtrip tests updated.
All90 local discovery tests pass. Independent review requested for these changes.
No service, serial device, VM or cloud API was used. Full goal remains open.

## Native adapter preparation and failure-path repair — September 7

Review found stop/collection exceptions could bypass export. They now preserve
a separate cleanup failure and still attempt summary-only export followed by
poweroff; two regressions pass. Collector now opens the attempt directory with
O_NOFOLLOW before opening its evidence child relative to that descriptor.

Added explicit native service adapters with fixed commands and bounded capture,
stop-state plus empty-cgroup verification, nonblocking bounded serial writes and
a bounded poweroff request. Five injected adapter tests pass; all97 discovery
tests pass. No native adapter was invoked. Serial queue drain before poweroff,
aggregate startup bounds and the full cloud binding still need review/binding.
A successful poweroff request is not independently confirmed VM termination.

## Native stop observation and transmit drain repair — September 7

Absence now requires exact successful systemctl-show output, not an unverified
exit4 exception. A nonzero stop command no longer prevents independent bounded
state/cgroup observation; both captures remain in the native stop receipt for
the outer export binding. Serial output now calls tcdrain under the remaining
90second deadline, restoring its signal handler/timer on success or failure.
It refuses an existing interval timer, so aggregate startup supervision must
be external rather than a conflicting Python alarm. Seven injected native tests
pass; real tty drain behavior remains unverified. No native/cloud execution.

## Guest boot composition — September 7

Added explicit Python boot entry binding startup to native adapters and journal
collection. Stop command/observation receipts now enter the serial export with
bounded captures; an integration test verifies their presence. A Bash wrapper
places this entry under an external330second timeout with10second kill grace
and an EXIT poweroff request bounded by8seconds plus2second kill grace. This
does not conflict with the serial-drain interval timer. Git Bash syntax-only
validation passed; neither the shell nor native entry was executed. Startup
tests7PASS. Independent review requested. Source authentication/extraction,
cloud lifecycle/serial retrieval and final receipt acceptance remain unfinished.

## Authenticated source package construction — September 7

Added read-only deterministic package construction with an explicit23-file
inventory, source byte counts/SHA-256 and attempt identity. Generated bootstrap
authenticates every source before exclusive extraction, fsyncs files/directories
and invokes only the diagnostic boot wrapper. Shutdown protection precedes
extraction; the outer bootstrap has a360second timeout plus10second grace.
Two package tests pass. Generated Python was compiled for syntax only and
generated Bash passed syntax-only validation; neither was executed. The package
is not frozen or uploaded. Independent source-closure review requested. Cloud
preflight/startup replacement, serial retrieval and durable acceptance remain
to be connected before requesting the consolidated execution authorization.

## Attached-clone historical and preflight binding — September 7

Authenticated the actual preserved88-record consumed-v2 chain and prior chains,
including its exact terminal hash, startup/restart/attach/stop actions, stopped
helper2570241336417567358 and attached read-only clone6517758864936518301.
These are historical observations, not a fresh cloud status claim.

Added fresh-read preflight composition for the helper, clone, boot, snapshot,
original source VM/disk and original build VM, comparing them to authenticated
history and requiring stopped/ready states plus the exact read-only attachment.
Two local preserved-evidence tests pass, including rejection of detached/writable
clone or changed source state. Independent review requested; no cloud reads ran.
The guest source package review passed locally. Mutating cloud operation binding,
serial retrieval and durable acceptance are still unfinished.

## Bounded cloud read adapter — September 7

Added an unexecuted SDK read adapter for this larger package. Helper descriptions
allow256KiB because the153844-byte startup alone exceeds the old128KiB response
cap. Diagnostic guest attributes allow128KiB and serial JSON2MiB. Captured output
is preserved in bounded indexed base64 records rather than one oversized event;
failed-process output is retained. Two injected tests pass. No API call occurred.
Independent review requested. Polling/storage aggregate limits and complete
operations/acceptance still need binding; these caps do not authorize execution.

## One-shot command adapter — September 7

Added an unexecuted startup/restart/stop-only SDK adapter with exact short-path
startup hashing, one-use action consumption and bounded process/output capture.
Stop dispatch remains possible when recording fails. No create/attach/delete
operation is admitted. Read review prompted exact cleanup scope, preserved
exception exit identity and a total5second post-process persistence budget;
an obtained cleanup observation remains available with receiptFailure when
persistence fails. Six injected command/read tests pass. Independent review
requested; operations composition, durable storage and final acceptance remain.

## Cloud lifecycle composition — September 7

Connected preflight, startup hash verification, one start, running chronology,
bounded serial observation and independent stop. At most six serial observations
are planned with60second waits and explicit remaining-time checks. The validated
receipt reference is compact; large raw bytes stay in the read adapter's bounded
records. Two composed tests pass using preserved history and injected cloud
responses, including incomplete serial observation followed by confirmed stop.
The real receipt validator and durable execution store are still unbound;
injected success is not a live diagnostic result. No API requests occurred.

## Serial audit and observation-window repair — September 7

Review found six polls ended before the360second startup allowance. The bounded
window now permits eight reads separated by60seconds (420second horizon), while
retaining operational-deadline checks and cleanup reserve. Added local raw-serial
audit linking frames, export, journal chain, guarded session, containment policy,
observation and publication acknowledgment. Three failure/rejection tests pass.
Successful-path integration and independent review remain open; this validator
is not yet the bound execution callback. No cloud call or authority promotion.

## Successful audit path and durable local store — September 7

Audit review identified missing worker/stop capture consistency checks. Added
exit/failure/cleanup flags, output caps, worker identities/input lengths and
captured stop-state validation. A complete synthetic journal/session/serial
fixture passes; rebuilt-hash failed-worker and stop-observation mutations are
rejected. Audit tests6PASS. This does not constitute live guest evidence.

Added exclusive serialized local evidence storage:1024 records,128KiB each,
32MiB aggregate, fsynced files, hash links and poison-on-write-failure. Two actual
local filesystem tests pass; temporary test evidence was retained. Independent
review requested. The validator process bridge and frozen runner remain open.

## Installed-Python audit bridge — September 7

Store creation now rejects symlink/junction ancestors. Added a one-use audit
bridge with exclusive hash-bound serial input, bounded installed-Python process
and retained stdout/stderr on failure. An actual local negative audit invocation
passed its test by rejecting incomplete evidence, preserving the input/result
and refusing another invocation. Two store tests also pass. This is a local
parser execution, not a guest diagnostic or scientific calculation. Independent
review requested; complete runner/freeze and live authorization remain open.

## Complete runner assembly under review — September 7

Assembled manifest/source closure, store, operations, audit bridge, publication
API hash comparison and independent stop.35 JavaScript and106 Python testsPASS;
no prepare/execute function invoked. Whole-package review found three remaining
runner issues: shared monotonic timing, ledger/authorization budget binding and
bounded final persistence. They must be repaired before freeze. The proposed
1200second/0.10USD execution is not authorized; no cloud action occurred.

## Runner admission corrections — September 7

Replaced wall-clock runtime accounting with one shared monotonic-derived clock.
Manifest now binds the current ledger hash, and execution requires a separately
hash-bound authorization record naming exact manifest/ledger/helper/scope/time
and ceilings. Aggregate arithmetic and enough remaining validity for1200seconds
are checked. Initial/final persistence now has bounded waits and post-write
deadline checks. Four admission/manifest tests pass; review requested. Neither
authorization nor execution/preparation files have been created. No cloud work.
