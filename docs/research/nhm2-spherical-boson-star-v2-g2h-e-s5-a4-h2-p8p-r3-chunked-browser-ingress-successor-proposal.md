Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R3 deterministic chunked browser-ingress successor
Current maturity: R2 exhausted at zero-item 236,492,800-byte browser upload; no guest or numerical process ran
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R3 record
Required frozen inputs: R2 result, immutable base/overlay, 15-chunk manifest, local materializer, guest ledger, unchanged P8P controller/executable/auditor
Required evidence: exact chunk derivation, one exact VM, one 16-file browser batch, guest reconstruction equality, one controller invocation, serial evidence, independent audit and P8Q classification
Stop/fail criteria: first materialization, allocation, transfer, reconstruction, build, execution or evidence failure terminal; no retry, fallback, second batch/process or scientific change
Explicit non-goals: frozen-candidate evaluation, retuning, P=65,536 execution, G3/SI/metric/lane work, evidence deletion or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R3 deterministic chunked browser-ingress successor

Status date: September 1, 2026.

Status: **FROZEN TRANSPORT-ONLY PREEXECUTION PROPOSAL / NO R3 RESOURCE, UPLOAD OR PROCESS CREATED**.

## Result-conditioned change

R2 authenticated the exact `c2d-standard-32` resource and browser-SSH guest,
but the browser rejected the first 236,492,800-byte archive with
`Transferred 0 items`. The overlay, guest ledger, Docker, build and P=1024
calibration were never reached. R2 is exhausted.

R3 changes only archive ingress. The same authenticated base bytes are divided
at fixed 16 MiB boundaries into fourteen 16,777,216-byte parts and one
1,611,776-byte tail. The 15 parts plus the unchanged 134,656-byte overlay are
selected in one browser-upload batch. The guest authenticates every part,
concatenates them in registered order, and requires exact equality with the
original 236,492,800-byte SHA-256 before the unchanged R2 extraction and
controller sequence begins.

This is not compression, content transformation, mathematical retuning,
numerical retry or scientific change. It is a bounded transport successor to a
zero-byte preexecution failure.

## Frozen local derivation

| Object | Bytes | SHA-256 |
| --- | ---: | --- |
| base archive | 236,492,800 | `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978` |
| overlay | 134,656 | `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e` |
| chunk manifest | 3,014 | `ec1c115461442e1ddba9ccc635aebdd722be5440f23e06c797406ce8cde6b52d` |
| materializer | 3,786 | `a1f7532d6adb39fa1aed42301df19a3067ec276b913ffc3c5f52d2369b6a3003` |
| guest ledger | 4,991 | `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96` |

The manifest path is
`tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r3_base_chunk_manifest_v1.json`.
It binds exactly 15 ordered names, byte counts and SHA-256 values, totals
236,492,800 bytes, and carries the all-false authority object.

The materializer path is
`scripts/nhm2_g2h_e_s5_c08_h2_p8p_r3_materialize_chunks.py`. After separate
authorization, it may run exactly once against an initially absent staging
directory. It rehashes the base and overlay, writes the 15 parts with exclusive
creation, stages one exact overlay copy, and requires exactly 16 output files.
It does not alter or delete the source archives. A materialization mismatch is
terminal and no cloud resource may then be created.

## Exact resource binding

After separate explicit authorization, exactly one creation attempt may use:

| Field | Frozen R3 value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM | `nhm2-h2-p8p-r3-c2d-32-20260901` |
| zone | `us-east1-d` |
| machine | on-demand `c2d-standard-32`, 32 CPUs |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260826` |
| boot disk | exactly 30 GB `pd-standard`, auto-delete |
| aggregate VM ceiling | 18,000 seconds |
| total ceiling | $9.00 compute plus prorated storage |
| numerical timeout | 14,400 seconds |

Before Create, require the exact R3 VM name absent and the exact tabled values
visible. One submission is permitted. A rejected or drifted resource is
terminal; no second zone, regional allocator, alternate family, disk/image
change, reservation, retry or fallback is permitted.

## One browser batch and guest reconstruction

Only after the exact VM is running, connect through authenticated Compute
Engine SSH-in-browser as `pestypig`. Require all 16 staged files absent in
`/home/pestypig`, then select exactly the complete 16-file staging directory in
one browser-upload batch. No single unsplit base archive is uploaded. Any
failed, partial, duplicate or extra transfer is terminal; do not use Retry.

After the ordinary action-time confirmation for possible Debian `docker.io`
installation, enter the exact guest ledger once through one literal `bash -s`
heredoc. The ledger authenticates all 15 part identities and the overlay,
requires the reconstructed base path initially absent, concatenates in index
order, and requires original byte/hash equality before Docker admission.

The remaining sequence is unchanged: base then overlay extraction, exact
11-entry source-manifest replay, candidate-neutral and all-false authority
guards, exact controller identity, no-pull/no-network build, executable
SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`,
and exactly one candidate-neutral P=1024 process on 32 CPUs.

## Evidence and P8Q

Preserve phase-specific selector/observer/total timing, at most 64 progress
lines, all 514 degree buckets, six aggregate totals, P8I equality, chronology
and neutrality locks. After PASS, FAIL, timeout or partial output, export one
deterministic evidence archive through stopped-VM serial output and stop the
VM. No restart or disk rescue is authorized by R3.

Authenticate the serial archive locally and run the unchanged independent
result auditor SHA-256
`cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`.
Only an authenticated calibration may produce
`P8Q_YES_PROPOSAL_READY` or `P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`.
Any unauthenticated or incomplete result produces
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. No outcome authorizes P=65,536.

## Authority locks

Candidate evaluations and positive samples remain zero. No candidate or
scientific root, token or handler may be created. No G3, SI, metric, lane,
replay or lamp work is permitted. Candidate, proof, geometry/state, lane,
lamp, physical, propulsion and transport authority remain false.

## Exact authorization text

> I authorize exactly one H2-P8P-R3 deterministic chunked browser-ingress P=1024 turnaround calibration under the final independently audited R3 proposal SHA-256. Materialize exactly the 15 manifest-bound base-archive chunks plus one unchanged overlay into the initially absent frozen local staging directory using materializer SHA-256 `a1f7532d6adb39fa1aed42301df19a3067ec276b913ffc3c5f52d2369b6a3003`; require all 16 files exact. Create exactly one temporary on-demand Google Compute Engine VM `nhm2-h2-p8p-r3-c2d-32-20260901` in project `dark-stratum-455714-h4`, zone `us-east1-d`, using exactly `c2d-standard-32`, exact image `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`, and exactly 30 GB `pd-standard` storage, with an 18,000-second aggregate runtime ceiling and a $9.00 total compute and prorated-storage ceiling. Through authenticated Compute Engine SSH-in-browser, upload those exact 16 files in one batch to initially absent paths; require all 15 chunks and the overlay exact; reconstruct and authenticate the original 236,492,800-byte base archive; install only Debian `docker.io` if absent after the required action-time confirmation; run exact guest ledger SHA-256 `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96`; require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`; and execute exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout. Preserve PASS, FAIL, timeout, preexecution failure or partial output; automatically stop the VM; recover only the one hash-bound serial archive; run the unchanged independent result audit; and apply only the preregistered P8Q rule. First failure is terminal. I do not authorize retry, browser Retry, fallback, a second upload batch, VM, zone, creation attempt or numerical process, resource substitution, additional upload, scientific change or retuning, frozen-candidate evaluation, positive sampling, candidate/scientific root, token or handler creation, evidence deletion, VM restart or disk rescue, P=65,536 execution, G3/SI/metric/lane work, IAM/firewall mutation, or any candidate, proof, geometry/state, lane, lamp, physical, propulsion or transport authority promotion.
