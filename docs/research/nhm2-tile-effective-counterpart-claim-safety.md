# NHM2 Tile-Effective Counterpart Claim Safety

A tile-effective counterpart is not any tile-side number. It is the tensor quantity compared to the metric-required stress tensor in the same chart, units, profile, region mask, aggregation mode, and normalization basis.

`gr_matter_channel_observation` may be measured and useful, but it is diagnostic only until it is transformed or derived into a `tile_effective_counterpart` with explicit source-model provenance.

A metric echo is forbidden as source closure. Copying `G_ab[g] / 8 pi` into the tile side proves only that the metric-required tensor can be restated. It does not show that a source supports the metric.

Full tensor evidence is required for promotion-sensitive source legitimacy. Diagonal proxy evidence remains reduced-order diagnostics.

QEI/QFT evidence is required before physical-mechanism language. Negative energy is not a permission slip.

Experimental mathematical or AI-assisted formula discovery cannot be treated as predictive physics unless it is converted into a derivation, tested against controls, and bound to primary literature or emitted artifacts. Hypothesis-only or numerically checked formulas may use words such as `suggests`, `diagnostic`, `candidate`, `bounded under declared assumptions`, and `requires validation`; they must not use `validated`, `predicts`, `proves`, or `physically realizes`.
