Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R26 authenticated local-gcloud regional execution successor
Current maturity: P8P implementation/equality proof complete; dedicated Google Cloud SDK authentication complete; P=1024 turnaround calibration unexecuted
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and frozen P8Q decision, or one immutable terminal R26 record
Required frozen inputs: R25 authentication result, exact R16 four-member archive, R26 local controller, unchanged guest ledger, executable and P8Q auditor
Required evidence: local account/project identity, preexecution inventory/quota, one regional request, exact VM/resource bindings, one SCP, one SSH handoff, serial evidence, automatic or failure stop, independent audit and P8Q classification
Stop/fail criteria: first identity, quota, allocation, resource, transport, build, execution, recovery or audit failure terminal; bounded exact-name cleanup only; no retry or fallback
Explicit non-goals: candidate evaluation, positive sampling, retuning, P=65,536 execution, G3/SI/metric/lane work, evidence deletion, resource substitution or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R26 authenticated local-gcloud successor

Status date: September 3, 2026.

Status: **FROZEN PREEXECUTION PROPOSAL / NO R26 COMPUTE ENGINE CALL OR RESOURCE ACTION**.

Change classification: authenticated transport and fail-safe execution control
only. Mathematical semantics and scientific authority are unchanged.

## Evidence-selected basis

R25 completed exactly once. Its immutable result is SHA-256
`4458c10c8da75d8cf2adb32c8fe8f6727c67c7482f7b781d93138656cdc68cc6`
and independently audits 14/14. Google Cloud SDK 583.0.0 is authenticated in
the dedicated configuration as `pestypig@gmail.com`, with project
`dark-stratum-455714-h4`. R25 made no Compute Engine call.

R26 replaces only the unreliable browser/Cloud-Shell transport. It uses that
dedicated local SDK to perform the already designed regional R16 allocation and
unchanged P8P calculation. It changes no equation, coefficient, precision,
selector, threshold, reduction order, observer event, output schema or P8Q
rule.

## Frozen inputs

| Input | Frozen identity |
| --- | --- |
| outer archive | `artifacts/nhm2/g2h-e-s5/candidate-neutral/h2-p8p-r16-regional-bulk-ingress-v1-20260902/h2-p8p-r16-regional-bulk-upload-v1.tar`; 236,640,768 bytes; SHA-256 `3c697fa3e238398d3b4c30a6c379fea8d6d545b2228d0c699dd66594a23670b5` |
| base archive | SHA-256 `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978` |
| P8P overlay | SHA-256 `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e` |
| guest ledger | SHA-256 `d7f98f542ee776eec9f6d24173031f2b671bb54e59fd74d44d5b637990f536a6` |
| retained R16 orchestrator member | SHA-256 `74d2b8ffe2f4501d22fbdb6a88449c5f00a127215e1b8e17aa7da2938c8e2938`; authenticated but not invoked |
| R26 local controller | `tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8p_r26_local_gcloud_controller_v1.ps1`; 13,377 bytes; SHA-256 `50c2743a6f2e61fc61a0e9df53b8806db8f1b41a26297cd647f1087517ae1a24`; PowerShell parser PASS |
| required executable | SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718` |
| result auditor | unchanged `scripts/nhm2_g2h_e_s5_c08_h2_p8p_turnaround_result_audit.py` |

The initially absent local evidence root is
`artifacts/nhm2/g2h-e-s5/candidate-neutral/h2-p8p-r26-local-gcloud-execution-v1-20260903`.
Every result, including preexecution failure or partial output, is preserved
there. It is never reused or replaced.

## Exact resource and execution boundary

R26 may execute its exact controller once. Before allocation it authenticates
the outer archive, active account, dedicated project, absence of the exact R26
name, absence of every non-terminated `nhm2-h2-` VM, and at least 32 unused
regional C2D CPUs.

It may issue exactly one regional bulk request:

| Field | Frozen value |
| --- | --- |
| project / region | `dark-stratum-455714-h4` / `us-east1` |
| predefined VM name | `nhm2-h2-p8p-r26-c2d-32-20260903` |
| count / minimum | `1` / `1` |
| distribution | `ANY_SINGLE_ZONE` across `us-east1-b`, `us-east1-c`, `us-east1-d` |
| machine / provisioning | exactly `c2d-standard-32` / `STANDARD` on-demand |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260817` |
| boot disk | exactly 30 GB `pd-standard`, auto-delete |
| provider stop | five hours, termination action `STOP`, no restart on failure |
| aggregate runtime / total cost ceiling | 18,000 seconds / `$9.00` |

The request must yield exactly one running, exact-name VM with all frozen
bindings. After one 180-second wait, the controller performs exactly one SCP of
the outer archive and one SSH handoff. The guest authenticates the exact four
members, uses the unchanged guest ledger, conditionally installs Debian
`docker.io` only if absent, loads only the archived pinned base images, builds
with no pull and no network, verifies the executable, and executes exactly one
candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second external
timeout.

The controller polls only the exact VM status, preserves PASS, FAIL, timeout,
preexecution failure or partial output, enforces the aggregate ceiling, obtains
serial output after termination, reconstructs and authenticates the one
evidence archive, and runs the frozen independent P8Q auditor.

If creation succeeded but later identity discovery fails before the selected
zone is bound, the failure path may make exactly one project-wide exact-name
discovery query solely to recover the allowed zone. If and only if exactly one
matching VM is found in the allowed zone set, it stops that exact VM. If the
zone is already authenticated it stops that exact VM directly. This cleanup is
mandatory and does not authorize deletion, retry, replacement or another
resource.

First failure is terminal and consumes R26. No second controller invocation,
regional request, VM, SCP, SSH handoff, build or numerical process is allowed.
No fallback, alternate region/zone/machine/disk/provisioning model, retune,
additional upload, evidence-root replacement or deletion is allowed.

## Scientific and authority boundary

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false. R26 cannot authorize P=65,536. It can only supply the frozen P8Q
resource-bounded yes/no decision.

## Exact authorization text

> I authorize exactly one H2-P8P-R26 authenticated local-gcloud regional P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Execute only the 13,377-byte controller SHA-256 `50c2743a6f2e61fc61a0e9df53b8806db8f1b41a26297cd647f1087517ae1a24` using the dedicated authenticated Google Cloud SDK configuration for `pestypig@gmail.com` and project `dark-stratum-455714-h4`. Authenticate and transfer only the 236,640,768-byte candidate-neutral archive SHA-256 `3c697fa3e238398d3b4c30a6c379fea8d6d545b2228d0c699dd66594a23670b5`. Create exactly one temporary on-demand `c2d-standard-32` VM named `nhm2-h2-p8p-r26-c2d-32-20260903` through one regional bulk request in `us-east1`, `count=1`, `min-count=1`, `ANY_SINGLE_ZONE` across `us-east1-b`, `us-east1-c` and `us-east1-d`, with exact Debian image `projects/debian-cloud/global/images/debian-12-bookworm-v20260817` and exactly 30 GB `pd-standard` auto-delete storage, under an 18,000-second aggregate runtime ceiling and a `$9.00` total ceiling. Perform exactly one SCP, one SSH handoff, one unchanged offline build and exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout; require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`; preserve all outcomes, stop the exact VM automatically or through the bounded failure cleanup, recover and independently audit the serial evidence, and apply only the frozen P8Q rule. First failure is terminal. I do not authorize a retry, second request, VM, controller invocation, SCP, SSH handoff, build or numerical process; fallback, resource substitution, alternate region, zone, machine, disk or provisioning model; additional upload; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or any authority promotion.
