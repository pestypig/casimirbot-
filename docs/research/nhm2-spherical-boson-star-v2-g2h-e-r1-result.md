Program gate: G2H-E-R1 — primary pre-math self-identity failure disposition
Workstream: authenticated classical and quantum control branch
Capability or component: candidate-neutral Linux self-executable identity and successor primary preexecution
Current maturity: independently audited infrastructure PASS; successor proposal frozen and unauthorized
Target maturity: separately authorized, versioned primary Tolman-VII proof execution
Required frozen inputs: exhausted proposal `bab85c21...b46e`, seven-line v1 authorization, four-file ledger, stopped exit-66 container, old primary image/source, G2G contract and unchanged duties
Required evidence: exact source/build/executable identities, actual-runtime positive and adversarial fixtures, immutable-evidence rehash, absent candidate roots, inert checkpoint/proposal and current-head verification
Stop/fail criteria: any old-evidence mutation, candidate entrypoint invocation, candidate-root creation, arbitrary-symlink acceptance, runtime hash disagreement, unauthorized independent activity or authority promotion
Explicit non-goals: Tolman-VII evaluation, surface prediction, candidate admission, Rust execution, G3, SI, 68-file lanes, lamp, physical viability, propulsion or transport claims
Downstream gate unlocked: separate user authorization decision for exactly one versioned primary-v2 execution

This packet changes runtime self-identity and future receipt/checkpoint semantics.
It does not change mathematical semantics, the candidate, any proof duty, or
current authority.

# G2H-E-R1 result: candidate-neutral self-identity repair

## Decision

`PASS_CANDIDATE_NEUTRAL_PRIMARY_V2_PREEXECUTION`.

The exit-66 cause is repaired without weakening generic path handling. The v2
primary uses `primary_hash_self_executable()` only for the fixed Linux
`/proc/self/exe` magic link. It opens that kernel-owned path without
`O_NOFOLLOW`, requires the opened descriptor to be a nonempty regular file via
`fstat`, and hashes the descriptor. `primary_hash_file(path, ...)` retains
`O_NOFOLLOW` for contracts, scientific sources, authorization records and all
arbitrary paths.

The successor source also selects a new `tolman-vii-primary-v2` output root and
fails closed unless both the exhausted v1 root and independent root remain
absent. The G2G candidate, surface gate and scientific duties are byte-unchanged.

## Frozen build and runtime identities

| Item | SHA-256 / identity |
| --- | --- |
| G2G contract | `30de966d...343d` |
| Exhausted v1 source | `8a458dbb...3603` |
| Primary-v2 source | `2d205130...59740` |
| Primary-v2 image | `sha256:8334e977...159ab` |
| Primary-v2 executable | `666ba126...69028` |
| Fixture image | `sha256:b64c6aa1...e870e` |
| Fixture executable | `9bbf05c0...5a6bd` |
| Build binding | `e7865e33...08388` |

The candidate-capable primary-v2 entrypoint was built but never invoked.

## Actual Linux fixture evidence

The candidate-neutral fixture runs in the successor's pinned Linux lineage and
passes all of these checks in one process:

- the old `open("/proc/self/exe", O_NOFOLLOW)` behavior returns Linux errno 40,
  `ELOOP`;
- the dedicated self-descriptor helper succeeds and its internal SHA-256 is
  exactly the external executable SHA-256 `9bbf05c0...5a6bd`;
- generic hashing still rejects an arbitrary symlink with `ELOOP`;
- generic hashing accepts a regular file;
- a same-length file mutation changes the digest;
- candidate-entrypoint invocations and candidate evaluations are zero.

The producer-independent closure audit passes `21/21`. It rehashes the old
proposal, authorization, ledger and source; inspects the retained container as
stopped at exit 66 and not OOM-killed; rehashes both new executables; reruns the
actual Linux fixture; checks proposal sidecars; and proves all candidate,
authorization, future-ledger and independent roots remain absent.

## Inert successor proposal

Proposal
[`nhm2-spherical-boson-star-v2-g2h-e-r1-successor-proposal.v1.json`](./nhm2-spherical-boson-star-v2-g2h-e-r1-successor-proposal.v1.json)
is 3,202 bytes with SHA-256
`65093d0084a513b101172e339123ecb2839cd76ef4350c6a351acdf054dbcfdd`.
It binds:

- primary image `sha256:8334e977...159ab` and executable
  `666ba126...69028`;
- token digest `d54ee55d...ff4b` and a new seven-line v2 authorization
  template;
- checkpoint `8de58e8a...20f5`, a new output root, container and immutable
  v2 ledger prefix;
- first persisted output as terminal evidence with no retry or retune;
- primary-only eligibility and no independent execution.

The exact token was tested only against the checkpoint's inert path. Without a
separately created exact authorization record and `--execute`, it returns
`INERT` and creates no evidence. The future authorization record, v2 container,
v2 ledger and v2 output root are absent.

## Preserved evidence and authority

The exhausted v1 proposal `bab85c21...b46e`, authorization
`348dc0f0...e87f3`, four-file ledger and container
`ed7ed5fd...0270c` remain byte-for-byte/stopped. The v1 candidate root remains
absent. The Rust authorization, invocation and root remain absent.

Candidate admission, mathematical proof, joint geometry/state acceptance, SI,
68-file lanes, pair agreement, diagnostic lamp, physical viability, propulsion
and transport authority all remain false.

## Verification

Verification receipt
[`nhm2-spherical-boson-star-v2-g2h-e-r1-verification-receipt.v1.json`](./nhm2-spherical-boson-star-v2-g2h-e-r1-verification-receipt.v1.json)
has SHA-256 `9e1107cc...3598c` and binds:

- math report/validation: `318` entries, `PASS`;
- every required WARP file: `18/18`, `179/179` tests;
- Casimir adapter run `2464`: `PASS`, first failure `null`, deltas empty;
- certificate `6e84f965...4e45`: `GREEN`, integrity true.

This certificate verifies the current diagnostic/preexecution constraint pack.
It does not certify the unexecuted Tolman-VII proof or establish physical
viability.
