Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R5 native Windows file-picker transport successor
Current maturity: frozen transport-only successor; R4 exhausted at browser-client filechooser admission; exact VM stopped
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R5 record
Required frozen inputs: R4 terminal result, stopped exact R3 VM, verified 16-file staging inventory, unchanged guest ledger/controller/executable/auditor
Required evidence: local extension/file-access preflight, one restart and browser-SSH surface, one native Windows picker transaction, exact ingress/reconstruction, one controller invocation, serial evidence, independent audit and P8Q classification
Stop/fail criteria: first identity, restart, SSH, picker, selection, transfer, reconstruction, build, execution or evidence failure terminal; no retry, fallback, second dialog/batch/process or scientific change
Explicit non-goals: browser-extension repair or settings mutation, frozen-candidate evaluation, retuning, P=65,536 execution, G3/SI/metric/lane work, evidence deletion or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R5 native Windows file-picker successor

Status date: September 1, 2026.

Status: **FROZEN TRANSPORT-ONLY SUCCESSOR / AWAITING SEPARATE BILLABLE-ACTION AUTHORIZATION**.

Change classification: transport procedure and receipt semantics only.
Mathematical semantics and runtime authority are unchanged.

## Narrow causal basis

R4 proved that the fresh-metadata browser-SSH repair worked: the exact retained
VM reached an authenticated `pestypig` prompt and returned
`R4_REMOTE_ABSENT_16`. It then stopped because the embedded SSH page did not
deliver a Playwright filechooser event for its visible Upload control or its
multiple-file input. Zero files and zero bytes were transferred.

The subsequent local read-only diagnosis establishes that the expected Chrome
permission is not missing:

- selected profile `Default`;
- ChatGPT extension ID `hehggadaopoacecdllhhajmbjkdcmajg`;
- installed and enabled extension version `1.26.827.12125_0`;
- Chrome secure-preference `newAllowFileAccess=true`;
- `withholding_permissions=false`;
- native-host manifest present, registry-bound and correct;
- both expected extension origins admitted by the native host.

Lightweight Chrome tab discovery succeeds, while read-only interaction with
the retained embedded Cloud Shell surface times out even after claiming the
current user tab. The supported browser filechooser API is therefore not an
eligible R5 dependency. R5 changes only the final local selection mechanism:
after the visible SSH Upload control is activated once, the ordinary Windows
file-open dialog is handled through the bounded Windows UI control surface.
No Chrome extension, native-host, profile, permission or security setting may
be changed.

## Preserved scientific and ingress bindings

R5 preserves without change:

- R4 proposal SHA-256
  `5202340ab57083b88fa874b9f2c2ec5b4dd5196b2da86716e33f15a9e169055e`;
- R4 result SHA-256
  `a7cefb8050fbab5518915852358813836aa7da58db88565e2f1532ba838a0c8f`;
- R3 chunk manifest SHA-256
  `ec1c115461442e1ddba9ccc635aebdd722be5440f23e06c797406ce8cde6b52d`;
- exactly fifteen ordered base parts totalling 236,492,800 bytes and one
  unchanged 134,656-byte overlay in the existing verified local staging root;
- original base SHA-256
  `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`;
- overlay SHA-256
  `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`;
- guest ledger SHA-256
  `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96`;
- executable SHA-256
  `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`;
- exactly one candidate-neutral P=1024 controller process on 32 CPUs with a
  14,400-second external timeout;
- phase timing, at most 64 progress lines, all 514 degree buckets, six
  aggregate totals, P8I equality, chronology, neutrality and authority locks;
- unchanged result auditor SHA-256
  `cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`;
- preregistered P8Q trinary classifier.

No candidate-dependent byte, numerical threshold, selector schedule,
precision, equation, observer semantics, reduction order or acceptance rule
changes.

## Exact existing resource and ceilings

R5 may use only the retained stopped resource:

| Field | Frozen value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM | `nhm2-h2-p8p-r3-c2d-32-20260901` |
| instance ID | `637527339076077505` |
| zone | `us-east1-d` |
| machine | on-demand `c2d-standard-32`, 32 CPUs, 128 GB |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260826` |
| boot disk | retained 30 GB `pd-standard`; no replacement or resize |
| aggregate restart ceiling | 18,000 seconds |
| total R5 ceiling | $9.00 compute plus prorated retained storage |

Before restart, require the exact VM `Stopped`, every tabled binding exact, the
sixteen local files unchanged, the R5 result root absent, the selected Chrome
profile and extension identity exact, and `newAllowFileAccess=true`. Authorize
exactly one restart of that VM, not creation of another VM.

After `Running`, freshly load the exact instance Details page and open exactly
one new authenticated browser-SSH surface. Any metadata conflict, connection
failure, identity drift or missing `pestypig` prompt is terminal. Run the exact
remote absence guard once and require `R4_REMOTE_ABSENT_16` before activating
the visible Upload control exactly once.

## Frozen native-picker transaction

The native-picker path is fail-closed and may perform only these ordered UI
actions:

1. Through the Chrome page, activate the visible SSH Upload control once,
   without starting a browser-client filechooser listener.
2. Through Windows UI control, enumerate current app/windows and require one
   unambiguous file-open dialog owned by the intended Chrome workflow. Do not
   construct a window handle or use stale coordinates.
3. Observe the dialog, focus its location field, enter only the exact absolute
   R3 staging-directory path, and navigate to it.
4. Reobserve the dialog and require that the location is the exact staging
   directory and that the selection surface contains exactly the sixteen
   manifest-bound files and no additional entry eligible for selection.
5. Select all sixteen entries once. Reobserve and require sixteen selected
   regular files before activating `Open` exactly once.
6. Return to Chrome and require one transfer batch containing exactly sixteen
   successful items. Any absent dialog, ambiguity, extra/missing selection,
   prompt, cancellation, partial/zero transfer or failed item is terminal.

No Windows terminal, Run dialog, File Explorer command surface, PowerShell,
batch file, browser-client `setFiles`, clipboard file path, drag/drop, retry,
second picker or alternate transport is permitted. The final `Open` action is
an outbound upload of the exact sixteen candidate-neutral files to the exact
authenticated Google Compute Engine guest and requires its specific execution
authorization.

## Guest execution and stop boundary

Only after all sixteen remote files are present may the unchanged guest ledger
be entered once. If Debian `docker.io` is absent, pause at that exact point for
the separately required action-time installation confirmation. If confirmed,
the ledger must authenticate every part, reconstruct the original archive,
replay the exact source manifest, build with no pull/no network, authenticate
the executable and invoke the controller exactly once.

Preserve PASS, FAIL, timeout, preexecution failure or partial output as
immutable evidence. Automatically stop the VM after any outcome. No restart,
disk rescue, evidence deletion or second evidence path is authorized by R5.

## P8Q and authority boundary

Only a complete authenticated calibration may produce
`P8Q_YES_PROPOSAL_READY` or `P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`. Any timeout,
failure, missing archive or incomplete audit produces
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. No result authorizes P=65,536.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.

## Exact authorization text

> I authorize exactly one H2-P8P-R5 native Windows file-picker P=1024 turnaround attempt under the final independently audited R5 proposal SHA-256. Restart only the existing stopped Google Compute Engine VM `nhm2-h2-p8p-r3-c2d-32-20260901`, instance ID `637527339076077505`, in project `dark-stratum-455714-h4`, zone `us-east1-d`, preserving its exact `c2d-standard-32` machine, Debian image and 30 GB `pd-standard` disk, under an 18,000-second aggregate restart ceiling and a $9.00 total compute and prorated-storage ceiling. Before restart, require the VM stopped, every resource binding exact, all sixteen existing local staged files exact, the R5 result root absent, the selected Chrome profile and ChatGPT extension identity exact, and `newAllowFileAccess=true`. After it is running, open exactly one fresh authenticated browser-SSH surface, require user `pestypig`, run the exact remote absence guard once, and activate the visible Upload control once. Use exactly one native Windows file-open dialog to navigate only to the exact R3 staging directory, require exactly the sixteen manifest-bound regular files, select all sixteen once, and activate Open once, uploading those exact candidate-neutral chunks and overlay only to the exact authenticated VM guest. Require exactly sixteen successful transfer items, reconstruct and authenticate the original 236,492,800-byte base archive, and—after the separately required action-time confirmation if Debian `docker.io` is absent—run unchanged guest ledger SHA-256 `f7112c5a547b48814ee63961cc6b441cd67f147b0bc47feb6ea8f98cddf8ca96`. Require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718` and execute exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second timeout. Preserve PASS, FAIL, timeout, preexecution failure or partial output; automatically stop the VM; recover only the one hash-bound serial archive; run the unchanged independent result audit; and apply only the preregistered P8Q rule. First failure is terminal. I do not authorize retrying R4 or R5; a second dialog, upload batch, SSH surface, restart, VM or numerical process; browser-client `setFiles`, drag/drop, clipboard transport, terminal or Run-dialog file transfer, alternate transport, resource creation, replacement, resize or rescue; changing Chrome extension, profile, file-access, native-host, metadata, SSH-key, `known_hosts`, IAM, firewall or security settings; additional upload; scientific change or retuning; frozen-candidate evaluation; positive sampling; candidate/scientific root, token or handler creation; evidence deletion; P=65,536 execution; G3/SI/metric/lane work; or any candidate, proof, geometry/state, lane, lamp, physical, propulsion or transport authority promotion.
