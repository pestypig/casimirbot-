Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: S5-A/A4 candidate-neutral H2 continuation diagnosis
Capability or component: H2-P8F-C2-R1 immutable result-ABI auditor
Current maturity: separately versioned definition implemented; synthetic interval/classification self-test 11/11 PASS; recovered capture not yet classified under this definition
Target maturity: independently audited immutable C2-R1 classification and one preregistered P8G lead
Required frozen inputs: rescue archive `60cb3bf0...977ad`, exact 13-file hash inventory, container `82dd2e3e...68d3e`, image `d6baac26...b1352`, executable `14140897...1bad6`, and unchanged P8F comparison order
Required evidence: source hash, independent definition audit, strict Arb-ball parser, interval ratio/sum replay, exact progress/identity/isolation checks, immutable result receipt and false authority locks
Stop/fail criteria: any hash/identity/inventory mismatch, non-strict interval comparison, malformed/negative interval, replay failure, candidate activity, evidence mutation, retry/retune or authority promotion
Explicit non-goals: changing the P8F mathematical result, rerunning C2-R1, candidate evaluation, scientific handler linkage, Rust/G3/SI/metric/lane work, or physical/propulsion/transport claims
Downstream gate unlocked: exactly the preregistered P8G definition named by an audit PASS; no numerical or candidate execution authority

# H2-P8F-C2-R1 result-ABI audit definition

Status: **FROZEN IMPLEMENTATION DEFINITION / SYNTHETIC SELF-TEST PASS / CAPTURE NOT YET EVALUATED**.

## Exact reader

The candidate-neutral reader is
`scripts/nhm2_g2h_e_s5_c08_h2_p8f_c2_r1_result_audit.py`, exactly 15,630
bytes with SHA-256
`c0b0196d7879f1e156cea6abeae6d5f216ac3f01e6f365affc81a459da8ee4b6`.
Its synthetic parser and four-branch classification self-test passes 11/11.
It neither imports nor invokes Docker, executes numerical code, mutates raw
evidence, evaluates a candidate, or grants scientific authority.

## Frozen ABI repair

The reader accepts exactly the 13 regular non-symlink files recovered from the
stopped disk and requires every observed file SHA-256 exactly. It binds the
C2-R1 container, image, executable, entrypoint, 32-CPU limit, no-network,
read-only-root, capability-drop, no-restart and exit-zero identities. It also
requires the exact 64 ordered 1,024-panel progress markers through 65,536, the
86,400-second controller bound, and the frozen target/work counts.

This is a representational correction to the predecessor auditor, not a change
to the numerical result or causal decision. The raw capture is never renamed or
normalized.

## Interval replay

Each Arb rendering must match exactly `[midpoint +/- radius]`, with finite
decimal values and nonnegative radius. It is interpreted as the closed interval
`[midpoint-radius, midpoint+radius]`. Positivity is proved on the lower bound.
Division uses positive interval endpoints; the two emitted ratio balls must
overlap their independently replayed quotient intervals. The sum of the four
slot-radius balls must overlap the emitted elementary-total ball.

Every causal comparison is strict and enclosure-safe:

1. `lower(final_radius) > upper(total_elementary_radius_sum)` selects
   `P8G_OUTER_ACCUMULATION_ARITHMETIC_LEAD`.
2. Otherwise `lower(boundary_panel_radius) > upper(nonboundary_panel_radius_sum)`
   selects `P8G_BOUNDARY_CONTRIBUTION_ENCLOSURE_LEAD`.
3. Otherwise one slot must have a lower bound strictly above every other slot's
   upper bound to select its `P8G_NONBOUNDARY_SLOT_i_ENCLOSURE_LEAD`.
4. Otherwise select `P8G_DISTRIBUTED_NONBOUNDARY_ENCLOSURE_LEAD`.

No tolerance is fitted after observing the result. Any ambiguity falls through
to the less-specific branch; any malformed or inconsistent interval fails the
audit and selects nothing.

## Authority boundary

An audit PASS classifies only this candidate-neutral coefficient-decomposition
diagnostic. It does not admit a boson-star candidate, establish a proof, accept
geometry/state, authorize a lane or lamp, or support physical, propulsion or
transport claims. All such authority remains false.
