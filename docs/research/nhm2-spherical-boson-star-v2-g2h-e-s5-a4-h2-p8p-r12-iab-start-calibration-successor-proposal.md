Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R12 authenticated in-app-browser retained-VM start and P=1024 successor
Current maturity: frozen billable successor; R11 proved one exact enabled semantic Start / Resume item without activation
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R12 record
Required frozen inputs: R11 result, retained authenticated tab/menu, exact stopped P8J-R9 VM, two-file stage, ledger/controller/executable/auditor and unchanged ceilings
Required evidence: exact stopped identity, one semantic Start action, optional exact confirmation, running state, one SSH surface, guarded two-file ingress, one controller, serial evidence, audit and P8Q result
Stop/fail criteria: first tab, identity, control, activation, confirmation, capacity, start, SSH, guard, chooser, transfer, Docker, build, execution or evidence failure terminal; no retry or fallback
Explicit non-goals: new resource, alternate zone/transport, Docker installation, candidate evaluation, retuning, P=65,536, P8J evidence mutation/deletion, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R12 authenticated in-app-browser start/calibration successor

Status date: September 2, 2026.

Status: **FROZEN BILLABLE SUCCESSOR / AWAITING ACTION-TIME AUTHORIZATION**.

## Causal basis

R9 exhausted because Chrome control was unavailable. R10 then proved the
Codex in-app browser is authenticated and bound the exact stopped resource.
R11 expanded the exact More-actions control once and observed exactly one
enabled semantic `Start / Resume` button without activating it. Its immutable
result is SHA-256
`d70b044f6596e5b9f161156110661dacfe4a28e833ca4653dc6f6042c1b5a9d1`
and independently audits 12/12.

R12 changes only the previously unavailable Chrome transport to the now-proven
authenticated in-app-browser semantic transport. It may reuse the one retained
R11 tab, which must still show exact stopped VM
`nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID
`1920090043510946854`, project `dark-stratum-455714-h4`, zone
`us-east1-c`, machine `c2d-standard-32` with 32 vCPUs/128 GB, image
`debian-12-bookworm-v20260817` and one 30 GB Standard persistent boot disk.

## One start and one execution

After separate action-time authorization, click exactly once on the uniquely
observed enabled semantic `Start / Resume` button. If Google presents exactly
one direct confirmation naming the exact VM, accept it once. Any different
warning, resource change, choice, capacity error or ambiguous outcome is
terminal. No second click, refresh, alternate tab, Cloud Shell, API command,
new VM, zone change or fallback is authorized.

After exact start completion, wait exactly 180 seconds and require the same
resource to show `Running`. Open exactly one fresh authenticated browser-SSH
surface as `pestypig`. Require one round-trip marker, 30 passive seconds and
the unchanged remote guard before ingress.

Select exactly these two local candidate-neutral files in one browser file
chooser and require exactly two successful transfers:

- 134,656-byte `h2-p8p-overlay-upload-v1.tar`, SHA-256
  `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`;
- 2,971-byte `h2_p8p_r7_retained_p8j_guest_sequence_v1.sh`, SHA-256
  `9af4180a714d9dfd24dd6cfe6d3952a73d75c932a3f05f5e3484f79467c94a5b`.

Require the retained 236,492,800-byte base archive SHA-256
`fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`
and existing Docker; Docker installation is forbidden. Invoke the unchanged
ledger exactly once, build offline with no pull/network, require executable
SHA-256
`7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`,
then execute exactly one candidate-neutral P=1024 controller process with 32
CPUs and a 14,400-second timeout.

Preserve PASS, FAIL, timeout, preexecution failure or partial evidence as
immutable serial evidence; run the unchanged result auditor SHA-256
`cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`;
and automatically stop the VM after any terminal outcome. Aggregate R12 VM
runtime is at most 18,000 seconds and total compute plus prorated retained
storage cost is at most $9.00.

First failure is terminal. Only a complete authenticated calibration may yield
`P8Q_YES_PROPOSAL_READY` or `P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`; otherwise
only `P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED` applies. No result authorizes
P=65,536.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.

## Exact authorization text

> I authorize exactly one H2-P8P-R12 authenticated in-app-browser retained-VM start and P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Reuse only the retained authenticated Details tab and require exact stopped VM `nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID `1920090043510946854`, project `dark-stratum-455714-h4`, zone `us-east1-c`, `c2d-standard-32`, Debian `v20260817` and 30 GB Standard persistent disk. Click exactly once on the uniquely observed enabled semantic `Start / Resume` menu item and accept at most one direct confirmation naming that exact VM. Preserve the 18,000-second and $9.00 ceilings. After exact running-state authentication and one 180-second wait, open exactly one fresh authenticated browser-SSH surface as `pestypig`; preserve the round-trip, 30-second stability, remote guard, one browser file chooser, exact two-file ingress, retained-base authentication, existing-Docker requirement, offline build, executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`, exactly one candidate-neutral P=1024 process with 32 CPUs and 14,400-second timeout, immutable serial evidence, automatic stop, independent audit and frozen P8Q rule. First failure is terminal. I do not authorize retrying R9 through R12; a second start click, confirmation, SSH surface, guard, chooser, upload batch, VM or numerical process; Chrome, Cloud Shell, API command, fallback, new resource, alternate zone, replacement, resize or rescue; Docker installation; P8J source/evidence/resource mutation or deletion; additional upload; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or any authority promotion.
