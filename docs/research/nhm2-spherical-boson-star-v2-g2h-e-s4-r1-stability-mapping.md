Program gate: G2H-E-S4-R1 — mini-boson-star proof-definition completeness review
Workstream: authenticated classical and quantum control branch
Capability or component: radial-stability operator mapping into the frozen R2 gauge and normalization
Current maturity: exact additive operator and proof-certificate definition passes internal symbolic/manufactured audit; unsealed pending independent theory review
Target maturity: independently audited source-bound ell=0 self-adjoint spectral proof ABI suitable for disjoint fixture producers
Required frozen inputs: R2 contract `041c406c...ed12a`, Saffin--Xie equations (13)--(25), and Alcubierre et al. equations (1)--(33)
Required evidence: convention derivation, exact coefficient substitution, Friedrichs domain, endpoint and essential-spectrum proof duties, strict lower and upper eigenvalue certificates, turning-side comparator, independent theory replay, and no candidate evaluation
Stop/fail criteria: normalization mismatch, unproved operator equivalence, importing turning-point evidence as eigenvalue proof, candidate evaluation, or authority promotion
Explicit non-goals: computing the selected eigenvalue during R1, proving nonspherical/nonlinear stability, implementing either scientific lane, authorizing execution, or making physical claims
Downstream gate unlocked: one sealed radial-stability definition inside the additive S4 contract

# G2H-E-S4-R1 radial-stability mapping

## Exact background map

The frozen R2 background uses

```text
ds^2 = -alpha(r)^2 dt^2 + b(r)^2 dr^2 + r^2 dOmega^2,
alpha = 1/(b*s),
mbar_P = 1,
G = 1/(8*pi).
```

Alcubierre et al. use

```text
ds^2 = -alpha_A(r)^2 dt^2 + gamma_A(r)^2 dr^2 + r^2 dOmega^2
```

and set `G=1`. Their standard boson star is the `ell=0`, `kappa_0=1`
case. The exact map into frozen variables is

```text
alpha_A = 1/(b*s),
gamma_A = b,
mu_A = 1,
omega_A = omega,
psi_A = sqrt(4*pi*G)*sigma = sigma/sqrt(2),
M_A = m = G*M_phys.
```

The field factor is not optional. It reconciles the spherical-harmonic/Planck
normalization in the Alcubierre equations with the two-real-field normalization
in Saffin--Xie. Under the map,

```text
M_A' = r^2/2 * [omega^2*psi_A^2/alpha_A^2
                 + psi_A'^2/gamma_A^2 + psi_A^2]
     = r^2*rho_R2/2
     = m'.
```

The metric identities `gamma_A^2=(1-2*M_A/r)^-1` and
`b^2=(1-2*m/r)^-1` then agree exactly.

## Selected perturbation formulation

The proposed additive definition uses the gauge-invariant two-channel system
of Alcubierre et al., specialized before discretization to
`ell=0`, `kappa_0=1`, and transformed by their equations (27)--(33) into

```text
H_T v = chi^2 v,
H_T = -d/dr[(alpha_A^2/gamma_A^2) d/dr] + R^T W R,
v in L2((0,infinity), dr; R^2).
```

All occurrences of `alpha_A,gamma_A,psi_A,omega_A` are replaced by the exact
background map above. The coefficient `alpha_A^2/gamma_A^2=1/(b^4*s^2)` is
strictly positive only after R2-C05 passes. No turning-point or time-evolution
comparator may substitute for the spectral enclosure.

## Domain and stability predicate

The additive contract selects the Friedrichs realization generated by the
closed quadratic form of `H_T` on `C_c^infinity((0,infinity);R^2)`, with the
source-derived endpoint behavior:

- at `ell=0`, `v1=O(r)` and `v2=O(r^2)` at the regular origin;
- both are square-integrable and decay at infinity;
- the Lagrange boundary form in Alcubierre equation (33) vanishes at both ends.

The first clause of R2-C13 is proved separately by strict positivity of
`dM/dlambda` on all 1,024 continuation cells.  It is a mandatory comparator,
but it is not spectral evidence.  The eigenvalue clause requires

```text
0 < L <= chi0_squared <= U < lower((1-omega)^2).
```

Here `(1-omega)^2` is the lower essential-spectrum threshold derived from the
two exact asymptotic dispersion branches.  A global symmetric matrix-Riccati
factorization proves `H_T>=L I`, including zero exclusion.  A compactly
supported co-rotating trial vector proves a Rayleigh upper bound `U` below the
essential threshold.  The min-max principle then forces a discrete lowest
radial eigenvalue in `[L,U]`.

The proof-carrying field is `S=R^T K R`, with exact-dyadic symmetric `K` on the
frozen 256-cell cover.  Every cell certifies the positive-definite residual

```text
C_L = W-(p*K)'-p*theta'*(K*J-J*K)-p*K^2-L*I,
```

and every exact intercell jump is positive semidefinite.  This produces a
whole-half-line quadratic-form proof without a finite wall.  The upper trial is
evaluated in the co-rotating variable `w=R*v`, where the rotation disappears
from the potential and enters the exact covariant derivative.

The binding machine-readable definition is
`nhm2-spherical-boson-star-v2-g2h-e-s4-r1-radial-stability-contract.v1.json`.
Its definition-only audit is:

```powershell
python scripts/nhm2_g2h_e_s4_radial_stability_definition_audit.py
```

It currently passes 37/37 exact and manufactured checks, including the
normalization map, rotation derivative, completed-square identity, asymptotic
matrix symbol, strict pass/touch/fail predicates, chronology and absent roots.
No candidate or positive continuation parameter is loaded.

## Open items before sealing

1. Obtain an independent theory review of the full coefficient map, Friedrichs
   realization, relative-compactness/essential-spectrum argument and matrix
   Riccati identity.
2. Bind each producer's deterministic `K` and trial builders, degrees,
   projections and work budgets before any scientific ingress.
3. Implement and independently audit only the allowed manufactured
   pass/fail/corruption/chronology fixtures.

Until those items close, the contract remains draft/unsealed.  The 37/37 result
is definition-fixture evidence, not a radial-stability proof, and no scientific
implementation or candidate evaluation is authorized.

Primary sources:

- [Saffin and Xie, *Quantum fields in boson star spacetime*](https://arxiv.org/abs/2601.05129)
- [Alcubierre et al., *On the linear stability of ell-boson stars with respect to radial perturbations*](https://arxiv.org/abs/2103.15012)
