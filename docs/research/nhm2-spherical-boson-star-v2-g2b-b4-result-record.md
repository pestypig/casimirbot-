# NHM2 spherical boson-star v2 G2B-B4 result record

Program gate: G2B — replacement classical proof attempt  
Workstream: authenticated classical branch closure  
Capability or component: M5-R1 initializer-to-policy binding and integrated four-grid attempt  
Current maturity: immutable authenticated prerequisite `FAIL`  
Target maturity: exact first-failure evidence  
Required frozen inputs: B4 packet/checkpoint, sealed runner/tests, B1-R2 initializer, B3 Linux runtime, branch policy and source closure  
Required evidence: exclusive self-hashed terminal receipt, exact mismatch words, zero later work, independent read-only audit  
Stop/fail criteria: preserve the first scalar-ABI mismatch without grid generation, solve, retry, retune, or reinterpretation  
Explicit non-goals: repair this immutable result, candidate admission, vacuum/no-fold proof, replay/lamp/physical/propulsion/transport authority  
Downstream gate unlocked: separately versioned initializer scalar-ABI reconciliation; vacuum-continuation remains locked

## Result

`FAIL` at `g2b_b4_initializer_scalar_recomputation_mismatch` before grid generation and before any N=64 continuation attempt.

The sole admitted Linux invocation authenticated the final branch policy, all eleven radial sources, the cross-grid evaluator, all six persisted initializer payloads, the B1-R2 persistence receipt, the B3 runtime manifest and loaded objects, the execution checkpoint, exact Python/MPFR/GMP identities, and nearest-mode fenv. It then applied the frozen initializer evaluator's mandatory scalar graph and stopped at the first mismatch.

| Field | Frozen evaluator recomputation | Persisted payload | Result |
|---|---|---|---|
| `N0` | `40485fa24bc6145c` | `40485fa24bc6145b` | bit mismatch (one binary64 ULP) |
| `kappa` | `3ff2d379a0d0a3e0` | `3ff2d379a0d0a3e0` | equal |
| `sigma` | `40025ff41467e26d` | `3fe815d49929ae09` | semantic/value mismatch |
| `lambda` | `3fa0000000000000` | `3fa0000000000000` | equal |
| `nu_star` | `bf4626bcc563863f` | `bf4626bcc563863f` | equal |
| `wSeed` | `3feffa75d60dd448` | `3feffa75d60dd448` | equal |

The `sigma` discrepancy is an upstream ABI contradiction: the B1 entry-binding packet defines the payload using `sigma = M/kappa - 1`, whereas the frozen evaluator requires `sigma = C/kappa - 1`. The `N0` payload was produced from higher-precision `C` before both were rounded, whereas the evaluator requires recomputation from `set_d(C64)` and exact bit identity. This result chooses neither meaning and changes no frozen byte.

## Immutable evidence

| Artifact | Bytes | Raw SHA-256 | Self hash |
|---|---:|---|---|
| preexecution binding | 6,781 | `7ec34f65e4999b9cd96be90bd3e5e2c8c61880e88c73fb31556995eeb3c2978e` | `dd473fcdb2c843c143e67893af14cd616004765a2022eb387d4088abfcf2bece` |
| terminal receipt | 2,222 | `871dd86266e77b85ce55552e319ea39f29736ee6a4b4260bc51dc4527b95f9eb` | `b5c47be2bef48e1e9b6a55667a8d83f712bcd552e2b0fcb5939dfc24f5065b0b` |

The exact output inventory contains only those two files. The independent read-only terminal audit passed `3/3` on both the host and the admitted network-disabled Linux image. The sealed preexecution implementation tests passed `7/7` in both environments.

## Chronology and authority

- Sole production invocation count: one.
- Attempted grid count: zero.
- Attempted continuation/Newton solve count: zero.
- Cross-grid pair count: zero.
- Retry, retune, fallback, overwrite, and deletion count: zero.
- Vacuum-continuation work unlocked: false.
- Candidate admission, joint geometry/state, 68-file execution, proof, execution, replay, pair-agreement, diagnostic lamp, Theory Graph, physical viability, propulsion, and transport authority: false.

The B4 result is immutable terminal evidence for this execution packet. Progress requires a separately versioned parent decision that reconciles the initializer scalar ABI and produces a new bound instance or evaluator successor; the failed output root must not be reused.

