# NHM2 spherical-boson-star v2 G2B-M5 tail-power API repair

Program gate: G2B-M5 — sole tail exponent API repair and projection  
Workstream: lambda-zero proof-center recovery  
Capability or component: corrected tail evaluation plus unchanged M4 ladder  
Current maturity: preregistered design; implementation and execution absent  
Target maturity: immutable projected core-duty pass or terminal falsifier  
Required frozen inputs: M4 receipt/source, M3 receipt, M2 codec, M1 engine  
Required evidence: tail unit regression, M3 replay, every fixed mode binding  
Stop/fail criteria: one shot; sole repair only; no retry, retune, or added mode  
Explicit non-goals: any mathematical, threshold, branch, or authority change  
Downstream gate unlocked: remaining classical G2B duties after exact pass

## Sole authorized implementation delta

Replace the unavailable tail expression:

```text
engine.gmpy2.pow(x / radius, sigma)
```

with the MPFR value operation:

```text
(x / radius) ** sigma
```

under the unchanged audited MPFR256 context. No other origin, interior, tail,
solve, center replay, DCT-I, residual, reconstruction, selection, receipt, or
authority rule changes.

Before execution, a focused synthetic tail test must prove the corrected helper
returns finite MPFR values for fixed positive `x/radius` and real `sigma`, and a
static test must prove the unavailable `.pow(` call is absent from the executed
corrected profile.

## Unchanged execution and decision

Reproduce the M3-selected 256-substep center byte-for-byte, materialize the full
state at 256 substeps, and run all MPFR mode ordinals `[128,256,512]`. Eligibility
remains exact projected residual `<=1/4*10^-10`, node `<=2^-40`, join `<=2^-28`,
and endpoint `<=2^-40`. Select the lowest eligible mode only after all ordinals
complete.

The only decisions remain `MPFR_PROJECTION_SELECTED`, `MPFR_PROJECTION_FAILED`,
and `MPFR_PROJECTION_SOLVE_OR_REPLAY_FAILED`. All authority remains false.
