Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R10 in-app-browser authentication and semantic-control preflight result
Current maturity: immutable authenticated read-only preflight; exact resource bound but direct Start control absent
Target maturity: preserve terminal evidence and select at most one separately frozen semantic-menu inspection successor
Required frozen inputs: R10 packet, exact in-app-browser tab, exact retained stopped P8J-R9 identity and no-input rule
Required evidence: authenticated page title/URL, exact resource fields, stopped state, direct Start-control count/state and zero activation
Stop/fail criteria: absent direct Start control terminal; no menu expansion, click, refresh, retry, start or alternate transport
Explicit non-goals: VM start, confirmation, SSH, upload, Docker, build, calibration, candidate evaluation, retuning, P=65,536, deletion, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: one separately frozen read-only semantic-menu inspection successor only; P8Q remains stopped

# H2-P8P-R10 authenticated in-app-browser preflight result

Status date: September 2, 2026.

Status: **AUTHENTICATED_EXACT_RESOURCE / DIRECT_START_CONTROL_ABSENT / R10 CLOSED**.

## Frozen authority

R10 was frozen at SHA-256
`12fe4a9851279e0dd8abba5d8a17d26e91f760fac314c898d2e3b70786494a55`
and its static audit passed 13/13. It authorized one visible in-app-browser tab
and one accessibility read only, with no click, input, authentication action,
refresh, start request or fallback.

## Observed chronology

1. Exactly one in-app-browser tab opened the frozen Details URL. Google added
   only `&pli=1` and rendered the title
   `nhm2-h2-p8j-r9-c2d-32-20260831 – Compute Engine – My First Project – Google Cloud console`.
2. The single bound accessibility read proved an authenticated project page and
   exact VM `nhm2-h2-p8j-r9-c2d-32-20260831` with instance ID
   `1920090043510946854`, status `Stopped`, zone `us-east1-c`, image
   `debian-12-bookworm-v20260817`, machine `c2d-standard-32 (32 vCPUs, 128 GB
   Memory)` and one 30 GB `Standard persistent disk` boot disk.
3. The toolbar exposed one collapsed semantic `More actions
   nhm2-h2-p8j-r9-c2d-32-20260831` button. It exposed zero direct buttons or
   links named `Start / Resume`.
4. No control was clicked or expanded. No confirmation, start request, VM boot,
   SSH, file selection, upload, Docker action, build or calculation occurred.

The result proves that authentication and exact-resource rendering are
available through the in-app browser. It does not prove that the hidden menu
contains an enabled start action, and it grants no start authority.

## P8Q and authority

Only `P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED` applies because no turnaround
calibration exists. Candidate evaluations and positive samples remain zero.
Candidate/scientific roots, tokens and handler linkage remain absent.
Candidate, proof, geometry/state, lane, lamp, physical, propulsion and
transport authority remain false.

The sole next lead is a separately frozen read-only successor permitting one
semantic expansion of the exact collapsed More-actions control followed by one
accessibility read, without activating any menu item.
