Program gate: G2H-E-S3 — primary-v3 exact authorization/execution
Workstream: authenticated classical and quantum control branch
Capability or component: sole primary-v3 Tolman-VII proof invocation and first-failure classification
Current maturity: immutable authenticated mathematical FAIL; one candidate evaluation
Target maturity: bounded theoretical disposition of the frozen surface-regularity failure
Required frozen inputs: proposal `af0a0394...67384`, authorization `29543f4b...5450`, image `sha256:5a790295...7ada` and executable `1764b6be...46e1`
Required evidence: exact four-file ledger, retained container, 21-file output root, hash chain and independent rational replay
Stop/fail criteria: any retry, retune, deletion, alternate root, downstream-duty execution, Rust execution or authority promotion
Explicit non-goals: reinterpretation as infrastructure failure, candidate admission, G3, SI, lanes, lamp or physical claims
Downstream gate unlocked: G2H-E-S3-R1 bounded surface-germ theoretical review only

This packet records runtime, mathematical and receipt evidence. It changes no
candidate definition, proof duty, threshold, arithmetic or claim authority.

# G2H-E-S3 primary result: immutable mathematical FAIL

## Decision

`FAIL_GLOBAL_STATIC_STATE_SURFACE_GERMS_DISJOINT_ORDER_2`.

The one authorized primary-v3 checkpoint ran exactly once. Preflight passed,
verifying the executable, contract and all seven sources. Candidate evaluation
then reached the frozen surface-regularity gate and failed before every later
classical and quantum duty.

For the radial metric coefficient `B`, the value and first derivative agree at
the material surface, but the exact one-sided second derivatives do not:

```text
B''_interior(1) = -350/27
B''_exterior(1) =  100/27
```

The first disjoint jet is therefore order 2 and the typed failure is
`GLOBAL_STATIC_STATE_FAIL`. A producer-independent `Fraction` replay derives
the same two rational values without using the C/FLINT implementation.

## Immutable evidence

- authorization: 380 bytes, `29543f4b...5450`;
- invocation: `6d024cac...caf9`;
- process result: `3ff31ae1...5c5`, return code 1;
- stdout: `108d3861...493f`, containing only the manifest hash;
- stderr: empty, `e3b0c442...b855`;
- retained container: `f972b7d4...2625e`, exited 1, not OOM-killed;
- output root: 21 files, 11,502 bytes;
- terminal manifest: `b4eec02b...edd98`;
- producer-independent result audit: 77/77.

All 12 later classical duties and all 6 quantum duties are
`INELIGIBLE_AFTER_FIRST_FAIL`; none was evaluated. The proposal is exhausted,
and no retry, retune, deletion or alternate root is eligible.

## Authority

Candidate admission and classical proof remain false. The independent Rust
authorization, ledger and root remain absent. G3, SI, lane, lamp, physical
viability, propulsion and transport authority all remain false.

The only eligible successor is a bounded theoretical review of whether the
frozen smooth-global-static-state requirement is appropriate and, if it is,
whether any genuinely new candidate family can satisfy it. This result does not
authorize changing or rerunning the failed Tolman-VII candidate.
