# NHM2 spherical boson-star v2 G2B-B4-R4 integrated four-grid successor

Program gate: G2B — replacement classical proof attempt  
Workstream: authenticated classical branch closure  
Capability or component: fresh-output four-grid execution with audited predictor and path semantics  
Current maturity: preregistered additive successor to immutable B4  
Target maturity: authenticated first-failure receipt or bounded four-grid `PASS`  
Required frozen inputs: B4-R1 initializer and receipt, B4-R3 reconciliation receipt, immutable B4 runner, branch policy, solver/evaluator sources and admitted Linux runtime  
Required evidence: sealed two-delta wrapper, host/image preexecution, one exclusive invocation, per-level/pair chronology and independent terminal audit  
Stop/fail criteria: first source/runtime/initializer/grid/solve/pair/persistence mismatch; no retry or retune  
Explicit non-goals: payload mutation or rescaling; changing amplitudes, grids, solvers, thresholds, chronology, proof duties, candidate admission, lane/lamp/physical/propulsion/transport authority  
Downstream gate unlocked: preparation of the vacuum-continuation execution packet only after exact four-level/three-pair `PASS`

## Authorized delta

This successor implements only the two corrections independently admitted by
G2B-B4-R3 receipt
`c067e2109f1aeba8bb3f1329d3ddf3c6db8663e1c35c4e35d443d86a896738d3`:

1. delete the B4-R2 pre-Newton assertion that the unchanged λ=`2^-5`
   caller predictor must already have origin amplitude `2^-16`; and
2. emit every initializer payload path from the actual B4-R1 initializer root,
   then re-open that exact path before recording its size and hash.

The first correction does not change a predictor byte. The frozen initializer
still materializes `varphi(0)=2^-10` (`3f50000000000000`). The separately
supplied first Newton target remains `2^-16` (`3ef0000000000000`), the final
target remains `2^-10`, and the continuation/Newton implementation remains
unchanged. The target is imposed by the Newton residual row after the caller
predictor is accepted.

The second correction does not copy or mutate a payload. Its emitted path is
computed as:

```text
(INITIALIZER_ROOT.relative_to(ROOT) / payload_relative_path).as_posix()
```

The wrapper must bind the immutable B4 spine, B4-R1 receipt and all six payload
bytes, the B4-R3 receipt, policy, evaluator, solver sources, runtime manifest,
cross-grid evaluator, its own bytes, test bytes and checkpoint. Host and
admitted-image preexecution must prove the exact delta and reject a legacy B1
payload path.

All other payload values, λ, amplitude schedule `2^-16` through `2^-10`, node
counts `64/96/128/256`, independent-full-solve rule, Newton/Armijo policy,
convergence thresholds, adjacent-pair ordering, output schemas, first-failure
chronology and authority locks are inherited unchanged from immutable B4.

## One-shot execution

The sole output root is:

```text
artifacts/nhm2-spherical-boson-star-v2-g2/g2b-b4-r4-four-grid-v1/
```

It must be absent before execution and must never be deleted, reused or retried
under this packet. The admitted image remains
`sha256:715f6ea4674ace8cd1758954b09fda66e7f4ca9373a28af7a796fe1f9926dfd1`
and the container must run with `--network none`. The execution token binds the
packet hash. No result may be inspected before the sole invocation terminates.

A normal numerical failure is terminal scientific evidence. If a level fails,
later levels and dependent pairs remain unexecuted. If a pair fails, later
proof duties remain locked. There is no retry, parameter search, candidate
substitution, rescaling, tolerance change or output-root reuse.

## Decision and authority

An exact terminal `PASS` requires all four level receipts, every frozen
amplitude stage at each level, and all three adjacent cross-grid comparisons to
pass. It unlocks only preparation of the separately sealed vacuum-continuation
duty. A numerical `FAIL` closes this candidate execution and activates bounded
scientific review; a missing prerequisite produces only the typed fail-closed
state named by the runner.

Candidate admission, vacuum connection, no-fold, positivity, boundary proof,
joint geometry/state, 68-file execution, replay, pair agreement, Theory Graph
lamp, physical viability, propulsion and transport authority remain false for
every possible B4-R4 result.
