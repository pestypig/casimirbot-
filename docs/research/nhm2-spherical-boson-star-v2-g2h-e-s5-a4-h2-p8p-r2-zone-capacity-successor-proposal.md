Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R2 zone-only capacity successor
Current maturity: R1 exhausted before VM allocation on `us-east1-c` C2D capacity
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R2 preexecution record
Required frozen inputs: original proposal `2b533a98...be80`, browser-SSH amendment `373cc75f...b682`, image correction `5f4639fd...a937`, R1 result `53ccf71d...4540708`, exact archives/controller/executable/auditor
Required evidence: exact-name absence, one exact creation attempt, exact resource bindings, two authenticated uploads, one controller invocation, serial evidence, independent audit and P8Q classification
Stop/fail criteria: first failure terminal; no second zone, retry, fallback, resource substitution, additional upload/process, candidate ingress or authority change
Explicit non-goals: scientific changes, retuning, frozen-candidate evaluation, P=65,536 execution, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full-run or scientific authority

# H2-P8P-R2 zone-capacity successor proposal

Status date: September 1, 2026.

Status: **FROZEN CAPACITY-ONLY PREEXECUTION PROPOSAL / NO R2 RESOURCE OR PROCESS CREATED**.

## Result-conditioned change

R1 consumed exactly one creation request for `c2d-standard-32` in
`us-east1-c`. Google terminated the insert operation after seven seconds with
`ZONE_RESOURCE_POOL_EXHAUSTED`; no usable VM, disk, upload, build or numerical
process followed. R1 is exhausted and cannot be retried.

R2 changes only:

1. the predefined VM name to
   `nhm2-h2-p8p-r2-c2d-32-20260901`; and
2. the zone from exhausted `us-east1-c` to the separately supported,
   previously unused C2D placement pool `us-east1-d`.

Every other R1 term remains unchanged: project, on-demand C2D machine, CPU
count, exact Debian image, disk type/size, runtime and cost ceilings, local
archive ingress, browser-SSH guest workflow, offline build, executable,
P=1024 workload, precision, observer schedule, timeout, evidence ABI,
serial-only recovery, result auditor, P8Q classifier, first-failure rule and all
authority locks.

This is a provider-capacity successor, not a numerical retry, mathematical
retune or scientific change. The R1 operation produced no calibration data to
observe or tune against.

## Exact resource binding

After separate explicit authorization, exactly one creation attempt may use:

| Field | Frozen R2 value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM | `nhm2-h2-p8p-r2-c2d-32-20260901` |
| zone | `us-east1-d` |
| machine | on-demand `c2d-standard-32`, 32 CPUs |
| image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260826` |
| boot disk | exactly 30 GB `pd-standard`, auto-delete |
| aggregate VM ceiling | 18,000 seconds |
| total ceiling | $9.00 compute plus prorated storage |
| numerical timeout | 14,400 seconds |

Before submission, the authenticated Compute Engine list must prove the exact
R2 name absent. The creation form must expose exactly the tabled values. One
submission is permitted. If Google rejects the request, produces a different
resource, or fails to expose one running exact-name VM, R2 terminates before
upload. No second zone, regional allocator, alternate family, disk/image
change, Spot/Flex-start request, reservation, retry or fallback is authorized.

## Unchanged browser-SSH ingress

Only after the exact VM is running, use the authenticated Compute Engine
SSH-in-browser surface. Require guest username `pestypig`; no user-directed
IAM, firewall, project-metadata or persistent SSH-key mutation is authorized.
Upload exactly these two local regular files to initially absent paths in
`/home/pestypig`:

| Archive | Bytes | SHA-256 |
| --- | ---: | --- |
| `h2-p8f-c2-r1-cloud-upload-v1.tar` | 236,492,800 | `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978` |
| `h2-p8p-overlay-upload-v1.tar` | 134,656 | `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e` |

No other upload is permitted. Require both remote files to be regular,
non-symlink files with exact byte counts and hashes. Require these paths absent
before extraction or execution:

- `/home/pestypig/nhm2-h2-p8p-source-v1`;
- `/home/pestypig/nhm2-h2-p8p-evidence-v1`;
- `/home/pestypig/nhm2-h2-p8p-evidence-export-v1.tgz`.

Install only Debian `docker.io` if Docker is absent. This browser action
requires the ordinary action-time installation confirmation. Add no repository,
key or unrelated package.

Extract base then overlay. Require the 11-entry source manifest SHA-256
`c6ee88481ae7842b176d4c8a2001601c38cc317132c446a086c92b75ddef5aa0` and
the 4,498-byte controller SHA-256
`5af4b629336e166d07a277ae59b0f9776ac9e86b728e762030f27237ed1c8f5b`.
Invoke that controller exactly once through `sudo bash`.

## Unchanged calibration and decision

The controller retains the no-pull/no-network offline Docker build and must
require executable SHA-256
`7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`
before starting exactly one candidate-neutral P=1024 process on 32 CPUs. It
must preserve phase-specific selector/observer/total timing, the bounded
64-line progress stream, all 514 degree buckets, six aggregate totals, P8I
equality, chronology and neutrality locks.

After PASS, FAIL, timeout or partial output, export exactly one deterministic
evidence archive through the stopped VM's serial output and stop the VM. Do not
restart it or create a disk-rescue path under R2. Recover and authenticate the
one serial archive locally, then run the unchanged independent result auditor
SHA-256
`cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`.

Only an authenticated calibration may apply the preregistered P8Q rule and
produce `P8Q_YES_PROPOSAL_READY` or
`P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`. Failure, timeout without a complete
receipt, or evidence drift produces
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. No outcome authorizes P=65,536.

## Authority locks

No frozen-candidate evaluation, positive sampling, candidate/scientific root,
token or handler is permitted. No G3, SI, metric, lane, replay or lamp work is
permitted. Candidate, proof, geometry/state, lane, lamp, physical, propulsion
and transport authority remain false.

## Exact authorization text

> I authorize exactly one H2-P8P-R2 browser-SSH P=1024 turnaround calibration under the final independently audited R2 proposal SHA-256. Create exactly one temporary on-demand Google Compute Engine VM `nhm2-h2-p8p-r2-c2d-32-20260901` in project `dark-stratum-455714-h4`, zone `us-east1-d`, using exactly `c2d-standard-32`, exact image `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`, and exactly 30 GB `pd-standard` storage, with an 18,000-second aggregate VM runtime ceiling and a $9.00 total compute and prorated-storage ceiling. Through authenticated Compute Engine SSH-in-browser, upload only the exact 236,492,800-byte base archive SHA-256 `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978` and exact 134,656-byte P8P overlay SHA-256 `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`; require guest identity and all ingress/output absence guards; install only Debian `docker.io` if absent after the required action-time confirmation; extract base then overlay; authenticate the manifest and controller; require executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`; and execute exactly one candidate-neutral P=1024 calibration with 32 CPUs and a 14,400-second external timeout. Preserve PASS, FAIL, timeout, preexecution failure or partial output, automatically stop the VM, recover the one hash-bound archive only through stopped-VM serial output, run the unchanged independent result audit and apply only the preregistered P8Q rule. First failure is terminal. I do not authorize retry, fallback, a second zone or creation attempt, regional allocation, resource substitution, additional upload or process, scientific change or retuning, frozen-candidate evaluation, positive sampling, candidate/scientific root, token or handler creation, evidence deletion, VM restart or disk rescue, P=65,536 execution, G3/SI/metric/lane work, IAM/firewall mutation, or any candidate, proof, geometry/state, lane, lamp, physical, propulsion or transport authority promotion.
