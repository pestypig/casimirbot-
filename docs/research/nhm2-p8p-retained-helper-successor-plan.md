Program gate: G2H-E-S5-A4 — P8P observer progress and turnaround calibration
Workstream: candidate-neutral existing R39 evidence recovery
Capability or component: same-helper successor after namespace readiness failure
Current maturity: second corrected successor authorized; local source review passed; execution pending
Target maturity: authenticated existing archive and confirmed helper stop
Required frozen inputs: charter v2, user approval in ledger0073, retained resource IDs, original56 and retained-v1 61 receipts, archive hash
Required evidence: installed-SDK argument tests, complete lifecycle tests, independent source/manifest review, immutable runtime receipts
Stop/fail criteria: identity or evidence mismatch, unrelated API error, exhausted deadline/budget, unsafe startup or cleanup
Explicit non-goals: new resources, old-file deletion, numerical execution, candidate evaluation, retuning, G3/SI/metric/lane work, authority promotion
Downstream gate unlocked: actual R39 build-evidence classification only

# Approved retained-helper successor: execution prerequisites

Use only helper nhm2-p8p-cv2-hostkey-helper-20260905,
ID2570241336417567358, and retained detached clone
nhm2-p8p-cv2-hostkey-clone-20260905, ID6517758864936518301,
in dark-stratum-455714-h4/us-east1-b. Preserve snapshot
4259767658325590729 and all original resources. One startup replacement,
one restart, one read-only clone attachment only after fresh fixture PASS,
bounded archive recovery, guest unmount and automatic/independent stop.
No repeated creation, old-script retry or original evidence mutation.

1. Authenticate stopped helper, boot disk, detached clone and retained snapshot
   against prior receipts and fresh read-only observations. Check original
   source remains stopped and unchanged. Preserve real creation timestamps:
   the old fresh-resource provenance contract cannot be reused by pretending
   these resources were newly created.
2. Bind startup to retained-helper-v2 exclusive guest root and a distinct
   attempt identity. Preserve existing guest root and files. Isolate new
   fixture/archive namespaces to avoid accepting or overwriting old receipts.
   Existing fixed namespace validators require a scoped successor variant;
   do not loosen them globally or ignore mismatching attempts.
3. Independently test actual SDK metadata-file and restart argument handling
   before dispatch. Replace only helper startup; verify resulting metadata
   and unchanged resource configuration before exactly one restart.
4. Gate attachment on fresh authenticated fixture PASS. Use corrected bounded
   pending-response observation, retaining unrelated-error first-failure
   semantics. Validate retained source chain, one read-only attachment and
   boot-before-attach chronology without backdating observations.
5. Authenticate exact12122-byte archive SHA-256
   73029fde08f14f9fcd01490c4e5d5bc188213eaa2f9a8d857eb5e456b86d0922,
   preserve exclusively locally and confirm stop. Do not rerun R39 build.
6. Freeze complete source inventory, exact command, root, budget and expiry
   after independent review. Reserve0.50USD/3600seconds conservatively inside
   remaining10.90USD/13200seconds, leaving10.40USD/9600seconds afterward.
   The remaining aggregate will constrain later calibration; do not promise
   the original maximum numerical timeout still fits.

The consumed retained-helper-v1 attempt stopped with a FileExistsError before
clone attachment. Its 61-record chain is preserved. The corrected successor
allocates every fixture case beneath a fresh attempt-bound path, in addition
to its fresh bootstrap root and receipt namespaces. Both predecessor chains
are authenticated and the latest stopped metadata is the preflight baseline.
All343 local infrastructure tests passed; independent source review found no
execution-critical blocker. Prepare-v2 and independent manifest verification
must precede the one execute-v2 invocation. This file is not a frozen execution
manifest, current cloud-state observation or recovery PASS receipt.
