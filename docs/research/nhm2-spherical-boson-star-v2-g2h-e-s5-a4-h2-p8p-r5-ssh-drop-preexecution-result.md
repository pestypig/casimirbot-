Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R5 native Windows file-picker preexecution result
Current maturity: terminal SSH connection loss before remote guard authentication; exact VM stopped; zero file-picker, upload, guest-ledger or numerical activity
Target maturity: immutable fail-closed R5 evidence and one separately versioned SSH-stability successor decision
Required frozen inputs: R5 proposal `650806a5...e6e3b03`, stopped exact R3 VM, verified 16-file local staging, unchanged guest ledger/controller/executable/auditor
Required evidence: exact local/resource preflight, one restart, one authenticated browser-SSH surface, terminal connection-loss chronology, zero-ingress boundary and terminal VM state
Stop/fail criteria: connection loss before the required guard marker is terminal; no retry, second SSH, picker, upload, Docker action, build, calibration or P8Q promotion
Explicit non-goals: scientific interpretation, candidate evaluation, retuning, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: separately frozen SSH-stability and transport-admission successor only; no calibration or R5 retry authority

# H2-P8P-R5 SSH-drop preexecution result

Status date: September 1, 2026.

Status: **BLOCKED_PREEXECUTION_SSH_CONNECTION_DROPPED_DURING_REMOTE_GUARD / R5 EXHAUSTED**.

Change classification: receipt semantics only. Mathematical semantics and
runtime authority are unchanged.

## Authorized boundary

The operator authorized exactly one H2-P8P-R5 attempt under proposal SHA-256
`650806a579fdf41dbe7bd6731439bf1872644a2f15091c95388e7ec86e6e3b03`.
It permitted one restart of the exact retained stopped VM, one authenticated
browser-SSH surface, one remote absence guard and—only after its authenticated
success marker—one native Windows file-picker transaction followed by the
unchanged bounded ingress and calibration sequence.

The authenticated resource remained:

- project `dark-stratum-455714-h4`;
- VM `nhm2-h2-p8p-r3-c2d-32-20260901`;
- instance ID `637527339076077505`;
- zone `us-east1-d`;
- on-demand `c2d-standard-32` with 32 vCPUs and 128 GB memory;
- exact image
  `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`;
- retained 30 GB `pd-standard` boot disk;
- 18,000-second aggregate restart ceiling and $9.00 total ceiling.

First failure was terminal. R5 did not authorize Retry, Troubleshoot, a second
SSH surface, a second restart, alternate transport, retuning, candidate
evaluation or authority promotion.

## Observed chronology

1. The independent R5 proposal audit passed `29/29`. The verification-only R3
   ingress materializer authenticated exactly fifteen ordered base chunks plus
   the unchanged overlay and reported `P8P_R3_INGRESS_VERIFIED 16/16`.
2. Read-only local preflight authenticated Chrome profile `Default`, enabled
   ChatGPT extension `hehggadaopoacecdllhhajmbjkdcmajg` version
   `1.26.827.12125_0`, `newAllowFileAccess=true` and
   `withholding_permissions=false` without changing browser state.
3. The exact VM was authenticated `Stopped` before the sole restart. The
   instance ID, `us-east1-d` placement, machine, image and retained disk matched
   the frozen table.
4. The one authorized restart succeeded. The exact VM reported running with
   external IP `34.74.57.207`. Its exact Details page was freshly reloaded once
   and reauthenticated the frozen resource bindings.
5. Exactly one new Compute Engine browser-SSH surface was opened. Its ordinary
   transient authorization succeeded and the guest prompt authenticated user
   `pestypig`.
6. The exact remote absence-guard command was entered and submitted once. The
   SSH surface immediately changed to `Connection Failed` and reported an
   unexpected connection error. Neither `R4_REMOTE_ABSENT_16` nor an
   authenticated guard-failure marker was observed. The guard outcome and the
   remote destination-path state therefore remain unauthenticated; this record
   does not claim that the paths were absent.
7. The connection loss was treated as the first terminal R5 failure. Retry,
   Troubleshoot, a second SSH surface and alternate transport were not used.
   The Upload control was not activated, no native file-open dialog appeared,
   no file was selected, no transfer item was created and zero bytes were
   uploaded.
8. No Docker presence check or installation, guest-ledger invocation, archive
   reconstruction, manifest replay, offline build, executable invocation,
   P=1024 calibration, serial evidence export or turnaround-result audit ran.
9. The exact VM accepted the cleanup stop request and was subsequently
   reauthenticated as `Stopped. This instance is stopped` in the Compute
   Engine instance list.

## Terminal inference

R5 did not reach the native-picker question it was designed to test. The only
new observation is a transient-looking but terminal-in-protocol loss of the
sole authenticated SSH session while the prerequisite remote guard was being
submitted. This is not evidence of archive corruption, remote path presence,
Docker failure, build failure or numerical behavior.

The local base chunks and overlay remain unchanged. Because no complete
authenticated calibration exists, the only valid preregistered classification
is `P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED`. This is not the P8Q yes/no decision
about one full P=65,536 representative observation.

R5 is exhausted. A next attempt requires a separately versioned successor that
first proves a stable authenticated SSH command round trip before admitting the
unchanged one-picker transport sequence. This record grants no restart,
execution, retry or fallback authority.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.
