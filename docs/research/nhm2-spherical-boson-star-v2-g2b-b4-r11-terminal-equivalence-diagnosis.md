Program gate: G2B — replacement classical proof attempt
Workstream: authenticated classical branch closure
Capability or component: B4-R10 terminal-equivalence diagnosis
Current maturity: preregistered bounded no-solve diagnostic
Target maturity: supported separate successor class or frozen-branch closure
Required frozen inputs: immutable R4/R5/R6/R7 and R9/R10 evidence
Required evidence: exact bindings, correction hash, 25 domain trials, comparison
Stop/fail criteria: mismatch, nonlinear solve, merit evaluation, update or mutation
Explicit non-goals: Newton/continuation, retry, retune, candidate/proof/lamp authority
Downstream gate unlocked: new-candidate research only if this branch closes

# NHM2 spherical-boson-star v2 G2B-B4-R11 terminal-equivalence diagnosis

## Change classification

This packet changes proof-maturity evidence only. It changes no equation,
coordinate, candidate, grid, initializer, predictor, threshold, merit,
backtracking schedule, runtime authority, or receipt from B4-R10.

## Fixed question

The sole R8-supported intervention was implemented by R9/R10 and failed at the
same N=64 stage-zero Armijo boundary as R4. This diagnosis asks whether the
immutable R10 bytes distinguish one further scientific successor class, or
whether the frozen candidate branch must close without a GO.

## Bounded computation

The diagnostic may perform exactly these read-only operations:

1. rehash the immutable R4, R5, R6, R7, R9 and R10 evidence named below;
2. reopen the R4 and R10 terminal states and stage metadata;
3. independently evaluate the unchanged residual/Jacobian once at the R10
   terminal state;
4. apply the already frozen R9 power-of-two equilibration and deterministic LU
   once to reconstruct only correction ordinal 29;
5. require the reconstructed direction hash to equal the recorded R10 trace;
6. form the 25 already attempted binary64 trial states for exponents 0..24 and
   classify only finiteness and the unchanged `0 < w < 1` domain predicate;
7. compare the R4/R10 accepted-exponent chronology and terminal words without
   evaluating any trial residual or merit.

This one fixed linear reconstruction is not a Newton or continuation
invocation. No trial residual, Armijo merit, candidate update, new stage, new
grid, or alternative state may be evaluated or persisted.

## Frozen decision rule

The diagnostic emits exactly one terminal decision:

- `SUPPORTED_SEPARATE_SUCCESSOR_CLASS` only if the exact recorded evidence
  identifies one already-defined proposal class that survived R10 and is not
  contradicted by R5–R9;
- `CLOSE_FROZEN_BRANCH_NO_SUPPORTED_SUCCESSOR` if the sole R8/R9 intervention
  is falsified and the recorded evidence supplies no unique remaining proposal;
- a typed diagnostic error if any binding, reconstruction, chronology, or
  authority invariant fails.

An outward frequency correction and 25 domain rejections identify the immediate
acceptance obstruction, but do not by themselves select a new frequency
coordinate, globalization rule, discretization, equation set, candidate, or
threshold. Such a selection would be post-result retuning and is forbidden.

## Frozen inputs

- R4 stage metadata/state and terminal receipt;
- R5 receipt `0cfb59144cf29beb0da94852ee872455a56017cbe3fc690fd6cb24cd401ea406`;
- R6 receipt `0430266c9efe338fecc4c4c01fd2e25d168f481611d2676d023ba6e211a0c001`;
- R7 receipt `c7547fb302f0e128bfce68faf7e60f87d9172715f392fcc0ca329e4f4d667ccb`;
- B4-R9 result and formulation source;
- immutable R10 stage metadata/state and terminal receipt
  `e8d0268f499f6e1cba9ccf26cc34dc602b4a40f7f96478bb7e59e7acce037706`;
- unchanged radial grid, compactified-system, and deterministic-LU sources.

## Output and authority

The sole output root is
`artifacts/nhm2-spherical-boson-star-v2-g2/g2b-b4-r11-terminal-equivalence-diagnosis-v1`.
It may contain exactly `receipt.json`. Existing R10 bytes remain untouched.

Candidate admission, vacuum continuation, proof, execution, replay, lane, pair
agreement, joint geometry/state, Theory Graph, diagnostic lamp, physical
viability, propulsion, and transport authority remain false for every outcome.
