Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: G2H-E-S5-A4 / H2 representative attribution
Capability or component: H2-P8J-R9 atomic Cloud Shell command-transport successor
Current maturity: frozen candidate-neutral staging and single-run proposal; no R9 cloud resource
Target maturity: authenticated controller staging followed by at most one unchanged regional allocation and representative process
Required frozen inputs: R8 terminal result `28f66693...9abc43`, R9 controller `7007992e...8e379`, command ledger `4c94a957...a8d54`, retained archives and unchanged P8J binaries
Required evidence: exact browser textbox admission, 33 ordered commands, 30 cumulative chunk markers, dual-hash reconstruction, cloud chronology, immutable scientific evidence and independent audit
Stop/fail criteria: any nonempty or mismatched textbox, missing/duplicate marker, hash or identity mismatch, command failure, second creation request, retry, fallback, retune, candidate ingress, evidence deletion or authority promotion
Explicit non-goals: scientific-code change, frozen-candidate evaluation, positive sampling, G3/SI/metric/lane work or physical claims
Downstream gate unlocked: observed `pd-standard` allocation boundary or one authenticated candidate-neutral P8J attribution result

# H2-P8J-R9 atomic command-transport successor proposal

Date: 2026-08-31

Status: **FROZEN PREEXECUTION PROPOSAL / NO R9 CLOUD RESOURCE CREATED**.

## Why R9 exists

R8 did not test Google Cloud capacity or the P8J calculation. Its complete
guard was concatenated with a partial unsent xterm prefix after a browser text
entry timeout. Bash rejected the compound line during parsing, before any
guard or `gcloud` operation executed. R8 is exhausted.

R9 changes only the browser-to-Cloud-Shell transport and the separately named
resource identity. It reconstructs one locally frozen orchestrator through
short, ordered, observable base64 chunks. The exact R8 `c2d-standard-32`,
regional allocation, 30 GB `pd-standard`, retained archives, offline build,
P8I fixture, P8J binary, representative input, precision, process, ceilings,
evidence rules and authority locks remain unchanged.

## Frozen transport identities

| Item | Frozen identity |
| --- | --- |
| orchestrator | `tools/nhm2-spherical-boson-star-v2-branch-proof/g2h/h2_p8j_r9_cloudshell_orchestrator_v1.sh` |
| orchestrator bytes / SHA-256 | `7,132` / `7007992e2538f7ecb981a4b0d588bb1cba0e7abafa22cdab63c35c266bc8e379` |
| base64 characters / SHA-256 | `9,512` / `9f8f00f03a8a0761e9f1c830818bb7065a779dbd083e254ad42e4f2459d3b500` |
| chunk size / count | at most `320` characters / exactly `30` |
| staging ledger | `artifacts/nhm2/g2h-e-s5/candidate-neutral/h2-p8j-r9-atomic-transport-preflight-v1-20260831/h2-p8j-r9-staging-command-ledger.v1.json` |
| ledger SHA-256 | `4c94a957d056ee32806b7d6b6ffc90118ca9d7578dd6ef9f877e3f54356a8d54` |
| rendered commands SHA-256 | `d89489c1543348d9aea2873c6b8643e996be489745fe46f9e6d3008b3f749422` |
| definition audit | `27/27 PASS` |
| staged destination | `/home/pestypig/h2-p8j-r9-cloudshell-orchestrator.v1.sh` |

Git Bash parses the orchestrator with `bash -n` before freezing. The
orchestrator contains exactly one `gcloud compute instances bulk create`, one
two-file `gcloud compute scp`, one setup `gcloud compute ssh`, and one
fail-closed VM stop path. It contains no candidate input or scientific retune.

## Atomic terminal admission

Use the authenticated Cloud Shell page for project
`dark-stratum-455714-h4`. Before command 1, require a visible shell prompt and
the terminal textbox DOM value to be exactly empty. For each of the exact 33
ledger commands:

1. fill, but do not submit, the exact ledger command;
2. read the textbox DOM value and require byte-for-byte equality with that
   command;
3. submit exactly once only after equality;
4. require the command's expected marker and a returned empty prompt before
   filling the next command.

Any mismatch is terminal before submission. Do not use incremental typing,
clipboard append, duplicate Enter, blank command, terminal fallback, editor
paste, file upload or a second terminal.

Command 1 requires the destination, base64 partial and decoded temporary all
absent and emits `R9_STAGE_READY`. Commands 2 through 31 write exactly 30
ordered chunks, verify cumulative length and emit `R9_CHUNK_01` through
`R9_CHUNK_30`. Command 32 verifies the exact base64 length/hash, decodes to an
initially absent temporary, verifies the 7,132-byte controller hash, moves it
non-clobberingly and emits `R9_STAGE_COMPLETE`. Command 33 reauthenticates the
regular non-symlink destination and invokes it exactly once through Bash.

## Sole cloud and scientific path

The authenticated controller first creates a new evidence root and rechecks:

- the two retained Cloud Shell archives as regular non-symlinks with exact
  bytes and SHA-256;
- exact R9 VM-name absence across the project;
- zero non-terminated `nhm2-h2-` VMs; and
- at least 32 unused `C2D_CPUS` in `us-east1`.

Only after `R9_PREEXECUTION_PASS` may it submit its sole regional request:

| Binding | Frozen value |
| --- | --- |
| project / region | `dark-stratum-455714-h4` / `us-east1` |
| name / count / minimum | `nhm2-h2-p8j-r9-c2d-32-20260831` / `1` / `1` |
| distribution / zones | `ANY_SINGLE_ZONE` / `us-east1-b,c,d` allow |
| machine / provisioning | `c2d-standard-32` / `STANDARD` on-demand |
| boot image | `projects/debian-cloud/global/images/debian-12-bookworm-v20260817` |
| boot disk | exactly 30 GB `pd-standard`, auto-delete |
| provider backstop | 25 hours, termination action `STOP`, no restart on failure |

After allocation it requires exactly one running exact-name VM, one allowed
zone, exact machine, image and disk bindings. Following one fixed 180-second
wait, it copies the two archives in one SCP, rehashes them remotely, extracts
base then overlay, validates the 16-entry overlay manifest, authenticates the
unchanged controller SHA-256 `4b8f5722...8e6`, installs Docker, and starts one
root-owned systemd oneshot service.

That service uses the unchanged P8J controller. It loads only the archived
pinned images, builds with no pull and no network, requires the P8I executable
`445e6a27...04ab2` and 14/14 fixture PASS, requires the P8J executable
`d40c6e51...de9d6`, and starts exactly one candidate-neutral P=65,536,
32-CPU, degree-3, ordered-second-jet-9, 512-bit-Arb attribution process with
the frozen 86,400-second timeout. It archives complete or partial evidence and
automatically stops the VM. The aggregate ceiling remains 90,000 seconds and
the total compute and prorated-storage ceiling remains `$40.00`.

## Terminal rules and interpretation

The first staging, guard, allocation, transfer, build, fixture, binary,
service or numerical failure is terminal. The orchestrator may stop the one
created VM after failure, but may not retry, recreate, substitute a resource,
change zones/machine/storage, upload another file, retune, change an evidence
root or start a second process.

Provider failure establishes only a provisioning boundary. A completed P8J
record may localize the candidate-neutral H2 failure under the already frozen
auditor. Neither outcome evaluates the frozen boson-star candidate or promotes
candidate, proof, geometry/state, lane, lamp, physical, propulsion or transport
authority. All such authority remains false.

No R9 command has been entered, no procedure staged, no VM or disk created and
no cloud or numerical process started by this proposal.
