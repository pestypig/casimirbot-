Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R12 authenticated in-app-browser retained-VM start/calibration result
Current maturity: immutable start-completion failure; R12 exhausted after one start action and one exact confirmation, before VM boot or SSH
Target maturity: preserve exact terminal evidence and select at most one separately frozen read-only start-operation diagnosis
Required frozen inputs: R12 proposal, exact retained authenticated tab/resource, one enabled Start item, one direct confirmation and first-failure rule
Required evidence: exact pre-start identity/state, one start action, one confirmation, post-action state, SSH availability and zero downstream activity
Stop/fail criteria: missing Running completion terminal; no second start, refresh, alternate transport, SSH, upload, Docker, build or calculation
Explicit non-goals: retry, new resource, alternate zone, capacity workaround, candidate evaluation, retuning, P=65,536, deletion, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: one separately frozen read-only operation-diagnosis lead only; P8Q remains stopped

# H2-P8P-R12 start-completion failure result

Status date: September 2, 2026.

Status: **BLOCKED_START_COMPLETION_NOT_OBSERVED / R12 EXHAUSTED**.

## Frozen authority

The operator authorized exactly one R12 attempt under proposal SHA-256
`5bf120d9cfb3f66435cf0f97dd8c6f0002b4c1511dfc5504b0ce1163e03be7d4`.
Its independent static audit passed 19/19. The proposal allowed one semantic
Start action, at most one exact confirmation, and no retry or fallback.

## Observed chronology

1. The retained authenticated Details tab re-bound exact stopped VM
   `nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID
   `1920090043510946854`, project `dark-stratum-455714-h4`, zone
   `us-east1-c`, image `debian-12-bookworm-v20260817`, machine
   `c2d-standard-32 (32 vCPUs, 128 GB Memory)` and one 30 GB Standard
   persistent boot disk.
2. Its expanded More-actions menu exposed exactly one enabled semantic
   `Start / Resume` button. That button was clicked exactly once.
3. Google displayed one direct dialog titled
   `Start nhm2-h2-p8j-r9-c2d-32-20260831?` and stated that the configured VM
   would incur charges. Its single `Start` confirmation was accepted exactly
   once.
4. The immediate returned Details state still showed `Stopped` and disabled
   SSH. After the prescribed boot interval was interrupted only by an operator
   check-in, a later read of the same retained page still showed exact state
   `Stopped` and disabled SSH. No second start or confirmation occurred.
5. The rendered page exposed no explicit capacity or operation error. R12 does
   not authorize opening notifications, refreshing, querying an API or using a
   fallback after missing start completion.
6. No VM boot was authenticated. No SSH surface, round-trip, remote guard,
   chooser, selected file, upload, Docker action, build, P=1024 process,
   evidence archive or result audit occurred. The resource was already stopped,
   so no additional stop action was necessary.

This is an environment/start-completion failure, not a P=1024 turnaround result
and not a scientific result. It does not establish whether the cause was
capacity, quota, operation rejection or UI propagation.

## P8Q and authority

Only `P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED` applies. Candidate evaluations
and positive samples remain zero. Candidate/scientific roots, tokens and
handler linkage remain absent. Candidate, proof, geometry/state, lane, lamp,
physical, propulsion and transport authority remain false.

The sole eligible next lead is a separately frozen read-only inspection of the
Google Cloud operation or activity record for this exact one start request. It
may diagnose the cause but cannot start, retry, stop or modify any resource.
