Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R1 browser-selectable Debian host-image correction
Current maturity: predecessor exhausted before resource creation because its exact image is absent from the browser selector
Target maturity: one unchanged browser-SSH P=1024 calibration under an exact currently selectable Debian 12 image
Required frozen inputs: original proposal `2b533a98...be80`, amendment `373cc75f...b682`, predecessor result, exact two archives, controller, executable and auditor
Required evidence: exact-name absence, exact current image selection, one VM, two authenticated uploads, one controller invocation, serial recovery and unchanged result audit
Stop/fail criteria: first failure terminal; no image/resource substitution, retry, additional upload/process, missing serial evidence, candidate ingress or authority change
Explicit non-goals: scientific changes, retuning, frozen-candidate evaluation, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: preregistered P8Q yes/no proposal-readiness decision only

# H2-P8P-R1 browser host-image binding correction

Status date: September 1, 2026.

Status: **FROZEN ADDITIVE PREEXECUTION CORRECTION / NO RESOURCE OR PROCESS CREATED**.

## Single corrected binding

The predecessor browser-SSH authorization stopped before resource creation
because the exact public image `debian-12-bookworm-v20260817` is no longer
selectable in the authenticated Compute Engine VM form. The same form exposed
Debian GNU/Linux 12 (bookworm), x86/64, built on `20260826`.

R1 changes only the exact host-image binding to:

`projects/debian-cloud/global/images/debian-12-bookworm-v20260826`

Every other original-proposal and browser-SSH-amendment term remains unchanged,
including project, name, zone, on-demand `c2d-standard-32`, 30 GB
`pd-standard`, 18,000-second aggregate runtime, $9.00 ceiling, exact two-file
upload boundary, offline build, binary identity, one P=1024 process,
14,400-second external timeout, serial-only recovery, P8Q rule, first-failure
rule and all authority locks.

This host update does not change container base images, source bytes,
mathematics, precision, selector, observer, schedule, thresholds, reduction
order or result semantics. The build remains offline after loading the same
archived pinned base images.

## Exact authorization text

> I authorize exactly one H2-P8P-R1 browser-SSH execution under the final SHA-256 of this image-binding correction. Replace only the unavailable host image `projects/debian-cloud/global/images/debian-12-bookworm-v20260817` with exact currently selectable image `projects/debian-cloud/global/images/debian-12-bookworm-v20260826`. Preserve every other term, ceiling, upload identity, build and executable identity, one-process rule, timeout, evidence rule, serial-only recovery rule, P8Q classifier, first-failure rule, scientific boundary and authority lock of original proposal SHA-256 `2b533a983120cd23e6b7bf3b4fbc5f3546e69000c67a91ee4033f98a2b14be80` and browser-SSH amendment SHA-256 `373cc75ffba5f944feda351b2e91f99d792edbced0f2e52ed555de63adddb682`. No retry, fallback, resource substitution, additional upload or process, scientific change, retuning, candidate evaluation, P=65,536 execution, G3/SI/metric/lane work, evidence deletion or authority promotion is authorized.
