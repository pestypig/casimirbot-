Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: candidate-neutral C08 implementation and performance equivalence
Capability or component: H2 convolution-selector performance successor and durable goal handoff
Current maturity: H2-P7 parent proposal `3f15f387...fdc3` frozen; producer preflight 12/12 and independent audit 27/27; zero cloud or numerical action
Target maturity: separately authorized candidate-neutral H2 parent execution and independent result audit
Required frozen inputs: checkpoint ABI `6fbf6cdb...911ca`, repaired Borel-growth definition `7dd4d30a...94737`, state-jet definition `75eff013...ffc`, 512-bit Arb arithmetic, order 128, 13-jet inventory, 43 elementary convolutions per subpanel, dyadic candidates `2^0..2^16`, first-passing selection, failure precedence, and every authority lock
Required evidence: preserved baseline and R1 blocker evidence, completed R2 clean-daemon replay, exact machine/storage/cost/runtime/run-count boundaries, archive and binary bindings, immutable outcome rules, independent proposal audit, and current math/WARP/Casimir verification
Stop/fail criteria: any changed equation, candidate byte, selector schedule, threshold, arithmetic precision, enclosure, counter, failure precedence, non-deterministic replay, R1 restart or build retry, selected-member ingress, positive sampling, root/token/authorization creation, or authority promotion
Explicit non-goals: restarting or changing any preserved H2 execution; evaluating the frozen `6/5` member; retuning C08; authorizing a scientific run; completing the boson-star benchmark, G3, SI/metric work, either 68-file lane, the Theory Graph lamp, physical viability, propulsion, or transport
Downstream gate unlocked: one separate exact H2-P7 execution authorization decision; no VM or scientific execution authority

# H2 performance-successor handoff

Status date: August 27, 2026.

## Decision

Do not cancel or replace the canonical G2H-E-S5 objective. The immediate work
pointer changes from waiting for the serial H2 fixture to a bounded corrective
sub-goal inside the already-active S5-A/A4 row:

```text
S5-A / A4
  -> preserve both existing H2 executions unchanged
  -> build and audit a candidate-neutral performance-equivalent H2 successor
  -> use the successor only after exact equivalence and scaling gates pass
  -> finish and independently audit H2/P2
  -> realize the remaining derivative-convolution ledgers
  -> complete C08 and advance to A5
```

This packet changes planning and implementation sequencing only. It does not
change mathematical semantics, runtime authority, receipt semantics, candidate
identity, execution authority, proof maturity, or claim authority.

## Position being preserved

The permanent program objective remains:

```text
frozen candidate
  -> mathematical proofs
  -> accepted joint geometry/state
  -> SI + metric inputs
  -> two source/runtime-disjoint 68-file lanes
  -> replay and pair agreement
  -> diagnostic Theory Graph lamp
```

The exact active pointer is much earlier: G2H-E-S5, stage S5-A, row A4, inside
the candidate-neutral C08 producer. The repaired Borel definition and its
independent acknowledgement are complete. C08-001 through the existing
C08-011c components remain preserved. H2 fixture evaluation is active, P2 is
provisional on H2, derivative-convolution ledgers remain absent, C08 is not
handler-complete, and the scientific dispatch matrix remains 0/19. G3 and all
SI, metric, lane, replay, lamp and physical-claim work remain ineligible.

## Preserved runtime evidence

No existing execution is a progress-capable runner, and neither may be
restarted, redirected, retuned, deleted, or treated as authority by this packet.

| Evidence | Preserved state at handoff |
| --- | --- |
| Local serial H2 | stopped August 27 after a zero-output capture; original auto-removing container identity and exact image are preserved in evidence, and the same name now exists as a non-running, non-auto-removing replacement container |
| Cloud serial H2 | VM `nhm2-h2-audit-c4-20260826` is `TERMINATED`; its disk, instance definition and available zero-log partial evidence remain preserved |
| Calibration VM | `nhm2-h2-calibration-c4-20260827`, confirmed `TERMINATED` after evidence preservation |
| Calibration executable | SHA-256 `0afc791ec06d1d9870f77b4a0cc95460a3d0dca61a103e47a106e9415c2b2b73` |
| Calibration evidence archive | Cloud Shell `~/nhm2-h2-calibration-evidence-20260827.tar.gz`, SHA-256 `ccd7069a3aaad9e5aad3d9416ee49bc28434cc2a706bae3b7a1da8288294f645` |

The sole `--max-exponent 2` calibration completed with exit code 0 in 82.373
seconds: exponent timings were 11.322, 23.105 and 47.945 seconds for 1, 2 and 4
subpanels. It evaluated 7 subpanels and 301 elementary convolutions, with zero
candidate evaluations, zero positive samples, no candidate root and no
authority promotion.

The observed mean is about 11.7676 seconds per subpanel. The frozen structural
schedule contains 131,071 cumulative subpanels per selector, giving a naive
single-core projection of about 17.85 days per selector and 35.70 days for two
selectors. This is a planning diagnostic, not proof or execution authority.

## Optimization hypotheses frozen before implementation

The first successor may investigate only these implementation-preserving
leads, in order:

1. Compute direct/reflected ledger coverage once per subpanel and pass immutable
   coverage views to the convolution jet and bivariate predecessor.
2. Prepare endpoint powers, beta moments, target geometry and scale factors once
   per subpanel instead of rebuilding them for each of 43 convolutions.
3. Translate and bound each unique source jet once per subpanel, then reuse the
   immutable prepared hull for every ordered jet pairing.
4. Replace repeated Arb allocation and teardown with bounded thread-local
   workspaces without changing arithmetic precision or operation order.
5. Evaluate independent subpanels concurrently, store results by ordinal, and
   reduce them serially in the original ordinal order. Refinement candidates
   remain sequential so first-passing selection and failure chronology remain
   unchanged.
6. Add progress receipts and restartable exponent-boundary checkpoints only in
   the versioned successor, with exact input, executable, completed-ordinal and
   accumulated-output hashes.

No optimization may infer a result from the timing calibration or inspect the
frozen selected member.

## Bounded successor stages

| Stage | Work | Passing evidence | Failure route |
| --- | --- | --- | --- |
| H2-P0 | Seal this handoff, baseline identities and benchmark rules | exact preserved-run/calibration references and unchanged authority locks | correct documentation only |
| H2-P1 | Add candidate-neutral phase profiling | **complete: diagnostic PASS**; the [v4 function-profile result](./nhm2-spherical-boson-star-v2-g2h-e-s5-a4-h2-p1-profile-result.md) binds 770 symbolized records, zero profiler overflow and exact semantic equivalence; it localizes repeated moment construction without changing numerical outputs | preserve the three failed `gmon` attempts; reopen only for a demonstrated evidence defect |
| H2-P2 | Implement prepared-subpanel caching and bounded workspace reuse | **complete: candidate-neutral PASS**; the [prepared-moment result](./nhm2-spherical-boson-star-v2-g2h-e-s5-a4-h2-p2-prepared-moment-result.md) binds manufactured 8/8, order-128 `arb_equal`, exact result/NDJSON equivalence, audit 19/19 and 10.21x same-process speedup; workspace reuse is unnecessary for this stage and remains a separately gated fallback | reopen at the first equivalence/evidence defect; do not weaken equivalence |
| H2-P3 | Implement deterministic subpanel parallelism | **complete PASS**; exact semantics agree across 1/2/4/8/16 threads, including repeated 16-thread replay, two 31/31 fixtures and audit 40/40 | return to serial successor at the first demonstrated equivalence defect; do not parallelize refinement candidates |
| H2-P4 | Run bounded scaling calibration | **complete for exponent 2**; valid seven-subpanel measurements, but the largest candidate has only four outer-parallel tasks | do not generalize the measured plateau to representative widths |
| H2-P5 | Bind a successor runtime proposal | **BLOCKED with audit 19/19**; the two-selector linear warning is 34.8899 hours, while representative-width scaling is absent | H2-P5A preparation is complete; run only its separately authorized candidate-neutral `P=1024` sequence |
| H2-P5A | Measure representative-width scaling | **COMPLETE PASS**; exact R2 sequence completed 5/5, slower 16-thread time 157,550 ms, two-selector projection 22.406940158 hours, independent audit 50/50 | preserve the immutable result; do not rerun |
| H2-P6 | Bind the parent runtime | **COMPLETE PASS INERT**; H2/P2 fixed to 16 threads, manufactured fixture 11/11 and independent audit 33/33 | freeze a candidate-neutral parent execution packet; do not execute without separate authority |
| H2-P7 | Freeze the parent execution | **PROPOSAL FROZEN / AWAITING SEPARATE AUTHORIZATION**; archive 44 entries, preflight 12/12, audit 27/27, zero cloud or numerical action | execute only after the packet's exact authorization; otherwise make no cloud action |

The preregistered performance targets are at least 4x from prepared-data reuse,
at least 70% parallel efficiency on 16 vCPUs, and at least 30x combined speedup,
with 40x preferred. These are scheduling criteria, not scientific thresholds;
missing them cannot justify changing the frozen mathematics.

## Equivalence and promotion rules

Before the successor can replace the serial implementation for later work, it
must demonstrate:

- `arb_equal` endpoints, coefficients, remainders and width margins for every
  bounded comparison fixture;
- identical selected refinement candidate, counters, failure detail and first-
  failure chronology;
- deterministic results across repeated runs and declared thread counts;
- unchanged 512-bit precision, order-128 inventory, 13-jet/43-convolution
  semantics, candidate schedule and width rules;
- candidate-neutral manufactured, corruption and resource-bound fixtures;
- independent audit and the applicable math, WARP and Casimir verification.

A performance PASS promotes only the implementation option available to finish
H2/P2 within S5-A/A4. It does not complete C08, close S5, authorize BMR-F, accept
the boson-star benchmark, begin NHM2 transfer, or promote any authority.

## Relation to the full NHM2 solve

```text
H2 performance successor
  -> H2/P2 candidate-neutral closure
  -> remaining C08 derivative ledgers and handler (S5-A/A5)
  -> validated classical and quantum checkpoint implementation (S5-B..S5-E)
  -> separately authorized primary and independent boson-star executions
     (BMR-F/BMR-G)
  -> pair agreement and benchmark decision (BMR-H)
  -> separate NHM2 transfer and same-chart source/material/quantum gates (BMR-I)
  -> only then the permanent geometry/state, SI/metric, two-lane and lamp path
```

The optimization is therefore important but narrow: it removes a computational
blocker in one C08 producer. Even complete success here is not a boson-star
solution and cannot substitute the star's later proof, independent replay or
the separate NHM2 source/material decision.

## Resume instruction

H2-P3 and P4 are complete within their exact evidence boundaries. H2-P5 is
sealed `BLOCKED` because exponent-2 cannot expose more than four outer worker
tasks. Both serial executions are stopped with partial evidence preserved.
H2-P5A-R1 is exhausted as immutable `BLOCKED_PREEXECUTION_BUILD_BINDING`:
archive and VM checks passed, both frozen tags loaded, their `RepoDigests` were
empty, and the frozen `name@repository-digest` `FROM` stopped before
compilation. Audit 22/22 and bundle `e9b0dcce...75f4d` are preserved; the R1
and predecessor VMs are `TERMINATED`; zero representative runs occurred. The
R2 now closes that build blocker without reinterpreting R1: it preserves all
37 R1 members, adds only a storage-safe local-tag Dockerfile and offline guard,
passes producer preflight 11/11, reproduces the exact clean-cloud config IDs
and binary `aa37562f...13b7`, and passes independent audit 28/28. Zero
calibrations or cloud actions ran during repair. The separately authorized R2
cloud execution then reproduced the exact binary and completed the single
frozen 1/4/8/16/16 `P=1024` sequence. Its slower 16-thread result is 157,550 ms
and its frozen two-selector projection is 22.406940158 hours. Independent
result audit passes 50/50; capture `01fa09a9...4336` is preserved; the VM is
`TERMINATED` with its disk retained. H2-P5A-R2 is exhausted and must not be
rerun. Resume candidate-neutral H2 parent closure and the remaining derivative
ledgers. No full selector, frozen-candidate ingress, root, token, scientific
authorization or authority promotion is eligible.
