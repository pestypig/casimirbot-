Program gate: G2H-E-S5 — inert primary execution-preflight decision
Workstream: authenticated classical and quantum control branch
Capability or component: pre-acknowledgement completeness audit of the S5-A Borel realization/verifier definition
Current maturity: the original `732a340c...95d4b` definition remains failed/withdrawn; 23 additive defects are repaired and expanded audits pass, pending replacement exact-byte acknowledgement
Target maturity: repaired additive definition in which every producer, selector, remainder, resource counter and wire field is executable without invention
Required frozen inputs: withdrawn growth contract `732a340c...95d4b`, state-jet definition `75eff013...cffc`, frozen flat-remainder ABI `09ca71e9...5103`, state/grid ABI `cd98fb11...013c`
Required evidence: exact closure of every defect below, updated independent replay, new byte hashes, and a replacement acknowledgement request
Stop/fail criteria: acknowledging the withdrawn hash, hiding a polynomial prefactor, inventing an implementation-time selector, weakening the frozen coefficient space, changing candidate semantics, or promoting authority
Explicit non-goals: candidate evaluation, positive sampling, C08 implementation, authorization/token/root creation, Rust/G3/SI/lane work, or scientific/physical promotion
Downstream gate unlocked: none until a replacement definition passes a completeness audit

# S5-A Borel pre-acknowledgement completeness review

## Verdict

`FAIL_DEFINITION_UNDER_SPECIFIED_ACKNOWLEDGEMENT_REQUEST_WITHDRAWN`

The exact Python audit 46/46 and cross-language replay 14/14 remain valid for
the identities and structural guards they actually test. They do not establish
that contract `732a340c...95d4b` is an executable total definition. The request
to acknowledge that hash is withdrawn before any acknowledgement occurred.

## First-failure inventory

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 1 | Gevrey witness producer | `origin_localization` consumes `C,A` but does not define how value and all parameter-jet witnesses are generated or rejected | exact coefficient-majorant algorithm, base-index coverage, parameter-box denominator guards and resource counter |
| 2 | Tail augmented systems | the LMI refers to `A(t,p)` without fixing the ordered augmented states, exact matrices, parameter-box substitution or cleared-denominator polynomial | explicit value/first/second homogeneous blocks and inhomogeneous forcing ABI for scalar and metric systems |
| 3 | Polynomial-prefactor closure | a free integer `p` is recorded but no selector/cap proves convolution, Jordan and parameter-forcing growth fits it | eliminate the free selector by fixed strict internal growth subtiers and an exact polynomial-to-exponential absorption majorant, or bind a proved finite degree/cap |
| 4 | Picard enclosure construction | “strict self-inclusion” does not state the initial remainder box, inflation sequence, norm, defect operator or exact containment predicate | fixed inflation list, component norm, residual/remainder map and first-failure precedence |
| 5 | Volterra convolution remainder | the bivariate diagonal rule has no polynomial order, mixed remainder formula, subdivision counter or exhaustion precedence | exact tensor order inherited from the accepted panel, all polynomial/remainder cross terms, dyadic subcell selector and fixed cap |
| 6 | Truncated coefficient tail | the total `sup(g)+556*B12` bound does not give the separate `k>=N` weighted tail needed after computing `0..N-1` coefficients | bind `sum_(k>=N)(1+k)^8|c_k| <= 512*B12/(3*(N-1)^3)` for `N>=2`, or a stronger exact directed bound |
| 7 | Frozen wire reconciliation | the S4 flat-remainder ABI requires a “formal-germ truncation order,” while a Borel realization has origin, panel and projection orders rather than one silent replacement | retain the legacy field with an explicit `not_applicable_borel_realization` tagged value and add the complete realization-order ledger, subject to additive ABI acknowledgement |
| 8 | Canonical hash/failure grammar | the wire requests a semantic hash and first-failure code without defining canonicalization, enumerated codes or their precedence | exact RFC8785-compatible byte/canonical hash rule and ordered failure-code table |
| 9 | Endpoint observable ingress | compressed jets name `h0` but do not bind its exact state-vector functional, gradient/Hessian and positive/vacuum flat indices | bind the canonical coefficient index and exact derivative tensors for every grid/chart |

## Subsequent defect found during repair

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 10 | Convolution onset and edge weighting | the first repair placed the scalar LMI at `T0` and the Laplace split at `T=2*T0`, but retained scalar constants anchored at `T`; moreover, an unweighted finite-history constant cannot bound a growing companion tail across an edge interval | store absolute-exponential scalar value/first/second constants valid from `T0`; split every convolution at `T0`; use directed `exp(-sigma*s)`-weighted finite-history integrals on both edges; record the onset in every growth-DAG node and re-audit independently |

This defect was found before any replacement acknowledgement or implementation.
It extends the open-binding inventory to ten and does not alter the original
withdrawal verdict.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 11 | Metric output derivative bounds | growth constants for `JD/JS` plus an upper homogeneous rate do not bound `BD=JD'` or `BS=JS'`, because an absolute bound for `mD/mS` and the differentiated output products was absent | select and record a common absolute `KM0`; define explicit value/first/ordered-second constants for `m*J+q*R`; retain every `m`, `q`, source and parameter-derivative product |

Defect 11 was likewise found before replacement acknowledgement or
implementation. The open-binding inventory is therefore eleven.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 12 | `P`-norm component extraction | the scalar LMI and variational estimates bound the augmented state in a `P`-norm, while `H`, `K`, `P` and the metric-source expression DAG consume individual `B,V,J1,J2` components | verify the exact rational inverse of `P`, derive and record a directed component-extraction constant from its diagonal, and require this conversion before any source-DAG bound |

Defect 12 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore twelve.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 13 | Rational growth multipliers | the growth algebra defined sums, polynomial multipliers and convolutions, but scalar/metric source rows also contain rational functions of `t` and the parameters | reduce each rational expression exactly, fix its polynomial degree by numerator/denominator degree, prove denominator exclusion, and enclose the normalized multiplier with the fixed compact-box algorithm |
| 14 | Weighted-history panel algorithm | naming `exp(-sigma*s)`-weighted edge integrals does not say how accepted Taylor models and their remainders produce those constants | sum positive-kernel Taylor-moment magnitude bounds and the uniform remainder contribution panel-by-panel in chronology, using directed incomplete-gamma moments (or exact polynomial moments at zero rate) |

Defects 13 and 14 were found before replacement acknowledgement or
implementation. The open-binding inventory is therefore fourteen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 15 | Derivative-convolution orientation | the boundary-plus-integral producer requires a bound for the differentiated right operand, while commutativity otherwise permits implementation-dependent orientation at each nonlinear source node | freeze the left/right orientation of every source-DAG convolution and give exact `t`-derivative identities for `B`, `P`, `F`, `E1`, and `E2`; prohibit swapping after acknowledgement |

Defect 15 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore fifteen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 16 | Borel endpoint factorial normalization | the endpoint rule stated `F(t)=sum f_n t^n` while returning `d_x^j L_F(0)=j! f_j`; that return is correct only for the repository's exponential-generating Borel input `sum f_n t^n/n!` | state the `/n!` convention explicitly and replay the Laplace transform of basis monomials through order 12 |

Defect 16 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore sixteen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 17 | All-orders Borel-Laplace realization | coordinate kernels through order 12 do not by themselves prove that the realized function has the complete prescribed formal germ, that the difference is `C-infinity` flat, or that derivative convolution maps to ordinary multiplication | bind the local EGF remainder plus exponentially flat far-ray proof for every order, uniform parameter-jet interchange, Fubini/integration-by-parts product homomorphism, and exact transformed-equation replay |

Defect 17 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore seventeen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 18 | Origin state-component tails | the origin selector gave a geometric tail for `B` but did not give the differentiated and integrated tails required to enclose `V=B'`, `J1`, and `J2` | bind exact differentiated-geometric tails for fixed orders `m=0..2`, exact integrated tails for `J1/J2`, and require all four augmented-state components and parameter jets to pass |

Defect 18 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore eighteen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 19 | Tail-selector chronology and failure mapping | trying larger `T0` values is legitimate for a tail witness but must not permit recomputation or bypass of an earlier finite continuation failure; `C08-011` versus `C08-012` was not distinguished | require append-only byte-identical panel reuse, make any finite producer/resource failure terminal, allow later `T0` only after a tail-predicate rejection, and reserve `C08-012` for selected-witness corruption/replay failure |

Defect 19 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore nineteen.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 20 | Interval `P`-norm evaluation | `C0O/C1O/C2O` referred to maximum `P`-norms of interval state boxes but did not define a common outward enclosure map | fix the ordered absolute quadratic majorant over all matrix/state-box entries, take its directed square root, reject invalid/nonfinite results, and record each intermediate bound |

Defect 20 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore twenty.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 21 | Analytic source parameter jets | `F`, `E1`, `E2`, positive `omega^2`, and vacuum `a` enter nonlinear source values and Hessians, but only their value formulas and `t` derivatives were fixed | bind exact first/second `mu` or `kappa` derivatives, the vacuum `mu=eta*Mbar_infinity` chain, zero mixed derivatives, and growth absorption for every resulting polynomial factor |

Defect 21 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore twenty-one.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 22 | Lyapunov/momentum symbol namespace | the draft used `P` for both the 4x4 Lyapunov matrix and the scalar momentum Borel component, permitting an ambiguous record/member binding | canonically name the matrix `P_lyap`, the scalar component `BP`, forbid a bare wire/member `P`, and update every tail-record/norm ingress |

Defect 22 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore twenty-two.

| Order | Missing binding | Why it is required | Required repair |
| ---: | --- | --- | --- |
| 23 | Gevrey/scalar-coefficient namespace | `P0/P1/P2` named both the predecessor scalar Borel-equation coefficients and the new recurrence-matrix majorants | retain frozen scalar `P0/P1/P2`; canonically rename the additive Gevrey majorants `G0/G1/G2` in the selector, induction proof and record |

Defect 23 was found before replacement acknowledgement or implementation. The
open-binding inventory is therefore twenty-three.

## Consequence

A2 is complete-unsealed after every listed row closed under the expanded exact
and cross-language audits. A3 is active, and A4 C08 implementation remains
forbidden. The withdrawn acknowledgement text must not be used. The repaired
contract must receive a new raw hash and a replacement byte-bound request; an
acknowledgement cannot be inferred from the author of this repair.

## Repair disposition

`PASS_TOTAL_DEFINITION_AUDITED_PENDING_INDEPENDENT_ACKNOWLEDGEMENT`

This disposition applies only to definition completeness. It is not an
instantiated growth witness, a proof for the frozen candidate, an execution
authorization, or scientific authority.

## Preserved invariants

- Frozen selected identity remains `shat(0)=6/5` and is not loaded.
- Candidate evaluations and positive samples remain zero.
- Candidate roots, token, authorization and ledgers remain absent.
- No scientific handler is linked or executed.
- Candidate, proof, geometry/state, lane, lamp, physical, propulsion and
  transport authority remain false.
