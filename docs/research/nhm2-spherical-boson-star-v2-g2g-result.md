Program gate: G2G — Tolman-VII candidate preregistration
Workstream: authenticated classical control branch
Capability or component: exact dimensionless candidate and proof-duty definition
Current maturity: closed definition-only `PASS`; candidate unadmitted and unexecuted
Target maturity: one byte-frozen candidate contract and independently audited proof plan
Required frozen inputs: G2F result, protocol digest and evidence matrix
Required evidence: two derivations, exact domain/junction identities, falsifiers, contract digest and independent replay
Stop/fail criteria: identity drift, G2D reuse, missing definition, candidate evaluation or authority promotion
Explicit non-goals: proof implementation/execution, candidate admission, G3, SI, lanes, lamp or physical claims
Downstream gate unlocked: G2H implementation/preexecution only

# G2G result: Tolman-VII definition frozen

## Decision

G2G closes as a definition-only `PASS`.

The selected scientific identity is frozen at
`G2F_TOLMAN_VII_NATURAL_BETA_1_5_SCALAR_HADAMARD_V1`, with `mu=1` and
`C=M/R=1/5` exactly. The machine-readable contract is bound by SHA-256:

```text
30de966d41d6342e8a047ee655a33e02f68d32a6ba49efcb39b0bbd7981c343d
```

Two predecessor digests were invalidated before implementation. Review first
found that the RSET prescription had not fixed its four-dimensional source
normalization, coincidence operator, local `v1` term and coefficient. The first
G2H feasibility check then found an impossible chronology that asked for an
executable digest before the executable was built, followed by a primary
runtime declaration that omitted Arb's required MPFR dependency. The corrected
rules freeze source/toolchain/container identities before build, freeze
reproducible executable bytes before any execution, and name the complete
Arb/FLINT/GMP/MPFR arithmetic lineage. Only the digest above is eligible.

## Exact definition outcome

Two definition routes agree exactly on

```text
RHO(x) = 3(1-x^2)
u(x)   = (5x^3-3x^5)/10
Z(x)   = 1-x^2+(3/5)x^4
Z_min  = 7/12 at x^2=5/6
```

The contract also freezes the lapse, pressure/EOS, TOV/isotropy identities,
origin and surface limits, Schwarzschild exterior, radial Sturm-Liouville
problem, global static scalar-state definition, Hadamard RSET prescription,
connected-noise definition, 12 classical duties, 6 quantum duties, 512-bit
arithmetic, fixed 256-cell interior/exterior partitions, failure precedence and
future source/runtime-disjoint implementations.

These are definitions and future proof obligations. They are not evidence that
pressure, causality, stability, state existence, RSET or noise duties pass.

## Definition audit

| Audit | Result |
| --- | --- |
| Primary exact-rational and contract audit | `PASS`, 24/24 |
| Independent TypeScript/BigInt replay | `PASS`, 19/19 |
| Candidate evaluations | 0 |
| Future primary proof root | absent |
| Future independent proof root | absent |
| Contract mutation during either audit | none |

Both audits reject identity, member, authority, source-inventory and adaptive-
partition mutations. The independent replay does not import the Python audit.

## Repository verification

| Gate | Result |
| --- | --- |
| Math report/validation | `PASS`, 318 entries |
| Physics root-to-leaf manifest | `PASS` |
| Required WARP regression suite | `PASS`, 179/179 |
| Casimir adapter | run 2460, `PASS`, `GREEN` |
| Certificate | `6e84f965957f63aad452981d2ede72e62f706d32e0a5b6b469899884e12a4e45` |
| Certificate integrity | true |

The Casimir result certifies repository guardrail integrity at this tree. It is
not a Tolman-VII proof or physical certificate.

## Authority boundary

Candidate admission, proof implementation, proof execution, geometry/state
acceptance, SI/lane execution, replay readiness, diagnostic lamp, physical
viability, propulsion and transport remain false. G2H may implement and test
the two future proof programs only on no-candidate fixtures. A separately
versioned authorization gate is required before either candidate path may run.
