Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: Cloud-Shell-independent browser-SSH transport for the one frozen P=1024 calibration
Current maturity: original proposal independently cleared; execution blocked only by exhausted Cloud Shell quota
Target maturity: exact local-archive ingress, unchanged one-process execution and serial evidence recovery through authenticated Compute Engine console surfaces
Required frozen inputs: original proposal SHA-256 `2b533a983120cd23e6b7bf3b4fbc5f3546e69000c67a91ee4033f98a2b14be80`, exact base and overlay archives, unchanged controller, executable and result auditor
Required evidence: initially absent VM and ingress paths, exact browser-upload byte/hash identities, authenticated source manifest, one controller invocation, serial export identity, immutable local recovery and unchanged P8Q audit
Stop/fail criteria: Cloud Shell use, wrong SSH identity, archive drift, preexisting ingress/output path, additional upload, second command sequence or process, retry, missing serial export, candidate ingress or authority change
Explicit non-goals: Cloud Workstations, Cloud Storage, local gcloud installation, IAM/firewall mutation, scientific changes, retuning, P=65,536, G3/SI/metric/lane work or authority promotion
Downstream gate unlocked: exactly the same one-run P8P calibration authority as the original proposal; no broader scientific or execution authority

# H2-P8P browser-SSH transport-only amendment

Status date: September 1, 2026.

Status: **FROZEN TRANSPORT AMENDMENT / NO VM, UPLOAD OR NUMERICAL PROCESS CREATED**.

## Reason and non-semantic boundary

The signed-in Google account has exhausted its weekly Cloud Shell quota, so the
retained Cloud Shell archive path cannot presently be used. This amendment
changes transport only. It does not change the P8P source, P8I/P8M/P8N inputs,
P=1024 width, 32-thread selector, degree 3, jet 9, MPFR/Arb 512 precision,
progress schedule, timeout, controller, executable, evidence schema, P8Q rule,
machine, zone, disk, cost ceiling or authority locks.

The governing original proposal remains SHA-256
`2b533a983120cd23e6b7bf3b4fbc5f3546e69000c67a91ee4033f98a2b14be80`.
Every original term not explicitly replaced below remains unchanged.

## Exact replacement transport

Replace only the Cloud Shell staging step with Google Compute Engine's
authenticated SSH-in-browser surface for the same VM:

- project `dark-stratum-455714-h4`;
- VM `nhm2-h2-p8p-c1-c2d-32-20260901`;
- zone `us-east1-c`;
- on-demand `c2d-standard-32`;
- exact Debian image
  `projects/debian-cloud/global/images/debian-12-bookworm-v20260817`;
- 30 GB `pd-standard` boot disk;
- unchanged 18,000-second aggregate VM and $9.00 combined ceiling.

Before creation, require the exact VM name absent. Browser SSH may create only
the ordinary ephemeral SSH credential required by the Google Console connection;
no user-directed IAM, project metadata, firewall or persistent-key mutation is
authorized. Require guest username `pestypig` and fail closed otherwise.

Through the SSH-in-browser upload control, upload exactly these two local files
to initially absent regular paths in `/home/pestypig`:

1. `h2-p8f-c2-r1-cloud-upload-v1.tar`, exactly 236,492,800 bytes, SHA-256
   `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`;
2. `h2-p8p-overlay-upload-v1.tar`, exactly 134,656 bytes, SHA-256
   `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`.

No other file upload is permitted. The first archive is the same authenticated
base bytes named by the original proposal; this amendment changes only their
transport origin from retained Cloud Shell storage to the authenticated local
copy. The second remains the same 12-entry overlay.

## Frozen guest sequence

After both uploads complete, enter one fail-fast guest sequence. It must:

1. require `whoami` to equal `pestypig` and both uploaded paths to be regular,
   non-symlink files with the exact bytes and hashes above;
2. require `/home/pestypig/nhm2-h2-p8p-source-v1`,
   `/home/pestypig/nhm2-h2-p8p-evidence-v1`, and
   `/home/pestypig/nhm2-h2-p8p-evidence-export-v1.tgz` all absent;
3. install only Debian's `docker.io` package if Docker is absent, without adding
   a repository, key or unrelated package;
4. create the source root, extract the base first and overlay second, and verify
   the 11-entry manifest at SHA-256
   `c6ee88481ae7842b176d4c8a2001601c38cc317132c446a086c92b75ddef5aa0`;
5. invoke the unchanged 4,498-byte controller exactly once through `sudo bash`,
   requiring controller SHA-256
   `5af4b629336e166d07a277ae59b0f9776ac9e86b728e762030f27237ed1c8f5b`.

The controller retains the frozen offline Docker build, executable SHA-256
`7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`,
single P=1024 process, 14,400-second numerical timeout, immutable output root,
serial base64 evidence export, and automatic shutdown. First failure is
terminal. No manual controller restart or alternate command is allowed.

## Serial-only recovery

After the VM reaches `TERMINATED`, use only the Compute Engine console's
read-only serial-port output surface. Require exactly one complete region
between `P8P_EVIDENCE_BASE64_BEGIN` and `P8P_EVIDENCE_BASE64_END`, plus the
reported archive byte count and SHA-256. Decode it once into the initially
absent local frozen capture directory, verify the reported identity, and run
the unchanged result auditor SHA-256
`cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`.

If the serial export is absent, duplicated, truncated or hash-inconsistent,
preserve the partial serial evidence and stop. Do not restart the VM, attach a
disk, create a rescue helper or recreate the calculation under this amendment.

## Authority and cleanup

The stopped VM, disk, uploaded archives, source root and evidence remain
retained after classification. Their deletion requires a separate decision.
Candidate, proof, geometry/state, lane, lamp, physical, propulsion and transport
authority remain false. The P8Q outcome still decides proposal-readiness only.

## Exact authorization text

> I authorize exactly one H2-P8P browser-SSH transport execution under the final independently audited amendment SHA-256. Preserve every term of original proposal SHA-256 `2b533a983120cd23e6b7bf3b4fbc5f3546e69000c67a91ee4033f98a2b14be80` except its Cloud Shell transport. Create only VM `nhm2-h2-p8p-c1-c2d-32-20260901` with the unchanged project, zone, machine, image, 30 GB disk, 18,000-second runtime and $9.00 ceilings. Through authenticated Compute Engine SSH-in-browser, upload only the exact 236,492,800-byte base archive SHA-256 `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978` and exact 134,656-byte overlay SHA-256 `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`; require guest identity and all ingress/output absence guards; install only Debian `docker.io` if absent; extract base then overlay; authenticate the manifest and controller; and invoke the controller exactly once. Preserve PASS, FAIL, timeout, preexecution failure or partial output, automatically stop the VM, recover the one hash-bound archive only through stopped-VM serial output, run the unchanged independent result audit and apply only the preregistered P8Q rule. First failure is terminal. I do not authorize Cloud Shell, Cloud Workstations, Cloud Storage, local gcloud installation, retry, fallback, resource substitution, additional upload or process, VM restart, disk rescue, scientific changes or retuning, frozen-candidate evaluation, positive sampling, candidate/scientific root, token or handler creation, evidence deletion, P=65,536 execution, G3/SI/metric/lane work, IAM/firewall mutation, or any authority promotion.
