# NHM2 spherical-boson-star v2 G2B replacement classical proof attempt

Program gate: G2B — replacement classical proof attempt  
Workstream: authenticated classical branch closure  
Capability or component: four-grid branch and mathematical proof receipts  
Current maturity: core duty independently passed; later duties unexecuted  
Target maturity: authenticated terminal N=256 classical seed receipt  
Required frozen inputs: M3/M5-R1 center, final branch policy, proof ABIs  
Required evidence: every ordered proof duty and independent residual replay  
Stop/fail criteria: first failed duty terminal; no retune or duty reordering  
Explicit non-goals: joint semiclassical acceptance, lane/lamp/physical authority  
Downstream gate unlocked: G3 accepted joint geometry and quantum state

## Entry evidence

The replacement lambda-zero center and 128-mode core representation now pass
under the unchanged rail. M5-R1 receipt
`c37c0a329765c558c99e559bfede6aed815244f372d289085953f7aed097d1a8`
independently binds exact `nu`, all six coefficient wires, all three exact
projected residuals, and the lowest-eligible selection.

## Ordered remaining duties

1. Bind the passing lambda-zero representation into the final branch policy.
2. Execute the frozen N=64/96/128/256 branch solves without using a coarse grid
   as a finer-grid predictor.
3. Admit all adjacent-pair cross-grid convergence checks.
4. Execute the repaired vacuum-continuation verifier and persist its full
   continuation/overlap receipt.
5. Execute no-fold and continuum-positivity proofs under their frozen ABIs.
6. Execute origin and tail boundary-remainder proofs against authenticated
   candidate jets, interval runtime, source, and executable identities.
7. Independently replay the complete residual/proof chain.
8. Emit the terminal N=256 classical seed receipt only if every earlier duty
   passes in order.

The first missing definition, implementation, runtime identity, or failed
mathematical inequality is terminal evidence for this gate. A failure may open
only a separately versioned diagnosis; it may not authorize threshold changes,
candidate retuning, or omission of the duty.
