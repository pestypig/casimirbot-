# NHM2 spherical-boson-star v2 G2B replacement classical proof attempt

Program gate: G2B — replacement classical proof attempt  
Workstream: authenticated classical branch closure  
Capability or component: four-grid branch and mathematical proof receipts  
Current maturity: closed without a supported terminal candidate
Target maturity: immutable branch-closure evidence and G2C handoff
Required frozen inputs: M3/M5-R1 center, final branch policy, proof ABIs  
Required evidence: every ordered proof duty and independent residual replay  
Stop/fail criteria: first failed duty terminal; no retune or duty reordering  
Explicit non-goals: joint semiclassical acceptance, lane/lamp/physical authority  
Downstream gate unlocked: G2C replacement-candidate research only

## Entry evidence

The replacement lambda-zero center and 128-mode core representation now pass
under the unchanged rail. M5-R1 receipt
`c37c0a329765c558c99e559bfede6aed815244f372d289085953f7aed097d1a8`
independently binds exact `nu`, all six coefficient wires, all three exact
projected residuals, and the lowest-eligible selection.

The immutable first B4 invocation stopped before grid generation on a scalar
role mismatch. B4-R1 receipt
`15c73b1e1ad1583dddf85f2276f661b3016704b113d02a26a995a49863d7e682`
now independently closes that prerequisite: it restores the frozen Coulomb/mass
meaning of `C`, recomputes all dependent scalar words, persists a fresh
authority-neutral six-payload instance, and leaves the old B4 failure unchanged.
It does not authorize a new solve; the next invocation requires a separately
sealed packet and fresh output root.

The sole B4-R2 successor subsequently stopped during N=64 state materialization,
before any solver call. Its immutable receipt `cafa0a8d0bc63ec3c8c166ef63e7be9cc52be278c79a0c112ed69763ab8a42f0`
records zero level receipts. The persisted unit-amplitude core becomes origin
`varphi=2^-10` under the frozen `lambda^2` lift, while the first continuation
stage requires `2^-16`, an exact factor-64 mismatch. Independent audit also
found legacy B1 payload path strings paired with successor hashes. No further
execution is eligible until a versioned parent decision resolves both defects.

B4-R3 receipt
`c067e2109f1aeba8bb3f1329d3ddf3c6db8663e1c35c4e35d443d86a896738d3`
now resolves both defects without changing a scientific byte. The frozen policy
uses the λ=`2^-5`, origin-`2^-10` state unchanged as caller predictor while
Newton separately receives target `2^-16`; pre-Newton origin equality is not a
policy requirement. All actual-root payload paths also independently rehash.
No grid or solver ran under B4-R3; it unlocked only the separately sealed B4-R4
successor.

The sole B4-R4 invocation then passed all bindings and entered the frozen N=64
stage-0 solve. Terminal receipt
`361116765976f0ebb4e8236780f09d77ee17a6dff7f6e640975e8687bfa10c28`
records 29 accepted Newton updates followed by
`armijo_schedule_exhausted_without_retry`. The endpoint residual L∞ is about
`3.90e-9`, scaled step L∞ about `1.98e-12`, and unused constraint L∞ about
`1.59e-4`; `varphi` remains positive/nonnegative but is not nonincreasing. The
independent audit passed 6/6 on host and Linux. No later level or pair ran, no
retry or retune occurred, and all authority remains false.

B4-R5 receipt
`0cfb59144cf29beb0da94852ee872455a56017cbe3fc690fd6cb24cd401ea406`
then independently reconstructs that terminal endpoint without invoking Newton
or continuation chronology. All 25 frozen trial steps cross the strict `w<1`
domain boundary before merit evaluation. The direction is strict descent, but
the U-diagonal spread is about `1.34e13` and the nodal `varphi` profile has 32
adjacent increases. Under preregistered rules the result is
`NO_UNIQUE_SUCCESSOR_JUSTIFIED`: extending backtracking, changing frequency
variables, increasing precision, scaling or changing discretization are not yet
distinguished by evidence. No trial was accepted or persisted and all authority
remains false.

B4-R6 receipt
`0430266c9efe338fecc4c4c01fd2e25d168f481611d2676d023ba6e211a0c001`
then executes the sealed no-solve mechanism-separation benchmark exactly once.
Producer-independent host and admitted-Linux audits pass 6/6. Coordinate and
precision triggers are false. Power-of-two equilibration reduces the direct-`w`
U-diagonal spread from about `1.34e13` to `52.77`, while the first unused-
constraint node is about `1908.18` times the interior median. Scaling and
first-node discretization/localization therefore both trigger. The frozen
decision is `MULTIPLE_MECHANISMS_SEPARATED_NO_UNIQUE_SUCCESSOR`: no successor,
solve, update, retry, retune, or authority promotion is authorized.

B4-R7 receipt
`c7547fb302f0e128bfce68faf7e60f87d9172715f392fcc0ca329e4f4d667ccb`
then executes the sealed causal-interaction review exactly once. Independent
host and admitted-Linux audits pass 6/6. Equilibration improves all four
controlled matrix cells, but first-node excision is not unique conditioning
leverage. Raw and local-term-normalized unused-constraint ratios remain near
`1908`, while raw and equilibrated Jacobian-sensitivity ratios are bit-identical
near `0.0148`. The frozen classification is
`CAUSAL_INTERACTION_UNRESOLVED_STOP`, with no proposal class and no solve,
correction, update, retry, retune, or authority promotion.

B4-R8 then derives the exact continuum propagation identity from the contracted
Bianchi identity and scalar equation without evaluating a candidate. It fixes
the regular constraint charge `q=x^2 exp(F0+2F1) Ex_x`, the integrated
propagation defect, endpoint limits, and Clenshaw–Curtis norm contract. Exact
residual expansion, an independent Christoffel/stress route, regular analytic
fields, and quadrature checks pass 5/5. Its decision supports preparation of at
most one proposal: unchanged `Et_t,Etheta_theta,KG` continuum rows, fixed
power-of-two equilibration, and charge/defect convergence monitors. Execution
remains unauthorized.

B4-R9 freezes the sole R8-supported formulation without reading a candidate:
unchanged `Et_t,Etheta_theta,KGbar` continuum rows, direct `w`, raw Armijo and
existing thresholds; deterministic power-of-two row/column equilibration only
inside the correction solve; and MPFR512 shifted-Chebyshev prefix integration
for scale-free four-grid `q`/`delta` contraction gates. Six focused tests pass
on host and admitted Linux, all 27 dependencies rehash, and the future output
root remains absent. Only implementation/preexecution preparation is unlocked.

B4-R10 now implements that exact proposal without reading or evaluating a
candidate. Its guarded correction wrapper, MPFR512 monitor, scale-free
constraint gate, ordered output/receipt schema, source-disjoint preexecution
audit, admitted-image identity, fixed token and exact command are byte-pinned.
Host and admitted-Linux no-execution audits pass 11/11, the preserved numerical
surface passes 62/62, and math/WARP/Casimir gates are green. The future output
root remains absent. This closes implementation preparation but does not grant
execution authority.

The separate authorization preflight now rehashes all five checkpointed files,
revalidates the host and admitted-Linux 11/11 suites, confirms the admitted
image is already local, and proves the exclusive root remains absent. Its
decision is `AUTHORIZE_EXACT_ONE_SHOT_AFTER_EXPLICIT_USER_CONFIRMATION`. This
is execution eligibility, not execution authority: the user must explicitly
name token `b1c408e2c3a3dbc48ceee5da6998ced66579bce65bc324ffb6dbc98857c36d20`
and accept the immutable one-shot/no-retry boundary before invocation.

That exact authorization was received and the checkpointed offline command ran
once. B4-R10 accepted 29 equilibrated Newton updates at N=64 stage 0, then
terminated `armijo_schedule_exhausted_without_retry`. The 11-file output prefix
and terminal receipt `e8d0268f499f6e1cba9ccf26cc34dc602b4a40f7f96478bb7e59e7acce037706`
are immutable. Host and admitted-Linux producer-independent audits pass 6/6 and
reproduce all four constraint profiles and norms. No later level or cross-grid
gate ran. No retry, retune, deletion, reuse, candidate admission or authority
promotion is eligible.

B4-R11 then attempted the sole bounded terminal-equivalence diagnosis and
stopped before evaluation on a Python module-identity mismatch. Its root remains
absent and its exact source/checkpoint are immutable. B4-R11-R1 changed only
canonical module loading and used a fresh root. Receipt
`3724e8eb50e49beee6354f205b4a0593538706484a9a38f0b6d50d654727a164`
exactly reproduces R10 correction trace 29 and shows every exponent 0..24 is
finite but has `w>=1`. R4/R10 accepted-alpha chronology is identical, the sole
R8/R9 intervention is falsified, and prior evidence supplies no unique
remaining proposal. Producer-independent host/Linux audits pass 6/6. The
decision is `CLOSE_FROZEN_BRANCH_NO_SUPPORTED_SUCCESSOR`. Math 318, WARP
179/179, and Casimir run 2449 are green with certificate integrity true.

## Terminal closure

1. Preserve B4-R4 through B4-R11-R1 and every exact output as immutable
   evidence.
2. Do not retry, retune, extend, reuse, or reinterpret B4-R10.
3. Do not execute vacuum continuation, no-fold, positivity, boundary remainder,
   cross-grid replay, or an N=256 receipt for this failed branch.
4. Treat G2C replacement-candidate research as the only active successor. It
   must select at most one new family before any candidate result exists.
5. Keep candidate admission and every proof, lane, lamp, physical, propulsion,
   and transport authority false.

G2B is complete as a scientifically authenticated stop, not as a successful
classical seed. It does not unlock G3.
