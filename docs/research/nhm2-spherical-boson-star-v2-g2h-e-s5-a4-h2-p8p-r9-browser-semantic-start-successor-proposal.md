Program gate: G2H-E-S5-A4 — candidate-neutral H2 continuation diagnosis
Workstream: P8P observer-progress turnaround calibration
Capability or component: P8P-R9 browser-semantic retained-VM start successor
Current maturity: frozen transport-only successor; R8 exhausted before command entry on Cloud Shell service limit
Target maturity: one authenticated candidate-neutral P=1024 turnaround result and P8Q decision, or one immutable terminal R9 record
Required frozen inputs: R8 terminal result, exact retained stopped P8J-R9 VM, exact Google Cloud Details URL, semantic Start control, two-file stage, unchanged ledger/controller/executable/auditor
Required evidence: exact signed-in Details page and resource fields, one semantic Start action, optional single Google confirmation, exact running state, one SSH session, guarded two-file ingress, one controller, serial evidence, audit and P8Q result
Stop/fail criteria: first page, identity, control ambiguity, click, confirmation, capacity, start, SSH, guard, picker, transfer, Docker, build, execution or evidence failure terminal; no retry or alternate start path
Explicit non-goals: Cloud Shell, new VM or disk, resource mutation beyond one start/automatic stop, P8J evidence mutation, candidate evaluation, retuning, P=65,536, G3/SI/metric/lane work, deletion or authority promotion
Downstream gate unlocked: result-conditioned P8Q yes/no decision only; no full representative run or scientific authority

# H2-P8P-R9 browser-semantic start successor

Status date: September 2, 2026.

Status: **FROZEN TRANSPORT-ONLY SUCCESSOR / AWAITING SEPARATE BILLABLE-ACTION AUTHORIZATION**.

Change classification: retained-VM start transport only. Scientific source,
runtime, resource, ingress, controller, evidence and P8Q semantics are unchanged.

## Narrow causal basis

R8 reached the authenticated Cloud Shell page but no command line became
available. The page remained in provisioning/connecting state and twice
reported `You have temporarily exceeded a Cloud Shell limit.` Zero command
characters were entered and no start request occurred. The immutable R8 result
is SHA-256
`b6890cf590f09c4e4499a9dc5d8c65d0deca6a6f427251f296f28a5480de94a5`
and independently audits 16/16; its auditor is
`f79f6ca15d3b1a634fea9ac7185ae893f23669b23aa7844b3c2eea8669dc6e50`.

R9 replaces only unavailable Cloud Shell command transport with the Google
Cloud Console's visible semantic `Start / Resume` control in a fresh browser
tab. The browser controller must locate the control by its visible accessible
role/name, not Windows coordinates. This is not a capacity retry because R8
submitted no start request. After a successful start, every R7/R8 SSH, ingress,
scientific, evidence, stop and P8Q binding remains unchanged.

## Exact retained resource and page

R9 may open exactly one fresh signed-in Chrome tab at:

```text
https://console.cloud.google.com/compute/instancesDetail/zones/us-east1-c/instances/nhm2-h2-p8j-r9-c2d-32-20260831?project=dark-stratum-455714-h4
```

Before action, require the rendered page to expose exactly:

| Field | Frozen value |
| --- | --- |
| project | `dark-stratum-455714-h4` |
| VM and boot disk | `nhm2-h2-p8j-r9-c2d-32-20260831` |
| instance ID | `1920090043510946854` |
| state | `Stopped` |
| zone | `us-east1-c` |
| machine | `c2d-standard-32`, 32 vCPUs, 128 GB |
| image | `debian-12-bookworm-v20260817` |
| boot disk | 30 GB Standard persistent disk |
| aggregate restart ceiling | 18,000 seconds |
| total R9 ceiling | $9.00 compute plus prorated retained storage |

Require exactly one enabled semantic button whose accessible name is
`Start / Resume`. Click it exactly once. If Google presents one direct
start-confirmation dialog, require it to name this exact VM and accept exactly
once; if it presents any materially different warning, resource change or
choice, fail closed. Any absent/duplicate/disabled control, navigation error,
identity mismatch, click ambiguity, capacity error or missing start completion
is terminal. No second click, refresh, Cloud Shell, coordinate action, API
command, alternate zone or fallback is authorized.

After start completion, wait exactly 180 seconds and require the same page to
show the exact resource `Running`. Open exactly one fresh authenticated
browser-SSH surface as `pestypig`.

## Preserved ingress and execution

R9 preserves:

- exact staged 134,656-byte overlay SHA-256
  `4ab0921f2bc7d6bfe85b8b45e294c2058b2f29644fe072e48e2064c7be772b8e`;
- exact staged 2,971-byte ledger SHA-256
  `9af4180a714d9dfd24dd6cfe6d3952a73d75c932a3f05f5e3484f79467c94a5b`;
- retained 236,492,800-byte base SHA-256
  `fa8d8c994b08fe8a31050feb125eb1d6e4ebb33c9f5de18d5f9e189716936978`;
- executable SHA-256
  `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`;
- unchanged result auditor SHA-256
  `cc25e6d79ec2d9fafd725285d39d1be5d6c004c2622884e95ed602bdc22411b9`;
- one SSH round-trip marker, 30 passive seconds, one retained-base/path/service
  guard, one native file-open dialog, exactly two successful transfers and one
  ledger invocation;
- existing Docker only, offline build with no pull/network, exactly one
  candidate-neutral P=1024 controller process on 32 CPUs with a 14,400-second
  timeout, immutable serial evidence, automatic stop and independent audit;
- exact observer-progress, chronology, P8Q and all-false authority rules.

First failure is terminal. Only a complete authenticated calibration may
produce `P8Q_YES_PROPOSAL_READY` or
`P8Q_NO_ALGORITHMIC_PERFORMANCE_LEAD`; otherwise only
`P8Q_STOP_CALIBRATION_NOT_AUTHENTICATED` applies. No result authorizes
P=65,536.

Candidate evaluations and positive samples remain zero. Candidate/scientific
roots, tokens and handler linkage remain absent. Candidate, proof,
geometry/state, lane, lamp, physical, propulsion and transport authority remain
false.

## Exact authorization text

> I authorize exactly one H2-P8P-R9 browser-semantic retained-VM start and P=1024 turnaround attempt under proposal SHA-256 `PROPOSAL_SHA256`. Open exactly one fresh signed-in Chrome tab at the frozen Google Cloud Details URL and require exact stopped VM `nhm2-h2-p8j-r9-c2d-32-20260831`, instance ID `1920090043510946854`, project `dark-stratum-455714-h4`, zone `us-east1-c`, `c2d-standard-32`, Debian `v20260817` and 30 GB Standard persistent disk. Click exactly one enabled semantic `Start / Resume` control without Windows coordinates; accept at most one direct confirmation naming that exact VM. Preserve the unchanged 18,000-second and $9.00 ceilings. After exact running-state authentication and one 180-second wait, open exactly one fresh authenticated browser-SSH surface as `pestypig`; preserve the R7 round-trip, 30-second stability, remote guard, one native picker, exact two-file ingress, retained-base authentication, existing-Docker requirement, offline build, executable SHA-256 `7c96648911ea74e43199e6c87291e2dd32a73f5d21fee8e20454cc8962e31718`, exactly one candidate-neutral P=1024 process with 32 CPUs and 14,400-second timeout, immutable serial evidence, automatic stop, independent audit and frozen P8Q rule. First failure is terminal. I do not authorize retrying R8 or R9; a second tab, start click, confirmation, SSH surface, marker, guard, dialog, upload batch, VM or numerical process; Cloud Shell, coordinate action, API command, fallback, new resource, alternate zone, replacement, resize or rescue; Docker installation; P8J source/evidence/resource mutation or deletion; additional upload; retuning; frozen-candidate evaluation; positive sampling; P=65,536 execution; evidence deletion; G3/SI/metric/lane work; or any authority promotion.
