# NHM2 spherical-boson-star v2 G2B-M5-R1 independent admission

Program gate: G2B-M5-R1 — exact projection evidence completion  
Workstream: lambda-zero proof-center recovery  
Capability or component: independent receipt and projected-residual admission  
Current maturity: preregistered after immutable M5 numerical PASS  
Target maturity: independently verified passing core-duty receipt or falsifier  
Required frozen inputs: M5 source/receipt, M3 receipt, frozen M2 solve engine  
Required evidence: exact nu replay, coefficient hashes, exact residual equality  
Stop/fail criteria: any pin, solve, binding, residual, or selection mismatch  
Explicit non-goals: projection rerun, retuning, full branch proof, authority  
Downstream gate unlocked: G2B replacement classical proof attempt

## Frozen decision rule

The verifier must:

1. admit the exact M5 receipt and its domain-separated self-hash;
2. admit the exact selected M3 center observation;
3. replay the frozen nonlinear solve refinements `(4,8)` only, without
   materializing the full state or rerunning the projection;
4. require both recovered exact 256-bit `nu` values to agree under the already
   frozen cross-refinement rule and bind the fine value canonically;
5. independently parse every stored coefficient dyadic and recompute each
   canonical coefficient-vector raw hash and size;
6. independently implement the exact-rational Chebyshev derivative, value,
   compactified radial chain rule, and normalized Schrödinger residual;
7. require exact equality to every stored projected residual for modes
   `[128,256,512]`;
8. re-evaluate the unchanged residual/reconstruction thresholds and require
   the stored lowest-eligible selection of 128;
9. write one create-new admission receipt containing complete per-mode checks,
   the exact `nu` binding, blocker inventory, and all authority locks false.

The verifier may not import the M5 projected-residual helper, accept caller
coefficients, rerun M5, add a mode, relax a threshold, or repair a mismatch.
Any mismatch produces a terminal falsifier receipt. Passing this gate closes
only the recovered lambda-zero core duty; all other classical proof duties and
all later semiclassical workflow gates remain pending.
