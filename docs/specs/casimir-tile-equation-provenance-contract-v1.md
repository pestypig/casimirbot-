# Casimir Tile Equation Provenance Contract v1

"This campaign defines falsifiable reduced-order full-solve gates and reproducible evidence requirements; it is not a physical warp feasibility claim."

## Purpose
Define deterministic derivation chains so every reported experimental parameter can be traced from raw observables to the theory-facing congruence terms.

## Required Fields
- `chain_id`
- `domain`
- `raw_observables`
- `derivation_equations`
- `uncertainty_propagation`
- `theory_mapping`
- `falsifier`
- `recompute_artifact`
- `status`

## Derivation Chains

| chain_id | domain | raw_observables | derivation_equations | uncertainty_propagation | theory_mapping | falsifier | recompute_artifact | status |
|---|---|---|---|---|---|---|---|---|
| CH-T-001 | timing | `t_master_i`, `t_slave_i`, path-delay samples `d_i` | `e_i = t_slave_i - t_master_i`; `sigma_t = sqrt((1/(N-1))*sum((e_i-mu_e)^2))`; `TIE_pp = max(e_i)-min(e_i)`; `PDV_pp = max(dhat_i)-min(dhat_i)` | `u_sigma_t_ps`, `u_TIE_pp_ps`, `u_PDV_pp_ps` from protocol uncertainty section | `sigma_t_ps`, `TIE_pp_ps`, `PDV_pp_ps` to timing lane in `C_congruence` | any confidence interval crosses profile bound | timing run bundle + protocol report | active |
| CH-NG-001 | nanogap | gap map `g_raw(x,y)`, tip-state calibration data, drift/environment logs | `g_corr = g_raw + c_tip + c_drift + c_env`; summary metrics `g_mean`, `g_sigma` | `u_g = sqrt(u_meas^2 + u_z^2 + u_tip^2 + u_drift^2 + u_env^2 + u_algo^2)`; `U_g = k*u_g` | `u_g_mean_nm`, `u_g_sigma_nm` to nanogap lane in `C_congruence` | profile requirement breach (`NG-STD-10` or `NG-ADV-5`) | nanogap budget sheet + Monte Carlo export | active |
| CH-Q-001 | q_spoiling | VNA sweep (`f0`, `Delta_f_3dB`), coupling data (`beta_i`), clean/spoiled runs | `Q_L = f0/Delta_f_3dB`; `Q_0 = Q_L*(1+sum(beta_i))`; `F_Q_spoil,m = Q_clean,m / Q_spoiled,m`; `Delta(1/Q0)_m = 1/Q_spoiled,m - 1/Q_clean,m` | `u_Q_L`, `u_Q0`, `u_FQ_spoil` using propagation formulas from protocol | `Q0_nb_2k`, `Q0_cu_ref`, `F_Q_spoil` to Q lane in `C_congruence` | missing coupling correction evidence or any spoil-mode bound breach | q-spoiling protocol report + raw sweep archive | active |
| CH-CS-001 | casimir_sign_control | force-gap traces `F(d)` across declared medium/material stack | sign detection over `F(d)` with transition interpolation for `g_transition_nm`; branch floor extraction for `P_rep_min` | uncertainty on zero-crossing and repulsive-branch fit (`u_gtransition_nm`, `u_prep_pa`) | sign-transition and repulsive-branch lanes in `C_congruence` | no reproducible sign transition in declared band | sign sweep parity report | active |
| CH-SE-001 | sem_ellipsometry_cross_validation | `d_sem_raw`, calibration reference dimensions, ellipsometry fit outputs, instrument drift logs | `d_sem_corr = d_sem_raw*scale_factor_sem + b_sem`; `delta_se = d_sem_corr - d_ellip`; `d_fused` inverse-variance weighted estimate | `u_sem`, `u_ellip`, `u_fused`, `U_fused=k*u_fused`, optional Monte Carlo interval | `d_sem_corr_nm`, `d_ellip_nm`, `delta_se_nm`, `U_fused_nm` to congruence lane | missing calibration chain or profile-bound breach on `delta_se`/`U_fused` | SEM+ellipsometry protocol report + calibration logs | active |
| CH-QEI-001 | worldline_qei_sampler | sampler definition `g(t)`, width scale (`t0` or `tau`), sampled energy-density lane `rho(t)` | `rho_bar = integral dt g(t) rho(t)`; `bound = -K/t0^d`; `zeta = abs(rho_bar)/abs(bound)`; checks: normalization, smoothness, scaling trend | `u_rho_bar`, `u_t0`, `u_K`, plus scaling residual `u_scaling`; optional log-fit uncertainty for `t0^-d` trend | `normalize_ok`, `smoothness_ok`, `scaling_ok`, `qei_worldline_applicability` | any sampler check failure, missing kernel normalization metadata, or trend mismatch under fixed semantics | worldline-QEI sampler audit report + sweep artifact | active |
| CH-QCD-001 | qcd_analog_spin_correlation | HEPData Tables `159491/t3` (short-range row `x=1`), `159491/t4` (long-range row `x=1`), and `159491/t5` (`DeltaR` bins); record-level metadata (`cmenergy`, DOI anchors) | `sigma_combined = sqrt(sigma_stat^2 + sigma_sys^2)`; `z = abs(P)/sigma_combined`; `contrast = abs(P_short)-abs(P_long)`; `dr_near_far_ratio = mean(abs(P)_near)/mean(abs(P)_far)` | table-level uncertainty uses provided stat/sys terms in quadrature; record-level contextual fields (event count, beam speed) are metadata anchors | `qcd_relative_polarization`, `qcd_relative_polarization_long`, `qcd_significance_sigma`, `qcd_decoherence_contrast`, `qcd_decoherence_vs_separation`, `qcd_collision_energy_gev`, `qcd_dataset_anchor` (exploratory only) | missing short/long rows, missing stat/sys uncertainties, significance parity mismatch, or no near-far decoherence trend in `DeltaR` bins | `artifacts/research/full-solve/qcd-analog-replay-*.json` + `docs/audits/research/warp-qcd-analog-replay-*.md` | active |
| CH-GR-001 | gr_mercury_perihelion_observable | weak-field solar-system inputs (`GM_sun`, `a`, `e`, `c`, orbital period) with published anomalous precession anchor | `Delta_varpi_orbit = 6*pi*GM_sun/(a*(1-e^2)*c^2)`; `arcsec_century = Delta_varpi_orbit * arcsec_per_rad * (36525/orbital_period_days)`; `residual = predicted-observed` | `u_observed_arcsec_century` plus replay tolerance `max_abs_residual_arcsec_century` (deterministic acceptance band) | `gr_observables.mercury_perihelion.{status,predicted_arcsec_per_century,observed_arcsec_per_century,residual_arcsec_per_century}` | missing equation anchor/constants or residual exceeding tolerance | `artifacts/research/full-solve/external-work/external-work-method-ext-gr-merc-001-*.json` + compare/matrix artifacts | active |
| CH-GR-002 | gr_lensing_deflection_observable | solar-limb weak-field constants (`GM_sun`, `c`, `b`) plus historical/modern light-deflection anchors (`alpha_historical`, `gamma_modern`) | `alpha_limb_rad = 4*GM_sun/(c^2*b)`; `alpha_limb_arcsec = alpha_limb_rad * arcsec_per_rad`; `residual_historical = alpha_limb_arcsec - alpha_historical`; `gamma_residual = gamma_modern - 1` | `u_alpha_historical_arcsec`, `u_gamma_modern`, and deterministic tolerance lanes (`max_abs_historical_residual_arcsec`, `max_abs_gamma_residual`) | `gr_observables.lensing_deflection.{status,predicted_limb_arcsec,historical_observed_arcsec,historical_residual_arcsec,modern_gamma_measured,modern_gamma_residual}` | missing equation anchor/constants/modern gamma anchor or residuals outside configured tolerance | `artifacts/research/full-solve/external-work/external-work-method-ext-gr-lens-001-*.json` + compare/matrix artifacts | active |
| CH-GR-003 | gr_frame_dragging_observable | published frame-dragging prediction/measurement anchors from GP-B and LAGEOS ratio lane | `delta_gpb = omega_gpb_measured - omega_gpb_gr_predicted`; `delta_lageos_ratio = ratio_lageos_measured - ratio_lageos_expected` | `u_gpb_mas_per_year`, `u_lageos_ratio`, and deterministic tolerance lanes (`max_abs_gpb_residual_mas_per_year`, `max_abs_lageos_ratio_residual`) | `gr_observables.frame_dragging.{status,gpb_predicted_mas_per_year,gpb_observed_mas_per_year,gpb_residual_mas_per_year,lageos_observed_ratio,lageos_residual_ratio}` | missing equation anchor/constants/observed anchors or residuals outside configured tolerance | `artifacts/research/full-solve/external-work/external-work-method-ext-gr-fd-001-*.json` + compare/matrix artifacts | active |
| CH-GR-004 | gr_shapiro_delay_observable | PPN gamma-delay anchors (`gamma_minus_one`) with historical delay-formula context and Cassini precision benchmark | `gamma_estimated = 1 + (gamma_minus_one)`; `gamma_residual = gamma_minus_one`; reference formula anchor: `Delta_t_shapiro = (1+gamma)*(2*GM_sun/c^3)*ln((r_E+r_R+R)/(r_E+r_R-R))` | `u_gamma_minus_one` and deterministic tolerance lane (`max_abs_gamma_minus_one`) | `gr_observables.shapiro_delay.{status,gamma_minus_one_measured,gamma_estimated,gamma_residual}` | missing equation anchor/observed anchor or `|gamma_minus_one|` outside configured tolerance | `artifacts/research/full-solve/external-work/external-work-method-ext-gr-shap-001-*.json` + compare/matrix artifacts | active |
| CH-WARP-001 | warp_family_geometry_core4 | Core-4 paper snapshots (`alcubierre-1994`, `natario-2002`, `vandenbroeck-1999`, `lentz-2021`) plus local geometry conformance baseline (`metric_form_alignment`, `shift_mapping`, `york_time_sign_parity`, `natario_control_behavior`, `metric_derived_t00_path`) | `signature(check_i)=status_i`; `status_i in {pass,fail,unknown}`; `comparison_i = equals(local_i, external_i)` when both are admissible and comparable; profile result from deterministic pass/fail/unknown tallies | uncertainty represented as comparability class + explicit blocker reason codes (`non_comparable_assumption_domain`, `conditional_b_equals_1_only`, `requires_region_ii_derivative_closure`) rather than numeric error bars in this geometry-first wave | `geometry_signature`, `comparison_result`, `reason_codes`, `recompute_ready` in external-work method track outputs | missing equation anchor, missing snapshot field, or non-comparable assumption domain without explicit blocker code | `artifacts/research/full-solve/external-work/external-work-method-ext-warp-*.json` + compare/matrix artifacts | active |
| CH-WARP-002 | warp_family_energetics_qei_core4 | Core-4 energetics snapshots (`alcubierre-1994-energetics`, `natario-2002-energetics`, `vandenbroeck-1999-energetics`, `lentz-2021-energetics`) plus local reduced-order energetics/QEI baseline signature in capsule | `signature(check_i)=status_i`; `status_i in {pass,fail,unknown}`; `comparison_i = equals(local_i, external_i)` only when assumption domains are comparable and both sides are non-unknown; profile status from deterministic pass/fail/inconclusive tallies | uncertainty represented by assumption-domain comparability classes and explicit blocker codes (`non_comparable_assumption_domain`, `requires_region_ii_derivative_closure`, `worldline_qei_not_explicit_in_source`) rather than numerical bars in this wave | `energetics_signature`, `comparison_result`, `reason_codes`, `recompute_ready` in external-work method track outputs | missing equation anchor, missing snapshot signature key, or baseline energetics signature not present in capsule | `artifacts/research/full-solve/external-work/external-work-method-ext-warp-*-e001*.json` + compare/matrix/capsule artifacts | active |
| CH-PROV-001 | provenance_attestation | signed layout metadata, signed link metadata, attestation statements (`subject`,`predicateType`,`predicate`) | layout/link verification continuity over `materials -> products`; attestation statement schema checks: `_type`, `subject[*].digest`, `predicateType`; optional `predicate` | schema completeness checks plus digest-match and signature verification uncertainty annotations (where applicable) | `provenance_step_contract`, `artifact_attestation_schema`, promotion-governance evidence lanes | missing signed layout/link continuity, missing required attestation fields, or unverifiable digest/signature chain | provenance verification report + CI attestation replay bundle | active |

## Recompute Rule
1. Every `entry_id` in the experimental registry must map to one `chain_id`.
2. `status=partial` requires an explicit blocker note and cannot be promoted to hard claim use.
3. Any missing `recompute_artifact` marks that chain `blocked`.

## Traceability
- `contract_id`: `casimir-tile-equation-provenance-contract-v1`
- `commit_pin`: `e240431948598a964a9042ed929a076f609b90d6`
- `owner`: `research-governance`
- `status`: `draft_v1`
