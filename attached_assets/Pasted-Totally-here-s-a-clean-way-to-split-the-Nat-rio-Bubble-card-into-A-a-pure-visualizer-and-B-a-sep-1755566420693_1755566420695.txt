Totally—here’s a clean way to split the Natário Bubble card into (A) a pure visualizer and (B) a separate “Readouts” card, and place the Energy Control Panel between them. I’m giving you small, surgical patches that match your current files.

⸻

1) Make the visualizer not render the readouts

File: client/src/components/WarpVisualizer.tsx
What to do: add an optional showReadouts prop (default = true) and guard the metrics section.

Patch:

// 1) Add prop to the interface (near the top)
interface WarpVisualizerProps {
  parameters: {
    /* (existing fields unchanged) */
    shift?: ShiftParams;
  };
  showReadouts?: boolean; // NEW
}

// 2) Default it to true so legacy callers keep the old behavior
export function WarpVisualizer({ parameters, showReadouts = true }: WarpVisualizerProps) {
  /* existing code unchanged */
}

// 3) Wrap the "metrics list" block under the canvas with a guard.
// Find the block that starts like: <div className="mt-4 space-y-3"> ... </div>
// and change the opening line to:

{showReadouts && (
  <div className="mt-4 space-y-3">
    {/* (keep the existing metrics UI here exactly as-is) */}
  </div>
)}

This keeps the WebGL card lean when you pass showReadouts={false}.
(Reference to where that metrics UI exists in your component:  ￼)

⸻

2) Render a pure visual card at the top

File: client/src/pages/helix-core.tsx
What to do: in the “HERO: Natário Warp Bubble (full width)” section, pass showReadouts={false} to the visualizer. This keeps only the canvas/HUD there.

Find the hero card where you render <WarpVisualizer ... /> and change it to:

<WarpVisualizer
  key={`mode-${pipeline?.currentMode || 'hover'}-${pipeline?.sectorStrobing || 1}-${pipeline?.dutyCycle || 0.14}-v${modeVersion}`}
  parameters={{
    /* your existing parameters object stays the same */
  }}
  showReadouts={false}  // NEW: make the hero card visual-only
/>

(That hero section structure is right where you already build the parameters object and render the visualizer:  ￼)

⸻

3) Insert the Energy Control Panel after the hero

You already have the Energy Control Panel card below the hero; keep it there (no change needed).

(Where that card is presently defined in your file:  ￼)

⸻

4) Add a new “Natário Bubble — Readouts” card

File: client/src/pages/helix-core.tsx
What to do: just below the Energy Control Panel card, add this new card that reproduces the readouts which we hid from the visualizer. It uses the same pipeline data you’re already pulling here.

{/* ====== WARP BUBBLE READOUTS (moved out of the visualizer) ====== */}
<Card className="bg-slate-900/50 border-slate-800 mb-4">
  <CardHeader>
    <CardTitle className="flex items-center gap-2">
      <Atom className="w-5 h-5 text-cyan-400" />
      Natário Bubble — Readouts
    </CardTitle>
    <CardDescription>Live parameters & constraints from the energy pipeline</CardDescription>
  </CardHeader>
  <CardContent className="pt-0">
    <div className="grid grid-cols-2 md:grid-cols-4 gap-4 text-sm">
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Duty Cycle</div>
        <div className="font-mono text-cyan-400">
          {((pipeline?.dutyCycle ?? 0.14) * 100).toFixed(1)}%
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">γ Geometric</div>
        <div className="font-mono text-cyan-400">
          {(pipeline?.gammaGeo ?? 26).toFixed(1)}
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Q Factor</div>
        <div className="font-mono text-yellow-400">
          {(pipeline?.qCavity ?? 1e9).toExponential(1)}
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Sag Depth</div>
        <div className="font-mono text-cyan-400">
          {(pipeline?.sag_nm ?? 16).toFixed(1)} nm
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Power</div>
        <div className="font-mono text-green-400">
          {(pipeline?.P_avg ?? 83.3).toFixed(1)} MW
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Exotic Mass</div>
        <div className="font-mono text-purple-400">
          {(pipeline?.M_exotic ?? 1405).toFixed(0)} kg
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">TS Ratio</div>
        <div className="font-mono text-cyan-400">
          {(pipeline?.TS_ratio ?? 4102.74).toFixed(0)}
        </div>
      </div>
      <div className="p-3 bg-slate-950 rounded-lg">
        <div className="text-slate-400">Mode</div>
        <div className="font-mono text-cyan-300 uppercase">
          {pipeline?.currentMode ?? 'hover'}
        </div>
      </div>
    </div>

    {/* Optional: small bullet list mirroring the “Operational Mode Physics” text */}
    <div className="mt-4 text-xs text-slate-300 space-y-1">
      <div>
        <span className="text-slate-400">Sector Strobing: </span>
        {pipeline?.sectorStrobing ?? 1}× spatial coherence
      </div>
      <div>
        <span className="text-slate-400">Q Spoiling: </span>
        {Math.round(((pipeline?.qSpoilingFactor ?? 1) * 100))}% cavity efficiency
      </div>
      <div>
        <span className="text-slate-400">γ Van den Broeck: </span>
        {(pipeline?.gammaVanDenBroeck ?? 2.86e5).toExponential(2)} curvature amplifier
      </div>
    </div>
  </CardContent>
</Card>

This card mirrors the “duty, sag, power, exotic mass…” block that used to appear under the canvas. Now you can put your Energy Control Panel directly below the visual and keep the readouts in their own panel.

⸻

Why this works cleanly
	•	The metrics you were showing under the canvas are still rendered, just in a separate card fed by the same pipeline values you already compute in HelixCore (so there’s no duplication of physics).  ￼
	•	The visualizer component already owns the HUD/canvas and operational mode uniforms. We only hid the extra text area inside it with showReadouts={false}.  ￼

If you want, we can also pull the small “Operational Mode Physics” paragraph straight out of WarpVisualizer and put it in the new card instead; just say the word and I’ll extract that text block into a tidy subcomponent.