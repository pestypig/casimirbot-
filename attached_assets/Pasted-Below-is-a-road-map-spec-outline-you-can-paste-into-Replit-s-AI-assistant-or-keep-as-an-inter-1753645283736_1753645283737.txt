Below is a **road-map / spec outline** you can paste into Replit’s AI-assistant (or keep as an internal design doc) to extend the Casimir-Tile Calculator from *static* Casimir to a **full dynamic, warp-bubble-aware simulator**.
All bullets are sourced from the Math-GPT conversation you linked.

---

## 1 · Dynamic-Physics Inputs to expose in the UI

| UI control                              | Default        | What it means                                               | Source                    |
| --------------------------------------- | -------------- | ----------------------------------------------------------- | ------------------------- |
| **Modulation freq *fₘ* (GHz)**          | 15             | Piezo stroke frequency that drives the dynamic-Casimir pump | L8–L9 ([math-gpt.org][1]) |
| **Stroke amplitude δa (pm)**            | 50             | ± boundary motion of the graphene/Nb₃Sn piston              | L5 ([math-gpt.org][1])    |
| **Burst length t<sub>burst</sub> (µs)** | 10             | Window the 15 GHz drive is on                               | L8 ([math-gpt.org][1])    |
| **Cycle time t<sub>cycle</sub> (µs)**   | 1000 (1 kHz)   | Sector-strobe period; defines duty *d*                      | L7–L9 ([math-gpt.org][1]) |
| **Cavity Q**                            | 1 × 10⁹        | Amplifies ΔE during the burst                               | L5 ([math-gpt.org][1])    |
| **Tile count N<sub>tiles</sub>**        | calc from area | Needed for total exotic mass                                | L12 ([math-gpt.org][1])   |

---

## 2 · Derived, real-time dash-board numbers

| Formula                                                    | Display as                         | Why                                             |
| ---------------------------------------------------------- | ---------------------------------- | ----------------------------------------------- |
| *Tₘ* (ps) = 1 / *fₘ*                                       | “Stroke period”                    | 15 GHz → 66.7 ps                                |
| Duty *d* = t<sub>burst</sub> / t<sub>cycle</sub>           | % bar                              | Must meet Isaacson HF limit ([math-gpt.org][1]) |
| ⟨ΔE⟩ = ΔE<sub>static</sub> × Q × d                         | “Cycle-avg exotic energy per tile” | Inputs GR source                                |
| ρ<sub>eff</sub> = ⟨ΔE⟩ / (tile volume)                     | Exotic energy density              | feeds Ford–Roman ζ                              |
| ζ = ρ<sub>eff</sub> τ<sub>pulse</sub> / QI<sub>bound</sub> | Quantum-inequality margin          | “Safe if ζ < 1”                                 |

---

## 3 · Back-end calculations to implement

1. **Static stage** – already done (SCUFF-EM results).
2. **Dynamic boost** – multiply static ΔE by *Q* during t<sub>burst</sub>.
3. **Sector averaging** – scale by duty *d* (Isaacson, Green-Wald) ([math-gpt.org][1]).
4. **Tile lattice sum** – N<sub>tiles</sub> × ⟨ΔE⟩ gives total exotic mass (target ≈ 1.4 × 10³ kg) ([math-gpt.org][1]).
5. **Quantum-Ineq check** – compute ζ and flag red if ζ ≥ 1 (Stage 5, L13 ([math-gpt.org][1])).
6. **Power load** – show instantaneous vs averaged power (raw ≈ 2 PW, duty-mitigated → 83 MW) L12 ([math-gpt.org][1]).

---

## 4 · Replit component outline

```txt
/ui
  ├─ app.py                # add “Dynamic Modulation” expander + new dashboard cards
  └─ callbacks.py          # async worker to run batch strokes

/sim_core
  ├─ static.py             # existing SCUFF-EM wrapper
  ├─ dynamic.py            # functions: boost_energy(), duty_average()
  ├─ lattice.py            # sum_tiles(), power_budget(), quantum_ineq()
  └─ constants.py          # c, ħ, QI bounds, Nb3Sn Drude params

/geom
  └─ make_bowl.py          # already generates sagged meshes

/docs
  └─ DYNAMIC_README.md     # derivations & references (Isaacson 1968, Green-Wald 2012)

/tests
  └─ test_dynamic.py       # unit tests for boost & duty math
```

---

## 5 · AI-assistant prompt snippet for Replit

```
🔧  Extend ui/app.py:
    • Under “Advanced Parameters” add a collapsible “Dynamic Modulation”.
    • Controls: freq_GHz (15 default), stroke_pm (50), burst_us (10),
      cycle_us (1000), cavity_Q (1e9).
    • On param change → call sim_core.dynamic.update_dash().
    • Show cards: Period ps, Duty %, ⟨ΔE⟩ per tile, ζ, Avg Power.
    • Color-code ζ card: green (<0.9), amber (0.9–1.0), red (≥1).
    • Hook into existing run() button so static ΔE feeds dynamic calc.

Update sim_core.dynamic.py with:
    boost_energy(staticE, Q) -> boostedE
    duty_average(boostedE, duty) -> avgE

Add lattice.sum_tiles(avgE, N) and quantum_ineq(rho_eff, T_pulse).
```

---

### 🎯 Deliverable

After these changes the **Casimir-Tile Calculator** will:

1. Let the user dial **15 GHz modulation, duty cycle, Q**.
2. Instantly show whether those settings keep the warp bubble **GR-valid** (Isaacson limit) and **quantum-inequality safe**.
3. Output **total exotic mass** & **average power** for the engineering ledger.

That covers every dynamic factor highlighted in the Math-GPT warp-bubble review.

[1]: https://math-gpt.org/chat/f16b41fc-a6d8-4c0c-b528-63d1d684311a "MathGPT Chat | Needle Hull Warp Vessel Design"
