Here’s exactly what to tell Replit so the Galactic (pc) map lines up and centers on Sol.

✅ What to change
Use the correct origin & scale for the 20,000×20,000 PNG

These are for your current PNG (20k×20k exported from the SVG):

ts
Copy
Edit
// Sol in image pixels (from SVG → PNG mapping)
const ORIGIN_PX = { x: 10123.142, y: 9480.491 };
// Pixels per parsec for this PNG
const PX_PER_PC  = 1.6666667;
Pass those into GalaxyMapPanZoom (and enable debug for a quick check).

Find where you render the galactic map (Mission Planner, Galactic mode). Replace the old props:

tsx
Copy
Edit
<GalaxyMapPanZoom
  imageUrl="/galaxymap.png"
  bodies={BODIES}
  routeIds={route}
  originPx={{ x: 10123.142, y: 9480.491 }}   // << here
  scalePxPerPc={1.6666667}                   // << and here
  width={900}
  height={420}
  debug                                     // turn on to see rings/crosshair
/>
Make sure the file actually exists at /public/galaxymap.png and is 20,000×20,000.

Keep the center-on-Sol + clamp logic

Your current components/GalaxyMapPanZoom.tsx already has both (good!). It should include:

Center on origin on load:

ts
Copy
Edit
const fit = Math.min(zx, zy) * 1.1;
const ox = width/2  - originPx.x * fit;
const oy = height/2 - originPx.y * fit;
setOff(clampOffset({ x: ox, y: oy }, fit, nat, { w: width, h: height }));
Container with overflow-hidden.

Drag/wheel handlers that use clampOffset(...).

You’ve got all of that in the file you sent.

🛠 Replit agent prompt (copy/paste)
Ensure /public/galaxymap.png exists and is 20000×20000.

Open the component/page that renders the Galactic (pc) map (Mission Planner).

Update the <GalaxyMapPanZoom> props to:

tsx
Copy
Edit
originPx={{ x: 10123.142, y: 9480.491 }}
scalePxPerPc={1.6666667}
debug
Do not change components/GalaxyMapPanZoom.tsx logic; it already centers on origin and clamps panning.

Run the app. In Galactic mode you should see the galaxy PNG centered, a red crosshair at Sol, and rings at 500/1000/2000 pc.

If the image name/path differs, fix imageUrl accordingly.

Commit changes.

