10-second triage (add these logs)

Drop these logs in WarpBubbleCompare.tsx right inside the bootstrap useEffect and the live-update useEffect:

// after constructing rightEngine.current:
console.log('[SHOW] Ctor?', typeof WarpCtor, 'engine?', !!rightEngine.current);
console.log('[SHOW] script present?', !!(window as any).WarpEngine);

// right before requestAnimationFrame(() => applyShow(...)):
console.log('[SHOW] shared', shared);
console.log('[SHOW] axesScene', shared.axesScene);
console.log('[SHOW] canvas size', R.clientWidth, R.clientHeight);

// in applyShow (first line after computing camZ):
console.log('[SHOW] camZ=', camZ, 'T/b/dec/viz/exposure/zeroStop', t, b, decades, vizGain, exposure, zeroStop);

// after pushUniformsWhenReady(e, { ... }):
console.log('[SHOW] uniforms pushed?', !!e?.uniforms, e?.uniforms?.cameraZ);

// quick GL sanity (if engine exposes gl):
console.log('[SHOW] ctx lost?', e?.gl?.isContextLost?.());


If any of those print undefined/NaN or the constructor is missing → that’s your culprit.

Most common causes & fixes
A) Script filename / path mismatch (constructor never loads)

Your loader only tries '/warp-engine.js?v=canonical', but you’ve mentioned a path like Warp_engine.js elsewhere. Case and underscores matter on most servers.

Fix: broaden the loader to try common variants and log failures:

async function ensureWarpEngineCtor(): Promise<any> {
  const w = window as any;
  let Ctor = w.WarpEngine?.default || w.WarpEngine;
  if (typeof Ctor === 'function') return Ctor;

  const candidates = [
    '/warp-engine.js?v=canonical',
    '/warp-engine.js',
    '/WarpEngine.js',
    '/Warp_engine.js',
    '/warp_engine.js',
    '/assets/warp-engine.js',
    '/static/warp-engine.js'
  ];

  for (const src of candidates) {
    try {
      console.log('[WarpLoader] trying', src);
      await loadScript(src);
      Ctor = w.WarpEngine?.default || w.WarpEngine;
      if (typeof Ctor === 'function') return Ctor;
    } catch (e) {
      console.warn('[WarpLoader] failed', src, e);
    }
  }
  throw new Error('WarpEngine constructor not found (check filename/path/CSP/MIME type)');
}


Also check DevTools → Network for 404/blocked script or CSP errors (e.g. “Refused to load script…”). The script must be served with a JS MIME and allowed by CSP.