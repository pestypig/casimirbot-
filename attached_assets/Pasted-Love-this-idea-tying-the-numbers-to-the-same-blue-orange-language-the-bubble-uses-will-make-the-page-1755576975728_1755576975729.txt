Love this idea—tying the numbers to the same blue/orange language the bubble uses will make the page “click” for viewers. Here’s a clean way to wire it up with minimal churn:

Color language (shared)
	•	Contraction (−β) → Blue (same as your grid)
	•	Expansion (+β) → Orange
	•	Interior tilt ε_tilt → Violet (distinct but legible in both light/dark)

Below are drop-in patches to (1) color the “Live β Field Samples” the same way the bubble reads, (2) add a tiny “Curvature Key” panel, and (3) tint the Shift Vector panel so users can trace where the violet tilt comes from.

⸻

1) Add a tiny legend: CurvatureKey.tsx

New file: client/src/components/CurvatureKey.tsx

import * as React from "react";

export function CurvatureKey() {
  const swatch = "inline-block w-3 h-3 rounded-full mr-2 align-middle";
  return (
    <div className="rounded-md border bg-card p-3 text-sm">
      <div className="font-semibold mb-2">Curvature Key</div>
      <div className="space-y-1">
        <div><span className={`${swatch} bg-sky-400`}></span><span className="text-sky-400 font-medium">Contraction</span> (−β)</div>
        <div><span className={`${swatch} bg-orange-400`}></span><span className="text-orange-400 font-medium">Expansion</span> (+β)</div>
        <div><span className={`${swatch} bg-violet-400`}></span><span className="text-violet-400 font-medium">Interior tilt</span> (ε_tilt)</div>
      </div>
    </div>
  );
}

Place it right under the Natário panel (or beside it on wide screens).

⸻

2) Make the “Live β Field Samples” adopt blue/orange + violet

File: client/src/components/WarpVisualizer.tsx

Replace your sampling block with this one (same structure you already have, just colored and split into Bell / Tilt / Total with consistent palette):

{/* Live β Sampling */}
<div className="space-y-2">
  <div className="text-cyan-300 font-semibold">Live β Field Samples:</div>
  {[
    { name: 'Center', s: 0.00 },
    { name: 'R/2',    s: 0.50 },
    { name: 'R',      s: 1.00 },
    { name: 'Edge',   s: 2.00 }
  ].map(point => {
    // Canonical Natário bell
    const beta0 = parameters.dutyCycle * parameters.g_y;       // β0 = duty·γ_geo
    const betaBell = beta0 * point.s * Math.exp(-point.s ** 2);

    // Interior tilt (small, interior-weighted)
    const interiorEnv = Math.exp(-Math.pow(point.s / 1.0, 2)); // 1 at center → 0 near wall
    const tiltMagnitude = epsilonTilt;                         // resolved above in your code
    const tiltProj = Math.abs(betaTiltVec?.[1] ?? 1);          // project roughly on “down”
    const betaTilt = tiltMagnitude * tiltProj * interiorEnv;

    const betaTotal = betaBell + betaTilt;

    // Color by sign for total: −β=blue, +β=orange
    const totalClass = betaTotal >= 0 ? "text-orange-400" : "text-sky-400";

    return (
      <div key={point.name} className="font-mono space-y-0.5">
        <div className={`flex justify-between ${totalClass}`}>
          <span>{point.name} (s={point.s.toFixed(2)}):</span>
          <span>β_total = {betaTotal.toExponential(2)}</span>
        </div>
        <div className="text-xs text-slate-400 flex justify-between">
          <span>• β_bell</span>
          <span className={betaBell >= 0 ? "text-orange-400" : "text-sky-400"}>
            {betaBell.toExponential(2)}
          </span>
        </div>
        <div className="text-xs text-slate-400 flex justify-between">
          <span>• β_tilt</span>
          <span className="text-violet-400">
            {betaTilt.toExponential(2)}
          </span>
        </div>
      </div>
    );
  })}
</div>

This keeps the UI’s semantics identical to the visualizer: totals read orange/blue by sign; tilt is always violet so viewers can trace its source.

⸻

3) Tint the Shift Vector panel consistently

File: client/src/components/ShiftVectorPanel.tsx

Lightly color the key numbers so readers see the violet lineage:

<h3 className="font-semibold text-sm mb-2">Shift Vector • Interior Gravity</h3>
<div className="font-mono text-sm space-y-1">
  <div>ε_tilt = <span className="text-violet-400">{epsilonTilt.toExponential(2)}</span></div>
  <div>g_target = <span className="text-violet-400">{gTarget.toFixed(3)}</span> m/s²</div>
  <div>R_geom = {R_geom.toFixed(1)} m</div>
  <div>β⃗_tilt = <span className="text-violet-400">[{betaTiltVec.join(", ")}]</span></div>
</div>

(Keep the rest of your math/explanations intact.)

⸻

4) Insert the legend panel in the layout

File: client/src/pages/helix-core.tsx

import { CurvatureKey } from "@/components/CurvatureKey";

// …inside your layout, near the Natário panel…
<div className="grid grid-cols-1 lg:grid-cols-3 gap-4">
  {/* Left / Center columns… */}
  <div className="lg:col-span-1 space-y-4">
    {/* Natário Warp Bubble panel already here */}
    <CurvatureKey />
  </div>
  {/* Rest of your columns… */}
</div>


⸻

Optional (nice-to-have): share hex codes with the engine

If you want the palette fully centralized, drop a tiny global in public/warp-engine-fixed.js:

// Near the top, once:
window.WARP_COLORS = {
  expansion: '#fb923c',   // tailwind orange-400
  contraction: '#38bdf8', // tailwind sky-400
  tilt: '#a78bfa'         // tailwind violet-400
};

Then in React you can read window.WARP_COLORS and avoid drift. (Totally optional; the Tailwind classes above already match your visual.)

⸻

Result
	•	The numbers now speak the same color language as the visual: blue = contraction (−β), orange = expansion (+β).
	•	A small Curvature Key panel orients new readers instantly.
	•	The tilt is consistently violet in both the legend and Shift Vector panel, so people can trace “why the center isn’t zero” when interior gravity is on.

If you want, I can also add tiny colored dots next to the mode selector (“Hover”, “Cruise”, …) to hint at the dominant sign profile for each mode’s instantaneous phase—but the above will get you a tidy, readable first pass that matches the simulator’s look and feel.