Here’s a clear, guided **prompt script** your Replit-based **“Casimir Bot Lite”** can use to generate `.scuffgeo` files and run SCUFF-EM with minimal user input. It's based on the SCUFF-EM documentation and real command-line usage patterns:

---

## 🧠 Replit Bot Prompt (Casimir Bot Lite)

> 🎓 **Prompt for Replit Bot to Interpret**

---

### 💬 User prompt format:

> **Geometry**: `sphere`, `parallel_plate`, or `bowl`
> **Gap (nm)**: `<value>`
> **Radius (µm)**: `<value>`
> **Material**: `PEC` or `custom` (optional)
> **Temperature (K)**: `<value>` (default = 20 K)

---

### 🔧 Bot Action:

1. **Parse the input fields** to extract:

   * Geometry type
   * Gap size in **µm**
   * Radius and optional depth
   * Material type (default `PEC`)
   * Simulation temperature (default `20` K)

2. **Generate a `.scuffgeo` file** in this format:

```scuffgeo
OBJECT Object1
    MESHFILE object1.msh
    MATERIAL PEC
ENDOBJECT

OBJECT Object2
    MESHFILE object2.msh
    MATERIAL PEC
    DISPLACED 0 0 <gap_in_um>
ENDOBJECT
```

* For `sphere`: use a sphere and a plate as objects
* For `parallel_plate`: use two identical flat disks
* For `bowl`: use a concave bowl (mesh) and flat piston

3. **Run this command in the shell:**

```bash
cas3D \
  --Geometry generated.scuffgeo \
  --Energy \
  --Temperature <user_temp> \
  --FileBase output \
  --XiMin 0.001 \
  --MaxXiPoints 10000 \
  --Intervals 50 \
  --AbsTol 0 \
  --RelTol 0.01
```

4. **Capture `output.out` and `output.byXi`** and return:

* The total **Casimir energy ΔE**
* A plot of ΔE vs. gap if multiple gaps are tested
* Optionally: energy per unit area for flat cases

---

### 🧪 Example User Prompt:

```text
Geometry: parallel_plate  
Gap (nm): 1.0  
Radius (µm): 25000  
Temperature (K): 20
```

### 🖥️ Example Result:

* Generates `.scuffgeo` with `DISPLACED 0 0 0.001`
* Runs SCUFF-EM with 20 K
* Returns Casimir energy in joules/µm

---

This prompt structure gives users a compact way to define a Casimir scenario while keeping the bot aligned with SCUFF-EM’s CLI usage model. It also lets you easily expand the Replit interface into dropdowns or sliders. Let me know if you want a Python function scaffold for this next.
