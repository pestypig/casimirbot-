import { describe, it, expect } from 'vitest';

// Placeholder suite to satisfy test runner until real pipeline adapter tests are authored.
describe('warp-pipeline-adapter placeholder', () => {
	it('placeholder true is true', () => {
		expect(true).toBe(true);
	});
});
