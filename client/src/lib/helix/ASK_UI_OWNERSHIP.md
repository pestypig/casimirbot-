# Helix Ask UI Ownership Map

This map keeps the `HelixAskPill.tsx` split readable while the component is being reduced. Pure display, formatting, parsing, and projection extractions must remain side-effect free. Behavior-goal extractions may use narrowly scoped React lifecycle hooks only when their effects are explicit, callbacks are injected, parity coverage exists, and they do not take ownership of stores, fetches, voice playback, request submission, route authority, terminal authority, or chat persistence.

`client/src/components/helix/ask-console/HelixAskConsoleState.ts` is the canonical recrown status contract. `HELIX_ASK_CONSOLE_RECROWN_PHASES` fixes the required order: stabilize the bridge, extract display owners, quarantine behavior-sensitive paths, build the minimal shell, replace the bridge after parity, then retire the legacy pill after proof. `HelixAskConsole` now enters through `HelixAskConsoleRuntimeShell`, and that shell defaults to the legacy bridge until side-by-side operator-surface parity is live-proven.

Bridge replacement is tracked explicitly by `bridgeReplacementProvenGates`, `bridgeReplacementOpenGates`, and `bridgeReplacementReady`. The proven gates record the parts already recrowned enough for the new shell to reuse, including entrypoint routing, named bridge quarantine, shell layout defaults, active-doc snapshot helpers, request-envelope helpers, latest-turn selectors, latest copy/debug/read-aloud control-target selection, final-answer projection, unclipped long-answer display, workstation trace projection, no-observation typed-failure preservation, calculator gateway trace parity, provider/model metadata projection, chat projection, chat persistence payload shaping, the minimal shell surface, minimal-shell submit-plan construction, minimal-shell optimistic turn start, the injected minimal-shell transport seam, minimal-shell stream-event recording, minimal-shell injected-turn completion/failure handling, the lazy backend-runner adapter, minimal-shell local turn rendering through recrowned final-answer display, minimal-shell structured workstation trace row rendering, minimal-shell latest control binding for copy/debug/read-aloud payloads, minimal-shell backend debug-export materialization for matching latest-turn refs, a runtime-shell seam that can select the minimal runtime without rendering `HelixAskPill`, and component smokes proving the runtime shell can submit through injected minimal transport, preserve the active `doc=...` context handoff, and render a completed turn without the legacy bridge. The open bridge-replacement gate is live operator-surface parity validation and `bridgeReplacementReady` is false; legacy `HelixAskPill` remains the default active runtime until that gate closes.

 `remainingBehaviorSensitivePaths` remains exposed in the status contract for the retirement phase: it records the original live surface that must be audited before deleting or hollowing out the legacy bridge.

Operator-surface parity is tracked separately by `operatorSurfaceParityItems`, `operatorSurfaceParityProvenItems`, `operatorSurfaceParityOpenItems`, and `operatorSurfaceParityReady`. `client/src/components/helix/ask-console/HelixAskOperatorSurfaceParityChecklist.ts` is the detailed parity checklist owner: it records each visible surface item, its legacy reference, its recrowned owner, the validation kind, and the remaining browser-visual parity gaps. `client/src/components/helix/ask-console/HelixAskOperatorSurfaceLayoutParity.ts` owns the concrete browser-evidence contract, pure browser-measurement-to-evidence projection, desktop/mobile evidence merge helper, viewport-record upsert helper, and evidence packet readiness projection for the remaining layout/dock and top-readability gaps. The evidence packet may prove that required desktop/mobile browser records are present, but it is not by itself permission to flip the default; user-visible parity acceptance remains required. `client/src/components/helix/ask-console/HelixAskOperatorSurfaceBrowserCapture.ts` owns the route-scoped, read-only selector capture helper for the side-by-side harness; it may read DOM geometry for evidence records, but it must not mutate UI state, run Ask transport, fetch, decide terminal authority, or change the default bridge. Runtime parity is not enough by itself. The recrowned minimal shell has unit coverage for the tracked surface pieces, but live testing showed the default swap changed the day-to-day console experience before parity was accepted. Layout/dock behavior and top-of-console readability remain open, and `operatorSurfaceParityReady` is false until the browser evidence contract is satisfied.

`client/src/components/helix/ask-console/HelixAskOperatorSurfaceParityHarness.tsx` is the explicit side-by-side browser comparison surface for operator parity. It renders the current legacy bridge and the recrowned minimal shell through `HelixAskConsoleRuntimeShell` under separate parity context ids. It is not the default product path and must not import `HelixAskPill` directly; it exists so visual parity can be reviewed without adding toggles or feature logic to the legacy file. `client/src/components/helix/ask-console/HelixAskOperatorSurfaceParityEvidencePanel.tsx` is the evidence strip for that harness: it names the remaining browser evidence, open gates, explicit route flag, browser-record schema, route-local capture status, captured viewport/readiness fields, hidden serialized browser records, and a route-local `sessionStorage` accumulator for desktop/mobile evidence packets, but it must not read app stores, fetch, mutate runtime state, make terminal-authority decisions, or change the default bridge.

`client/src/components/helix/ask-console/HelixAskOperatorSurfaceParityRoute.ts` owns the explicit browser route flag for that harness: `?helixAskParity=operator_surface`. `HelixAskConsole.tsx` may use this flag to render the harness for comparison, but the default path must remain `HelixAskConsoleRuntimeShell` with `runtimeImplementation = "legacy_bridge"` until parity is accepted.

Legacy behavior inventory is tracked by `legacyBehaviorClassifications`. The minimum complete runtime and operator surface chrome are classified as `used_must_move`; the current legacy bridge default is `used_temporary_adapter`; development fallbacks/trap doors remain `unknown_quarantined`; conflicting legacy runtime paths are `conflicting_remove_after_golden_path_proof`. Do not delete or hollow out `HelixAskPill` paths without moving the used items or proving a classification change.

The future shell target is tracked as `minimalRuntimeShellOwns` and `minimalRuntimeShellForbiddenOwnership`: the shell may coordinate the live console surface, but it must not own backend terminal authority, model/tool execution, private retries, a private agent runtime, final-prose tool-observation scraping, or duplicated backend route authority.

`HelixAskConsoleRuntimeShell` defaults to the legacy bridge, owns safe layout defaults tracked as `runtimeShellActiveOwnership`, and can still select the minimal runtime explicitly for side-by-side comparison. The shell must not statically import `HelixAskPill`, fetch, write clipboard, run TTS, or make route/terminal authority decisions; those behaviors either live behind tested minimal runtime owners or remain isolated behind the explicit legacy bridge.

`HelixAskMinimalRuntimeShell` is the replacement crown candidate. It composes the recrowned runtime layout, runtime picker, composer, runtime status line, latest-turn debug drawer, and visible-surface slots for recrowned goal/steering/supplement chrome with local draft/runtime-picker/debug state without importing `HelixAskLegacyRuntimeBridge` or `HelixAskPill`. It submits through the injected/backend runner, preserves the active docs-viewer URL handoff, records stream events, renders structured workstation trace rows from debug/transcript payloads, binds latest controls, materializes matching-turn debug export, and persists/hydrates chat sessions.

The tested minimal runtime shell is a replacement candidate, not the default path. The lazy legacy bridge remains the active day-to-day UI until live operator-surface parity is accepted.

## HelixAskPill no-growth guard

`HelixAskPill.tsx` is capped by the recrown no-growth guard at 25,485 Node-counted lines. New UI, display, projection, catalog, filtering, prompt-scaffold, attachment policy, and pure helper work belongs under `client/src/components/helix/ask-console/` or another documented recrowned owner. `HelixAskPill.tsx` may receive only thin bridge wiring when unavoidable. Only lower this ceiling after extraction shrinks the bridge; do not raise it to make new behavior fit.

`HelixAskMinimalRuntimeSubmitPlan.ts` is the pure submit-planning boundary for the inactive shell: it normalizes the draft through the recrowned submit-admission helper, reads the active docs-viewer path from a supplied desktop URL, and builds the request envelope. It must not fetch, start streams, write clipboard, run TTS, import the legacy bridge, or make terminal-authority decisions.

`HelixAskMinimalRuntimeLifecycle.ts` is the pure optimistic turn-start and injected-turn completion boundary for the inactive shell: it appends the "Reasoning in progress..." reply, marks the shell busy, records the active turn id and start time, creates the initial client live event, then can complete or fail that same local turn from an injected transport result/error. It must not fetch, start streams, write clipboard, run TTS, import the legacy bridge, or make terminal-authority decisions.

`HelixAskMinimalRuntimeTransport.ts` is the injected transport seam for the inactive shell: it builds the backend turn payload and calls a caller-supplied runner. The module must not import `runAskTurn`, `runAskTurnStream`, `HelixAskPill`, or the legacy bridge, and it must not call `fetch` directly. Real backend transport remains an open parity gate until the runner is wired and tested against the live bridge behavior.

`HelixAskMinimalRuntimeBackendRunner.ts` adapts the injected transport seam to the existing Ask backend API by lazy-loading `runAskTurnStream` and falling back to `runAskTurn`. It keeps API imports out of module initialization and remains inactive until the minimal shell replaces the legacy bridge.

`HelixAskMinimalRuntimeTurnList.tsx` is the inactive shell's local reply display owner: it projects minimal runtime replies into prompt/final-answer cards, using existing provider/model metadata display helpers and `HelixAskFinalAnswer`. It must not fetch, start streams, write clipboard, run TTS, materialize debug exports, infer workstation observations from final prose, import the legacy bridge, or make terminal-authority decisions.

`HelixAskMinimalRuntimeControls.ts` is the inactive shell's side-effect boundary for latest visible turn controls. It builds copy, debug-copy, and read-aloud payloads from the latest minimal runtime reply and provides browser clipboard/speech adapters. It must not fetch, start Ask turns, import the legacy bridge, read stores, execute tools, or make route/terminal-authority decisions. Backend debug-export materialization is delegated to `HelixAskMinimalRuntimeDebugExport.ts`.

`HelixAskMinimalRuntimeDebugExport.ts` is the inactive shell's minimal debug-export materialization boundary. It builds a latest-turn scoped backend debug-export request from the control payload, accepts only matching turn refs, falls back to the local scoped debug JSON on stale/missing/failing backend materialization, and owns the browser `fetch` call for `/api/agi/ask/turn/:turnId/debug-export`. It must not import `HelixAskPill`, start Ask turns, execute tools, read stores, scrape final prose, or make route/terminal-authority decisions.

`HelixAskMinimalRuntimeChatSession.ts` is the inactive shell's durable chat hydration helper: it projects existing `useAgiChatStore` context-session messages into minimal runtime replies. Store reads/writes live only in `HelixAskMinimalRuntimeShell`, where the runtime shell owns desktop chat/session persistence; display helpers must not import the chat store.

`HelixAskLegacyConsoleInventory.ts` is the live legacy-slicing tracker for the recrown goal. It records the current active path through `HelixAskLegacyRuntimeBridge -> HelixAskPill`, the `HelixAskPill.tsx` source snapshot, the render-slice line range, already-recrowned display slices, behavior-sensitive quarantined slices, and unknown trap-door slices. This file is not proof that bridge replacement is ready; it is the map that prevents future work from confusing the tiny bridge wrapper with the still-active 29k-line legacy implementation.

`HelixAskConsoleStatusSurfaces.tsx` owns visible composition of the live console error line, voice-status pill, and context-memory status line. It must not own error state mutation, voice capture state, memory derivation, runtime transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskConsoleErrorLineSurface.tsx` owns the active legacy-console error-line slot. It delegates to the status surface and must not own `askError` state mutation, request error handling, clearing behavior, runtime transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskConsoleErrorLineState.ts` owns side-effect-free packaging of the already-derived error-line message for the active legacy console. It must not own `askError` state mutation, request error handling, clearing behavior, runtime transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleView.tsx` owns the normal `errorLineState` / fallback `errorMessage` to error-line slot composition. It may still accept an explicit `errorLine` override for layout tests, but must not own `askError` state mutation, request error handling, clearing behavior, runtime transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskComposerActionToolbarSurface.tsx` owns visible composition of the live console action toolbar, runtime picker, and submit/stop button. It must not own haptic decisions, attachment selection behavior, mic/voice state, visual/audio capture behavior, runtime preference mutation, submit transport, stop execution, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyComposerSurface.tsx` owns visible composition of the legacy console composer slot from already-derived sub-surface props: voice-level monitor, mood avatar, action toolbar, and textarea. It must not own draft state, haptic decisions, attachment mutation, mic/voice state, visual/audio capture behavior, runtime preference mutation, submit transport, stop execution, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskSurfaceFrameSurface.tsx` owns the active legacy-console frame/chrome slot and delegates rendering to `HelixAskSurfaceFrame`. `HelixAskSurfaceFrameState.ts` owns side-effect-free packaging of already-derived frame props. These modules must not own submit transport, audio-prime behavior, width/palette derivation, offline state derivation, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleView.tsx` owns surface-frame state-to-slot composition. It may still accept an explicit `surface` override for layout tests, but must not own submit transport, audio-prime behavior, width/palette derivation, offline state derivation, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleRootState.ts` owns side-effect-free packaging of already-derived root view props (`className` and `layoutVariant`) passed into the active legacy console view. It must not own width/layout derivation, runtime shell selection, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleViewState.ts` owns side-effect-free packaging of the already-derived top-level display props passed into the active legacy console view. It must not own reply rendering, refs, debug drawer close mutation, stream lifecycle, latest-turn selection, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacySurfaceContent.tsx` owns visible composition of the legacy surface content slot from already-derived composer, supplement, and reasoning-theater props. `HelixAskLegacySurfaceContentState.ts` owns side-effect-free packaging of those already-derived props for the active bridge handoff. These modules must not own draft state, haptic decisions, media capture behavior, attachment mutation, runtime preference mutation, submit/stop execution, reasoning-theater state derivation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskVoiceLevelMonitorSurface.tsx` owns the active composer voice-level meter slot and delegates the meter rendering to `HelixAskVoiceLevelMonitor`. It must not own voice capture state, voice signal metric derivation, media capture lifecycle, mic toggles, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskVoiceLevelMonitorState.ts` owns side-effect-free packaging of active composer voice-level meter props and derives meter visibility from supplied mic-arm state. It must not own voice capture state, voice signal metric derivation, resize measurement, media capture lifecycle, mic toggles, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskVoiceCaptureHealthState.ts` owns side-effect-free packaging of already-derived voice meter, recorder, warning, checkpoint, and roundtrip fields for console/debug health projection. It must not own `MediaRecorder` setup, mic state mutation, voice capture lifecycle, diagnostics publication, browser audio inspection, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskVoiceCaptureDiagnosticsState.ts` owns side-effect-free packaging of already-derived voice health, checkpoint, segment, pending-confirmation, and feature-flag fields for the shared voice diagnostics base snapshot. `HelixAskVoiceFeatureFlagsState.ts` owns side-effect-free packaging of already-derived voice diagnostics feature flags. These modules must not own diagnostics publication, playback diagnostics, browser audio inspection, `MediaRecorder` setup, mic state mutation, voice capture lifecycle, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskMoodAvatarSurface.tsx` owns the active composer mood-avatar slot and delegates avatar rendering to `HelixAskMoodAvatar`. It must not own mood asset selection, mood state mutation, broken-image state mutation, prompt/mood inference, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskMoodAvatarState.ts` owns side-effect-free packaging of active composer mood-avatar props. It must not own mood asset selection, mood state mutation, broken-image state mutation, prompt/mood inference, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskComposerActionToolbarState.ts` owns side-effect-free packaging of active composer action-toolbar props. It must not own haptics, attachment selection side effects, mic/media lifecycle, runtime selection behavior, submit/stop behavior, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskComposerTextareaSurface.tsx` owns the active composer textarea slot and delegates DOM rendering to `HelixAskComposerTextarea`. It must not own draft state mutation, paste materialization, submit admission, request transport, route authority, terminal authority, clipboard, fetch, TTS, or input lifecycle beyond forwarding the bridge ref.

`HelixAskComposerTextareaState.ts` owns side-effect-free packaging of active composer textarea props. It must not own draft state mutation, paste materialization, submit admission, request transport, route authority, terminal authority, clipboard, fetch, TTS, or input lifecycle beyond receiving the bridge-owned callbacks.

`HelixAskPromptHistory.ts` owns deterministic prompt-history entry shaping, up/down navigation, draft restoration, and textarea-boundary key admission. It must not own React refs, draft mutation, textarea focus/selection mutation, request submission, chat persistence, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskSlashCommandCatalog.ts`, `HelixAskSlashCommandMenuState.ts`, `HelixAskSlashCommandInsertion.ts`, and `HelixAskSlashCommandMenu.tsx` own slash-command discovery catalog shaping, generated commands from the active account capability policy, account-policy filtering, menu open/filter/selection projection, slash-token replacement, and the visible menu surface. They must not execute tools, submit prompts, fetch capability data, write clipboard/TTS/chat state, mutate route or terminal authority, or infer tool success.

`HelixAskTurnListSurface.tsx` owns the completed/active turn-list lane slot, empty-list null state, and active-turn reply node composition, then delegates rendering to `HelixAskTurnList`. `HelixAskActiveTurnListState.ts` owns the side-effect-free active-turn list state projection for already-derived active stream rows, ids, refs, status line, debug snapshot, and display callbacks. These owners must not own chronological reply selection, active stream row derivation, stream event admission, quiet-gap timing, console debug snapshot construction, scroll scheduling, latest-turn selection, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleView.tsx` owns turn-list state-to-slot composition. It may still accept an explicit `turnList` override for layout tests, but must not own chronological reply selection, completed reply mapping, active stream row derivation, stream event admission, console debug snapshot construction, scroll scheduling, latest-turn selection, terminal authority, clipboard, fetch, or TTS.

`HelixAskFinalAnswer.tsx` owns safe presentation of already-selected final-answer Markdown blocks, including headings, GFM inline emphasis, pipe tables, code fences, and delegation of fenced Mermaid and LaTeX blocks to their renderers. It must preserve math delimiters through inline Markdown tokenization and must not select answer text, interpret raw HTML as trusted markup, infer route or tool intent, decide terminal authority, fetch application data, use the clipboard, or invoke TTS.

`HelixAskFinalAnswerSurface.tsx` owns the completed-turn final-answer block slot and delegates rendering to `HelixAskFinalAnswer`. It must not own visible answer selection, source labeling, path-link callbacks, math rendering callbacks, terminal authority, copy/read-aloud targets, debug payload materialization, clipboard, fetch, or TTS.

`HelixAskInlineCodeSurface.tsx` owns visible rendering of inline-code and code-style math token chips. It must not own math tokenization, inline-code segmentation, code-style classification, path-link callbacks, terminal authority, clipboard, fetch, or TTS.

`HelixAskMathHtmlSurface.tsx` owns visible rendering of KaTeX-produced math HTML spans. It must not own KaTeX HTML generation, math tokenization, display-mode selection, error fallback handling, terminal authority, clipboard, fetch, or TTS.

`HelixAskMermaidBlock.tsx` owns lazy, strict-security Mermaid rendering for already-selected fenced `mermaid` final-answer blocks and a readable raw-source fallback when rendering fails. It must not infer diagrams from prose, select answer text, admit routes or tools, decide terminal authority, fetch application data, use the clipboard, or invoke TTS.

`HelixAskRenderedContentSurface.tsx` owns visible math-aware final-answer content rendering from already-selected text: inline-code segmentation, math tokenization, KaTeX rendering, inline-code chip placement, and math HTML surface placement. It must not own path-link panel resolution, panel opening, final answer selection, envelope selection, calculator-panel affordance decisions, terminal authority, clipboard, fetch, or TTS.

`HelixAskPathLinkedTextSurface.tsx` owns visible file-path text/link composition for already-selected final-answer text, including path-segment button styling and click handoff. It must not own path-to-panel resolution policy, panel registry reads, `openPanelById` ownership, final answer selection, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyContentRenderers.tsx` owns the React callback bundle that composes already-selected final-answer text through `HelixAskPathLinkedTextSurface`, `HelixAskRenderedContentSurface`, the standalone `HelixAskLegacyAnswerEnvelopeSlot` final-answer mode, and already-derived plain/response-envelope answer slot props. It must not own path-to-panel resolution policy, panel registry reads, `openPanelById` ownership, answer/envelope selection, section grouping, extension open-state mutation, calculator-panel affordance decisions, terminal authority, copy/read-aloud targets, debug payload materialization, clipboard, fetch, or TTS.

`HelixAskLegacyAnswerEnvelopeSlot.tsx` owns visible composition of the already-derived plain answer or response-envelope answer region, including nested final-answer, calculator-launch, envelope-supplement, and envelope-section display surfaces. It must not own answer text selection, envelope selection, detail/proof grouping, extension availability or open-state mutation, calculator-panel visibility decisions, panel id selection, `openPanelById` ownership, terminal authority, copy/read-aloud targets, debug payload materialization, clipboard, fetch, or TTS.

`HelixAskPlainAnswerSurface.tsx` owns visible layout for non-envelope plain answer bodies and their supplement slot. It must not own reply content selection, content rendering callbacks, calculator-panel affordance decisions, panel opening, terminal authority, clipboard, fetch, or TTS.

`HelixAskEnvelopeAnswerSurface.tsx` owns visible layout for response-envelope answer blocks and their final-answer, calculator-launch, and supplement slots. It must not own answer text selection, final-answer rendering callbacks, calculator-panel affordance decisions, extension state mutation, terminal authority, clipboard, fetch, or TTS.

`HelixAskCalculatorPanelLaunchSurface.tsx` owns visible rendering of the final-answer calculator-panel launch affordance. It must not own equation-family detection, calculator-panel availability checks, panel id selection, `openPanelById`, terminal authority, clipboard, fetch, or TTS.

`HelixAskEnvelopeSectionsSurface.tsx` owns visible rendering of response-envelope detail/proof section rows, titles, body slots, and proof citation rows. It must not own envelope answer selection, detail/proof grouping, extension state, citation policy, calculator-panel affordance decisions, path-link callbacks, math rendering callbacks, terminal authority, clipboard, fetch, or TTS.

`HelixAskEnvelopeSupplementSurface.tsx` owns visible rendering of response-envelope extension, Details, and Proof disclosure wrappers. It must not own extension availability/open state, reply-scoped toggle state mutation, detail/proof grouping, citation policy, calculator-panel affordance decisions, path-link callbacks, math rendering callbacks, terminal authority, clipboard, fetch, or TTS.

`HelixAskReplyTurnSurface.tsx` owns the completed reply turn slot and delegates rendering to `HelixAskReplyTurn`. It must not own latest reply selection, stream row derivation, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskReplyTurnItemSurface.tsx` owns the completed reply item wrapper div. It must not own reply projection, latest-turn selection, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, clipboard, fetch, or TTS.

`HelixAskCompletedReplyTurnSurface.tsx` owns the completed reply item wrapper plus reply-turn surface composition. It must not own reply projection, latest-turn selection, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, clipboard, fetch, or TTS.

`HelixAskLegacyCompletedReplySlot.tsx` owns the completed legacy-console reply slot from an already-derived turn surface state object. It must not own reply projection, latest-turn selection, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, clipboard, fetch, or TTS.

`HelixAskLegacyCompletedReplyState.ts` owns side-effect-free packaging of the already-derived completed reply slot props. It must not own reply projection, latest-turn selection, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskReplyTurnState.ts` owns side-effect-free packaging of the already-derived reply-turn props: latest flag, card props, and stream props. It must not own reply projection, latest-turn selection, stream row derivation, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskCompletedReplyCardState.ts` owns side-effect-free packaging of already-derived completed-turn card props: card test id, tint class, context capsule, runtime-goal debug summary, and prompt-ingested flag. It must not own reply projection, latest-turn selection, terminal projection, debug payload materialization, copy/read-aloud callbacks, job execution, reply state, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskCompletedReplyBattleState.ts` owns side-effect-free completed-turn battle/tint state projection from already-derived causal timeline events, live events, and reasoning-theater state. It must not own event collection, reasoning-theater debug extraction, active theater state, stream lifecycle, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskCompletedReplyStreamState.ts` owns side-effect-free packaging of already-derived completed-turn stream props, including row props, final-answer display props, provider/model metadata labels, control state labels, and callback references. It must not own stream row derivation, latest-turn selection, terminal projection, debug payload materialization, clipboard/debug/TTS execution, job execution, route authority, terminal authority, fetch, or actual side effects.

`HelixAskActiveTurnReplySurface.tsx` owns the active live-progress reply slot and delegates rendering to `HelixAskActiveTurnReply`, including the empty-row null state. It must not own stream event admission, visible-row derivation, active turn ids, quiet-gap timing, scroll scheduling, terminal authority, clipboard, fetch, or TTS.

`HelixAskGoalPillSurface.tsx` owns visible composition of the live console goal-pill slot and its empty-session null state. `HelixAskGoalPillState.ts` owns the side-effect-free state projection for already-derived session, expanded flag, busy/error state, and callbacks. These owners must not own goal session state, expansion state mutation, busy/error state mutation, goal action execution, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleView.tsx` owns goal-pill state-to-slot composition. It may still accept an explicit `goalPill` override for layout tests, but must not own goal session state, expansion state mutation, busy/error state mutation, goal action execution, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskSteeringQueueSurface.tsx` owns the active legacy-console steering-queue slot and preserves the current empty-slot null state when no queue items are provided. It may delegate to `HelixAskSteeringQueuePanel` for visible queue rows, but it must not own queue admission, wake execution, mailbox mutation, workstation actions, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskLegacyConsoleView.tsx` owns the default empty steering-queue slot composition. It may still accept an explicit `steeringQueue` override for layout tests, but must not own queue admission, wake execution, mailbox mutation, workstation actions, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskDebugDrawerSurface.tsx` owns visible composition of the live console debug drawer slot, the drawer-state-to-visible-props projection, and its empty-drawer null state. It must not own selected debug state, drawer close mutation, debug payload materialization, backend debug-export fetch, clipboard writes, copied-id timers, request transport, route authority, terminal authority, or TTS.

`HelixAskLegacyConsoleView.tsx` owns debug drawer state-to-slot composition. It may still accept an explicit `debugDrawer` override for layout tests, but must not own selected debug state, drawer close mutation, debug payload materialization, backend debug-export fetch, clipboard writes, copied-id timers, request transport, route authority, terminal authority, or TTS.

`HelixAskConsoleSupplementSurface.tsx` owns visible composition of the live console supplement stack: attachments, context capsule preview, voice status, Situation Room source panel, voice command confirmation, transcript confirmation, context chooser, conversation brief, observer lane, and context-memory status. It must not own attachment mutation, voice command execution, transcript confirmation policy, context chooser execution, observer event selection, source capture, audio capture lifecycle, context-memory derivation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskAttachmentStripState.ts` owns side-effect-free packaging of already-derived attachment strip items and the bridge-owned remove callback. It must not own attachment mutation, attachment validation, request payload shaping, visual evidence admission, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskSupplementClipTextState.ts` owns side-effect-free packaging of the already-imported supplement text-clipping callback. It must not own text normalization, trimming rules, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskContextCapsuleState.ts` owns side-effect-free packaging of already-derived context capsule preview and auto-applied supplement props, plus active context-capsule preview/status display projection from an already-derived session capsule state. It must not own session capsule derivation, ledger ranking/selection, capsule copy behavior, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskSituationRoomSourceState.ts` owns side-effect-free packaging and visible display derivation for Situation Room source panel props from supplied visual/audio/source snapshots, including label, status, count, transcript preview, visibility, and audio-active state. It must not own store reads, source refs, source selection, visual capture, display-audio lifecycle, stop-audio execution, voice command policy, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`useHelixAskVisualSourceCaptureRuntime.ts` owns the recrowned browser screen/camera and optional shared-tab-audio capture lifecycle: media permission, source registration, frame-producer cadence, descriptor/heartbeat publication, transcript chunk forwarding, cancellation/replacement safety, Situation Room source snapshots, capture preferences, and unmount cleanup. `HelixAskPill.tsx` may only inject account availability, panel opening, synthetic observation events, and user-facing error projection, then consume the hook's source identity, evidence, status, and action handlers. This runtime produces observation evidence only; it does not own source admission authority, request routing, model/tool execution, follow-up reasoning, or terminal authority.

`HelixAskVisualFramePromotion.ts` owns the typed, ephemeral client bridge for explicit selected-frame promotion to GPT Live or the next Ask turn. `useHelixAskLiveRuntimeSession.ts` remains the sole owner of Vision consent and Realtime frame transport; `useHelixAskVisualFrameAttachmentIngress.ts` owns selected-frame data-URL validation, attachment-slot admission, and composer insertion. `LiveAnswerEnvironmentPanel.tsx` owns the three explicit operator actions and frozen-frame Image Lens handoff. `HelixAskPill.tsx` may only provide its existing attachment ref, setter, and error projection to the recrowned ingress hook. Promotion receipts are observations with `answer_authority=false`, `assistant_answer=false`, and `terminal_eligible=false`; they must not auto-submit an Ask turn or become a final answer.

`HelixAskVoiceConfirmationState.ts` owns side-effect-free packaging of already-derived voice command confirmation and transcript confirmation panel props. It must not own voice command policy, transcript confirmation policy, countdown lifecycle, voice execution, retry execution, state mutation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskVoiceConfirmationRuntime.tsx` owns the shared three-second voice command/transcript confirmation countdown lifecycle, candidate identity guards, transcript-over-command precedence, live-activity reset behavior, transcript auto-confirm policy rechecks, cleanup, typed runtime events, and the minimal-shell confirmation surface. Candidate creation, mic/STT browser control, admission decisions, command/transcript state mutation, voice command execution, retry execution, Ask submission, stores, fetches, route authority, terminal authority, and TTS remain injected by the active runtime.

`HelixAskVoiceStatusState.ts` owns side-effect-free packaging of voice status label/state supplement props and derives the visible status label from supplied mic/input/error state through the pure voice-copy display helper. It must not own voice capture, transcription, recorder lifecycle, mic state mutation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskContextChooserState.ts` owns side-effect-free packaging of already-derived context chooser panel props. The legacy attached-workspace-context chooser is retired behind `HELIX_ASK_LEGACY_ATTACHED_CONTEXT_CHOOSER_ENABLED` and must not be extracted as active request-context behavior. This module may preserve the generic quick-decision shape, but it must not own context chooser policy, countdown lifecycle, attached/isolated execution, cancellation mutation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskObserverSupplementState.ts` owns side-effect-free packaging of already-derived observer-lane and conversation-brief supplement props. It must not own timeline selection, observer event derivation, busy-state policy, observer execution, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskContextMemoryStatusState.ts` owns side-effect-free packaging of the already-derived context-memory status supplement prop. It must not own session capsule confidence derivation, memory state ownership, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskReasoningMeterSurface.tsx` owns visible composition of the live busy reasoning meter: battle-stage placement, Mirek bar particles, pressure overlay, stance meter fill, frontier icon/particle slots, and floating action text rows. It must not own reasoning-theater state derivation, beat/ambient construction, animation timing decisions, ref lifecycle, icon-broken state mutation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskReasoningTheaterSurface.tsx` owns visible composition of the live busy reasoning theater surface: busy panel chrome, Mirek field placement, background particle overlay, status/medal strip placement, and reasoning meter placement. `HelixAskReasoningTheaterState.ts` owns side-effect-free packaging of already-derived reasoning-theater surface props. `HelixAskReasoningTheaterStatusState.ts` owns side-effect-free packaging of already-derived reasoning-theater status and medal strip props. `HelixAskReasoningTheaterMeterState.ts` owns side-effect-free packaging of already-derived reasoning meter and frontier props. These modules must not own reasoning-theater state derivation, medal queue state, beat/ambient construction, animation timing decisions, ref lifecycle, broken-image state mutation, request transport, route authority, terminal authority, clipboard, fetch, or TTS.

`HelixAskImageAttachment.ts` is the deterministic image attachment materialization owner for file/clipboard image selection, image-only admission, 8 MB size limits, base64/object-url dependency seams, and pasted-image filename fallback. `HelixAskTextAttachment.ts` is the deterministic pasted-text attachment materialization owner for text byte measurement, the 1 MB pasted-text attachment limit and message, base64 encoding, hashing, preview clipping, and text turn-input item shaping. `HelixAskAttachmentCommit.ts` is the deterministic attachment commit-check owner for image/text attachment submit readiness, composer commit-check list projection, ready-state selection, structured preview metadata, and stale/too-large/unsupported/error reason shaping. `HelixAskAttachmentPayload.ts` is the deterministic submitted-attachment selection, native-image selection, first submit-ready image selection, submitted validation-entry shaping, first-invalid selection, context-pack, visual-evidence turn-input context, turn-input item shaping, and submit run-options payload selection owner. Attachment mutation, source admission, image-lens execution, request-envelope construction, attachment payload authority, and turn submission stay in the legacy bridge.

`HelixAskVoiceTimelineBuildInfo.ts` is the deterministic voice timeline build-info projection owner for initial client build-info state, `/version` success/error payload shaping, and the visible build-info timeline event projection. Browser/env reads, `/version` fetch and abort lifecycle, event source admission, diagnostics publication, mic/STT runtime, playback runtime, and TTS stay in the legacy bridge.

## Extracted Non-React Owners

| Owner module | Owns | Does not own |
| --- | --- | --- |
| `ask-agibot-preflight-error.ts` | Deterministic AGIBOT desktop joint-scope preflight error classification from thrown/string/message-shaped errors. | Retry/downgrade behavior, `askLocal` transport, fallback execution, mission interface state, or request admission. |
| `ask-agent-runtime-display.ts` | Agent runtime provider normalization, selected/next runtime fallback, short labels, and actual provider label projection. | Provider fetch transport, runtime persistence, request payload mutation, agent execution. |
| `ask-answer-rendering.ts` | Math tokenization, answer-render debug summaries, calculator-panel display decisions, final-answer file-path text segmentation, and final-answer inline-code text segmentation. | Final answer authority, solver routing, fallback answers, panel-opening side effects, React rendering. |
| `ask-attachment-prompt-policy.ts` | Deterministic attachment/context prompt predicates for visual-input prompts, pasted-text resume recall cues, and explicit pasted-text attachment use. | Request-envelope construction, route metadata, source admission authority, attachment materialization, attachment payload authority, active-doc context, or turn submission. |
| `ask-visual-evidence-readers.ts` | Deterministic visual evidence summary extraction and diagnostic vision-provider summary classification. | Visual diagnostic evidence admission, request-envelope construction, route metadata, attachment payload authority, active-doc context, or turn submission. |
| `ask-active-turn-stream.ts` | Active turn event projection, active-turn event membership admission, stream row filtering, client trace attachment, console stream ingress debug initialization, and dropped-reason counter formatting. | Starting turns, stream lifecycle, retries, terminal completion, network streams, reply mutation, or debug/copy binding. |
| `ask-context-capsule-display.ts` | Context-capsule token cleanup, palette selection, compact copy text, stamp image projection, and session-capsule confidence band/label projection. | Capsule commit creation, automaton stepping, ledger ranking, selected capsule identity, prompt admission. |
| `ask-convergence-display.ts` | Context-capsule convergence source/proof/maturity display labels, debug snapshot projection, and visible strip state comparison. | Convergence state derivation, proof gates, capsule commits, collapse events, state updates. |
| `ask-continuous-turn-display.ts` | Continuous turn rows and live-answer bridge class/state projection. | Live answer runtime, mailbox wake decisions. |
| `ask-context-capsule-ledger.ts` | Deterministic context-capsule ledger ranking, session-capsule state projection, summary rank comparison, bounded upsert/eviction, selected capsule-id ordering, and latest-wins capsule-id ordering. | Ledger React state/refs, capsule commit creation, automaton stepping, prompt/request admission, source admission, or applying selected capsule identity to turns. |
| `ask-debug-event-display.ts` | Live event log lines, timestamp parsing, event identity display parsing, metadata debug-context display extraction, circular-safe debug JSON formatting, compact tool-trace disclosure copy, debug observation/artifact/runtime-loop summary projection, debug detail payload formatting, queued prompt normalization, rendered debug text cleanup, and shared in-progress placeholder display recognition. | Debug export authority, active-turn admission, active-turn event filtering, reply selection, reply lifecycle hiding, DOM traversal, fetches. |
| `ask-display-text.ts` | Shared deterministic display text normalization for Ask event labels. | Classification, routing, execution decisions. |
| `ask-doc-viewer-context.ts` | Deterministic docs-viewer debug snapshot projection from supplied state/current path, docs-viewer path normalization, safe docs-relative Ask snapshot path normalization, Ask snapshot path source precedence, current-doc/current-whitepaper/docs-viewer anchor-path cue resolution, explicit path extraction from prompt text, debug path comparison normalization, and atomic viewer launch suppression predicate for explicit docs-context summary prompts. | Active-doc handoff, docs-viewer store/URL reads, active current-path reads, last-known cache mutation, workspace snapshots, request envelope construction, source admission, route metadata, or panel launch side effects. |
| `ask-env-config.ts` | Deterministic env boolean, number, percent, exact-one flag, enabled-unless-zero flag, and exact-zero opt-out parsing from provided env records and raw values. | Env key selection, runtime feature policy, `import.meta` access, state mutation, route/request admission, numeric clamp/floor policy. |
| `ask-envelope-copy.ts` | Response-envelope copy text and citation display normalization. | Response selection, envelope authority, fallback construction. |
| `ask-exploration-policy.ts` | Deterministic observe/act/verify mode inference, exploration-policy prompt predicates, repo file-location request detection, repo-code evidence prompt detection, context chooser auto-mode classification, exploration escalation prompt construction, and artifact-retry prompt construction. | Exploration ladder decisions, retry execution, context chooser state/countdown, source chooser execution, request submission, route authority, or terminal authority. |
| `ask-external-prompt-claim.ts` | Deterministic external prompt claim-id normalization from explicit prompt ids or created-at/question fallback text. | Pending prompt consumption, browser-window single-flight claim tracking, duplicate dispatch suppression, prompt lifecycle cleanup, route admission, or request submission. |
| `../agi/debugExport.ts` | Shared debug export envelope helpers, deterministic debug payload hashing via `hashDebugExportText`, and debug drawer fallback result formatting. | Reply selection, rendered-button fallback extraction, clipboard writes/readback, backend debug export fetching. |
| `ask-goal-pill-display.ts` | Goal-pill label and cadence text formatting. | Goal session transport, edits, pause/resume/stop actions. |
| `ask-interim-voice-callout.ts` | Structured interim voice callout artifact parsing, authority-safe receipt validation, playback intent projection, nested artifact collection, and duplicate/spoken receipt suppression. | Voice queue mutation, spoken-receipt refs, TTS transport, audio playback scheduling, playback diagnostics, terminal authority, or debug export authority. |
| `ask-local-fallback-classification.ts` | Deterministic Ask-local fallback response classification for multilang confidence gate failures and mission/ideology domain-leak detection. | `askLocal` transport, retry/fail-open execution, mode downgrade behavior, prompt construction, route authority, or terminal authority. |
| `ask-live-source-display.ts` | Live-source mail transcript and turn stream display rows. | Mailbox reads, source admission, decision wakes. |
| `ask-luma-mood-display.ts` | Ask shell Luma mood palette class projection. | Mood classification, mood randomization, theme broadcasts, asset selection, state updates. |
| `ask-observer-commentary-display.ts` | Observer lane user-facing commentary text. | Workstation dispatch, observer execution, user-input requests. |
| `HelixAskObserverLaneEvents.ts` | Deterministic observer lane event projection from supplied timeline entries, including active-trace filtering, observer-commentary admission, timestamp selection, and duplicate suppression. | Timeline React state, event publication, observer execution, workstation dispatch, request submission, route authority, terminal authority, or panel rendering. |
| `HelixAskTimelineFeed.ts` | Deterministic timeline feed sorting and active-turn filtering from supplied timeline entries and active trace id. | Timeline React state, event publication, observer execution, stream lifecycle, request submission, route authority, terminal authority, or panel rendering. |
| `ask-observer-events.ts` | Observer lifecycle event object builders. | Event publication, runtime execution. |
| `ask-output-cleanup.ts` | Deterministic answer-boundary, stage-tag, prompt-line, echoed-question cleanup, and cleanup-format hint primitives used by legacy fallback presentation. | Fallback answer selection, route authority, terminal authority, solver output admission, or model response rewriting. |
| `ask-pending-input-readers.ts` | Deterministic pending-input record detection plus pending-transition marker and trace normalization. | Pending-cancellation classification, pending request resolution, visible terminal-kind selection, latest-turn lifecycle, terminal projection, or workstation transition mutation. |
| `ask-procedural-display.ts` | Procedural timeline/debug-copy action label formatting, workstation intent-stage detail copy, workstation lifecycle event display classification, observation-grounded reply text formatting, and workstation lifecycle status/receipt copy. | Timeline rendering, route selection, router fail IDs, calculator fast-path replies, workstation dispatch, tool execution, reply insertion. |
| `ask-read-aloud-display.ts` | Read-aloud button labels, deterministic mic/read-aloud UI state transitions, read-aloud button press action projection, read-aloud queue filtering by clicked reply, read-aloud per-reply state-map transition projection, voice utterance ID/hash formatting, voice rollout hash gating, structural voice playback display predicates, and mic-off utterance playback admission predicate. | TTS transport, audio playback, audio graph behavior, voice diagnostics, playback scheduling, mic arm state mutation, queue mutation, or playback side effects. |
| `ask-reasoning-battle-display.ts` | Reasoning battle visual class names, deterministic positions/styles, and answer tint palette projection. | Battle event construction, reasoning state, JSX rendering, terminal authority. |
| `ask-reasoning-frontier-display.ts` | Reasoning frontier action labels and floating action text/class projection. | Frontier tracker state updates, timers, rendering, route/terminal decisions. |
| `ask-reasoning-theater-hard-failure.ts` | Deterministic reasoning-theater hard-failure signal extraction from debug records and live events for visual fail-closed projection. | Theater state derivation, terminal authority, route/product authority, timers, refs, JSX rendering, or reply lifecycle. |
| `ask-reasoning-theater-display.ts` | Reasoning theater labels, phase/suppression display bucket resolution, certainty display bucket mapping, deterministic medal-event projection, stance meter classes, suppression copy, medal asset paths, Mirek cell visual classes, Mirek artifact particle projection, Mirek display-grid density/simulation projection, and frontier particle node projection. | Theater state derivation, hard-failure interpretation, medal queue/timers, Mirek evidence-anchor collection, frontier tracker updates, refs, JSX rendering, route/terminal decisions. |
| `ask-reasoning-theater-evidence.ts` | Deterministic Mirek evidence path recognition, nested evidence-path collection, evidence-anchor projection, and shared exact-path continuity ratio. | Theater state derivation, hard-failure interpretation, grid density/simulation, timers, refs, JSX rendering, route/terminal decisions, or evidence/source admission. |
| `ask-runtime-authority-readers.ts` | Deterministic runtime/action authority readers: object-record and array coercion, theory-reflection extraction, runtime action-key normalization, decision/gateway selected-capability collection, and workstation action runtime-key projection. | Backend route guardrails, action-envelope allow/deny decisions, debug authority attachment, workstation dispatch, terminal authority, or shortcut prevention. |
| `ask-stage-play-ledger.ts` | Stage Play chat ledger display events. | Stage Play runtime or chat persistence. |
| `ask-stage-play-mailbox-wake-policy.ts` | Deterministic compact Stage Play mailbox wake prompt detection, structured route-metadata validation, and missing-metadata block predicate. | Pending prompt consumption, wake execution, request submission, route authority, terminal authority, chat projection, or live-source mailbox reads. |
| `ask-status-classnames.ts` | Procedural and causal trace row class-name mapping. | React rendering, timeline construction. |
| `ask-stable-hash.ts` | Shared deterministic unsigned 32-bit hash and fixed-width projection hash formatting for stable UI ids, visual jitter, and payload fingerprints. | Debug export authority, reply selection, clipboard/export bounding, terminal authority, state mutation, or cryptographic integrity. |
| `ask-steering-queue-display.ts` | Steering queue item normalization and classes. | Auto-wake execution, mailbox mutation, workstation actions. |
| `ask-terminal-projection.ts` | Typed visible final-answer projection helpers, pending-input request reading, live-answer context-pack answer copy, job-ready link visibility filtering, and terminal answer text normalization/invalid-placeholder detection. | Model sampling, terminal completion, server-side authority, JSX timeline rendering, reply lifecycle state. |
| `ask-turn-transcript.ts` | Runtime transcript, transcript-record live-event projection, runtime Ask live-event projection, structured workstation gateway transcript projection, and causal trace row building. | Tool execution, live source materialization, terminal decisions, JSX timeline rendering, active-turn admission. |
| `ask-value-normalization.ts` | Shared deterministic value normalization helpers for generic number parsing, numeric/unit-interval clamping, text clipping, safe text coercion, non-empty string trimming, plain-record checks, string-array cleanup, and case-insensitive string dedupe. | Env key selection/policy, route/request admission, terminal authority, debug export authority, mutation, or side effects. Keyed env parsing lives in `ask-env-config.ts`. |
| `ask-voice-text-display.ts` | Voice/read-aloud text cleanup, conversation brief text sanitization, citation stripping, artifact-spill display cleanup, and compact debug summaries. | TTS transport, playback scheduling, audio graph behavior, voice capture, route decisions, brief pinning and suppression heuristics. |
| `ask-voice-turn-scoring.ts` | Deterministic voice conversation completion, turn-complete banding, and intent-shift scoring. | Voice dispatch, in-flight merge policy, supersede/restart decisions, STT lifecycle, mic runtime, queue mutation, or request submission. |
| `ask-workstation-fast-path.ts` | Deterministic workstation action argument text extraction for calculator fast-path and docs-viewer workstation action sync, calculator prompt expression extraction, and calculator fast-path action selection. | Calculator solve execution, docs-viewer store mutation, workstation dispatch, panel mutation, reply insertion, terminal authority, route authority, or final-answer projection. |
| `ask-workstation-command-text.ts` | Deterministic workstation command text normalization, panel-query normalization, operator subgoal restatement, lexicon alias normalization, injected panel text resolution, docs-path panel detection, and open-panel command parsing. | Live panel registry reads, panel capability matching, action construction, workstation dispatch, panel mutation, route authority, or terminal authority. |
| `ask-workspace-context-snapshot.ts` | Deterministic workstation layout debug snapshot projection and Ask turn workspace context snapshot shaping from supplied state-shaped data, including active panel, camelCase and snake_case doc context identity fields, calculator context clipping, note context clipping, focused empty Workstation Notes stale-recent-note suppression, Situation Room context passthrough, clipboard-panel detection, group count, sorted/deduped open panel ids, chat dock fields, mobile drawer fields, and injected timestamps. | Workstation layout/notes/calculator/Situation Room store reads, docs-viewer store/URL reads, active-doc handoff/path resolution, `Date.now`, source admission, route metadata, turn submission, or panel side effects. |
| `HelixAskQueuedTurn.ts` | Deterministic queued Ask turn object shaping, including question trimming, capsule id preservation, injected queue timestamp, context-resume route metadata attachment, and pasted-text resume recall queued-route metadata. | Queue React state, queue draining, retry scheduling, context-chooser handoff, `Date.now`, request submission, runtime transport, route authority, or terminal authority. |
| `ask-workstation-pending-input.ts` | Deterministic workstation confirmation parsing, pending-argument extraction, run-panel action arg cloning, docs-topic resolution metadata reading, and docs-topic private metadata stripping. | Pending request creation, pending request state, panel capability lookup, request/turn IDs, workstation dispatch, docs-viewer handoff, panel mutation, route authority, or terminal authority. |
| `ask-voice-copy-display.ts` | Voice command labels, input status/error labels, reasoning lifecycle copy, brief copy joining, timeline prompt display text, and reasoning attempt detail text. | Voice dispatch, lifecycle policy, timeline mutation, auto-speak scheduling, retry policy, mic capture, memory-pressure handling. |
| `ask-voice-barge-policy.ts` | Deterministic voice barge hard-cut reason resolution, held-playback resume predicate, supersede interruption predicate, and preempt-policy-to-cancel-reason mapping. | Mic effects, STT queue mutation, playback queue mutation, audio element scheduling, TTS transport, or cancellation side effects. |
| `ask-voice-brief-policy.ts` | Deterministic voice brief/status predicates, brief-source normalization, terminal-state speech suppression predicate, and suppressed-speech text shaping. | Voice queue mutation, pinned-brief state selection, TTS transport, audio scheduling, terminal authority, or reply lifecycle decisions. |
| `ask-voice-diagnostics-export.ts` | Deterministic voice diagnostics export projection: client build-stamp fallback formatting, segment count summaries, bounded playback receipts, bounded voice calls, bounded timeline rows, monitor rounding, client playback reconciliation debug projection, and non-terminal voice authority debug summary separating input capture, output playback, tool admission, playback receipts, and terminal-answer authority. | Diagnostics snapshot construction/publication, browser/env reads, refs/effects, mic/STT runtime, playback runtime, TTS transport, terminal authority, or debug export authority. |
| `ask-voice-playback-intent.ts` | Deterministic voice playback intent/task projection: manual read-aloud intent creation and queue-task object shaping with stable utterance IDs. | Voice queue mutation, TTS transport, audio playback scheduling, audio graph behavior, playback diagnostics, spoken-receipt refs, or terminal authority. |
| `ask-voice-confirmation-command.ts` | Deterministic parsing of short transcript confirmation voice commands into confirm/retry/cancel decisions. | Transcript confirmation policy, dispatch state, auto-confirm gating, command-lane envelope policy, STT lifecycle, or request submission. |
| `ask-voice-command-lane-policy.ts` | Deterministic voice command-lane envelope normalization and low-quality transcript barge suppression predicates. | Voice command parsing, transcript confirmation state mutation, STT lifecycle, active voice playback/reasoning state ownership, queue mutation, dispatch-window mutation, or request submission. |
| `ask-voice-transcript-confidence.ts` | Deterministic STT transcript confidence derivation, confirmation threshold constants, threshold/translation/provider confirmation predicate, transcript-confirm policy reason parsing, dispatch-state normalization, low-pivot translation block detection, and auto-confirm policy projection. | Transcript confirmation state mutation, live-activity observation, command-lane normalization, STT lifecycle, voice dispatch, or request submission. |
| `ask-voice-auto-dispatch-governance.ts` | Deterministic voice auto-dispatch admission trace projection, explicit voice Ask candidate detection, confidence/echo/queue/budget/mic gates, and env-backed auto-dispatch defaults. | Voice dispatch, dispatch-window mutation, STT lifecycle, queue mutation, transcript lifecycle, active-turn cancellation, or request submission. |
| `ask-voice-continuation-lexical.ts` | Deterministic voice continuation lexical predicates and merge policy: intent-term extraction, dangling/low-information tail checks, latest continuation focus extraction, lexical carryover, near-turn continuation classification, turn/in-flight merge predicates, supersede restart predicate, addendum classification, and tail-fragment classification. | Voice dispatch, STT lifecycle, mic runtime, queue mutation, transcript lifecycle, or request submission. |
| `ask-voice-held-transcript-policy.ts` | Deterministic held-transcript and pending-confirmation policy: turn-close silence constants, held-transcript flush age constants, no-transcript recovery predicate, watchdog flush predicate, and pending-confirmation merge predicate. | Voice capture/STT lifecycle, transcript queue mutation, held-transcript state writes, confirmation state mutation, active-turn cancellation, voice dispatch, or request submission. |
| `ask-voice-language-policy.ts` | Deterministic voice language-tag normalization, English-like tag classification, lightweight script-based source-language inference, source/response language selection, and high-risk translation-context detection. | Translation transport, STT lifecycle, transcript confirmation, workstation fast-path execution, response generation, voice dispatch, request submission, or route/terminal authority. |
| `ask-voice-playback-classification.ts` | Deterministic voice playback retry/error classification: graph fallback retry predicates, direct retry predicates, recoverable playback-error-as-ended predicates, retryable synthesis error detection, and memory-pressure error detection. | Audio graph construction, playback scheduling, TTS transport, user-agent/env playback policy, voice queue mutation, or cancellation/preemption policy. |
| `ask-voice-playback-runtime.ts` | Deterministic voice playback user-agent gain selection, mobile/iOS-desktop-mode classification with navigator touch-point checks, graph/direct attempt path selection, graph-bypass window checks, and active audio identity checks. | Audio graph construction, playback scheduling, retry counters, TTS transport, voice diagnostics, voice queue mutation, cancellation/preemption policy, or `speakVoice`. |
| `ask-voice-steering-client.ts` | Deterministic active-turn voice steering transcript classification, structured client request shaping, and structured live-event tool-call timing detection. | Voice capture/STT lifecycle, active-turn reservation, dispatch queue mutation, tool recording, cancellation/preemption policy, or Ask request submission. |
| `ask-voice-capture-display.ts` | Deterministic voice-capture signal display predicates: level smoothing, flat-signal/stall checks, loopback label matching, container-header priming decisions, explicit-UA recorder MIME candidate ordering, injected recorder MIME support probing, noise profile thresholds, low-quality signal flags, barge-in speech qualification, and media error debug labels. | Mic recorder lifecycle, direct browser capability reads, `MediaRecorder` runtime access, stream capture, STT dispatch, voice confirmation, held transcript recovery, or barge-in state transitions. |
| `ask-voice-capture-checkpoints.ts` | Deterministic voice-capture checkpoint schema: ordered checkpoint keys, display labels, checkpoint status types, and fresh idle checkpoint map construction. | Mic recorder lifecycle, checkpoint state updates, stream capture, STT dispatch, voice confirmation, diagnostics publication, or barge-in state transitions. |
| `voice/voice-transcript.ts` | Deterministic voice transcript draft merging and dispatch transcript source resolution. | STT queue mutation, dispatch submission, transcript confirmation state, mic runtime, or active turn lifecycle. |
| `voice/voice-turn-authority.ts` | Deterministic voice turn seal gating, voice reasoning response authority decisions, and authority suppression reason projection. | Starting/restarting Ask turns, request dispatch, queue mutation, playback/TTS, or terminal answer authority. |

## Still Quarantined In `HelixAskPill.tsx`

The following categories remain behavior-sensitive and should not be moved into display modules as part of presentation cleanup:

- Timeline entry creation, ordering, patching, filtering, and feed state. Unused timeline label constants should be deleted instead of preserved as new display owners.
- Legacy fallback and fallback reply construction.
- Transport and fetch calls, including Ask turns, live-source mail, goal sessions, and debug export endpoints.
- Stale completion and in-flight turn lifecycle.
- Route metadata, source admission, route authority, and terminal authority.
- Chat persistence and session merging.
- TTS, voice playback, voice diagnostics, mic capture, and audio graph behavior.
- Workstation dispatch, panel mutation, goal-session actions, and user-input request handling.
- Debug export authority, rendered-button export fallback, and clipboard/export bounding.

## Remaining Local Cluster Map

These are the major `HelixAskPill.tsx` clusters that still need either behavior-goal extraction or deliberate quarantine. Representative functions are listed to make the remaining monolith easier to scan; the list is not a permission slip to move the cluster as a pure display helper.

| Local cluster | Representative anchors | Current boundary |
| --- | --- | --- |
| Visual/audio capture preferences and runtime | `HelixAskVisualCapturePreference`, `useHelixAskVisualSourceCaptureRuntime` | Preference storage/sync plus the browser screen/camera, optional shared-tab-audio transcript, descriptor/heartbeat, cancellation, source-snapshot, and cleanup lifecycle are recrowned in `ask-console/`; the legacy pill only injects product wiring and consumes observation state; stale live projection components were removed instead of preserved. |
| Microphone arm session state | `HelixAskMicrophoneSessionState`, `micArmState` | Per-loaded-session default-off authority is recrowned in `ask-console/`; microphone state is never restored from browser or account storage. The explicit toggle, React state, capture loop, recorder, transcription, and deterministic stop runtime remain local bridge behavior. |
| Prompt interpretation and planner policy | `inferAskMode`, `classifyHelixReasoningIntent`, `deriveHelixPlannerContract`, `evaluateEvidenceFinalizationGate`, `resolveHelixDispatchPolicyAtTurnStart`, `hasExplicitWorkspaceThenReasoningCue`, `hasExplicitReasoningOnlyCue`, `isSimpleConversationTurnCandidate`, `stripQuotedPayloadsForEvidenceGate`, `isQuotedTransformOnlyForEvidenceGate` | Observe/act/verify mode inference is recrowned in `ask-exploration-policy.ts`; route admission, simple-conversation bypass, quoted-payload evidence-gate exceptions, and terminal/finalization policy remain behavior-goal only. |
| Workstation pending input and dispatch arbitration | `buildWorkstationUserInputRequest`, `resolvePendingWorkstationUserInput`, `resolvePanelActionDefinition`, `parseWorkstationConfirmationReply`, `extractPendingArgFromReply`, `cloneRunPanelActionWithArgs`, `readDocTopicResolutionMeta`, `stripDocTopicResolutionMetaFromArgs`, `parseWorkstationActionCommand`, `resolveWorkstationRouterFailId`, `syncDocViewerStateFromWorkstationAction` | Deterministic confirmation parsing, required-argument extraction, docs-viewer topic clarification metadata reading, action arg cloning, and private metadata stripping are recrowned in `ask-workstation-pending-input.ts`; docs-viewer workstation action arg extraction is recrowned through `ask-workstation-fast-path.ts`; pending request creation/state, panel action capability lookup, router fail-id mapping, docs-viewer handoff/store mutation, workstation dispatch, and workstation mutation remain local bridge behavior. |
| Pending input and cancellation terminal visibility | `isWorkstationTurnTransitionPendingRequest`, `registerTurnTerminalOutcome`, `readHelixPendingInputRecord`, `normalizeHelixPendingTransitionMarker`, `readHelixPendingTransitionTrace`, `hasHelixPendingCancellationMarker`, `isHelixCanceledPendingTurn`, `resolveHelixPendingInputRecord`, `resolveHelixVisibleTerminalKind` | Deterministic pending-input record reading, transition-trace normalization, cancellation-marker interpretation, recursive canceled-pending turn detection, and nested pending request resolution are recrowned in `ask-pending-input-readers.ts`; turn transition cancellation mutation, single-terminal outcome registration, visible terminal-kind selection, latest-turn lifecycle, and terminal projection remain bridge behavior. |
| Workstation command lexicon and panel resolution | `normalizePanelQuery`, `resolvePanelIdFromText`, `resolvePanelIdFromPath`, `parseOpenPanelCommand`, `restateWorkstationSubgoal`, `normalizeWorkstationCommandText`, `buildLexiconPanelAction`, `normalizeLexiconAlias`, `resolveCapabilityAliasPanelAction`, `parseWorkstationNoteCreateLexiconArgs`, `parseWorkstationLexiconAction` | Deterministic command text normalization, note-create lexicon argument parsing, panel-query normalization, operator subgoal restatement, alias normalization, injected panel text resolution, docs-path panel detection, and open-panel command parsing are recrowned in `ask-workstation-command-text.ts`; live panel registry reads, panel capability matching, `HelixWorkstationAction` construction, workstation dispatch, and panel mutation remain bridge behavior. |
| Stage Play generated wake admission and projection suppression | `isStagePlayMailboxWakePromptText`, `hasStagePlayMailboxWakeRouteMetadata`, `shouldBlockStagePlayMailboxWakePromptWithoutRouteMetadata`, `isGeneratedStagePlayMailWakePrompt`, `isGeneratedStagePlayMailWakeAssistantProjection`, `shouldSuppressGeneratedStagePlayMailWakeChatProjection` | Deterministic compact wake prompt and route-metadata admission predicates are recrowned in `ask-stage-play-mailbox-wake-policy.ts`; pending prompt consumption, wake execution, request submission, route authority, and terminal authority remain in the legacy bridge. Generated durable-chat projection suppression is recrowned in `client/src/components/helix/ask-console/HelixAskChatProjection.ts`, with policy injected by the bridge. |
| External prompt lifecycle and single-flight claims | `resolveExternalPromptClaimId`, `claimExternalPromptSingleFlight`, `pendingExternalAskPromptRef`, `consumePendingHelixAskPrompt`, `clearPendingHelixAskPrompt` | Deterministic external prompt claim-id normalization is recrowned in `ask-external-prompt-claim.ts`; pending prompt consumption, browser-window claim tracking, duplicate dispatch suppression, route admission, request submission, and lifecycle cleanup stay in the bridge because they decide which external Ask prompt becomes a turn. |
| Voice capture, STT, confirmation, continuation, and auto-dispatch | `resolveTranscriptConfirmPolicy`, `normalizeTranscriptConfirmPolicyReason`, `normalizeVoiceConfirmDispatchState`, `isLowPivotBlocked`, `deriveVoiceSegmentLocalAnalysis`, `resolveSpeakerFromSessionProfiles`, `shouldAutoConfirmTranscriptPrompt`, `normalizeVoiceCommandLaneEnvelope`, `shouldIgnoreLowQualityTranscriptBargeIn`, `buildVoiceReasoningDispatchPrompt`, `deriveVoiceTimelineSuppressionMeta`, `HelixAskVoiceTurnAssemblyController`, `HelixAskVoiceConfirmationRuntime` | Deterministic transcript confidence, transcript-confirm policy resolution, reason parsing, dispatch-state normalization, low-pivot translation blocking, and auto-confirm projection live in `ask-voice-transcript-confidence.ts`; deterministic voice command-lane envelope normalization and low-quality transcript barge suppression live in `ask-voice-command-lane-policy.ts`; deterministic voice auto-dispatch governance and explicit voice Ask candidate detection live in `ask-voice-auto-dispatch-governance.ts`; deterministic confirmation command parsing lives in `ask-voice-confirmation-command.ts`; deterministic active-turn steering transcript/request/event predicates live in `ask-voice-steering-client.ts`; deterministic barge/preempt predicates live in `ask-voice-barge-policy.ts`; deterministic barge-in speech qualification, local audio segment analysis, and speaker-profile matching live in `ask-voice-capture-display.ts`; deterministic continuation merge/restart predicates live in `ask-voice-continuation-lexical.ts`; deterministic held-transcript recovery/flush and pending-confirmation merge predicates live in `ask-voice-held-transcript-policy.ts`; deterministic context-dependent follow-up prompt shaping lives in `ask-voice-reasoning-dispatch-prompt.ts`; voice turn assembly state creation, incoming segment turn-key reuse/new-turn selection, held-transcript recovery/watchdog projection, held-transcript recovery scoring projection, transcript completion/turn-complete scoring projection, held-prefix merge/clear projection, transcript confirmation/admission projection, pending-confirmation policy projection, transcript confirm auto-timer policy projection, auto-dispatch rate-window/governance projection, monotonic assembly updates, bounded turn-map pruning, draft transcript merge/update projection, pending-confirmation merge/update projection, silence/dead-air seal evaluation, runtime queue-state refresh projection, and sealed assembler-state projection live in `client/src/components/helix/ask-console/HelixAskVoiceTurnAssemblyController.ts`; the shared three-second confirmation timer lifecycle, candidate identity checks, transcript-over-command precedence, live-activity reset, policy rechecks, cleanup, and typed runtime events live in `client/src/components/helix/ask-console/HelixAskVoiceConfirmationRuntime.tsx`; mic/STT browser runtime control, speaker-profile ref mutation, held transcript state writes, confirmation state mutation, reservation capture, STT queue mutation, dispatch-window ref mutation, dispatch submission, command/transcript execution callbacks, and tool recording stay behavior-sensitive in the bridge. |
| Voice reasoning dispatch and suppression rescue policy | `shouldDispatchReasoningAttempt`, `inferSuppressionCauseFromRouteReason`, `shouldForceObserveDispatchFromSuppression`, `resolveSuppressedDispatchRescueTranscript`, `isStrongQuestionDispatchCandidate`, `deriveVoiceTimelineSuppressionMeta`, `isLikelyContextDependentTurn`, `extractPriorUserContext`, `buildVoiceReasoningDispatchPrompt`, `isSimpleDirectPromptLaneCandidate`, `isSimpleTitleOrPathOnlyPrompt`, `shouldQueueWorkspaceBackgroundReasoning`, `normalizeConversationModeForDispatch`, `deriveObserverDispatchPlan` | Deterministic reasoning-dispatch lexical admission, suppressed-route cause mapping, timeline suppression metadata projection, forced-observe rescue checks, and low-info draft rescue transcript selection are recrowned in `ask-voice-dispatch-suppression.ts`; deterministic context-dependent voice follow-up classification, prior-user anchor extraction, dispatch prompt shaping, document quick-lane detection, title/path-only prompt classification, background workspace reasoning queue predicate, dispatch-mode normalization, and observer dispatch-plan derivation are recrowned in `ask-voice-reasoning-dispatch-prompt.ts`, with compatibility exports from the legacy bridge where existing tests/callers need them. Workspace dispatch, reasoning queue mutation, request submission, and in-flight voice lifecycle remain behavior-goal only. |
| Voice continuation and intent-shift policy | `shouldMergeVoiceContinuationTurn`, `shouldMergeVoiceContinuationInFlight`, `shouldRestartExplorationLadderOnSupersede`, `isLikelyContinuationAddendum`, `isLikelyContinuationTailFragment` | Latest-turn continuation semantics, supersede/restart policy, and voice intent arbitration; deterministic merge/restart/addendum/tail predicates live in `ask-voice-continuation-lexical.ts`, deterministic scoring lives in `ask-voice-turn-scoring.ts`, while transcript lifecycle, queue mutation, active-turn cancellation, and request dispatch remain behavior-sensitive. |
| Voice language and workstation fast-path policy | `normalizeVoiceLanguageTag`, `isEnglishLikeLanguageTag`, `inferLanguageTagFromSourceText`, `resolveVoiceSourceLanguage`, `resolveVoiceResponseLanguage`, `isHighRiskTranslationContext`, `readWorkstationActionArgText`, `extractCalculatorFastPathExpressionFromPrompt`, `getWorkstationFastPathReplyText`, `renderCalculatorFastPathReply`, `selectWorkstationFastPathReplyAction` | Deterministic language normalization, lightweight script-based language inference, response-language selection, and high-risk translation-context detection are recrowned in `ask-voice-language-policy.ts`; workstation arg reading for calculator fast-path and docs-viewer workstation sync, calculator prompt expression extraction, and calculator fast-path action selection are recrowned in `ask-workstation-fast-path.ts`; calculator fast-path solve execution, docs-viewer store mutation, and workstation reply text selection remain local behavior. |
| Voice playback audio runtime | `speakVoice`, audio graph refs, playback scheduling, retry counters, diagnostics, queue mutation, `HelixAskVoicePlaybackToolController` | Browser audio graph setup, direct audio scheduling, retry counters, TTS transport, voice diagnostics, queue mutation, mic arm state mutation, and `speakVoice` stay local. Governed TTS/interim handoff collection, deterministic voice speak request shaping for manual read-aloud/chunked playback, voice speak JSON response classification, playback intent classification, mic/input versus output-armed separation, mic-off playback permission projection, suppression/enqueue debug planning, injected handoff execution for emit/enqueue/receipt callbacks, and playback receipt ledger/post orchestration live in `client/src/components/helix/ask-console/HelixAskVoicePlaybackToolController.ts`; deterministic user-agent gain/path/bypass/active-audio helpers live in `ask-voice-playback-runtime.ts`; deterministic retry/error classifiers live in `ask-voice-playback-classification.ts`, pure ID/predicate formatting, read-aloud button press action projection, read-aloud queue filtering by clicked reply, read-aloud per-reply state-map transition projection, and mic-off utterance playback admission predicate live in `ask-read-aloud-display.ts`; deterministic intent/task projection lives in `ask-voice-playback-intent.ts`, and structured interim callout artifact parsing lives in `ask-interim-voice-callout.ts`. |
| Voice recorder runtime and MIME handling | `pickSupportedMicRecorderMimeType`, `pickMicRecorderMimeType`, `readVoiceTimelineClientBuildStamp`, `buildHelixAskVoiceTimelineInitialBuildInfo`, `applyHelixAskVoiceTimelineVersionPayload`, `applyHelixAskVoiceTimelineVersionError`, `buildHelixAskVoiceTimelineBuildInfoEvent`, mic recorder effects | Direct browser capability reads, `MediaRecorder` runtime access, stream capture, browser/env build-stamp reads, `/version` fetch and abort lifecycle, STT dispatch, capture resilience, diagnostics publication, and barge-in state transitions stay local; deterministic signal/header/noise threshold predicates, explicit-UA MIME candidate ordering, and injected MIME support probing live in `ask-voice-capture-display.ts`, deterministic voice timeline build-info projection and visible build-info timeline event shaping live in `client/src/components/helix/ask-console/HelixAskVoiceTimelineBuildInfo.ts`, checkpoint schema/idle-map construction lives in `ask-voice-capture-checkpoints.ts`, and diagnostics export projection lives in `ask-voice-diagnostics-export.ts`. |
| Voice brief pinning and speech suppression | `shouldAutoSpeakVoiceDecisionLifecycle`, `shouldAutoSpeakAnswerForTurn`, `isGenericRunningVoiceStatus`, `isPinnedVoiceBriefCandidate`, `normalizeConversationBriefSource`, `buildSuppressedVoiceSpeechText`, `shouldSuppressVoiceForTerminalState` | Deterministic brief/status predicates and suppressed-speech formatting live in `ask-voice-brief-policy.ts`; auto-speak eligibility, pinned brief state selection, voice queue mutation, terminal lifecycle, and speech scheduling stay local. |
| Exploration ladder and context chooser policy | `inferAskMode`, `decideExplorationLadderAction`, `buildExplorationEscalationPrompt`, `buildExplorationArtifactRetryPrompt`, `isRepoFileLocationRequestPrompt`, `buildConversationFallbackBrief`, `resolveAskContextChooserAutoMode`, `isRepoCodeEvidencePrompt` | Deterministic mode inference, repo file-location prompt detection, repo-code evidence prompt detection, context chooser auto-mode classification, escalation prompt construction, and artifact-retry prompt construction are recrowned in `ask-exploration-policy.ts`; retry execution, fallback brief construction, context chooser state/countdown, source chooser execution, exploration ladder decisions, request submission, route authority, and terminal authority remain bridge behavior. The old attached-workspace-context chooser is disabled and quarantined in the bridge; do not recrown it as active UI request-context policy. Deterministic mission/ideology domain-leak classification lives in `ask-local-fallback-classification.ts`. |
| Atomic viewer launch suppression | `shouldSuppressAtomicViewerLaunch` | Docs-summary prompts with explicit docs-viewer context/path should not auto-open the atomic viewer; deterministic predicate lives in `ask-doc-viewer-context.ts` with launch-contract tests, while the actual panel launch side effect remains local. |
| Context-capsule ledger selection | `contextCapsuleSessionLedger`, `setContextCapsuleSessionLedger`, `contextCapsuleSessionLedgerRef`, capsule application flow | Ledger React state/refs, selected capsule identity application, and session capsule flow remain local; deterministic ranking, bounded upsert/eviction, selected-id ordering, latest-wins ordering, and session-capsule state projection live in `ask-context-capsule-ledger.ts`. |
| Ask request construction and attachment admission | `isHelixAskVisualPrompt`, `isHelixAskPastedTextResumeRecallPrompt`, `isHelixAskUsePastedTextAttachmentPrompt`, `buildHelixAskPastedTextResumeRecallRouteMetadata`, `resolveHelixAskBackendEntrypointFamily`, `buildHelixAskHardBackendEntrypointRouteMetadata`, `buildQueuedAskTurn`, `validateHelixAskAttachmentForSubmit`, `validateHelixAskImageAttachmentForSubmit`, `validateHelixAskTextAttachmentForSubmit`, `buildHelixAskAttachmentCommitChecks`, `hasReadyHelixAskAttachmentCommitCheck`, `resolveHelixAskSubmittedAttachments`, `selectHelixAskNativeImageAttachments`, `selectFirstHelixAskSubmitReadyImageAttachment`, `buildHelixAskSubmittedAttachmentChecks`, `selectFirstInvalidHelixAskSubmittedAttachment`, `buildHelixAskAttachmentContextPack`, `buildHelixAskAttachmentTurnInputItems`, `buildHelixAskTurnInputItemsForSubmit`, `buildHelixAskSubmitRunOptionsPayload`, `buildHelixAskVisualEvidenceTurnInputContext`, `readVisualEvidenceSummary`, `isDiagnosticVisualEvidence`, `buildAskTurnWorkspaceContextSnapshot` | Deterministic attachment/context prompt predicates are recrowned in `ask-attachment-prompt-policy.ts`; deterministic visual evidence summary and diagnostic summary readers are recrowned in `ask-visual-evidence-readers.ts`; deterministic attachment commit validation, composer commit-check list projection, and ready-state selection are recrowned in `client/src/components/helix/ask-console/HelixAskAttachmentCommit.ts`; deterministic submitted-attachment selection, native-image selection, first submit-ready image selection, submitted validation-entry shaping, first-invalid selection, submitted-attachment context-pack, visual-evidence turn-input context, explicit-item precedence, turn-input item shaping, and submit run-options payload selection are recrowned in `client/src/components/helix/ask-console/HelixAskAttachmentPayload.ts`; deterministic backend-entrypoint family detection, pasted-text recall route metadata, and hard route metadata are recrowned in `client/src/components/helix/ask-console/HelixAskBackendEntrypointPolicy.ts`, and the live bridge imports the hard route-metadata builder directly from that owner; deterministic queued Ask turn object shaping, context-resume metadata attachment, and pasted-text resume recall queued-route shaping are recrowned in `client/src/components/helix/ask-console/HelixAskQueuedTurn.ts`; deterministic backend turn payload shaping, including top-level `doc_path` and `active_doc_path` from active doc context files, is recrowned in `client/src/components/helix/ask-console/HelixAskRequestEnvelope.ts`. Source admission authority, active-doc URL/store reads, visual diagnostic evidence admission, pasted-text recall admission behavior, image-lens execution, attachment payload authority, queue React state/draining, `Date.now`, and turn submission remain quarantined in the bridge. Pasted-text attachment byte measurement, size policy, encoding, hashing, materialization, and text turn-input item shaping are recrowned in `client/src/components/helix/ask-console/HelixAskTextAttachment.ts` through `getHelixAskTextAttachmentSizeBytes`, `getHelixAskTextAttachmentTooLargeReason`, `base64FromText`, `sha256TextHex`, `buildHelixAskTextAttachmentFromText`, and `buildHelixAskTextAttachmentTurnInputItem`. |
| Docs-viewer and workspace context snapshots | `readDocViewerDebugSnapshot`, `resolveAskTurnDocViewerSnapshotPath`, `readDocViewerPathFromDesktopUrlForAskSnapshot`, `rememberDocViewerPathForAskSnapshot`, `resolveDocsViewerAnchorPathForQuestion`, `readWorkstationLayoutDebugSnapshot`, `buildAskTurnWorkspaceContextSnapshot` | Active-doc store reads, active current-path reads, layout/notes/calculator/Situation Room store reads, `Date.now`, route metadata, and turn submission remain quarantined in the bridge. Active-doc last-known cache mutation, desktop URL `doc=` parsing for Ask snapshots, docs-viewer debug snapshot binding, and store/debug/url/last-known path resolution are recrowned in `client/src/components/helix/ask-console/HelixAskActiveDocContextBinding.ts` through `rememberHelixAskDocViewerPathForSnapshot`, `readHelixAskDocViewerPathFromDesktopUrlForSnapshot`, `buildHelixAskDocViewerDebugSnapshotBinding`, and `resolveHelixAskDocViewerSnapshotPathBinding`, with bridge wrappers preserving existing exported names. Active workstation layout debug snapshot binding and Ask turn workspace context snapshot binding are recrowned in `client/src/components/helix/ask-console/HelixAskWorkspaceContextBinding.ts` through `buildHelixAskWorkstationLayoutDebugSnapshotBinding` and `buildHelixAskWorkspaceContextSnapshotBinding`. Deterministic docs-viewer debug snapshot projection, path normalization/extraction, store > debug snapshot > desktop URL doc param > last-known source precedence, and current-document/current-whitepaper anchor-path cue resolution live in `ask-doc-viewer-context.ts` through `buildDocViewerDebugSnapshotFromState`, `normalizeDocViewerPathForAskSnapshot`, `resolveDocViewerSnapshotPathCandidate`, and `resolveDocsViewerAnchorPathCandidate`. Deterministic workstation layout debug snapshot projection and Ask turn workspace context snapshot shaping, including camelCase and snake_case active-doc identity fields plus focused empty Workstation Notes stale-recent-note suppression, live in `ask-workspace-context-snapshot.ts` through `buildWorkstationLayoutDebugSnapshotFromState` and `buildAskTurnWorkspaceContextSnapshotFromState`. Context-file shaping plus top-level backend payload `doc_path`/`active_doc_path` binding are recrowned in `HelixAskRequestEnvelope.ts`; the live submit path calls that owner directly for context files. Server-side workspace snapshot merge now treats the focused Workstation Notes panel as authoritative for note fields so stored prior note bodies cannot bleed into an empty current editor while prior doc context remains preserved. HASK-BSQ-001 still needs live browser parity proof. |
| Agent runtime/provider controller wiring | `HelixAskRuntimePreference`, provider fetch effect, `handleAgentRuntimeButtonClick` | Runtime preference storage is recrowned in `ask-console/`; backend provider fetch transport, selected runtime React state, haptics, persistence side effects, and request payload wiring stay local; pure provider normalization, labels, selectable-runtime resolution, and runtime picker control decisions live in `ask-agent-runtime-display.ts`. |
| Context compaction resume-frame handoff | `HelixAskContextCompactionResumeFrameStorage`, `HELIX_ASK_CONTEXT_RESUME_FRAME_STORAGE_KEY`, `readStoredHelixAskContextCompactionResumeFrame`, `writeStoredHelixAskContextCompactionResumeFrame`, `isHelixAskContextCompactionPauseText`, `isHelixAskContextCompactionPausePendingReply`, `extractHelixAskContextCompactionResumeFrame`, `extractLatestHelixAskContextCompactionResumeFrameFromReplies` | Deterministic pause text parsing, pause-pending reply classification, resume-frame extraction, and session-storage resume-frame cache are recrowned in `ask-console/`; context-compaction state mutation, queue unblocking, and request metadata handoff remain local behavior. |
| Chat projection, persistence payloads, and reply lifecycle | `parseHelixAskChatMessageTimeMs`, `buildHelixAskChatProjectionId`, `buildHelixAskRepliesFromChatSessionProjection`, `buildHelixAskConsoleChatMessagePayload`, `buildHelixAskConsoleChatTurnPayloads`, `buildHelixAskRepliesFromChatSession`, `resolveHelixAskConsoleReplyCanonicalKey`, `resolveHelixAskConsoleReplyOrderMs`, `mergeHelixAskConsoleReplyPreservingOrder`, `mergeHelixAskConsoleRepliesByCanonicalTurn`, `sortHelixAskConsoleRepliesChronologically`, `limitHelixAskConsoleRepliesChronologically`, `appendHelixAskConsoleReplyChronologically`, `isHelixAskConsoleProgressPlaceholderReply`, `shouldRenderHelixAskConsoleActiveTurnStream`, `shouldKeepHelixAskConsoleReplyInBriefLane`, `shouldHideHelixAskConsoleTranscriptReply`, bridge wrappers including `resolveHelixAskReplyCanonicalKey`, `resolveHelixAskReplyOrderMs`, `sortHelixAskRepliesChronologically`, `appendHelixAskReplyChronologically`, `shouldRenderHelixAskActiveTurnStream`, `shouldKeepHelixReplyInBriefLane`, and `shouldHideHelixAskTranscriptReply` | Pure durable chat-session projection is recrowned in `client/src/components/helix/ask-console/HelixAskChatProjection.ts`. Pure chat persistence payload shaping is recrowned in `client/src/components/helix/ask-console/HelixAskChatPersistence.ts`. Pure reply lifecycle projection, canonical keys, order timestamps, chronological limiting, active-turn stream visibility, brief-lane retention, transcript hiding, and reply ordering are recrowned in `client/src/components/helix/ask-console/HelixAskReplyLifecycle.ts`, with event-aware timestamp callbacks injected by the legacy bridge. Chat store mutation, session creation/activation, and stale completion remain in the legacy bridge until behavior tests prove a full move. |
| Backend entrypoint and runtime authority guards | `requiresHelixAskBackendEntrypoint`, `shouldUseHelixAskBackendTurnEntrypoint`, `hasHelixAskBackendEntrypointTurnId`, `buildHelixAskBackendEntrypointRuntimeFingerprint`, `buildHelixActionEnvelopeRuntimeAuthority`, `attachHelixActionEnvelopeRuntimeAuthorityDebug`, `readAgentLoopAuditRecord`, `readAgentLoopAuditArray`, `extractAskLevelTheoryReflection`, `normalizeHelixRuntimeActionKey`, `readHelixDecisionCapabilityKeys`, `readHelixGatewayCapabilityKeys`, `collectHelixAgentSelectedCapabilities`, `readHelixWorkstationActionRuntimeKeys` | Deterministic backend-entrypoint prompt predicates live in `client/src/components/helix/ask-console/HelixAskBackendEntrypointPolicy.ts`; deterministic runtime/action authority readers and backend Ask turn-id shape detection are recrowned in `ask-runtime-authority-readers.ts`; selected-capability admission behavior beyond the hard entrypoint route, runtime/action-envelope allow/deny authority, theory-reflection handoff behavior, debug authority attachment, and shortcut prevention remain bridge behavior. |
| Visible terminal and route authority | `shouldPreserveAuthoritativeTerminalOverEvidenceGate`, `resolveHelixAskVisibleTerminal`, `resolveHelixAskHardPromptProjectionGuard`, `requiresHelixAskBackendEntrypoint` | Terminal projection, route/product authority, backend-entrypoint guard behavior, and authoritative-terminal preservation over client evidence gates. Runtime transcript event projection is crowned in `ask-turn-transcript.ts`. |
| Procedural timeline JSX rendering | `renderHelixAskLegacyProceduralTimeline`, `HelixAskLegacyProceduralTimelineProjection`, `HelixAskLegacyProceduralTimelineSlot` | Procedural row projection, truth-table/runtime-record reading, selected tool disclosure, route display, and runtime action labeling now live in `client/src/components/helix/ask-console/HelixAskLegacyProceduralTimelineProjection.tsx`; visible terminal-kind selection is still supplied by the bridge, and terminal authority stays outside display. The JSX handoff runs through `HelixAskLegacyProceduralTimelineSlot.tsx`, which delegates to `HelixAskProceduralTimeline` and must not own route authority, terminal authority, reply mutation, fetch, clipboard, or TTS; deterministic procedural labels/status classes/event builders live in crowned helper modules. |
| Debug export and clipboard authority | `buildReplyMasterEventClockExport`, `debugPayloadMatchesRenderedTurnPayload`, `enforceDebugExportMatchesClickedButton`, `buildHelixDebugExportEnvelopeFromMasterPayload`, `normalizeReplyMasterDebugPayload`, `buildClientProjectionDebugFields`, `buildReplyScopedDebugExportFromRenderedReply`, `buildReplyScopedDebugExportFromRenderedButton`, `buildHelixAskReplyCopyText`, `buildHelixAskLegacyTurnControlActionPayload`, `resolveAuthoritativeDebugExportPayload`, `copyDebugPayloadToClipboard` | Reply-scoped event-clock export, rendered-button fallback export construction, backend debug export materialization/fetch, authoritative backend merge, selected drawer React state mutation, copied-id timer scheduling, and window debug globals remain local. Plain final-answer, context-capsule, and prepared debug JSON clipboard write/readback, stale-readback rejection, plus textarea fallback are recrowned in `client/src/components/helix/ask-console/HelixAskClipboard.ts`; backend debug export fetch/materialization and drawer mutation remain local. Debug export size-control, critical rail field copy shaping, and bounded UI debug JSON projection are recrowned in `client/src/components/helix/ask-console/HelixAskDebugExportSizeControl.ts` through `copyHelixRailCriticalDebugFieldsForUi` and `boundHelixDebugExportTextForUi`; clipboard writes and backend materialization stay local. Final-answer copy/read-aloud action payload selection is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyTurnControls.ts` through `buildHelixAskLegacyTurnControlActionPayload`; final-answer copy text selection is recrowned there through `buildHelixAskReplyCopyText`; terminal transcript text collection for rendered final matching is recrowned there through `collectHelixAskLegacyReplyTerminalTranscriptTexts`; rendered-reply debug payload matching is recrowned there through `debugPayloadMatchesHelixAskLegacyRenderedReply`; stale-safe copied-debug-id timer clearing is recrowned there through `clearHelixAskLegacyCopiedDebugIdIfCurrent`. Backend Ask turn debug-ref eligibility, rendered-button backend turn-id trust gating, deterministic backend-target selection, backend-merge client turn-id selection, selected debug turn-id resolution with UI reply-id fallback, clicked DOM client-reply selection, deterministic local debug-copy payload selection, final clicked-button payload mismatch guarding, clicked-turn debug payload identity matching, post-fetch reply-scoped stale export fallback guarding, and DOM clicked-button shared `data-turn-control-*` / `data-debug-copy-*` scope extraction with visible-row stale-attribute veto are recrowned in `client/src/components/helix/ask-console/HelixAskLegacyTurnControls.ts` through `isHelixAskLegacyBackendDebugExportEligibleTurnId`, `isHelixAskLegacyRenderedButtonBackendTurnScopeTrusted`, `resolveHelixAskLegacyDebugExportBackendTarget`, `resolveHelixAskLegacyDebugExportClientTurnId`, `resolveHelixAskLegacyReplyDebugTurnId`, `resolveHelixAskLegacyClickedDebugReply`, `selectHelixAskLegacyDebugCopyLocalPayload`, `selectHelixAskLegacyGuardedDebugExportPayload`, `selectHelixAskLegacyReplyScopedDebugExportPayload`, `debugPayloadMatchesHelixAskLegacyRenderedReply`, `debugPayloadMatchesHelixAskLegacyRenderedTurnPayload`, `enforceHelixAskLegacyDebugExportMatchesClickedButton`, and `extractHelixAskLegacyClickedTurnDebugScope`; stale visible-row attributes and unproven rendered ask turn ids may not carry a stale backend turn id or stale client reply id into the click handler. The bridge wrapper now delegates clicked-reply, rendered-reply debug payload matching, terminal transcript text collection, backend-merge client-id selection, and debug export size-control to recrowned owners before backend debug export fetch/copy. Deterministic copy-result-to-drawer-state projection and stale selected-debug drawer clearing live in `client/src/components/helix/ask-console/HelixAskDebugDrawerState.ts`. Deterministic debug payload hashing and pure drawer fallback result formatting live in `../agi/debugExport.ts`, and pure debug observation/artifact/runtime-loop summary projection lives in `ask-debug-event-display.ts`. |
| Debug context summary projection | `buildHelixAskDebugContextSummary` | Local bridge between rendered timeline/debug context and pending-cancellation visibility; depends on `isHelixCanceledPendingTurn`, terminal/failure debug fields, pending request masking, and latest-turn debug surfaces, so this is behavior-goal only rather than a pure debug display helper. |
| Runtime shell prop defaults | `buildHelixAskConsoleRuntimeBridgeProps`, `HELIX_ASK_CONSOLE_HERO_REPLY_LIST_CLASS_NAME`, `HELIX_ASK_CONSOLE_DOCK_REPLY_LIST_CLASS_NAME` | Safe pre-bridge defaults for `layoutVariant` and `replyListClassName` are recrowned in `client/src/components/helix/ask-console/HelixAskConsoleRuntimeShellProps.ts`; this helper is pure and must not import the legacy bridge, `HelixAskPill`, fetch, clipboard, stores, TTS, route authority, terminal authority, or stream lifecycle behavior. |
| Legacy console view and runtime layout slot composition | `HelixAskLegacyConsoleView`, `HelixAskLegacyConsoleRootState`, `HelixAskLegacyConsoleViewState`, `HelixAskConsoleRuntimeLayout`, `HelixAskSurfaceFrameSurface`, `HelixAskSurfaceFrameState`, `HelixAskLegacySurfaceContent`, `HelixAskLegacySurfaceContentState`, `HelixAskGoalPillState`, `HelixAskConsoleErrorLineState` | The live legacy console now enters `client/src/components/helix/ask-console/HelixAskLegacyConsoleView.tsx` from `HelixAskPill`, root display prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyConsoleRootState.ts`, top-level display prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyConsoleViewState.ts`, that view delegates to the pure shell-facing slot order in `HelixAskConsoleRuntimeLayout.tsx`, the active frame/chrome slot is recrowned in `client/src/components/helix/ask-console/HelixAskSurfaceFrameSurface.tsx`, side-effect-free frame prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskSurfaceFrameState.ts`, the composer/supplement/reasoning-theater surface-content order is recrowned in `client/src/components/helix/ask-console/HelixAskLegacySurfaceContent.tsx`, side-effect-free surface-content prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskLegacySurfaceContentState.ts`, visible goal-pill state packaging is recrowned in `client/src/components/helix/ask-console/HelixAskGoalPillState.ts`, and visible error-line state packaging is recrowned in `client/src/components/helix/ask-console/HelixAskConsoleErrorLineState.ts`; default surface-frame, surface-content, goal-pill, error-line, steering-queue, turn-list, and debug-drawer slot composition lives in the view while live state, layout derivation, latest-turn selection, copy/debug/read-aloud execution, stream lifecycle, submit/prime callbacks, goal action execution, media behavior, and debug drawer mutation stay in the bridge until a legacy-equivalent recrowned runtime proves parity. |
| Legacy console view state projection | `HelixAskLegacyConsoleRootState`, `HelixAskLegacyConsoleViewState` | Side-effect-free root display prop packaging now lives in `client/src/components/helix/ask-console/HelixAskLegacyConsoleRootState.ts`; side-effect-free top-level display prop packaging lives in `client/src/components/helix/ask-console/HelixAskLegacyConsoleViewState.ts`; layout derivation, reply rendering, refs, debug drawer close mutation, stream lifecycle, latest-turn selection, copy/debug/read-aloud execution, request transport, route authority, terminal authority, clipboard, fetch, and TTS remain bridge-owned. |
| Workflow demo and next-prompt QTE | `HelixAskWorkflowSuggestionRuntime`, `HelixAskWorkflowQteBridge`, `HelixAskWorkflowDebugProjection`, `WorkflowDemoLabPanel`, `useHelixWorkflowDemoStore`, `workflow-demo-context.ts`, `research-paper-to-proposal.ts`, `workflow-demo-debug.ts`, `helix-workflow-demo.v1.ts` | The versioned demo/session/context/QTE/debug contracts live in `shared/contracts/helix-workflow-demo.v1.ts`; bounded current-chat candidate selection, operator-confirmed objective binding, and template rendering live in `client/src/lib/helix/workflow-demos/workflow-demo-context.ts`; deterministic typed-evidence normalization and sequential step projection live in `client/src/lib/helix/workflow-demos/research-paper-to-proposal.ts`; turn/reply correlation, append-only debug event construction, prompt hashing, and reply-scoped `helix.workflow_demo_debug.v1` export shaping live in `client/src/lib/helix/workflow-demos/workflow-demo-debug.ts`; the bounded persisted public demo session and event ledger live in `client/src/store/useHelixWorkflowDemoStore.ts`; the user/guest-accessible panel lives in `client/src/components/workstation/WorkflowDemoLabPanel.tsx`; and both legacy and minimal runtimes render `client/src/components/helix/ask-console/HelixAskWorkflowSuggestionRuntime.tsx` through the shared runtime-layout slot. Pending editable-QTE capture, one-shot consumption, chat/turn validation, and `markPromptSubmitted` dispatch are owned by `client/src/components/helix/ask-console/HelixAskWorkflowQteBridge.ts`, which both runtimes call instead of maintaining private workflow refs or writing the workflow store directly. Workflow store selection for debug, reply-scoped export construction, master-clock `workflow_demo` row projection, authoritative/local debug merge, and final bounded Debug Copy attachment are owned by `client/src/components/helix/ask-console/HelixAskWorkflowDebugProjection.ts`; `HelixAskPill.tsx` retains only adapter calls because the legacy bridge still assembles its overall master event clock and click lifecycle. `client/src/lib/agi/debugExport.ts` retains the observation-only channel as `workflow_demo_debug`, including post-final amendments keyed to the source turn. A run may bind the latest non-synthetic research request from the active Helix Ask chat or an explicit custom objective; blank runs emit no QTE, and a binding cannot be changed after typed evidence advances a step. The QTE is always editable, `assistantAnswer=false`, `terminalEligible=false`, and `autoSubmit=false`; debug events additionally carry `answer_authority=false` and `raw_content_included=false`. It may insert a prompt into the composer but must not sample a model, execute a tool, auto-submit, retain raw edited prompt text in debug, infer completion from prose, own route authority, or own terminal authority. Future goal-lane coaching may rewrite a suggestion only after the deterministic step reducer identifies the current unmet step. |
| Legacy surface-content state projection | `HelixAskLegacySurfaceContentState` | Side-effect-free packaging of already-derived composer, supplement, and reasoning-theater props now lives in `client/src/components/helix/ask-console/HelixAskLegacySurfaceContentState.ts`; state derivation, callbacks, refs, submit/stop behavior, media behavior, request transport, route authority, terminal authority, clipboard, fetch, and TTS remain bridge-owned. |
| Console error-line state projection | `HelixAskConsoleErrorLineState` | Side-effect-free error-line prop packaging now lives in `client/src/components/helix/ask-console/HelixAskConsoleErrorLineState.ts`; error mutation, clearing, request failure handling, runtime transport, route authority, terminal authority, clipboard, fetch, and TTS remain bridge-owned. |
| Surface frame state projection | `HelixAskSurfaceFrameState` | Side-effect-free surface-frame prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskSurfaceFrameState.ts`; width/palette derivation, submit handling, audio-prime behavior, request transport, voice playback side effects, route authority, and terminal authority stay in the bridge. |
| Completed and active reply turn composition | `HelixAskTurnListSurface`, `HelixAskActiveTurnListState`, `HelixAskCalculatorPanelLaunchSurface`, `HelixAskEnvelopeAnswerSurface`, `HelixAskEnvelopeSectionsSurface`, `HelixAskEnvelopeSupplementSurface`, `HelixAskFinalAnswerSurface`, `HelixAskInlineCodeSurface`, `HelixAskMathHtmlSurface`, `HelixAskRenderedContentSurface`, `HelixAskPathLinkedTextSurface`, `HelixAskLegacyContentRenderers`, `HelixAskLegacyAnswerEnvelopeSlot`, `HelixAskPlainAnswerSurface`, `HelixAskReplyTurn`, `HelixAskReplyTurnState`, `HelixAskCompletedReplyBattleState`, `HelixAskCompletedReplyCardState`, `HelixAskCompletedReplyStreamState`, `HelixAskCompletedReplyTurnSurface`, `HelixAskLegacyCompletedReplySlot`, `HelixAskLegacyCompletedReplyState`, `HelixAskReplyTurnItemSurface`, `HelixAskReplyTurnSurface`, `HelixAskActiveTurnReply`, `HelixAskActiveTurnReplySurface` | The completed/active turn-list lane slot, empty-list null state, and active-turn reply node composition are recrowned in `client/src/components/helix/ask-console/HelixAskTurnListSurface.tsx`; active-turn list state packaging for already-derived rows, ids, refs, status line, debug snapshot, and display callbacks is recrowned in `client/src/components/helix/ask-console/HelixAskActiveTurnListState.ts` while stream admission, row derivation, quiet-gap timing, and terminal authority stay in the bridge; the final-answer block slot is recrowned in `client/src/components/helix/ask-console/HelixAskFinalAnswerSurface.tsx`; non-envelope plain answer body layout is recrowned in `client/src/components/helix/ask-console/HelixAskPlainAnswerSurface.tsx` while reply content selection, content rendering callbacks, and calculator affordance decisions stay in the bridge; response-envelope answer block layout is recrowned in `client/src/components/helix/ask-console/HelixAskEnvelopeAnswerSurface.tsx` while answer text selection, child-node construction, calculator affordance decisions, and extension state mutation stay in the bridge; `client/src/components/helix/ask-console/HelixAskLegacyAnswerEnvelopeSlot.tsx` now owns the active plain/envelope answer display composition and nested final-answer/calculator/supplement/section surface placement while the bridge still owns the selected text, grouping, extension mutation callback, calculator visibility decision, panel-open callback, and terminal authority; `client/src/components/helix/ask-console/HelixAskLegacyContentRenderers.tsx` owns the callback bundle that composes already-selected final-answer text through path-linked text, math-aware content, standalone final-answer surfaces, and already-derived plain/response-envelope answer slot props while path-to-panel resolution, panel registry reads, `openPanelById`, envelope selection, section grouping, extension toggle mutation, calculator affordance decisions, and terminal authority stay in the bridge; inline-code and code-style math token chip rendering is recrowned in `client/src/components/helix/ask-console/HelixAskInlineCodeSurface.tsx`; KaTeX math HTML span rendering is recrowned in `client/src/components/helix/ask-console/HelixAskMathHtmlSurface.tsx`; math-aware final-answer content rendering, inline-code segmentation, math tokenization, KaTeX rendering, and inline math/code surface composition are recrowned in `client/src/components/helix/ask-console/HelixAskRenderedContentSurface.tsx` while path-link panel resolution/opening, final answer selection, envelope selection, calculator-panel affordance decisions, and terminal authority stay in the bridge; visible file-path link button rendering is recrowned in `client/src/components/helix/ask-console/HelixAskPathLinkedTextSurface.tsx` while path-to-panel resolution, panel registry reads, and `openPanelById` stay bridge-owned callbacks; the calculator-panel launch button is recrowned in `client/src/components/helix/ask-console/HelixAskCalculatorPanelLaunchSurface.tsx` while equation-family detection, calculator availability checks, panel id selection, and `openPanelById` stay in the bridge; envelope detail/proof section row display is recrowned in `client/src/components/helix/ask-console/HelixAskEnvelopeSectionsSurface.tsx` while envelope answer selection, detail/proof grouping, extension state, calculator-panel affordance decisions, citation normalization, and content rendering callbacks stay in the bridge; envelope extension, Details, and Proof disclosure wrappers are recrowned in `client/src/components/helix/ask-console/HelixAskEnvelopeSupplementSurface.tsx` while extension availability/open state, reply-scoped toggle mutation, section grouping, and citation normalization stay in the bridge; final-answer file-path text segmentation is recrowned in `client/src/lib/helix/ask-answer-rendering.ts` via `splitHelixAskTextPathSegments`; pure composition of `HelixAskReplyCard` and `HelixAskTurnStreamPanel` is recrowned in `client/src/components/helix/ask-console/HelixAskReplyTurn.tsx`; side-effect-free packaging of already-derived reply-turn props is recrowned in `client/src/components/helix/ask-console/HelixAskReplyTurnState.ts`; side-effect-free completed-turn battle/tint state projection is recrowned in `client/src/components/helix/ask-console/HelixAskCompletedReplyBattleState.ts`; side-effect-free packaging of already-derived completed-turn card props is recrowned in `client/src/components/helix/ask-console/HelixAskCompletedReplyCardState.ts`; side-effect-free packaging of already-derived completed-turn stream props is recrowned in `client/src/components/helix/ask-console/HelixAskCompletedReplyStreamState.ts` while row derivation, terminal projection, debug payload materialization, and side-effect execution stay in the bridge; the completed reply item wrapper is recrowned in `client/src/components/helix/ask-console/HelixAskReplyTurnItemSurface.tsx`; the completed reply item plus reply-turn surface composition is recrowned in `client/src/components/helix/ask-console/HelixAskCompletedReplyTurnSurface.tsx`; the completed legacy-console reply slot state boundary is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyCompletedReplySlot.tsx`; side-effect-free packaging of already-derived completed reply slot props is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyCompletedReplyState.ts`; the completed legacy-console reply slot is recrowned in `client/src/components/helix/ask-console/HelixAskReplyTurnSurface.tsx`; the in-progress active-turn reply card composition is recrowned in `client/src/components/helix/ask-console/HelixAskActiveTurnReply.tsx` with disabled controls, empty-row null rendering, default no-final placeholder rendering, and no stream state ownership; the active legacy-console live-progress reply slot is recrowned in `client/src/components/helix/ask-console/HelixAskActiveTurnReplySurface.tsx` via `HelixAskTurnListSurface.tsx` while visible-row derivation stays in the bridge. Reply projection, terminal projection, debug payload materialization, copy/read-aloud callbacks, and job execution stay in the bridge. Latest visible turn ids, final-answer test ids, and the side-effect-free latest control target are recrowned in `client/src/components/helix/ask-console/HelixAskLatestTurnBinding.ts`. Deterministic visible-final text selection for copy/read-aloud, copy/read-aloud action payload selection, final-answer copy text selection, local debug-copy payload selection, backend debug-export target selection, stale-safe copied-debug-id timer clearing, and copy/debug/read-aloud button-state projection are recrowned in `client/src/components/helix/ask-console/HelixAskLegacyTurnControls.ts`; plain final-answer, context-capsule, and prepared debug JSON clipboard writes are recrowned in `client/src/components/helix/ask-console/HelixAskClipboard.ts`; shared clicked-turn data attributes for Copy, Debug copy, and Read aloud live in `client/src/components/helix/ask-console/HelixAskTurnControls.tsx`; deterministic debug drawer state projection is recrowned in `client/src/components/helix/ask-console/HelixAskDebugDrawerState.ts`; drawer-state-to-visible-props projection is recrowned in `client/src/components/helix/ask-console/HelixAskDebugDrawerSurface.tsx`; click handlers, backend debug export fetch/materialization, rendered-button fallback construction, selected debug React state mutation, copied-id timer scheduling, window debug globals, and TTS remain in the bridge. |
| Surface composer slot layout | `HelixAskLegacyComposerSurface`, `HelixAskLegacyComposerState`, `HelixAskSurfaceComposerPanel`, `HelixAskPromptHistory`, `HelixAskSlashCommandCatalog`, `HelixAskSlashCommandMenuState`, `HelixAskSlashCommandInsertion`, `HelixAskSlashCommandMenu` | Pure composer-surface slot order for voice level, mood avatar, action toolbar, textarea, and slash-command overlay is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyComposerSurface.tsx` and `client/src/components/helix/ask-console/HelixAskSurfaceComposerPanel.tsx`; side-effect-free composer prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskLegacyComposerState.ts`; deterministic prompt-history entry shaping, up/down navigation, draft restoration, and textarea-boundary key admission are recrowned in `client/src/components/helix/ask-console/HelixAskPromptHistory.ts`; deterministic slash-command curated override shaping, dynamic generation from active account capability policy, account-policy filtering, menu state, and insertion text replacement are recrowned in `client/src/components/helix/ask-console/HelixAskSlashCommand*.ts*`; haptics, attachment selection, mic toggles, runtime selection, draft syncing, prompt-history refs, slash-command open/query refs, focus mutation, stop/submit behavior, refs, and request submission stay in the bridge. |
| Surface supplemental slot order | `HelixAskSurfaceSupplementStack`, `HelixAskConsoleSupplementState`, `HelixAskAttachmentStripState`, `HelixAskSupplementClipTextState`, `HelixAskContextCapsuleState`, `HelixAskSituationRoomSourceState`, `HelixAskVoiceConfirmationState`, `HelixAskVoiceConfirmationRuntime`, `HelixAskVoiceStatusState`, `HelixAskVoiceCaptureHealthState`, `HelixAskVoiceCaptureDiagnosticsState`, `HelixAskVoiceFeatureFlagsState`, `HelixAskContextChooserState`, `HelixAskObserverSupplementState`, `HelixAskContextMemoryStatusState` | Pure supplemental surface slot order for attachments, context capsule preview, voice status, Situation Room source, voice confirmations, context chooser, observer lane, and context-memory status is recrowned in `client/src/components/helix/ask-console/HelixAskSurfaceSupplementStack.tsx`; side-effect-free supplement prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskConsoleSupplementState.ts`; side-effect-free attachment strip prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskAttachmentStripState.ts`; side-effect-free text-clipping callback packaging is recrowned in `client/src/components/helix/ask-console/HelixAskSupplementClipTextState.ts`; side-effect-free context capsule prop packaging and active context-capsule preview/status display projection are recrowned in `client/src/components/helix/ask-console/HelixAskContextCapsuleState.ts`; side-effect-free Situation Room source panel prop packaging and visible display derivation are recrowned in `client/src/components/helix/ask-console/HelixAskSituationRoomSourceState.ts`; side-effect-free voice command and transcript confirmation prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskVoiceConfirmationState.ts`; shared voice confirmation countdown lifecycle and the minimal-shell confirmation surface are recrowned in `client/src/components/helix/ask-console/HelixAskVoiceConfirmationRuntime.tsx`; side-effect-free voice status prop packaging and label derivation are recrowned in `client/src/components/helix/ask-console/HelixAskVoiceStatusState.ts`; side-effect-free voice capture health packaging is recrowned in `client/src/components/helix/ask-console/HelixAskVoiceCaptureHealthState.ts`; side-effect-free shared voice diagnostics base packaging is recrowned in `client/src/components/helix/ask-console/HelixAskVoiceCaptureDiagnosticsState.ts`; side-effect-free voice diagnostics feature-flag packaging is recrowned in `client/src/components/helix/ask-console/HelixAskVoiceFeatureFlagsState.ts`; side-effect-free context chooser prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskContextChooserState.ts`; side-effect-free observer lane and conversation-brief prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskObserverSupplementState.ts`; side-effect-free context-memory status prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskContextMemoryStatusState.ts`; attachment mutation, attachment validation, request payload shaping, text normalization/trimming rules, voice capture/transcription, diagnostics publication, playback diagnostics, browser audio inspection, voice command policy, transcript confirmation policy, context chooser policy/execution, observer selection/derivation, command/transcript candidate mutation and execution, session capsule confidence derivation, ledger ranking/selection, capsule state mutation, and capsule copy behavior stay in the bridge. |
| Reasoning theater state projection | `HelixAskReasoningTheaterState`, `HelixAskReasoningTheaterStatusState`, `HelixAskReasoningTheaterMeterState` | Side-effect-free reasoning-theater prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskReasoningTheaterState.ts`; side-effect-free status/medal prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskReasoningTheaterStatusState.ts`; side-effect-free meter/frontier prop packaging is recrowned in `client/src/components/helix/ask-console/HelixAskReasoningTheaterMeterState.ts`; reasoning-theater state derivation, medal/frontier broken-image mutation, ref lifecycle, stream behavior, request transport, route authority, and terminal authority stay in the bridge. |
| Busy reasoning panel chrome | `HelixAskBusyReasoningPanel` | Pure busy-reasoning strip chrome, tint overlay, and keyframe mount are recrowned in `client/src/components/helix/ask-console/HelixAskBusyReasoningPanel.tsx`; reasoning-theater state, meter refs, frontier icon fallback state, particles, pressure projection, and terminal/stream behavior stay in the bridge. |
| Reasoning animation keyframes | `HelixAskReasoningAnimationStyles` | Pure reasoning animation keyframes are recrowned in `client/src/components/helix/ask-console/HelixAskReasoningAnimationStyles.tsx`; the legacy bridge only mounts the display component. |
| Reasoning battle JSX rendering | `HelixAskReasoningBattleStage` | React battle-stage composition and JSX rendering are recrowned in `client/src/components/helix/ask-console/HelixAskReasoningBattleStage.tsx`; the legacy bridge still builds beat/ambient/tint state until runtime-shell parity tests prove that behavior can move. Deterministic visual class/style/tint projection lives in `ask-reasoning-battle-display.ts`. |
| Mirek reasoning field display | `HelixAskReasoningMirekField` | Pure background grid rendering for an already-built Mirek reasoning display grid is recrowned in `client/src/components/helix/ask-console/HelixAskReasoningMirekField.tsx`; grid derivation, reasoning-theater state, particles, meter/frontier refs, and all lifecycle behavior stay in the bridge. |
| Reasoning status and medal strip display | `HelixAskReasoningStatusMedalStrip` | Pure stance/archetype/phase/certainty label rendering and medal image/fallback chrome are recrowned in `client/src/components/helix/ask-console/HelixAskReasoningStatusMedalStrip.tsx`; medal queue state, timers, broken-image state mutation, reasoning-theater state derivation, and frontier/meter animation refs stay in the bridge. |
| Reasoning theater state and simulation | `deriveReasoningTheaterState`, `deriveReasoningTheaterStateFromCanonical`, `resolveReasoningTheaterFailureOverrideArchetype`, `applyReasoningTheaterFailureOverride` | State derivation, canonical-state adaptation, failure override application, medal queue/timers, and React rendering remain local; deterministic hard-failure signal extraction lives in `ask-reasoning-theater-hard-failure.ts`, deterministic evidence-anchor projection lives in `ask-reasoning-theater-evidence.ts`, and display labels/classes plus phase/suppression/medal/grid projection live in `ask-reasoning-theater-display.ts`. |
| Legacy local Ask fallback output cleanup | `stripPromptEcho`, `extractAnswerBlock`, `stripEvidencePromptBlock` | Deterministic fallback answer cleanup and answer-block selection are recrowned in `ask-output-cleanup.ts`; the bridge still decides when the legacy/local fallback path runs and still treats backend Ask as authoritative. Deterministic AGIBOT preflight error classification lives in `ask-agibot-preflight-error.ts`; deterministic multilang confidence gate and mission/ideology domain-leak response classification lives in `ask-local-fallback-classification.ts`; dead prompt/query/context builders were removed instead of preserved while backend Ask is authoritative. |

## Behavior-Sensitive Queue

These issues came from live Helix-mode UI testing and must be handled as behavior-goal work, not display-helper extraction. Do not move the named paths into non-React display modules unless behavior-preserving tests prove the move is safe.

### HASK-BSQ-001: Active document context handoff into Helix Ask requests

Evidence:

- Browser URL had `doc=docs/research/nhm2-current-status-whitepaper.md`.
- UI visible focus included `docs-viewer` and `scientific-calculator`.
- Prompt using "current NHM2 whitepaper" failed with: "I need retrieval before finalizing this claim. I do not yet have grounded evidence references for it."
- The same prompt succeeds through the direct API when `doc_path` is supplied.
- The same UI prompt succeeds when the doc path is explicitly written in the prompt.

Likely boundary:

- `HelixAskPill` request construction is not consistently passing retained active docs-viewer context, doc path, or document evidence into Helix golden-path requests, especially for compound prompts.

Expected:

- When the UI is on a docs-viewer URL with `doc=...`, Helix-mode Ask request assembly should include the active doc path/context in the backend request envelope without requiring the user to restate the path.

Quarantine:

- Request-envelope assembly, active-doc context collection, workspace context snapshots, route metadata, source admission, and golden-path request transport.

### HASK-BSQ-002: Scoped Debug copy binds to stale prior turn

Evidence:

- After a complex Helix UI compound answer rendered visibly, clicking latest Debug copy copied an older calculator turn.
- Copied payload had marker mismatch, `terminal_artifact_kind = workstation_tool_evaluation`, and answer "3 + 5 = 8".
- The visible answer was the later compound answer.
- Therefore latest visible reply and debug-copy target diverged.

Likely boundary:

- `helix-ask-latest-debug-copy` or selected debug state still points to a cached prior debug payload or previous latest reply after complex/streamed golden-path turns.

Expected:

- For every rendered answer, Debug copy must bind to that reply's `turn_id` and `debug_export_ref`, not the last successful cached debug export.

Quarantine:

- Stream final projection, latest reply selection, selected debug state, debug-copy binding, rendered-button export fallback, and clipboard/export bounding.

### HASK-BSQ-003: Backend compound terminal authority is not the first suspect

Evidence:

- Direct Helix API returns `terminal_artifact_kind = compound_evidence_synthesis_answer`.
- Direct Helix API returns `final_answer_source = compound_evidence_synthesis_answer`.
- Direct Helix API returns `subgoals = 5` and `terminal_error_code = null`.
- UI explicit-doc compound prompt renders a compound answer.

Implication:

- Patch UI request context and debug binding first. Do not treat this as a backend compound failure or golden-path terminal-authority failure without new contrary evidence.

### HASK-BSQ-004: Stored Workstation Notes context bleeds into current empty notes panel

Evidence:

- Live browser showed the focused Workstation Notes editor as an empty `Untitled note`.
- Browser `localStorage` had no `workstation-notes:v1` note body.
- The Ask transcript still reported an older Common Core/self-direction note as current visible context.
- The old note body was found in prior debug artifacts, proving it was historical data rather than current editor text.

Likely boundary:

- Server-side workspace snapshot merge preserved stored note bodies when the fresh focused notes-panel snapshot carried an empty body, and note-title resolution could prefer `lastCreatedNoteTitle` over the active focused note.

Expected:

- When `activePanel === "workstation-notes"`, incoming active note fields are authoritative for note context, including an empty body. Prior doc context may still be preserved, but prior note bodies must not merge into the current empty editor.

Status:

- Guarded by `server/__tests__/helix.ask.turn.e10.28.ui-regressions.test.ts` focused regression for stale stored Common Core note plus fresh empty notes panel snapshot.

## Extraction Rule

Before moving a helper out of `HelixAskPill.tsx`, classify it:

- `presentation`: display rows, labels, class names, copy formatting, deterministic tokenization.
- `input parsing`: non-mutating parsing that only returns structured display/input data.
- `quarantined behavior`: any helper that reads/writes stores, calls fetch, starts/resumes turns, changes panels, owns voice/TTS, constructs fallbacks, or decides route/terminal authority.

Only the first two categories belong in the non-React owner modules during this cleanup. Quarantined behavior needs a separate behavior-goal patch with contract tests.
