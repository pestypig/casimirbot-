# Helix Ask UI Ownership Map

This map keeps the `HelixAskPill.tsx` split readable while the component is being reduced. New extractions should land in one of these owner modules only when the code is pure display, formatting, parsing, or projection logic with no React hooks, store writes, fetches, voice playback, route authority, terminal authority, or chat persistence.

## Extracted Non-React Owners

| Owner module | Owns | Does not own |
| --- | --- | --- |
| `ask-agibot-preflight-error.ts` | Deterministic AGIBOT desktop joint-scope preflight error classification from thrown/string/message-shaped errors. | Retry/downgrade behavior, `askLocal` transport, fallback execution, mission interface state, or request admission. |
| `ask-agent-runtime-display.ts` | Agent runtime provider normalization, selected/next runtime fallback, short labels, and actual provider label projection. | Provider fetch transport, runtime persistence, request payload mutation, agent execution. |
| `ask-answer-rendering.ts` | Math tokenization, answer-render debug summaries, calculator-panel display decisions. | Final answer authority, solver routing, fallback answers. |
| `ask-active-turn-stream.ts` | Active turn event projection, stream row filtering, client trace attachment, console stream ingress debug initialization, and dropped-reason counter formatting. | Starting turns, retries, terminal completion, network streams. |
| `ask-context-capsule-display.ts` | Context-capsule token cleanup, palette selection, compact copy text, stamp image projection, and session-capsule confidence band/label projection. | Capsule commit creation, automaton stepping, ledger ranking, selected capsule identity, prompt admission. |
| `ask-convergence-display.ts` | Context-capsule convergence source/proof/maturity display labels, debug snapshot projection, and visible strip state comparison. | Convergence state derivation, proof gates, capsule commits, collapse events, state updates. |
| `ask-continuous-turn-display.ts` | Continuous turn rows and live-answer bridge class/state projection. | Live answer runtime, mailbox wake decisions. |
| `ask-context-capsule-ledger.ts` | Deterministic context-capsule ledger ranking, session-capsule state projection, summary rank comparison, bounded upsert/eviction, selected capsule-id ordering, and latest-wins capsule-id ordering. | Ledger React state/refs, capsule commit creation, automaton stepping, prompt/request admission, source admission, or applying selected capsule identity to turns. |
| `ask-debug-event-display.ts` | Live event log lines, timestamp parsing, event identity display parsing, metadata debug-context display extraction, circular-safe debug JSON formatting, compact tool-trace disclosure copy, debug observation/artifact/runtime-loop summary projection, debug detail payload formatting, queued prompt normalization, rendered debug text cleanup, and shared in-progress placeholder display recognition. | Debug export authority, active-turn admission, active-turn event filtering, reply selection, reply lifecycle hiding, DOM traversal, fetches. |
| `ask-display-text.ts` | Shared deterministic display text normalization for Ask event labels. | Classification, routing, execution decisions. |
| `ask-doc-viewer-context.ts` | Deterministic docs-viewer path normalization, safe docs-relative Ask snapshot path normalization, explicit path extraction from prompt text, and debug path comparison normalization. | Active-doc handoff, docs-viewer store/URL reads, workspace snapshots, request envelope construction, source admission, or route metadata. |
| `ask-env-config.ts` | Deterministic env boolean, number, percent, exact-one flag, enabled-unless-zero flag, and exact-zero opt-out parsing from provided env records and raw values. | Env key selection, runtime feature policy, `import.meta` access, state mutation, route/request admission, numeric clamp/floor policy. |
| `ask-envelope-copy.ts` | Response-envelope copy text and citation display normalization. | Response selection, envelope authority, fallback construction. |
| `../agi/debugExport.ts` | Shared debug export envelope helpers, deterministic debug payload hashing via `hashDebugExportText`, and debug drawer fallback result formatting. | Reply selection, rendered-button fallback extraction, clipboard writes/readback, backend debug export fetching. |
| `ask-goal-pill-display.ts` | Goal-pill label and cadence text formatting. | Goal session transport, edits, pause/resume/stop actions. |
| `ask-interim-voice-callout.ts` | Structured interim voice callout artifact parsing, authority-safe receipt validation, playback intent projection, nested artifact collection, and duplicate/spoken receipt suppression. | Voice queue mutation, spoken-receipt refs, TTS transport, audio playback scheduling, playback diagnostics, terminal authority, or debug export authority. |
| `ask-local-fallback-classification.ts` | Deterministic Ask-local fallback response classification for multilang confidence gate failures and mission/ideology domain-leak detection. | `askLocal` transport, retry/fail-open execution, mode downgrade behavior, prompt construction, route authority, or terminal authority. |
| `ask-live-source-display.ts` | Live-source mail transcript and turn stream display rows. | Mailbox reads, source admission, decision wakes. |
| `ask-luma-mood-display.ts` | Ask shell Luma mood palette class projection. | Mood classification, mood randomization, theme broadcasts, asset selection, state updates. |
| `ask-observer-commentary-display.ts` | Observer lane user-facing commentary text. | Workstation dispatch, observer execution, user-input requests. |
| `ask-observer-events.ts` | Observer lifecycle event object builders. | Event publication, runtime execution. |
| `ask-output-cleanup.ts` | Deterministic answer-boundary, stage-tag, prompt-line, echoed-question cleanup, and cleanup-format hint primitives used by legacy fallback presentation. | Fallback answer selection, route authority, terminal authority, solver output admission, or model response rewriting. |
| `ask-procedural-display.ts` | Procedural timeline/debug-copy action label formatting, workstation intent-stage detail copy, workstation lifecycle event display classification, observation-grounded reply text formatting, and workstation lifecycle status/receipt copy. | Timeline rendering, route selection, router fail IDs, calculator fast-path replies, workstation dispatch, tool execution, reply insertion. |
| `ask-read-aloud-display.ts` | Read-aloud button labels, deterministic mic/read-aloud UI state transitions, voice utterance ID/hash formatting, voice rollout hash gating, and structural voice playback display predicates. | TTS transport, audio playback, audio graph behavior, voice diagnostics, playback scheduling, mic-off playback policy. |
| `ask-reasoning-battle-display.ts` | Reasoning battle visual class names, deterministic positions/styles, and answer tint palette projection. | Battle event construction, reasoning state, JSX rendering, terminal authority. |
| `ask-reasoning-frontier-display.ts` | Reasoning frontier action labels and floating action text/class projection. | Frontier tracker state updates, timers, rendering, route/terminal decisions. |
| `ask-reasoning-theater-hard-failure.ts` | Deterministic reasoning-theater hard-failure signal extraction from debug records and live events for visual fail-closed projection. | Theater state derivation, terminal authority, route/product authority, timers, refs, JSX rendering, or reply lifecycle. |
| `ask-reasoning-theater-display.ts` | Reasoning theater labels, phase/suppression display bucket resolution, certainty display bucket mapping, deterministic medal-event projection, stance meter classes, suppression copy, medal asset paths, Mirek cell visual classes, Mirek artifact particle projection, Mirek display-grid density/simulation projection, and frontier particle node projection. | Theater state derivation, hard-failure interpretation, medal queue/timers, Mirek evidence-anchor collection, frontier tracker updates, refs, JSX rendering, route/terminal decisions. |
| `ask-reasoning-theater-evidence.ts` | Deterministic Mirek evidence path recognition, nested evidence-path collection, evidence-anchor projection, and shared exact-path continuity ratio. | Theater state derivation, hard-failure interpretation, grid density/simulation, timers, refs, JSX rendering, route/terminal decisions, or evidence/source admission. |
| `ask-stage-play-ledger.ts` | Stage Play chat ledger display events. | Stage Play runtime or chat persistence. |
| `ask-status-classnames.ts` | Procedural and causal trace row class-name mapping. | React rendering, timeline construction. |
| `ask-stable-hash.ts` | Shared deterministic unsigned 32-bit hash and fixed-width projection hash formatting for stable UI ids, visual jitter, and payload fingerprints. | Debug export authority, reply selection, clipboard/export bounding, terminal authority, state mutation, or cryptographic integrity. |
| `ask-steering-queue-display.ts` | Steering queue item normalization and classes. | Auto-wake execution, mailbox mutation, workstation actions. |
| `ask-terminal-projection.ts` | Typed visible final-answer projection helpers, pending-input request reading, live-answer context-pack answer copy, job-ready link visibility filtering, and terminal answer text normalization/invalid-placeholder detection. | Model sampling, terminal completion, server-side authority, JSX timeline rendering, reply lifecycle state. |
| `ask-turn-transcript.ts` | Runtime transcript, transcript-record live-event projection, runtime Ask live-event projection, structured workstation gateway transcript projection, and causal trace row building. | Tool execution, live source materialization, terminal decisions, JSX timeline rendering, active-turn admission. |
| `ask-value-normalization.ts` | Shared deterministic value normalization helpers for generic number parsing, numeric/unit-interval clamping, text clipping, safe text coercion, non-empty string trimming, plain-record checks, string-array cleanup, and case-insensitive string dedupe. | Env key selection/policy, route/request admission, terminal authority, debug export authority, mutation, or side effects. Keyed env parsing lives in `ask-env-config.ts`. |
| `ask-voice-text-display.ts` | Voice/read-aloud text cleanup, conversation brief text sanitization, citation stripping, artifact-spill display cleanup, and compact debug summaries. | TTS transport, playback scheduling, audio graph behavior, voice capture, route decisions, brief pinning and suppression heuristics. |
| `ask-voice-turn-scoring.ts` | Deterministic voice conversation completion, turn-complete banding, and intent-shift scoring. | Voice dispatch, in-flight merge policy, supersede/restart decisions, STT lifecycle, mic runtime, queue mutation, or request submission. |
| `ask-voice-copy-display.ts` | Voice command labels, input status/error labels, reasoning lifecycle copy, brief copy joining, timeline prompt display text, and reasoning attempt detail text. | Voice dispatch, lifecycle policy, timeline mutation, auto-speak scheduling, retry policy, mic capture, memory-pressure handling. |
| `ask-voice-barge-policy.ts` | Deterministic voice barge hard-cut reason resolution, held-playback resume predicate, supersede interruption predicate, and preempt-policy-to-cancel-reason mapping. | Mic effects, STT queue mutation, playback queue mutation, audio element scheduling, TTS transport, or cancellation side effects. |
| `ask-voice-brief-policy.ts` | Deterministic voice brief/status predicates, brief-source normalization, terminal-state speech suppression predicate, and suppressed-speech text shaping. | Voice queue mutation, pinned-brief state selection, TTS transport, audio scheduling, terminal authority, or reply lifecycle decisions. |
| `ask-voice-diagnostics-export.ts` | Deterministic voice diagnostics export projection: client build-stamp fallback formatting, segment count summaries, bounded playback receipts, bounded voice calls, bounded timeline rows, monitor rounding, and client playback reconciliation debug projection. | Diagnostics snapshot construction/publication, browser/env reads, refs/effects, mic/STT runtime, playback runtime, TTS transport, terminal authority, or debug export authority. |
| `ask-voice-playback-intent.ts` | Deterministic voice playback intent/task projection: manual read-aloud intent creation and queue-task object shaping with stable utterance IDs. | Voice queue mutation, TTS transport, audio playback scheduling, audio graph behavior, playback diagnostics, spoken-receipt refs, or terminal authority. |
| `ask-voice-confirmation-command.ts` | Deterministic parsing of short transcript confirmation voice commands into confirm/retry/cancel decisions. | Transcript confirmation policy, dispatch state, auto-confirm gating, command-lane normalization, STT lifecycle, or request submission. |
| `ask-voice-transcript-confidence.ts` | Deterministic STT transcript confidence derivation, confirmation threshold constant, and threshold/translation/provider confirmation predicate. | Transcript confirmation policy, auto-confirm gating, live-activity checks, translation-risk blocking, command-lane normalization, STT lifecycle, or request submission. |
| `ask-voice-continuation-lexical.ts` | Deterministic voice continuation lexical predicates: intent-term extraction, dangling/low-information tail checks, latest continuation focus extraction, lexical carryover, and near-turn continuation classification. | Voice dispatch, in-flight merge policy, supersede/restart decisions, STT lifecycle, mic runtime, or request submission. |
| `ask-voice-playback-classification.ts` | Deterministic voice playback retry/error classification: graph fallback retry predicates, direct retry predicates, recoverable playback-error-as-ended predicates, retryable synthesis error detection, and memory-pressure error detection. | Audio graph construction, playback scheduling, TTS transport, user-agent/env playback policy, voice queue mutation, or cancellation/preemption policy. |
| `ask-voice-steering-client.ts` | Deterministic active-turn voice steering transcript classification, structured client request shaping, and structured live-event tool-call timing detection. | Voice capture/STT lifecycle, active-turn reservation, dispatch queue mutation, tool recording, cancellation/preemption policy, or Ask request submission. |
| `ask-voice-capture-display.ts` | Deterministic voice-capture signal display predicates: level smoothing, flat-signal/stall checks, loopback label matching, container-header priming decisions, explicit-UA recorder MIME candidate ordering, noise profile thresholds, low-quality signal flags, barge-in speech qualification, and media error debug labels. | Mic recorder lifecycle, browser capability probing, `MediaRecorder` MIME support selection, stream capture, STT dispatch, voice confirmation, held transcript recovery, or barge-in state transitions. |
| `ask-voice-capture-checkpoints.ts` | Deterministic voice-capture checkpoint schema: ordered checkpoint keys, display labels, checkpoint status types, and fresh idle checkpoint map construction. | Mic recorder lifecycle, checkpoint state updates, stream capture, STT dispatch, voice confirmation, diagnostics publication, or barge-in state transitions. |
| `voice/voice-transcript.ts` | Deterministic voice transcript draft merging and dispatch transcript source resolution. | STT queue mutation, dispatch submission, transcript confirmation state, mic runtime, or active turn lifecycle. |
| `voice/voice-turn-authority.ts` | Deterministic voice turn seal gating, voice reasoning response authority decisions, and authority suppression reason projection. | Starting/restarting Ask turns, request dispatch, queue mutation, playback/TTS, or terminal answer authority. |

## Still Quarantined In `HelixAskPill.tsx`

The following categories remain behavior-sensitive and should not be moved into display modules as part of presentation cleanup:

- Timeline entry creation, ordering, patching, filtering, and feed state. Unused timeline label constants should be deleted instead of preserved as new display owners.
- Legacy fallback and fallback reply construction.
- Transport and fetch calls, including Ask turns, live-source mail, goal sessions, and debug export endpoints.
- Stale completion and in-flight turn lifecycle.
- Route metadata, source admission, route authority, and terminal authority.
- Chat persistence and session merging.
- TTS, voice playback, voice diagnostics, mic capture, and audio graph behavior.
- Workstation dispatch, panel mutation, goal-session actions, and user-input request handling.
- Debug export authority, rendered-button export fallback, and clipboard/export bounding.

## Remaining Local Cluster Map

These are the major `HelixAskPill.tsx` clusters that still need either behavior-goal extraction or deliberate quarantine. Representative functions are listed to make the remaining monolith easier to scan; the list is not a permission slip to move the cluster as a pure display helper.

| Local cluster | Representative anchors | Current boundary |
| --- | --- | --- |
| Visual/audio capture preferences | `readHelixAskVisualCaptureAudioPreference`, `syncHelixAskVisualCaptureRoutePreference` | Reads browser state/stores and mutates capture route preferences; stale live projection components were removed instead of preserved. |
| Prompt interpretation and planner policy | `inferAskMode`, `classifyHelixReasoningIntent`, `deriveHelixPlannerContract`, `evaluateEvidenceFinalizationGate`, `resolveHelixDispatchPolicyAtTurnStart`, `hasExplicitWorkspaceThenReasoningCue`, `hasExplicitReasoningOnlyCue`, `isSimpleConversationTurnCandidate`, `stripQuotedPayloadsForEvidenceGate`, `isQuotedTransformOnlyForEvidenceGate` | Observe/act/verify mode inference, route admission, simple-conversation bypass, quoted-payload evidence-gate exceptions, and terminal/finalization policy; behavior-goal only. |
| Workstation pending input and dispatch arbitration | `buildWorkstationUserInputRequest`, `resolvePendingWorkstationUserInput`, `resolvePanelActionDefinition`, `parseWorkstationConfirmationReply`, `extractPendingArgFromReply`, `cloneRunPanelActionWithArgs`, `readDocTopicResolutionMeta`, `stripDocTopicResolutionMetaFromArgs`, `parseWorkstationActionCommand`, `resolveWorkstationRouterFailId`, `syncDocViewerStateFromWorkstationAction`, `readWorkstationActionArgString` | User-input state, panel action capability lookup, confirmation parsing, required-argument extraction, docs-viewer topic clarification, router fail-id mapping, docs-viewer handoff, workstation arg reads, and workstation mutation; behavior-goal only. |
| Pending input and cancellation terminal visibility | `isWorkstationTurnTransitionPendingRequest`, `registerTurnTerminalOutcome`, `readHelixPendingInputRecord`, `normalizeHelixPendingTransitionMarker`, `readHelixPendingTransitionTrace`, `hasHelixPendingCancellationMarker`, `isHelixCanceledPendingTurn`, `resolveHelixPendingInputRecord`, `resolveHelixVisibleTerminalKind` | Turn transition cancellation, single-terminal outcome registration, pending request resolution, cancellation markers, and visible terminal-kind selection affect latest-turn lifecycle and terminal projection; behavior-goal only. |
| Workstation command lexicon and panel resolution | `normalizePanelQuery`, `resolvePanelIdFromText`, `resolvePanelIdFromPath`, `parseOpenPanelCommand`, `restateWorkstationSubgoal`, `normalizeWorkstationCommandText`, `buildLexiconPanelAction`, `normalizeLexiconAlias`, `resolveCapabilityAliasPanelAction`, `parseWorkstationLexiconAction` | Natural-language workstation command parsing, alias normalization, panel capability matching, and `HelixWorkstationAction` construction; behavior-goal only. |
| Stage Play generated wake admission and projection suppression | `isStagePlayMailboxWakePromptText`, `hasStagePlayMailboxWakeRouteMetadata`, `shouldBlockStagePlayMailboxWakePromptWithoutRouteMetadata`, `isGeneratedStagePlayMailWakePrompt`, `isGeneratedStagePlayMailWakeAssistantProjection`, `shouldSuppressGeneratedStagePlayMailWakeChatProjection` | Route-metadata admission and chat projection filtering for generated mailbox wakes and generated wake assistant projections; behavior-sensitive because it changes which wake prompts may execute and which durable chat turns appear in Ask. |
| External prompt lifecycle and single-flight claims | `resolveExternalPromptClaimId`, `claimExternalPromptSingleFlight`, `pendingExternalAskPromptRef`, `consumePendingHelixAskPrompt`, `clearPendingHelixAskPrompt` | Pending prompt consumption, browser-window claim tracking, duplicate dispatch suppression, and lifecycle cleanup affect which external Ask prompt becomes a turn; behavior-goal only. |
| Voice capture, STT, confirmation, continuation, and auto-dispatch | `resolveTranscriptConfirmPolicy`, `normalizeTranscriptConfirmPolicyReason`, `normalizeVoiceConfirmDispatchState`, `isLowPivotBlocked`, `evaluateVoiceAutoDispatchGovernance`, `isExplicitVoiceAskTurnCandidate`, `deriveVoiceSegmentLocalAnalysis`, `resolveSpeakerFromSessionProfiles`, `shouldMergeVoiceContinuationTurn`, `shouldMergePendingConfirmationTranscript`, `shouldRecoverHeldTranscriptAfterNoTranscript`, `shouldFlushHeldTranscriptFromWatchdog`, `shouldAutoConfirmTranscriptPrompt`, `normalizeVoiceCommandLaneEnvelope`, `shouldIgnoreLowQualityTranscriptBargeIn`, `buildVoiceReasoningDispatchPrompt`, `deriveVoiceTimelineSuppressionMeta` | Mic/STT/runtime control, transcript lifecycle, local audio analysis, speaker profile matching, held transcript recovery, pending-confirmation merge policy, confirmation/auto-dispatch gates, translation pivot-confidence blocking, and dispatch policy; do not move as display. Deterministic transcript confidence/threshold helpers live in `ask-voice-transcript-confidence.ts`; deterministic confirmation command parsing lives in `ask-voice-confirmation-command.ts`; deterministic active-turn steering transcript/request/event predicates live in `ask-voice-steering-client.ts`; deterministic barge/preempt predicates live in `ask-voice-barge-policy.ts`, deterministic barge-in speech qualification lives in `ask-voice-capture-display.ts`, while reservation, queue mutation, and tool recording stay behavior-sensitive. |
| Voice reasoning dispatch and suppression rescue policy | `shouldDispatchReasoningAttempt`, `inferSuppressionCauseFromRouteReason`, `shouldForceObserveDispatchFromSuppression`, `resolveSuppressedDispatchRescueTranscript`, `isStrongQuestionDispatchCandidate`, `isSimpleDirectPromptLaneCandidate`, `isSimpleTitleOrPathOnlyPrompt`, `shouldQueueWorkspaceBackgroundReasoning`, `isLikelyContextDependentTurn`, `extractPriorUserContext`, `buildVoiceReasoningDispatchPrompt`, `normalizeConversationModeForDispatch`, `deriveObserverDispatchPlan` | Reasoning dispatch admission, observer workspace/reasoning arbitration, suppression rescue/retry policy, document quick-lane detection, background workspace reasoning queue policy, context-dependent turn classification, and dispatch-mode normalization affect request submission and in-flight voice lifecycle; behavior-goal only. |
| Voice continuation and intent-shift policy | `shouldMergeVoiceContinuationInFlight`, `shouldRestartExplorationLadderOnSupersede`, `isLikelyContinuationAddendum`, `isLikelyContinuationTailFragment` | Latest-turn continuation semantics, supersede/restart policy, and voice intent arbitration; deterministic lexical predicates live in `ask-voice-continuation-lexical.ts`, deterministic scoring lives in `ask-voice-turn-scoring.ts`, while in-flight merge and restart policy remain behavior-sensitive. |
| Voice language and workstation fast-path policy | `normalizeVoiceLanguageTag`, `isEnglishLikeLanguageTag`, `inferLanguageTagFromSourceText`, `resolveVoiceSourceLanguage`, `resolveVoiceResponseLanguage`, `isHighRiskTranslationContext`, `readWorkstationActionArgText`, `extractCalculatorFastPathExpressionFromPrompt`, `getWorkstationFastPathReplyText`, `renderCalculatorFastPathReply`, `selectWorkstationFastPathReplyAction` | Translation uncertainty handling, lightweight script-based language inference, response-language selection, workstation arg reading, calculator fast-path solve execution, and workstation reply selection; behavior-goal only. |
| Voice playback audio runtime | `isLikelyIOSDesktopModeUserAgent`, `isLikelyMobileAudioUserAgent`, `resolveVoicePlaybackGain`, `shouldUseVoicePlaybackAudioGraph`, `resolveVoicePlaybackAttemptPath`, `shouldBypassVoicePlaybackGraph`, `isActivePlayback` | Browser audio capability heuristics, navigator touch-point checks, active audio identity, playback scheduling, and audio graph behavior; deterministic retry/error classifiers live in `ask-voice-playback-classification.ts`, pure ID/predicate formatting lives in `ask-read-aloud-display.ts`, deterministic intent/task projection lives in `ask-voice-playback-intent.ts`, and structured interim callout artifact parsing lives in `ask-interim-voice-callout.ts`. |
| Voice recorder runtime and MIME handling | `pickSupportedMicRecorderMimeType`, `pickMicRecorderMimeType`, `readVoiceTimelineClientBuildStamp`, mic recorder effects | Browser capability probing, `MediaRecorder` MIME support selection, stream capture, browser/env build-stamp reads, STT dispatch, capture resilience, diagnostics publication, and barge-in state transitions stay local; deterministic signal/header/noise threshold predicates and explicit-UA MIME candidate ordering live in `ask-voice-capture-display.ts`, checkpoint schema/idle-map construction lives in `ask-voice-capture-checkpoints.ts`, and diagnostics export projection lives in `ask-voice-diagnostics-export.ts`. |
| Voice brief pinning and speech suppression | `shouldAutoSpeakVoiceDecisionLifecycle`, `shouldAutoSpeakAnswerForTurn`, `isGenericRunningVoiceStatus`, `isPinnedVoiceBriefCandidate`, `normalizeConversationBriefSource`, `buildSuppressedVoiceSpeechText`, `shouldSuppressVoiceForTerminalState` | Deterministic brief/status predicates and suppressed-speech formatting live in `ask-voice-brief-policy.ts`; auto-speak eligibility, pinned brief state selection, voice queue mutation, terminal lifecycle, and speech scheduling stay local. |
| Exploration ladder and context chooser policy | `decideExplorationLadderAction`, `buildExplorationEscalationPrompt`, `buildExplorationArtifactRetryPrompt`, `isRepoFileLocationRequestPrompt`, `buildConversationFallbackBrief`, `resolveAskContextChooserAutoMode`, `isRepoCodeEvidencePrompt` | Retry/escalation prompts, artifact retry behavior, repo file-location prompt detection, fallback brief construction, and context/source chooser policy; behavior-goal only. Deterministic mission/ideology domain-leak classification lives in `ask-local-fallback-classification.ts`. |
| Atomic viewer launch suppression | `shouldSuppressAtomicViewerLaunch` | Docs-summary prompts with explicit docs-viewer context/path should not auto-open the atomic viewer; this gates panel launch behavior and stays local unless a behavior-goal patch adds launch contract tests. |
| Context-capsule ledger selection | `contextCapsuleSessionLedger`, `setContextCapsuleSessionLedger`, `contextCapsuleSessionLedgerRef`, capsule application flow | Ledger React state/refs, selected capsule identity application, and session capsule flow remain local; deterministic ranking, bounded upsert/eviction, selected-id ordering, latest-wins ordering, and session-capsule state projection live in `ask-context-capsule-ledger.ts`. |
| Ask request construction and attachment admission | `isHelixAskVisualPrompt`, `isHelixAskPastedTextResumeRecallPrompt`, `isHelixAskUsePastedTextAttachmentPrompt`, `buildHelixAskPastedTextResumeRecallRouteMetadata`, `resolveHelixAskHardBackendEntrypointFamily`, `buildHelixAskHardBackendEntrypointRouteMetadata`, `buildHelixAskTextAttachmentTurnInputItem`, `buildQueuedAskTurn`, `validateHelixAskAttachmentForSubmit`, `readVisualEvidenceSummary`, `isDiagnosticVisualEvidence`, `buildAskTurnWorkspaceContextSnapshot` | Request envelope, route metadata, source admission, active-doc context, visual diagnostic evidence admission, pasted-text recall admission, turn input item construction, and attachment payload authority. |
| Docs-viewer and workspace context snapshots | `readDocViewerDebugSnapshot`, `resolveAskTurnDocViewerSnapshotPath`, `readDocViewerPathFromDesktopUrlForAskSnapshot`, `rememberDocViewerPathForAskSnapshot`, `resolveDocsViewerAnchorPathForQuestion`, `readWorkstationLayoutDebugSnapshot`, `buildAskTurnWorkspaceContextSnapshot` | Active-doc handoff, URL/store reads, layout state reads, and request-envelope context; deterministic path normalization/extraction including `normalizeDocViewerPathForAskSnapshot` lives in `ask-doc-viewer-context.ts`, but HASK-BSQ-001 handoff behavior remains quarantined. |
| Agent runtime/provider controller wiring | `readStoredHelixAskAgentRuntime`, `persistHelixAskAgentRuntime`, provider fetch effect, `handleAgentRuntimeButtonClick` | Runtime storage, backend provider fetch transport, selected runtime state, and request payload wiring stay local; pure provider normalization and labels live in `ask-agent-runtime-display.ts`. |
| Context compaction resume-frame handoff | `isHelixAskContextCompactionPauseText`, `extractHelixAskContextCompactionResumeFrame`, `extractLatestHelixAskContextCompactionResumeFrameFromReplies`, `readStoredHelixAskContextCompactionResumeFrame`, `writeStoredHelixAskContextCompactionResumeFrame`, `isHelixAskContextCompactionPausePendingReply` | Session-storage resume-frame cache, compaction pause detection, queue unblocking, and request metadata handoff; behavior-goal only. |
| Chat projection and reply lifecycle | `parseChatMessageTimeMs`, `buildHelixAskChatProjectionId`, `buildHelixAskRepliesFromChatSession`, `resolveHelixAskReplyCanonicalKey`, `resolveHelixAskReplyOrderMs`, `mergeHelixAskReplyPreservingOrder`, `mergeHelixAskRepliesByCanonicalTurn`, `sortHelixAskRepliesChronologically`, `limitHelixAskRepliesChronologically`, `appendHelixAskReplyChronologically`, `isHelixAskProgressPlaceholderReply`, `shouldRenderHelixAskActiveTurnStream`, `shouldKeepHelixReplyInBriefLane`, `shouldHideHelixAskTranscriptReply` | Chat persistence, stale completion, canonical reply keys, order timestamps, chronological limiting, active-turn stream visibility, brief-lane retention, durable projection guards, transcript hiding, and reply ordering. |
| Backend entrypoint and runtime authority guards | `requiresHelixAskBackendEntrypoint`, `shouldUseHelixAskBackendTurnEntrypoint`, `hasHelixAskBackendEntrypointTurnId`, `buildHelixAskBackendEntrypointRuntimeFingerprint`, `buildHelixActionEnvelopeRuntimeAuthority`, `attachHelixActionEnvelopeRuntimeAuthorityDebug`, `readAgentLoopAuditRecord`, `readAgentLoopAuditArray`, `extractAskLevelTheoryReflection`, `normalizeHelixRuntimeActionKey`, `readHelixDecisionCapabilityKeys`, `readHelixGatewayCapabilityKeys`, `collectHelixAgentSelectedCapabilities`, `readHelixWorkstationActionRuntimeKeys` | Backend route guardrails, backend turn-id recognition, selected-capability admission, runtime/action-envelope authority, theory-reflection handoff, debug authority attachment, and shortcut prevention; behavior-goal only. |
| Visible terminal and route authority | `shouldPreserveAuthoritativeTerminalOverEvidenceGate`, `resolveHelixAskVisibleTerminal`, `resolveHelixAskHardPromptProjectionGuard`, `requiresHelixAskBackendEntrypoint` | Terminal projection, route/product authority, backend-entrypoint guard behavior, and authoritative-terminal preservation over client evidence gates. Runtime transcript event projection is crowned in `ask-turn-transcript.ts`. |
| Procedural timeline JSX rendering | `renderProceduralTurnTimeline` | React timeline composition, selected tool disclosure, row ordering/layout, and JSX rendering stay local; deterministic procedural labels/status classes/event builders live in crowned helper modules. |
| Debug export and clipboard authority | `base64FromText`, `sha256TextHex`, `isBackendAskTurnDebugExportEligibleTurnId`, `buildReplyMasterEventClockExport`, `extractHelixRenderedTurnDebugFromButton`, `debugPayloadMatchesRenderedReply`, `debugPayloadMatchesRenderedTurnPayload`, `buildHelixDebugExportEnvelopeFromMasterPayload`, `normalizeReplyMasterDebugPayload`, `buildClientProjectionDebugFields`, `copyHelixRailCriticalDebugFieldsForUi`, `boundHelixDebugExportTextForUi`, `buildReplyScopedDebugExportFromRenderedReply`, `buildReplyScopedDebugExportFromRenderedButton`, `resolveAuthoritativeDebugExportPayload`, `copyDebugPayloadToClipboard` | Reply-scoped debug binding, rendered-button turn matching, backend debug export materialization, backend Ask turn debug-ref eligibility, event-clock export, payload encoding, fallback bounding, and clipboard readback; deterministic debug payload hashing and pure drawer fallback result formatting live in `../agi/debugExport.ts`, and pure debug observation/artifact/runtime-loop summary projection lives in `ask-debug-event-display.ts`. |
| Debug context summary projection | `buildHelixAskDebugContextSummary` | Local bridge between rendered timeline/debug context and pending-cancellation visibility; depends on `isHelixCanceledPendingTurn`, terminal/failure debug fields, pending request masking, and latest-turn debug surfaces, so this is behavior-goal only rather than a pure debug display helper. |
| Reasoning battle JSX rendering | `renderReasoningBattleStage` | React battle-stage composition and JSX rendering stay local; deterministic visual class/style/tint projection lives in `ask-reasoning-battle-display.ts`. |
| Reasoning theater state and simulation | `deriveReasoningTheaterState`, `deriveReasoningTheaterStateFromCanonical`, `resolveReasoningTheaterFailureOverrideArchetype`, `applyReasoningTheaterFailureOverride` | State derivation, canonical-state adaptation, failure override application, medal queue/timers, and React rendering remain local; deterministic hard-failure signal extraction lives in `ask-reasoning-theater-hard-failure.ts`, deterministic evidence-anchor projection lives in `ask-reasoning-theater-evidence.ts`, and display labels/classes plus phase/suppression/medal/grid projection live in `ask-reasoning-theater-display.ts`. |
| Legacy local Ask fallback output cleanup | `stripPromptEcho`, `extractAnswerBlock`, `stripEvidencePromptBlock` | Legacy/local fallback prompt construction and answer-block selection; deterministic answer-boundary, stage-tag, prompt-line, echoed-question cleanup, and cleanup-format hint primitives live in `ask-output-cleanup.ts`; deterministic AGIBOT preflight error classification lives in `ask-agibot-preflight-error.ts`; deterministic multilang confidence gate and mission/ideology domain-leak response classification lives in `ask-local-fallback-classification.ts`; dead prompt/query/context builders were removed instead of preserved while backend Ask is authoritative. |

## Behavior-Sensitive Queue

These issues came from live Helix-mode UI testing and must be handled as behavior-goal work, not display-helper extraction. Do not move the named paths into non-React display modules unless behavior-preserving tests prove the move is safe.

### HASK-BSQ-001: Active document context handoff into Helix Ask requests

Evidence:

- Browser URL had `doc=docs/research/nhm2-current-status-whitepaper-2026-05-02.md`.
- UI visible focus included `docs-viewer` and `scientific-calculator`.
- Prompt using "current NHM2 whitepaper" failed with: "I need retrieval before finalizing this claim. I do not yet have grounded evidence references for it."
- The same prompt succeeds through the direct API when `doc_path` is supplied.
- The same UI prompt succeeds when the doc path is explicitly written in the prompt.

Likely boundary:

- `HelixAskPill` request construction is not consistently passing retained active docs-viewer context, doc path, or document evidence into Helix golden-path requests, especially for compound prompts.

Expected:

- When the UI is on a docs-viewer URL with `doc=...`, Helix-mode Ask request assembly should include the active doc path/context in the backend request envelope without requiring the user to restate the path.

Quarantine:

- Request-envelope assembly, active-doc context collection, workspace context snapshots, route metadata, source admission, and golden-path request transport.

### HASK-BSQ-002: Scoped Debug copy binds to stale prior turn

Evidence:

- After a complex Helix UI compound answer rendered visibly, clicking latest Debug copy copied an older calculator turn.
- Copied payload had marker mismatch, `terminal_artifact_kind = workstation_tool_evaluation`, and answer "3 + 5 = 8".
- The visible answer was the later compound answer.
- Therefore latest visible reply and debug-copy target diverged.

Likely boundary:

- `helix-ask-latest-debug-copy` or selected debug state still points to a cached prior debug payload or previous latest reply after complex/streamed golden-path turns.

Expected:

- For every rendered answer, Debug copy must bind to that reply's `turn_id` and `debug_export_ref`, not the last successful cached debug export.

Quarantine:

- Stream final projection, latest reply selection, selected debug state, debug-copy binding, rendered-button export fallback, and clipboard/export bounding.

### HASK-BSQ-003: Backend compound terminal authority is not the first suspect

Evidence:

- Direct Helix API returns `terminal_artifact_kind = compound_evidence_synthesis_answer`.
- Direct Helix API returns `final_answer_source = compound_evidence_synthesis_answer`.
- Direct Helix API returns `subgoals = 5` and `terminal_error_code = null`.
- UI explicit-doc compound prompt renders a compound answer.

Implication:

- Patch UI request context and debug binding first. Do not treat this as a backend compound failure or golden-path terminal-authority failure without new contrary evidence.

## Extraction Rule

Before moving a helper out of `HelixAskPill.tsx`, classify it:

- `presentation`: display rows, labels, class names, copy formatting, deterministic tokenization.
- `input parsing`: non-mutating parsing that only returns structured display/input data.
- `quarantined behavior`: any helper that reads/writes stores, calls fetch, starts/resumes turns, changes panels, owns voice/TTS, constructs fallbacks, or decides route/terminal authority.

Only the first two categories belong in the non-React owner modules during this cleanup. Quarantined behavior needs a separate behavior-goal patch with contract tests.
